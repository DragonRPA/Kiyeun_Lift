
# INITIAL_DB_UPLOAD.md의 섹션 13~14를 새 내용으로 교체
filepath = r'd:\01.AntiGravity\Kiyuen_Lift\INITIAL_DB_UPLOAD.md'

with open(filepath, 'r', encoding='utf-8') as f:
    content = f.read()

old_section = """## 13. 알려진 이슈 및 해결 이력 (Build.22~27)

| 빌드 | 이슈 | 원인 | 해결 방법 |
|------|------|------|---------|
| Build.22 | 자산번호 '미지정' | ownAssetNo fallback이 전대칸(13) 참조 | r[10] 직접 읽기로 수정 |
| Build.23 | 구 시트명 불인식 | `'202608'` 하드코딩 | `'계약현황' \\|\\| '202608'` fallback |
| Build.24 | 자산번호 여전히 미지정 | getCol 키가 헤더에 없어 fallback [13] 사용 | 직접 인덱스 방식으로 전면 교체 |
| Build.25 | contractStatusStr 오파싱 | `['상태','결재상태']` 키 없음→fallback r[10]=관리번호 | r[8](계약구분) 직접 읽기 |
| Build.26 | 전대 장비 모델명=45845 | 중복 헤더→Col[9]=빈값→fallback r[3]=날짜시리얼 | r[9]||r[12] 직접 인덱스 분리 |
| Build.27 | 유령 고객사 생성 | 마감일자 시트 빈 행에서 r[0]=순번→고객명 | r[1](업체명) 직접 읽기, 숫자 건너뜀 |

---

## 14. 향후 개선 과제 (Known Limitations)

| # | 한계 | 내용 | 권고 조치 |
|---|------|------|---------|
| 1 | 배차/검수 이력 미생성 | 과거 입출고 이력은 엑셀에 없어 생성 불가 | 도입 후 수동 입력 |
| 2 | 감가상각 누계액 초기화 | 취득일/취득가격 불완전으로 `accumDepreciation = 0` | 자산 관리 화면에서 수동 수정 |
| 3 | `보유장비 임대현황` 미활용 | 가동상태(임대/대기) 시트를 파싱에 반영 못함 | 추후 교차 검증 로직 추가 예정 |
| 4 | 직원/영업담당자 자동 매핑 불가 | 엑셀에 담당자 컬럼 없어 `salespersonId = null` | 도입 후 계약별 수동 지정 |
| 5 | 계약번호 신규 부번 | `C{YYMM}-{SEQ}` 형식으로 자동 부번, 수기 번호와 다를 수 있음 | 계약번호 수동 확인/수정 가능 |"""

new_section = """## 13. 알려진 이슈 및 해결 이력 (Build.22~31)

| 빌드 | 이슈 | 원인 | 해결 방법 |
|------|------|------|---------|
| Build.22 | 자산번호 '미지정' | ownAssetNo fallback이 전대칸(13) 참조 | r[10] 직접 읽기로 수정 |
| Build.23 | 구 시트명 불인식 | `'202608'` 하드코딩 | `'계약현황' \\|\\| '202608'` fallback |
| Build.24 | 자산번호 여전히 미지정 | getCol 키가 헤더에 없어 fallback [13] 사용 | 직접 인덱스 방식으로 전면 교체 |
| Build.25 | contractStatusStr 오파싱 | `['상태','결재상태']` 키 없음→fallback r[10]=관리번호 | r[8](계약구분) 직접 읽기 |
| Build.26 | 전대 장비 모델명=45845 | 중복 헤더→Col[9]=빈값→fallback r[3]=날짜시리얼 | r[9]\\|\\|r[12] 직접 인덱스 분리 |
| Build.27 | 유령 고객사 생성 | 마감일자 시트 빈 행에서 r[0]=순번→고객명 | r[1](업체명) 직접 읽기, 숫자 건너뜀 |
| Build.28 | 운반비 fallback 오류 | fallback=20이 임차단가(Col[20]) 참조 | fallback=7(운반비 Col[7])로 수정 |
| Build.29 | 소급 청구서 12건 | 소급 기준일로 Col[4](당월 기산일) 사용 | Col[3](최초개시일) 직접 읽기로 수정 |
| Build.30 | 소급 청구서 자동 일괄 생성 | 업로드 시 전 기간 소급 자동 생성 → 수납 재정리 부담 | 기간 선택 UI 추가, 기본 미생성 전환 |
| Build.31 | 기간 만료 자산 AVAILABLE 처리 | 종료일 < 2026-08-01 조건으로 자동 회수 처리 | Col[8]='종료' 명시 시에만 COMPLETED |

---

## 14. 소급 청구서 생성 원칙 (Build.30 이후)

### 14.1 기본 동작: 미생성

업로드 시 소급 청구서는 **기본적으로 생성하지 않는다.** 담당자가 UI에서 명시적으로 기간을 지정해야만 생성된다.

### 14.2 소급 청구 기준일: `Col[3] 최초개시일`

```
Col[3] = 최초개시일  ← 실제 계약이 처음 시작된 날 (소급 청구 기산일)
Col[4] = 개시일      ← 당월 청구 기산일 (소급에 사용 금지)
```

### 14.3 기간 설정 규칙

| 설정 | 결과 |
|------|------|
| 체크박스 OFF (기본) | 소급 청구서 0건 |
| 시작 월 지정 (예: 2026-01) | 해당 월부터 생성 |
| 종료 월 지정 (예: 2026-07) | 해당 월까지 생성 |
| 계약 최초개시월 > 시작월 | max(계약개시월, 시작월)부터 자동 조정 |

> **⚠️ 주의**: 종료 월에 `2026-08` 이상을 입력하면 당월 청구서와 중복 생성됨. 반드시 `2026-07` 이하로 설정.

### 14.4 소급 청구서 상태

- `status: 'PAID'` — 과거 청구는 모두 수납 완료로 간주
- `paidAmount = totalAmount` — 전액 수납 완료 처리
- 담당자가 실제 수납 이력과 대조하여 미수금 건만 별도 수정 가능

---

## 15. 향후 개선 과제 (Known Limitations)

| # | 한계 | 내용 | 권고 조치 |
|---|------|------|---------|
| 1 | 배차/검수 이력 미생성 | 과거 입출고 이력은 엑셀에 없어 생성 불가 | 도입 후 수동 입력 |
| 2 | 감가상각 누계액 초기화 | 취득일/취득가격 불완전으로 `accumDepreciation = 0` | 자산 관리 화면에서 수동 수정 |
| 3 | `보유장비 임대현황` 미활용 | 가동상태(임대/대기) 시트를 파싱에 반영 못함 | 추후 교차 검증 로직 추가 예정 |
| 4 | 직원/영업담당자 자동 매핑 불가 | 엑셀에 담당자 컬럼 없어 `salespersonId = null` | 도입 후 계약별 수동 지정 |
| 5 | 계약번호 신규 부번 | `C{YYMM}-{SEQ}` 형식으로 자동 부번, 수기 번호와 다를 수 있음 | 계약번호 수동 확인/수정 가능 |
| 6 | 소급 청구 수납 상태 일괄 PAID | 소급 청구 생성 시 전건 PAID 처리 → 실제 미수금 건 별도 확인 필요 | 담당자가 미수금 건 수동 수정 |"""

if old_section in content:
    new_content = content.replace(old_section, new_section)
    with open(filepath, 'w', encoding='utf-8') as f:
        f.write(new_content)
    print("SUCCESS: 섹션 13~15 업데이트 완료")
else:
    print("ERROR: 대상 텍스트를 찾지 못했습니다")
    # 실제 파일에서 확인
    print(repr(content[content.find("## 13."):content.find("## 13.")+200]))
