# 초기 DB 업로드 운영 원칙 및 엔터프라이즈 상세 절차서 (SSOT)

> **문서 버전**: v3.0 (2026-09-04 기준 전면 최신화)  
> **최종 적용 빌드**: v1.2.1.Build.56+  
> **대상 핵심 소스**: `src/services/migrationEngine.ts`, `src/pages/InitialDbUploader.tsx`, `src/services/db.ts`, `schema.sql`, `scripts/reorganize_tables_zero_loss.sql`, `scripts/migrate_run.cjs`  
> **원칙 선언**: 본 문서는 기연리프트 ERP 시스템의 초기 데이터 적재(마이그레이션), 소급 청구서 생성, 배차 이력 연동, 밴드 출고요청 마스터 동기화, 밴드 AS 이력 적재, 백업 및 초기화에 대한 **전사 단일 진실의 원천(SSOT)**이며, 어떠한 절차나 로직도 요약 없이 100% 상세히 기술한다.

---

## 1. 목적 및 시스템 최우선 헌장 준수

### 1.1 시스템 개발 헌장 연계
- **제1.1조 (최대 편익 사명)**: 임직원의 수동 입력 노력을 최소화하고, 수기 엑셀 6,450여 행의 비정형 데이터를 단 1건의 누락·오차 없이 Supabase PostgreSQL DB로 자동 변환하여 최대의 업무 편익을 창출한다.
- **제1.2조 (3대 핵심 가치)**:
  1. **자산 라이프사이클 일치**: 엑셀의 가동 현황(`Col[8]`)을 분석하여 자산 상태(`RENTED`, `AVAILABLE`, `RENTED_RETURNED`)를 현장 상태와 완벽히 동기화.
  2. **무누락 DB 저장**: 계약, 체결장비, 청구, 전대임차, 수납, 고객현장, 담당자 등 모든 비즈니스 객체를 DB에 무누락 저장.
  3. **최소 조작 완결**: 단 1개의 엑셀 파일 드래그앤드롭으로 42개 테이블에 대한 13단계 DAG 순차 적재 완결.
- **제5.2조 (Zero Silent Failures)**: 적재 실패 시 무음 처리(Silent Swallow)를 영구 금지하며, `await db.awaitPendingWrites()`를 통해 트랜잭션 성공 여부를 검증하고 오류 발생 시 즉시 에러 모달을 표출한다.
- **제5.3조 (단일 진실의 원천 SSOT)**: 본 절차서에 정의된 컬럼 인덱스, 정규화 파서, DAG 순서만을 전사 단일 표준으로 강제한다.

---

## 2. 입력 데이터 소스 규격 및 전수 컬럼 맵 (Exhaustive Column Map)

초기 DB 업로드 메뉴는 총 5가지의 입력 소스를 처리한다:
1. **메인 초기 DB 엑셀 (`초기DB현황1.xlsx`)**: 5개 시트 (자산, 임대현황, 고객사, 마감일자, 계약대장)
2. **배차 이력 엑셀 (`배차대장.xlsx`)**: 과거 배차 및 운송 기사 배정 내역
3. **네이버 밴드 출고요청 텍스트 로그**: 자연어 출고 의뢰문 (고객사 기본 요구사항 분석용)
4. **네이버 밴드 과거 AS/정비 텍스트 로그**: 현장 AS 일지 및 부품 교체 내역
5. **전체 DB 백업 JSON (`backup_*.json`)**: 42개 테이블 복구용 스냅샷

---

### 2.1 메인 엑셀 5대 시트 구조 명세

#### [시트 1] `보유자산현황` (자산 마스터 원장 - 726행)
- **헤더 위치**: Row 2 (0-indexed: 1행)
- **데이터 시작**: Row 3 (0-indexed: 2행)
- **파싱 대상 컬럼 목록**:
  - `순번` (Col 0): 행 식별용
  - `관리번호` / `자산번호` (Col 1): 당사 자산 고유번호 (예: `SJ-001`, `1001`, `K-105`) ➔ 대문자 정규화 (`toUpperCase()`)
  - `모델명` (Col 2): 장비 모델명 (예: `SJ3219`, `GS1930`, `OPT090`) ➔ `sanitizeModelName()` 거쳐 51종 표준 모델과 매핑
  - `제조사` (Col 3): 제조사명 (예: `Skyjack`, `Genie`, `Dingli`, `Haulotte`, `Sinoboom`)
  - `제조년월` / `연식` (Col 4): 제조년도 (예: `2018`, `2018-05`)
  - `차대번호` / `S/N` (Col 5): 실물 시리얼 번호
  - `취득일자` (Col 6): 장비 최초 매입일 ➔ `sanitizeExcelDate()` 포맷팅
  - `취득가액` / `매입가` (Col 7): 취득 원가 (원 단위 정수)
  - `상태` (Col 8): `보유`, `매각`, `폐기` 등
  - `리스/렌탈비고` (Col 9): 리스사, 잔여개월, 취득 메모

#### [시트 2] `보유장비 임대현황` (구 시트명 `26.08` - 임대 교차 검증)
- **헤더 위치**: Row 2
- **역할**: 보유자산 726대의 실제 가동 여부(임대중 / 대기중) 및 현장명 교차 검증 원장

#### [시트 3] `거래처정보현황` (고객사 마스터 - 265행)
- **헤더 위치**: Row 2 (`순번`이 포함된 헤더 행 동적 탐색)
- **데이터 시작**: Row 3
- **파싱 대상 컬럼 목록**:
  - `순번` (Col 0): 숫자 순번 (스킵 대상)
  - `업체명` / `상호` (Col 1): 고객사 법인/상호명 (예: `(주)대우건설`) ➔ `normalizeCustomerName()`으로 키 생성
  - `사업자등록번호` (Col 2): `000-00-00000` 형식 정규화
  - `대표자명` (Col 3): 대표자 성명
  - `대표연락처` (Col 4): 대표 전화번호 / 휴대전화
  - `대표이메일` (Col 5): 대표 이메일
  - `사업장주소` (Col 6): 본사 주소
  - `현장명` (Col 7): 대표 등록 현장명 ➔ `customer_sites` 테이블 자동 생성
  - `현장담당자` (Col 8): 현장 소장/공무 담당자명 ➔ `customer_contacts` (position: '현장담당자')
  - `현장연락처` (Col 9): 현장 담당자 전화번호
  - `청구담당자` (Col 10): 세금계산서/정산 담당자명 ➔ `customer_contacts` (position: '청구담당자')
  - `청구연락처` (Col 11): 청구 담당자 전화번호
  - `청구이메일` (Col 12): 세금계산서 발행용 이메일 ➔ `taxBillEmail`

#### [시트 4] `업체별마감일자` (청구/결제 조건 원장 - 189행)
- **헤더 위치**: Row 2
- **데이터 시작**: Row 4
- **컬럼 맵 (주의: Col 0은 숫자 순번이므로 Col 1을 읽어야 함)**:
  - `순번` (Col 0): 숫자 1, 2, 3... (유령 고객사 생성 방지를 위해 절대 업체명으로 읽지 않음)
  - `업체명` (Col 1): 고객사명 (예: `기연리프트`)
  - `마감일` (Col 2): 청구 마감일 (예: `말일`, `20일`, `25일`, `15일`, `말`) ➔ `parseClosingDay()` 거쳐 정수(31, 20, 25, 15)로 변환
  - `결재일` / `지급조건` (Col 3): 대금 결제 조건 (예: `익월 15일`, `당월 말일`, `익익월 10일`, `익월말`) ➔ `parsePaymentDueTerm()`으로 변환
  - `비고` (Col 4): 특이사항 (예: `계산서 역발행`, `거래명세표 20일 우편 발송`, `원캔홈페이지 확인 필수`) ➔ `customers.memo`로 적재
  - `결재현황` (Col 5): 수납 상태 메모

#### [시트 5] `계약현황` (구 시트명 `202608` - 메인 계약/청구 원장 - 1,294행)
- **헤더 위치**: Row 2 (0-indexed: index 1)
- **데이터 시작**: Row 3 (0-indexed: index 2)
- **33개 전 컬럼 0-indexed 직접 매핑표**:

| Index | 컬럼 명칭 | 데이터 예시 | 정규화 파서 및 DB 매핑 필드 |
|:-----:|:---------|:-----------|:---------------------------|
| **`r[0]`** | **업체명** | `(주)삼호개발` | `normalizeCustomerName()` ➔ `contracts.customerId` 매핑 |
| **`r[1]`** | **순번** | `1`, `2` | 행 카운터 (내부 디버깅용) |
| **`r[2]`** | **설치장소 (현장명)** | `화성 향남 제약공단 (7/18 하차)` | `extractSiteNameAndMemo()` ➔ `customer_sites.name`, `contracts.siteId` |
| **`r[3]`** | **최초개시일** | `2026.03.15`, `45731` | `sanitizeExcelDate()` ➔ **소급 청구 기산일 (`historicalBillings`)** |
| **`r[4]`** | **개시일 (당월)** | `2026.08.01` | `sanitizeExcelDate()` ➔ `contracts.startDate` (당월 기산일) |
| **`r[5]`** | **종료일** | `2026.08.31`, `미정` | `normalizeEndDate()` ➔ `contracts.endDate` (미정은 `9999-12-31`) |
| **`r[6]`** | **일수** | `31`, `15` | `sanitizeNumber()` ➔ 가동 일수 |
| **`r[7]`** | **운반비** | `70,000`, `46086` | 참조용 운송비 (과거 계약 참조값, 외상미수금 생성 금지) |
| **`r[8]`** | **계약구분** | `연장`, `종료`, `신규`, `가상` | **자산 상태 결정 절대 기준 (`status: ACTIVE/COMPLETED`)** |
| **`r[9]`** | **장비명 (당사)** | `SJ3219`, `GS1930` | `sanitizeModelName()` ➔ 당사 모델명 (`expectedModel`) |
| **`r[10]`** | **관리번호 (당사)** | `SJ-012`, `1004` | `ownAssetNo` ➔ 당사 보유 자산 (`assets.assetNo`) 1:1 바인딩 |
| **`r[11]`** | **수량 (당사)** | `1`, `2` | `sanitizeNumber()` ➔ 당사 자산 수량 |
| **`r[12]`** | **장비명 (전대)** | `OPT090`, `10M` | `sanitizeModelName()` ➔ 전대 모델명 (`expectedModel`) |
| **`r[13]`** | **관리번호 (전대)** | `R-001`, `1004` | `leaseAssetNo` ➔ 외부 전대 자산 (`assets.assetNo`) 1:1 바인딩 |
| **`r[14]`** | **수량 (전대)** | `1` | `sanitizeNumber()` ➔ 전대 자산 수량 |
| **`r[15]`** | **임차업체 (원사)** | `(주)고려렌탈`, `태성` | `normalizeCustomerName()` ➔ `vendors.id` 매핑 (임차처) |
| **`r[16]`** | **협착소유** | `자사`, `임차`, `유` | 협착방지봉 소유 구분 |
| **`r[17]`** | **공장입고일** | `2026.07.28` | 전대 장비 당사 공장 입고 일자 |
| **`r[18]`** | **임차개시일** | `2026.08.01` | `external_leases.leaseStartDate` (원사 임차 시작일) |
| **`r[19]`** | **반납일** | `2026.08.20` | `external_leases.leaseEndDate` (원사 반납 예정/완료일) |
| **`r[20]`** | **임차단가** | `450,000` | `sanitizeNumber()` ➔ `external_leases.monthlyRentFee` (매입 원가) |
| **`r[21]`** | **월렌탈료** | `500,000` | `sanitizeNumber()` ➔ `contract_assets.monthlyRentalFee` (정상 단가) |
| **`r[22]`** | **당월렌탈료** | `500,000` | `sanitizeNumber()` ➔ `billing_details.amount` (당월 순수 렌탈료) |
| **`r[23]`** | **당월기타청구** | `50,000` | `sanitizeNumber()` ➔ 수리비/부대비용 기타 청구액 |
| **`r[24]`** | **기타내역** | `7월 미청구분 추가` | `billing_details.memo` (기타 청구 사유) |
| **`r[25]`** | **당월청구합계** | `550,000` | `sanitizeNumber()` ➔ `billings.totalAmount` (청구 총액) |
| **`r[26]`** | **마감일** | `말일`, `20` | `parseClosingDay()` ➔ `contracts.billingDay` |
| **`r[27]`** | **거래명세표** | `발송완료`, `20일` | 거래명세표 발행 상태 메모 |
| **`r[28]`** | **계산서** | `발행완료`, `역발행` | 세금계산서 발행 상태 메모 |
| **`r[29]`** | **결재조건** | `익월 15일` | `parsePaymentDueTerm()` ➔ `contracts.paymentDueDay` |
| **`r[30]`** | **결재일** | `2026-09-15` | 대금 결제 예정일 |
| **`r[31]`** | **결재금액** | `550,000` | 당월 입금 확인액 ➔ `billings.paidAmount` |
| **`r[32]`** | **(공란/예비)** | - | 예비 확장 컬럼 |

> **⚠️ 중복 헤더 파싱 절대 규칙**:
> 엑셀 시트 내 `장비명`, `관리번호`, `수량` 컬럼이 당사장비(`Col 9, 10, 11`)와 전대장비(`Col 12, 13, 14`)로 2회 중복 등장하므로, 헤더 텍스트 검색을 절대 사용하지 말고 **반드시 위 명시된 0-indexed 직접 배열 인덱스(`r[N]`)로 파싱**해야 한다.

---

## 3. 핵심 파싱 및 데이터 정규화 엔진 (Field Normalization Engine)

시스템은 비정형 엑셀 텍스트를 무결한 DB 레코드로 정제하기 위해 다음 7대 전용 파서 엔진을 실행한다:

### 3.1 업체명 정규화 파서 (`normalizeCustomerName`)
- 법인격 표기어(`(주)`, `주식회사`, `(유)`, `유한회사`, `(합)`, `합자회사`, `사단법인`, `재단법인`) 및 모든 특수기호(`_`, `-`, `[`, `]`, `(`, `)`, `.`, `,`, 공백)를 정규식으로 완벽히 제거하고 소문자로 변환하여 단일 비교 키를 생성한다.
```typescript
export function normalizeCustomerName(name: string): string {
  if (!name) return '';
  return String(name)
    .replace(/주식회사|\(주\)|（주）|㈜|\(유\)|유한회사|\(합\)|합자회사|사단법인|재단법인/gi, '')
    .replace(/[\s\(\)\[\]._,\-]/g, '')
    .toLowerCase();
}
```

### 3.2 엑셀 날짜 정규화 엔진 (`sanitizeExcelDate`)
- 엑셀 시리얼 넘버(예: `45845`), 한글 표기(`7월 18일`, `18일`), 다변형 슬래시/도트 표기(`2026.07/01`, `2026-08-01`, `26.08.01`)를 단 하나의 오차 없이 `YYYY-MM-DD` 표준 포맷으로 변환한다.
- 엑셀 시리얼 날짜 변환 공식: `(serial - 25569) * 86400 * 1000` (UTC 기준 밀리초).
- 모델명이나 메모 셀에 유입된 순수 날짜 시리얼 번호는 필터링하여 문자열 오염을 원천 차단한다.

### 3.3 숫자 및 금액 정규화 (`sanitizeNumber`)
- 콤마(`,`), 통화기호(`₩`, `$`), 공백, `원` 단위를 정규식으로 제거하고 `Math.round(Number(val))`를 적용하여 IEEE 754 부동소수점 오차를 방지하고 순수 정수로 변환한다. 음수 금액(할인, 감액)도 정상 보존한다.

### 3.4 모델명/제원/제조사 자동 추론 엔진
- 엑셀의 비표준 모델명(예: `3219`, `1930`, `090`, `SJ-3219`, `GS 1930`)을 분석하여 사전에 등록된 **51종 표준 모델 카탈로그(`PRESET_PRODUCT_SPECS`)**와 자동 매핑한다.
- 모델명에서 높이(Feet: 19ft, 26ft, 32ft, 45ft)와 제조사(`Skyjack`, `Genie`, `Dingli`, `Sinoboom`, `Haulotte`)를 자동 추론하여 `products` 테이블의 FK 무결성을 100% 보장한다.

### 3.5 마감일 및 결제조건 계산 파서 (`parseClosingDay`, `parsePaymentDueTerm`)
- 마감일: `말일`, `말`, `31` ➔ `31` / `20일`, `20` ➔ `20` / `25일` ➔ `25`
- 결제조건: `익월 15일` ➔ `paymentTermDays = 45` / `당월 말일` ➔ `paymentTermDays = 30` / `익익월 10일` ➔ `paymentTermDays = 70`

### 3.6 현장명 및 특이 메모 분리 파서 (`extractSiteNameAndMemo`)
- 현장명 셀에 함께 기재된 비고(예: `평택 고덕 P3 (7/18 하차 08:00 도착)`)에서 순수 현장명(`평택 고덕 P3`)과 현장 특이 메모(`(7/18 하차 08:00 도착)`)를 분리 추출하여 각각 `customer_sites.name`과 `customer_sites.address_detail` 또는 `contracts.memo`에 나누어 저장한다.

---

## 4. 전사 42대 테이블 스키마 체계 및 13단계 DAG 적재 순서

Supabase PostgreSQL의 외래키(FK) 참조 무결성을 위해, 메인 적재 엔진은 아래의 엄격한 **13단계 순차 DAG(Directed Acyclic Graph)**에 따라 실행된다:

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ [Step 0]   기존 비즈니스 데이터 FK 역순 완전 삭제 (Stale 행 100% 차단)                  │
│ [Step 1]   products                  ← 51종 모델 마스터 & R2 제원표/인증서 연동 (100건 Chunk)│
│ [Step 2]   vendors                   ← 매입/임차 거래처 마스터 (100건 Chunk)            │
│ [Step 3]   customers                 ← 고객사 마스터 189개사 (100건 Chunk)              │
│ [Step 4]   customer_sites & contacts ← 현장 265개 및 청구/현장 담당자 (100건 Chunk)     │
│ [Step 5]   assets                    ← 자사/전대 자산 726대 & 감가상각 누계액 (100건 Chunk)│
│ [Step 6]   contracts & history       ← 렌탈 계약 마스터 & 변경 타임라인 (200건 Chunk)    │
│ [Step 7]   contract_assets & leases  ← 계약 체결 장비 & 전대 임차 대장 (200건 Chunk)    │
│ [Step 8]   deliveries (SKIP)         ← 배차 데이터 (배차 엑셀 파이프라인에서 독립 적재) │
│ [Step 9]   inspections & logs (SKIP) ← 출고검수 및 입출고일지 (배차 엑셀 연계)          │
│ [Step 10]  billings & details        ← 과거 소급 청구서 & 8월 당월 청구서 (200건 Chunk) │
│ [Step 11]  purchase_billings & recs  ← 전대 매입 정산서 & 외상미수금 원장 (100건 Chunk) │
│ [Step 12]  reconciliation_reports    ← 6대 대차대조 무결성 검증 보고서 DB 영구 적재     │
│ [Step 13]  fullRefreshFromServer     ← 로컬 캐시 무효화 및 Supabase 실시간 동기화       │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

### 4.1 청크(Chunk) 분할 및 PostgREST Payload 방어
- Supabase REST API의 단일 요청 Payload Limit(대량 인서트 시 500 에러 및 타임아웃)을 방어하기 위해 **모든 테이블은 100건~200건 단위 청크로 분할 Upsert**된다:
```typescript
async function batchUpsertChunked(table: string, rows: any[], chunkSize = 100) {
  const cols = TABLE_COLUMNS[table];
  for (let i = 0; i < rows.length; i += chunkSize) {
    const chunk = rows.slice(i, i + chunkSize).map(r => {
      const filtered: any = {};
      cols.forEach(k => { if (r[k] !== undefined) filtered[k] = r[k]; });
      return filtered;
    });
    const { error } = await supabase.from(table).upsert(chunk, { onConflict: 'id' });
    if (error) throw new Error(`${table} upsert chunk error: ${error.message}`);
  }
}
```

---

## 5. 5대 서브 파이프라인별 상세 동작 메커니즘

### 5.1 [파이프라인 1] 메인 초기 DB 엑셀 일괄 적재 (`ingestExcelInitialData`)
- **트리거**: 초기 DB 업로드 화면 ➔ `[초기DB현황1.xlsx]` 파일 선택 ➔ `[전체 데이터 일괄 적재 시작]` 클릭.
- **주요 생성 데이터**:
  1. `products`: 51종 표준 고소작업대 제원표, Cloudflare R2 버킷(`drcf`)의 안전인증서 PDF 및 카탈로그 링크 자동 바인딩.
  2. `assets`: 726대 자산 마스터 적재. `acquisitionDate` 기준 2026-08-31까지의 **정액법 감가상각 누계액(`accumDepreciation`) 및 장부가액(`bookValue`) 자동 계산 적재**.
  3. `contracts` & `contract_assets`: 동일 고객사, 동일 현장, 동일 기간인 경우 1건의 계약(`contracts`)으로 자동 그룹핑(`contractGroupKey = ${customerId}_${siteId}_${startDate}_${endDate}`)하고, 투입 장비 N대를 `contract_assets`로 1:N 바인딩.
  4. `billings` & `billing_details`: 2026년 8월 당월 청구서 및 품목별 청구 상세(렌탈료, 당월기타청구, 기타내역) 생성.

---

### 5.2 [파이프라인 2] 과거 소급 청구서 독립 생성 및 적재 (`generateAndIngestHistoricalBillingsDirect`)
- **도메인 배경**: 엑셀의 `Col[3] 최초개시일`부터 `2026년 7월`까지의 과거 렌탈료 청구 이력을 생성하여 고객사별 누적 매출 및 수납 통계를 완비한다.
- **동작 방식**:
  - 시작 연월(`histBillingStart`, 예: `2026-01`)과 종료 연월(`histBillingEnd`, 예: `2026-07`)을 사용자가 직접 지정.
  - DB의 모든 유효 계약을 순회하며, 각 계약의 `startDate` 또는 `firstStartDate`가 해당 월에 유효한 경우 월별 청구서(`billings`)와 상세(`billing_details`)를 일할 계산하여 생성.
  - **회계 왜곡 방지 원칙**: 과거 청구서는 이미 수납이 완료된 과거 사건이므로 **`status: 'PAID'`, `paidAmount = totalAmount`**로 생성하여 현재 미수금 원장을 왜곡시키지 않는다.

---

### 5.3 [파이프라인 3] 과거 배차 이력 엑셀 업로드 (`parseDispatchExcelWorkbook` & `ingestDispatchData`)
- **도메인 배경**: 과거 배차/운송 이력 엑셀 파일을 업로드하여 배차 원장(`deliveries`)을 구축한다.
- **대차/교체 `EXCHANGE` 체인 원칙 (헌장 제2.3조 준수)**:
  - 배차 엑셀 파싱 시 교체/대차 건은 출고와 회수를 2건으로 쪼개지 않고, **속성 `dispatchCategory: '교환'`, `type: 'EXCHANGE'` 1건의 왕복 배차 의뢰로 통합 관리**한다.
  - 상차지(출고지) ➔ 하차지(현장) ➔ 회수지(주기장) 경로 및 왕복 운송비 할인 정산 속성을 1건에 바인딩하여 적재.

---

### 5.4 [파이프라인 4] 밴드 출고요청문 기반 고객사/현장 기본 요구사항 마스터 동기화 (`ingestCustomerDefaultsFromDispatchHistory`)
- **도메인 배경**: 네이버 밴드에 수년간 축적된 수천 건의 자연어 출고 요청 텍스트를 AI/정규식으로 분석하여, 고객사 및 현장별 맞춤 기본 요구사항을 마스터 테이블에 자동 구축한다.
- **분석 및 추출 항목**:
  1. **필수 서류**: `비파괴검사 성적서`, `안전인증서`, `보험증권`, `반입전 검수표` 등.
  2. **장비 사양**: `협착방지봉(유/무)`, `상부센서`, `경광등`, `소화기`, `과상승방지봉`.
  3. **현장 진입 조건**: `지하 진입 가능 여부`, `바닥보양지(부직포) 필수`, `지게차 하차 필요`.
  4. **지정 기사 및 연락처**: 선호하는 운송사 및 배차 기사.
- **결과**: `customer_sites.default_requirements` JSON 필드에 영구 저장되어, 향후 신규 출고 요청 시 100% 자동 적용됨.

---

### 5.5 [파이프라인 5] 밴드 과거 AS/정비 일지 파싱 및 적재 (`ingestBandAsHistoryDirect`)
- **도메인 배경**: 밴드 게시글로 작성된 메카닉의 과거 AS 수리 일지를 파싱하여 자산별 정비 이력(`repairs`, `repair_timeline_events`)을 복원한다.
- **파싱 추출 규칙**:
  - `현장명`, `업체명`, `장비번호`(자산 관리번호 정규식 `[A-Z]\d{3,5}` 탐색), `고장 증상`, `수리 조치 내용`, `교체 부품`, `정비 일자`.
  - 추출된 자산번호를 기반으로 `assets` 및 `contracts`와 1:1 매핑하여 과거 수리비 누계액(`cumRepairCost`)을 갱신.

---

### 5.6 [부가 기능 1] 42대 테이블 원클릭 JSON 전체 백업 (`exportFullDatabaseBackup`)
- **동작**: 브라우저 메모리 및 Supabase DB에 적재된 전사 42대 테이블의 모든 데이터를 순회 조회하여 단일 JSON 객체(`backupData`)로 병합.
- **파일명 형식**: `backup_kiyeun_erp_YYYYMMDD_HHMMSS.json` 파일로 즉시 다운로드 제공.

---

### 5.7 [부가 기능 2] 42대 테이블 FK 역순 CASCADE 완전 초기화 (`resetAllDatabaseTables`)
- **동작**: 외래키 제약조건 오류를 원천 차단하기 위해, 가장 말단 자식 테이블부터 최상위 부모 테이블 순서로 `DELETE ALL`을 실행.
- **관리자 계정 보존**: `keepAdminUser = true` 옵션 선택 시 `users` 테이블의 최고관리자(`ADMIN`) 계정 및 `permissions`, `departments`, `google_configs` 테이블은 보존하여 시스템 접근 권한을 유지.

---

## 6. 자산 상태(`status`) 부여 및 라이프사이클 헌장 원칙

초기 마이그레이션 시 자산 상태는 엑셀 `Col[8] 계약구분`의 명시된 값만을 엄격히 기준으로 부여한다. **종료일 날짜가 과거라고 해서 임의로 자산을 반납/대여가능으로 전환하는 것을 엄격히 금지한다.**

| 엑셀 Col[8] 계약구분 | 자산 소유 구분 | 부여되는 계약 `status` | 부여되는 자산 `status` | 현장 바인딩 | 실무 의미 및 조치 |
|:-------------------|:-------------|:---------------------|:---------------------|:----------|:-----------------|
| **값 없음 / '신규' / 기타** | 자사 자산 (`OWNED`) | `ACTIVE` | **`RENTED`** | ✅ 유지 | 현장 정상 대여 가동 중 |
| **'연장' (종료일 경과)** | 자사 자산 (`OWNED`) | `ACTIVE` | **`RENTED`** | ✅ 유지 | **연장 합의 후 반납 미결 상태 (가동 지속)** |
| **'종료' 명시** | 자사 자산 (`OWNED`) | `COMPLETED` | **`AVAILABLE`** | ❌ 해제 | 현장 계약 종료 후 당사 주기장 입고 완료 |
| **값 없음 / '신규' / '연장'** | 전대 자산 (`RENTED`) | `ACTIVE` | **`RENTED`** | ✅ 유지 | 원사로부터 임차하여 고객사 현장 가동 중 |
| **'종료' 명시** | 전대 자산 (`RENTED`) | `COMPLETED` | **`RENTED_RETURNED`** | ❌ 해제 | 고객사 사용 종료 후 **원사(임차업체)로 반납 완료** |

---

## 7. 전대(외부 임차) 자산 및 원천 손익 원장 (`v1.2.1` 고도화)

### 7.1 장비 식별 & 재전대 라이프사이클 영속성
- **`vendorAssetNo` (원사 원래번호)**: 타사 장비의 실물 번호(예: `고려-1004`)를 기연 관리번호(예: `R-001`)와 1:1 매핑하여 DB에 영속 저장.
- **재임차 시 상태 전환 원칙**: 타사에 반납했던 장비(`RENTED_RETURNED`)가 동일 번호로 재임차되는 경우, 새 자산번호를 채번하지 않고 기존 레코드의 상태를 `AVAILABLE`로 1-Click 재활성화하여 감가상각 및 정비 이력 연속성 유지.

### 7.2 대차대조 손익 원장 (Spread Margin) 체계 연동
- **매출 원천**: 확정 청구서(`billings`) 및 세금계산서.
- **원가 원천**: 원사 매입세금계산서(`purchase_settlements`) + 배차 운송비(`deliveries`).
- **대차대조 검증**: `[계약 확정 청구액] - [매입원가 + 직송운송비] = 🟢 순마진 (스프레드 마진율 %)`.

---

## 8. 부대비용 및 외상미수금 / 타사 구상채권 (`VENDOR_CLAIM`)

### 8.1 엑셀 운반비 자동 추출 제외 원칙 (Build.63 개정)
- **도메인 원칙**: 과거 엑셀의 `Col[7] 운반비`는 과거 계약 체결 시점의 참조값이거나 날짜 오입력 셀(시리얼 번호 `46086`)이므로, **초기 DB 업로드 시 현재 시점의 미청구 외상채권(`receivables`)으로 자동 생성하지 않는다.**
- **외상미수금 생성 조건**: 외상미수금은 오직 ERP 정식 가동 후 **"실제 배차가 완료되어 고객 청구(`billableToCustomer: true`)로 승인된 건"**이나 **"입고 검수/정비 시 고객 과실로 판정된 수리비 건"** 등 현장 이벤트에 의해서만 실시간 생성된다.

### 8.2 타사 구상채권 (`type: 'VENDOR_CLAIM'`) 통합 관리
- 원사가 기연에 청구한 파손 수리비/세척비/부대비용은 `Receivables`에 `type: 'VENDOR_CLAIM'`으로 적재되며, 고객사 정기 청구서 발행 시 1회 또는 수회에 걸쳐 분할 상계 처리.

---

## 9. 대사 검증(Reconciliation Report) 6대 무결성 검증식

적재 완료 직후 시스템은 자동으로 아래 6가지 대차대조 검증을 수행하고 결과를 `reconciliation_reports` 테이블에 영구 저장한다:

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ ① 자산 수 일치식      : 엑셀 자산 행 수 (726대) = DB assets 레코드 수 (726대)          │
│ ② 당월 청구 합계 일치식: 엑셀 Col[25] 당월청구합계 = DB billings 합계 (오차 ₩0 원 단위) │
│ ③ 청구 헤더-상세 대사  : billings 공급가액 합계 = billing_details 합계 (오차 ₩0)       │
│ ④ 전대 임차료 대사    : 엑셀 Col[20] 임차단가 합계 = DB external_leases 합계          │
│ ⑤ 계약-배차 체인 검증 : contracts 건수 = 출고 의뢰 건수 (도입 후 연계)                │
│ ⑥ 고아(Orphan) 검증   : 계약 없는 자산 수 = 0, 자산 없는 계약 수 = 0                  │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 10. 사용자 운영 절차 (Step-by-Step 실무 가이드)

```
[1단계: 권한 확인 및 진입]
  - 최고관리자(ADMIN) 계정으로 ERP 로그인 ➔ [설정 / 마스터] ➔ [초기 DB 업로드] 메뉴 진입.

[2단계: (선택) 기존 데이터 백업]
  - 상단 [DB 전체 백업] 탭 클릭 ➔ [JSON 백업 파일 다운로드] 실행 (안전 자산 확보).

[3단계: (선택) DB 완전 초기화]
  - 상단 [DB 완전 초기화] 탭 클릭 ➔ [관리자 계정 보존] 체크 확인 ➔ [초기화 실행] 클릭.

[4단계: 메인 엑셀 파일 선택 및 분석]
  - [초기 데이터 적재] 탭 ➔ [초기DB현황1.xlsx] 파일 드래그앤드롭.
  - [소급 청구서 생성] 옵션 확인 (기본: ON, 시작월 2026-01 ~ 종료월 2026-07).
  - 엑셀 파싱 통계 확인 (자산 726대, 계약 1,294건, 거래처 189개사 등).

[5단계: 메인 데이터 일괄 적재 실행]
  - [전체 데이터 일괄 적재 시작] 버튼 클릭.
  - 0/13부터 13/13까지 실시간 프로그레스 바 및 청크 적재 로그 확인.

[6단계: 6대 대사 리포트 녹색 검증]
  - 적재 완료 후 화면 하단 [대사 검증 보고서]의 6개 항목이 모두 100% 녹색(Match)인지 확인.

[7단계: 배차 이력 및 밴드 데이터 부가 적재 (선택)]
  - [배차 이력 업로드] 섹션: 배차 엑셀 파일 선택 ➔ [배차 이력 일괄 적재].
  - [밴드 출고요청 분석] 섹션: 텍스트 파일 선택 ➔ [고객사 기본 요구사항 마스터 동기화].
  - [밴드 AS 이력 업로드] 섹션: 텍스트 파일 선택 ➔ [과거 AS 이력 일괄 적재].

[8단계: 최종 서버 데이터 동기화]
  - 브라우저 F5 또는 상단 [서버 데이터 전체 새로고침] 클릭 ➔ 정상 운영 개시.
```

---

## 11. 누적 이슈 및 해결 이력 (Build.22 ~ Build.56 전수 상세표)

| 빌드 | 해결 이슈 및 기능 고도화 | 근본 원인 (Root Cause) | 적용된 엔지니어링 조치 |
|:-----|:------------------------|:----------------------|:----------------------|
| **Build.22** | 자산번호 '미지정' 발생 | 당사 자산 fallback이 전대 컬럼(13) 참조 | `r[10]` 직접 읽기로 수정 |
| **Build.23** | 구 시트명 인식 불가 | `'202608'` 시트명 하드코딩 | `'계약현황' \|\| '202608'` 동적 fallback 구현 |
| **Build.24** | 헤더 누락 자산 미지정 | getCol 키 불일치로 fallback 오작동 | 직접 인덱스 파싱 방식으로 전면 교체 |
| **Build.25** | 계약 상태 오파싱 | 키 매핑 부재로 관리번호를 상태로 오인 | `r[8]`(계약구분) 직접 추출 |
| **Build.26** | 전대 장비 모델명=45845 오염 | 중복 헤더로 인해 날짜 시리얼 유입 | `r[9] \|\| r[12]` 직접 인덱스 분리 및 날짜 시리얼 방어 |
| **Build.27** | 유령 고객사(1, 2, 3...) 생성 | 순번 숫자 컬럼(`r[0]`)을 고객명으로 파싱 | `r[1]`(업체명) 직접 추출 및 숫자 스킵 로직 적용 |
| **Build.28** | 운반비 컬럼 오류 | fallback 인덱스가 임차단가(20) 참조 | `r[7]`(운반비) 직접 읽기로 수정 |
| **Build.29** | 소급 청구서 12건 누락 | 소급 기준일로 당월 기산일(Col[4]) 사용 | `Col[3]`(최초개시일) 직접 파싱으로 수정 |
| **Build.30** | 소급 청구 자동 생성 부담 | 무조건 자동 생성으로 수납 대사 혼란 | 소급 생성 여부 선택 UI 탑재 및 기본값 OFF/선택 전환 |
| **Build.31** | 만료 자산 자동 회수 처리 | 날짜 경과로 자산을 자동 AVAILABLE 처리 | 날짜 기준 자동 회수 영구 금지, `Col[8]='종료'` 명시 시에만 반영 |
| **Build.40** | 소급청구 독립 선택 생성 | 메인 적재와 소급 생성이 강결합되어 분리 불가 | `generateAndIngestHistoricalBillingsDirect` 독립 함수 신설 |
| **Build.41** | 밴드 출고요청 분석 마스터화 | 자연어 요청문 내 필수서류/진입조건 수기 관리 | 밴드 출고요청문 3,000건 분석 ➔ `customer_defaults` 자동 동기화 |
| **Build.42** | 밴드 AS 정비 이력 복원 | 수리 일지 텍스트 파싱 누락 | `ingestBandAsHistoryDirect` 신설 및 `repairs` 타임라인 연동 |
| **Build.54** | 전대 자산 원천정보 손익원장 | 전대 장비 매입/매출/운송비 파편화 | `vendorAssetNo` 탑재, 계약별/자산별 대차대조 손익원장 신설 |
| **Build.55** | DB 스키마 6대 도메인 재정돈 | 잦은 패치로 물리적 컬럼 배치 누더기화 | 전사 42대 테이블 6단계 표준 논리적 배치 & 무손실 원자적 스왑 DDL 구축 |
| **Build.56** | 엑셀 내보내기 스키마 표준 동기화 | 화면별 엑셀 출력 컬럼 순서 불일치 | 전 화면 엑셀 내보내기를 스키마 재배치 표준과 100% 동기화 |

---

## 12. 시스템 무결성 보장 선언

기연리프트 ERP 시스템의 초기 DB 업로드 엔진은 **"단 하나의 데이터 유실 없이, 현장의 실물 라이프사이클과 회계 대차대조를 1원 단위까지 100% 완벽히 일치시킨다"**는 전사 시스템 개발 표준 헌장의 원칙을 엄격히 준수한다.
