# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 좌우 분할 1:1 페어링 대사 스튜디오 & 대사 취소 & 통합 매입 지급요청 생성 시스템 구축 (v1.13.1.Build.00000)

### 1. 주요 구현 내용
1. **좌우 분할 1:1 Pair 대사 스튜디오 (Split Pair Reconciliation Studio)**:
   - 엑셀 거래명세서 업로드 시 **[좌측: 시스템 운송완료 내역] ↔ [우측: 업로드 거래명세서 엑셀 내역]** 을 좌우 분할 1:1 짝(Pair) 행으로 직관적 나란히 표시.
   - 매칭 판단 기준: **`날짜 + 하차지(도착지) + 금액`** (배차ID / 기사명 보조).

2. **스마트 자동 짝짓기 (Auto-Pairing Engine)**:
   - 엑셀 업로드 시 날짜, 하차지, 금액이 일치하는 항목을 **자동으로 1:1 Pair로 결합**하여 `🟢 대사완료` 상태 세팅.
   - 날짜/하차지는 일치하나 금액이 다른 경우 `🟡 금액 불일치 Pair` (차액 표시) 로 배치.
   - 한쪽에만 존재하는 항목은 `🔴 엑셀 전용 (시스템 미등록)` 또는 `⚪ 시스템 전용 (명세서 누락)` 으로 분리 노출.

3. **건별 대사완료 & 대사 취소 (대기 원복) & 수동 Pair 조작**:
   - 담당자가 건별로 **`[✅ 건별 대사완료]`** 및 **`[↩️ 대사 취소 (대기 전환)]`**, **`[🔗 짝짓기 변경/해제]`** 버튼 조작 가능.
   - 실수를 방지하기 위해 이미 완료된 건도 1-Click 으로 언제든 대사 취소하여 대기(`PENDING`) 상태로 원복 처리.

4. **1건의 통합 매입 지급요청 생성 (Payment Request Bundle)**:
   - 업로드된 거래명세서 엑셀의 모든 항목 대사가 완료되면, **`[💳 대사 완료 1건의 통합 매입 지급요청 생성]`** 메인 버튼 활성화.
   - 실행 시 해당 명세서에 포함된 전체 대사목록 + 엑셀 파일이 1건의 지급 요청 번호(`PAY-BUNDLE-YYYYMMDD-XXXX`) 건으로 묶여 저장 및 처리됨.

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.13.1.Build.00000).
