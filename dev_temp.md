# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 우측 패널 엑셀 카드 군더더기 전면 제거 & '비고:비고' 중복 텍스트 수술 & 슬림화 (v1.14.0.Build.00004)

### 1. 주요 구현 내용
1. **군더더기 텍스트/뱃지 전면 제거**:
   - `📄 행 #29` 불필요 행번호 표시 제거.
   - 우측 상단 금액과 중복되던 `합계: 220000` 뱃지 및 `일자:` 중복 뱃지 제거.
   - 기사 미지정 시 표시되던 빈 기사 아이콘(`🚛 -`) 및 대기 상태 시의 `⚪ 대사 대기` 불필요 뱃지 제거.
2. **`비고: 비고` 접두어 중복 텍스트 전면 수술**:
   - `비고: 비고 세보 엠이씨` ➔ `비고: 세보 엠이씨` 로 깔끔하게 정돈 노출.
3. **미니멀 & 에센셜 뷰 (Minimalistic View)**:
   - 상단 날짜(`📅 16일`) & 청구 금액(`₩220,000원`), 중앙 에센셜 컬럼 뱃지(`상차지`, `하차지`, `차종`, `수량`, `업체명`, `비고` 등)로 우측 패널 카드 정돈 완비.

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.14.0.Build.00004).
