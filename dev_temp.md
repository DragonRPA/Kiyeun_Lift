# 개발 지시 및 개편 완료 내역 (dev_temp.md)

## 🚀 [배차 이력 업로드] 실무 기억 보조 원칙: 3대 핵심 Key(날짜·업체명·금액) 기반 완전성 수집 체계 구축 (v1.3.0.Build.67 - 2026-09-03)

### 1. 개발 내용 요약
- **3대 핵심 Key 기반 유효 행 판정**: "담당자의 기억을 돕기 위한 실무적 단순기록" 원칙에 따라 장비명 기반 스킵 전면 철폐.
  - KEY 1 (날짜): 상차일/하차일 중 1개 필수
  - KEY 2 (업체명/현장명): 업체명/현장명 중 1개 필수 (합계/소계 행 제외)
  - KEY 3 (금액): 운반비 유효 금액 수집
- **숫자 4자리 모델명 누락 결함 해결**: `1008`, `1330`, `1012` 등 고소작업대 정규 모델이 숫자합계로 오인되어 누락되던 `/^\d{4,}$/` 정규식 완전 제거. 장비명은 `memo`에 `장비: [모델명]`으로 영구 보존.
- **고객사명 표출 개선**: `TruckDispatch.tsx`에서 계약 미매핑 건도 `memo`에 기록된 업체명을 추출하여 `'고객사미지정'` 대신 실제 업체명(HDC랩스, 아주렌탈 등)으로 표출.

---

### 1. 개발 내용 요약
- **현장 AS 중복 제거 및 도메인 R&R 엄격 분리**: 출장 AS 접수/스케줄러 제거, 순수 본사 주기장(Yard) 수리·점검·소모품 투입 및 임대가능 복귀 본연의 업무로 단일화.
- **주기장 정비 스튜디오 워크벤치 (헌장 3.6 유형 A)**: 좌측 정비 대상 자산 큐 + 우측 10대 퀵 정비 칩, 중앙창고 소모품 차감, Gutenberg Z-패턴 `[정비 완료 ➔ 임대가능(AVAILABLE) 전환]` 탑재.
- **자산 상태 머신(FSM) 단절 버그 시정**: 완료 시 `AVAILABLE` 즉시 전환, 이상점수 0점 리셋, 중앙창고 재고 차감 및 로그 무누락 저장.
- **주기장 정비 대장 (헌장 3.6 유형 B)**: 고밀도 기간별 조회 및 엑셀 다운로드.
- **스켈톤 연동**: `skelton/계획/2026-09_주기장_정비_관리_전면_재설계.md` 커밋/푸시 완료.

---

## 🚀 [전사 체크박스] 전 화면 잔존 체크박스 전수 점검 및 고선명 "V" 표준 컴포넌트 100% 완전 동기화 (v1.3.0.Build.65 - 2026-09-03)

### 1. 개발 내용 요약
- **전사 잔존 체크박스 전수 감사 및 정돈**: 인라인 크기(`width: 13~16px`) 및 `accentColor` 설정 전면 제거하여 `index.css`의 18×18px 고선명 화이트 "V" 벡터 체크마크와 100% 동기화.
- **적용 대상**: `smart_dispatch.tsx`, `Contracts.tsx`, `CorporateCardPage.tsx`, `FieldAsManagement.tsx`, `Products.tsx`, `users_permissions.tsx`, `App.tsx` 로그인 화면 등 전 메뉴.

---

## 🚀 [UI/UX] 전사 체크박스 고대비 선명한 "V" (Checkmark) 표준 컴포넌트 전면 적용 (v1.3.0.Build.64 - 2026-09-03)

### 1. 개발 내용 요약
- **체크박스 스타일 전면 개편**: 텍스트 인풋 스타일 상속(`appearance: none`)으로 인해 체크마크가 사라졌던 문제 수정.
- **선명한 "V" 벡터 SVG 적용**: 체크 시 선명한 화이트 "V"(`polyline`)가 블루 박스 안에 뚜렷하게 보이도록 전사 CSS 일괄 적용.
- **패널 배경 톤 정돈**: `smart_dispatch.tsx` 고객사 기본값 등록 패널의 배경 대비 개선.

---

## 🚀 [외상미수금 대장] 초기DB 마이그레이션 과거 운반비 자동생성 완전 제거 및 허위 75건 전량 정화 (v1.3.0.Build.63 - 2026-09-03)

### 1. 개발 내용 요약
- **외상미수금 도메인 원칙 회복**: 과거 엑셀의 Col[7] 운반비를 현재 시점의 미청구 외상(`receivables`)으로 생성하던 로직 완전 제거 (`migrationEngine.ts`).
- **허위 데이터 75건 전량 삭제**: 엑셀 날짜 `2026-03-05` (시리얼 `46086`)가 46,086원 운반비로 둔갑해 생성되었던 75건 (₩3,456,450)을 Supabase DB에서 삭제 정화 완료.
- **문서 동기화**: `INITIAL_DB_UPLOAD.md` 8.1조 개정.

---

## 🚀 [당사자산 취득/매각] 상하 간격 고밀도 압축 및 1화면 100% 원스크린 완결 (v1.3.0.Build.62 - 2026-09-03)

### 1. 개발 내용 요약
- **상하 여백 압축**: 타이틀/요약바/탭/카드 간 `margin-bottom: 24px` ➔ `10px`로 통일 압축.
- **3열(3-Column) 고밀도 그리드 배치**: 폼 필드를 3열 그리드로 정렬하여 스크롤 없이 **1화면 안에 취득/매각 폼과 [확정 버튼]까지 모두 표시**되도록 최적화.

---

## 🚀 [화면 하단 뷰포트 100% 활용] 테이블 고정 높이 제거 및 남는 하단 공간 전면 확장 (v1.3.0.Build.61 - 2026-09-03)

### 1. 개발 내용 요약
- **제품 모델 관리(`Products.tsx`)**: 하드코딩된 `maxHeight: 590px` 제거, `flex: 1, height: 100%, minHeight: 0`을 통해 화면 하단 끝까지 테이블이 100% 꽉 차도록 확장.
- **매입처 관리(`Vendors.tsx`)**: 동일하게 `flex: 1, maxHeight: none` 적용하여 하단 여백 완전 제거.

---

## 🚀 [매출 청구] 기본 UI 고밀도 간소화 및 작업대 면적 85% 극대화 (v1.3.0.Build.60 - 2026-09-03)

### 1. 개발 내용 요약
- **타이틀/탭 간소화**: `매출 청구 관리` 및 3대 탭(`[미청구 정산]`, `[청구 대장]`, `[청구서통합]`)
- **1줄 컴팩트 필터 바**: 2단 흩어진 필터를 가로 1열 고밀도 바로 압축 + `Enter` 키 즉시 조회
- **1줄 실시간 집계 요약 스트립**: 거대 6개 KPI 박스를 1줄 슬림 요약 바로 전환 (화면 수직 작업대 150px+ 확보)

---

## 🚀 [청구서통합] UI 전사 단일 표준 명칭 '청구서통합' 변경 및 DB 스키마 정합성 보완 (v1.3.0.Build.59 - 2026-09-03)

### 1. 개발 내용 요약
- **UI 표기 전면 '청구서통합' 표준화**: `Billings.tsx` 메인 탭 명칭(`청구 인보이스` ➔ `청구서통합`), `BillingInvoiceTab.tsx` 툴바 및 버튼(`[청구서통합 생성]`, `[기존 청구 소급 묶기]`), 테이블 컬럼(`통합청구번호`), 안내 메시지 전면 변경 완료.
- **DB 스키마 및 마이그레이션 엔진 정합성 완결**: `schema.sql` `billing_invoices` 마스터 및 `billings.invoiceId` 외래키 동기화, `migrationEngine.ts` `TABLE_COLUMNS` 화이트리스트 동기화 완료.

---

## 🚀 [모바일 PWA] 현장 기동 업무 전용 모바일 PWA (`/m`) 분리 구축 완결 (v1.3.0.Build.58 - 2026-09-02)

### 1. 개발 내용 요약
- **모바일 전용 셸 및 라우팅 분리 (`src/mobile/MobileApp.tsx`, `App.tsx`)**: URL `/m`, `?view=mobile` 쿼리, 또는 768px 미만 모바일 기기 접속 시 모바일 전용 PWA 레이아웃 자동 구동.
- **PC / 모바일 1-Click 상호 전환**: PC 헤더 `[📱 모바일화면]` / 모바일 헤더 `[🖥️ PC화면]` 전환 토글 상시 배치.
- **2대 필드 전용 테마 & 56px+ 대형 터치 아키텍처**: `Field High-Contrast Dark` (`#0B0F19` Deep Dark) 테마로 야외 직사광선 아래 시인성 극대화 및 배터리 절약. 횡스크롤 0% 수직 카드 피드 및 하단 5버튼 고정 내비게이션 바.
- **4대 핵심 모바일 스튜디오**:
  1. **현장 AS 360도 스튜디오 (`MobileAsDetail.tsx`, `MobileAsList.tsx`, `MobileAsCreate.tsx`)**: TMAP 길안내, 전화걸기, 전/후 사진 촬영 (`CameraUploader.tsx`), 차량 탑차 부품 차감 (`RepairPartUsed`), 터치 고객 서명 (`SignatureCanvas.tsx`).
  2. **배차 운송 지시 카드 (`MobileDispatchList.tsx`)**: 상/하차지 확인, 원터치 통화, 하차 완료 1-Click 보고.
  3. **출고 전 기능 검수 스튜디오 (`MobileInspectionList.tsx`)**: 10대 법정/기능 점검 체크리스트, 외관 사진 촬영 및 승인 시 자산 상태 `RENTED` 자동 전환 (헌장 제1.3조 준수).
  4. **가용 렌탈 자산 실시간 조회기 (`MobileAssetSearch.tsx`)**: 영업용 19~53ft 규격별 실시간 재고 & 단가 조회.
- **Skelton 기록 완결**: `skelton/계획/` 및 `skelton/경험/` 2건 커밋 & 푸시 완료.

---

## 🚀 [엑셀 내보내기] 전사 대장 스키마 6단계 논리적 배치 표준 동기화 완결 (v1.2.1.Build.56 - 2026-09-02)

### 1. 개발 내용 요약
- **자산 대장 (`Assets.tsx`)**: `vendorAssetNo` 추가 및 6단계 논리적 컬럼 순서 재배치.
- **배차 대장 (`Deliveries.tsx`)**: 타사 직출고지/혼적 경유지 및 확정운송비 반영 논리적 순서 재정돈.
- **임차 자산 & 전대 손익 원장 (`rent_assets.tsx`)**: 임차대장 서식화 및 `[📥 전대 손익 원장 엑셀 다운로드]` 신설.
- **외상미수금 대장 (`Receivables.tsx`)**: `[📥 엑셀 다운로드]` 신설 (타사구상채권/부대비용 대사 서식).
- **정비/AS, 계약, 청구 대장 동기화**: 수식 계산 호환성 및 표준 스키마 100% 일치.

---

## 🚀 [DB 스키마] 전사 42대 테이블 6대 도메인 표준 논리적 배치 전면 재정돈 & 무손실 원자적 스왑 DDL 패치 (v1.2.1.Build.55 - 2026-09-02)

### 1. 개발 내용 요약
- **단일 진실의 원천(SSOT) [`schema.sql`](file:///d:/01.AntiGravity/Kiyuen_Lift/schema.sql) 전면 리팩토링**: 누적 패치로 누더기화되었던 중복 테이블 및 흩어진 컬럼들을 전면 정돈, 전사 단일 표준 6단계 논리적 컬럼 배치 원칙 100% 적용.
- **기존 운영 데이터 100% 무손실 보존 원자적 테이블 스왑 마이그레이션 DDL 구축**: [`scripts/reorganize_tables_zero_loss.sql`](file:///d:/01.AntiGravity/Kiyuen_Lift/scripts/reorganize_tables_zero_loss.sql) 생성.

---

## 🚀 [전대(임차) 자산] 5대 마스터 라이프사이클 & 원천정보 기반 대차대조 손익 원장 시스템 구축 (v1.2.1.Build.54 - 2026-09-02)

### 1. 개발 내용 요약
- **장비 식별 & 재임차 라이프사이클**: `vendorAssetNo` 매핑, 번호 난발 방지, 반납 후 1-Click 재임차 활성화.
- **원천정보 기반 대차대조 전대 손익 원장 (`PROFIT_LEDGER` 탭)**: 계약별 및 자산별 매출 청구서 vs 매입세금계산서 + 운송비 대조 순마진/마진율 실시간 산출.
- **단일 경유 혼적 회수 배차 및 직출고 체인 (`TruckDispatch.tsx`)**: 직출고 상차지 및 복수 하차지 경유 배차표 지원.
- **타사 부대비용 구상 미수금 등록 (`Receivables.tsx` & `rent_assets.tsx`)**: `VENDOR_CLAIM` 구상 채권 생성 및 고객 청구서 분할 상계 지원.

---

## 🚀 [현장 AS] 기사별 선호 내비(T맵/카카오/네이버) 맞춤 연동 & 전화·출발 타임라인 자동 로깅 시스템 구축 (v1.2.1.Build.53 - 2026-09-02)

### 1. 개발 내용 요약
- **기사별 맞춤형 3대 내비게이션(T맵 / 카카오내비 / 네이버지도) 원터치 딥링크 연동**: 기사 스마트폰 기본 내비 기억 기능, 상단 1초 내비 변경 스위처, 3대 내비 선택 바텀시트 모달.
- **전화걸기 & 이동 시작 무자각 타임라인 자동 로깅 (`logFieldAsTimelineEvent`)**: `[📞 전화걸기]` 시 `CALL_MADE`, `[🗺️ 내비 길안내]` 시 `TRANSIT_START` 타임라인 자동 저장 및 티켓 상태 자동 전환.
- **현장 티켓 카드 내 최근 진행 이력 실시간 표출**.

---

## 🚀 [모바일 현장 AS] 갤럭시 S24 최적화 터치 UI & 현장 부품 원스톱 실시간 자동 차감 시스템 구축 (v1.2.1.Build.52 - 2026-09-02)

### 1. 개발 내용 요약
- **AS팀원 전용 모바일 최적화 터치 UI 구축 (`FieldAsManagement.tsx`)**: 갤럭시 S24(393×852px) 기준 1열 세로 스택 자동 전환, 3대 상단 탭(`[출동 대기]`, `[내차 부품고]`, `[완료 내역]`), 1-Touch 전화걸기(`tel:`) 및 카카오내비 딥링크.
- **50px 대형 스텝퍼 부품 카트 & 원스톱 차량 재고 자동 차감**: 가상 키보드 팝업을 차단하고 작업 장갑 상태에서도 편한 50px 대형 스텝퍼 `[➖]` `[➕]` 버튼, AS 완료 시 차량 재고(`mechanic_consumable_stocks`) 자동 차감 및 마이너스 방지.
- **한 손 조작 슬라이드업 바텀시트**: 1초 원터치 빈출 조치 태그 칩, 다이렉트 후면 카메라 촬영(`capture="environment"`), 최하단 고정 54px 초록색 완료 버튼.
- **모바일 ↔ PC 대화면 뷰 토글 스위처 탑재**.

---

## 🚀 [초기DB업로드] 밴드 과거 AS 빅데이터(4,262건) 전수 분석 & 정비 마스터(`repairs`) DB 일괄 동기화 작업대 구축 (v1.2.1.Build.51 - 2026-09-02)

### 1. 개발 내용 요약
- **AS 빅데이터 전수 파싱 엔진 (`parseBandAsHistoryText`)**: `(AS)band_as_history_all.txt` 1.4MB 대용량 파일 내 4,262건의 AS 게시글을 1초 만에 전수 파싱. 작성자/정비사, 접수일자, 고객사, 현장명, 관리번호(자산), 고장내용, 접수자 연락처, 조치결과 키워드 추출.
- **5대 매트릭스 & 1대 단독계약 자동 추정 엔진 (`analyzeBandAsHistory`)**:
  - **사장님 확정 1번 원칙 반영**: 관리번호가 생략되었거나 '전체장비'인 경우, 단 1대뿐인 단독 계약 현장이면 해당 자산으로 자동 추정 매핑하여 자산 이력까지 완벽 복원.
  - **작성자 ➔ 정비사 1:1 매핑**: 최영석, 한상찬, 김재현, 김동우 등 사내 정비사 마스터와 자동 연결.
  - **조치 상태 3대 자동 분류**: 현장 조치완료(`COMPLETED`), 익일 재방문(`REVISIT`), 전화 안내종결(`GUIDED`).
- **초기DB업로드 메뉴 내 고밀도 AS 분석 작업대 탑재 (`InitialDbUploader.tsx`)**:
  - **5대 핵심 지표 바**: `총 AS 분석 건수`, `고유 장비 매핑`, `유효 계약 연동(1대 추정 포함)`, `현장 조치완료`, `익일방문/안내`.
  - **인라인 다기능 필터 & 검색**: 현장/고객/장비/고장/작성자 실시간 검색, 상태별 및 계약 연동별 즉시 필터링.
  - **고밀도 1:1 대사 그리드 & 단건 상세 모달**: 행 높이 38px 슬림 테이블 및 원문 대조 팝업 제공.
- **원클릭 정비 마스터(`repairs`) & 자산/계약 타임라인 일괄 적재 (`ingestBandAsHistoryDirect`)**:
  - `repairs` 단일 테이블 적재 및 `assetInOutLogs`, `contract_history`(`AS_SERVICE`) 동시 무누락 저장.
  - 중복 방지(Deduplication) 및 `await db.awaitPendingWrites()` 동기 검증을 통한 Zero Silent Failure 보장.

---

## 🚀 [현장 AS & 내근 정비 통합 관리체계(`repairs`) 및 계약 이력(타임라인) 양방향 연동 시스템 구축] (v1.2.1.Build.50 - 2026-09-02)

### 1. 사장님 확정 핵심 도메인 원칙 100% 완벽 이행
- **5대 매트릭스 AS 기록 & 단독 계약 자산 자동 추정 (사장님 1번 확정 원칙)**:
  - 관리번호, 고객/현장, 유효계약 3대 조건 조합에 따라 정비 대장(`repairs`), 자산 이력(`assetInOutLogs`), 계약 타임라인(`contract_history`)에 100% 무누락 자동 기록.
  - 현장 계약에 장비가 **단 1대뿐인 단독 계약**인 경우, 관리번호가 누락되어도 해당 1대 자산으로 자동 추정 매핑하여 자산 이력까지 완벽 복원. 복수 장비 계약은 자산 오염 방지를 위해 계약 이력에만 안전 기록.
- **수리비 독립 관리 원칙 (사장님 2번 확정 원칙)**:
  - 유상 수리비(`billableAmount`)는 정기 렌탈료 청구서(`billings`)와 혼선되지 않도록 **정비 대장(`repairs`)에서 독립적으로 집계 및 수납 관리**.
- **계약 상세 화면(`Contracts.tsx`) 360도 AS 이력 전용 섹션 신설**:
  - **섹션 5**: `[🛠️ 계약 현장 AS 및 정비 이력 ({contractRepairs.length}건)]` 고밀도 대사 테이블 탑재.
  - **⚠️ AS 빈발 알림 뱃지**: 동일 계약 현장에서 AS가 2회 이상 발생 시 `[⚠️ AS N회 발생 - 장비 대차/교체 검토 요망]` 경고 뱃지 자동 표출.
  - **계약 변경 및 이력 타임라인(`contract_history`)**: `AS_SERVICE` 이벤트 자동 누적 기록.

---

## 🚀 [초기DB업로드] 밴드 출고요청 분석 기반 유효 계약 고객·현장 기본 요구사항(옵션·보양·스펙) 마스터 DB 동기화 시스템 구축 (v1.2.1.Build.49 - 2026-09-02)

### 1. 사장님 확정 3대 도메인 원칙 100% 완벽 이행
- **유효 계약처 선별 필터링 (Contract Scope Filter)**: 현재 DB에 유효 계약(`contracts`)이 존재하는 고객사 및 현장만 선별 매칭하여 업데이트. 계약이 없는 과거 거래처, 이미 거래 종료된 고객, 완공된 과거 현장은 대장 오염 방지를 위해 100% 안전하게 [제외] 처리.
- **시계열 최신값 우선 원칙 (Latest Timestamp Precedence)**: 동일 고객/현장에 복수의 출고요청이 존재할 경우, 타임스탬프 기준 가장 최신 글(Latest Entry)의 값을 최우선 적용.
- **빈칸 안전 보완 원칙 (Fill Missing Fields Only)**: 기존 마스터에 이미 등록된 데이터는 훼손하지 않고, 비어있는 옵션/보양/스펙/담당자 항목만 안전하게 채움.

### 2. 세부 구현 내역
- **DB 스키마 정합성 보강 (`schema.sql` & `db.ts`)**: `Customer` 인터페이스 및 `customers` 테이블에 `specialNotes TEXT` (거래처 특이사항 메모) 정식 추가.
- **마이그레이션 엔진 신설 (`migrationEngine.ts`)**: `parseDispatchHistoryText`, `analyzeDispatchHistoryForCustomerDefaults`, `ingestCustomerDefaultsFromDispatchHistory` 함수 구현.
- **초기DB업로드 UI 화면 전용 작업대 신설 (`InitialDbUploader.tsx`)**: Card ⑤ 탑재, 5대 통계 지표, 고객별 기본 요구사항 고밀도 대사 그리드, 제외 상세 목록 모달, 원클릭 일괄 DB 동기화 버튼 탑재.

---

## 🚀 [고객·현장별 옵션·보양·기술스펙 자동 재사용 및 전사 일괄 상속 시스템 구축] (v1.2.1.Build.48 - 2026-09-02)

### 1. 사장님 지시사항 100% 완벽 이행
- **고객사 기본 옵션·보양 마스터 & 자동 재사용 (Auto-Reuse)**:
  - `customers` 테이블에 `defaultPaidOptions`(기본 유상옵션), `defaultProtection`(기본 보양작업), `defaultCheckedSpecs`(기본 21대 기술요구스펙) 컬럼 및 인터페이스 추가.
  - 고객사 등록/수정 모달(`Customers.tsx`)에서 기본 옵션/보양 및 21대 표준 스펙 체크리스트를 한 번 저장해 두면, 향후 모든 출고요청(`SmartDispatch`) 시 자동 상속 및 재사용되어 반복 작성 수고를 100% 제거.
- **현장 전용 옵션·보양 설정 & 3계층 상속 체계 (3-Tier Hierarchy)**:
  - `customer_sites` 테이블에 `paidOptions`, `protection`, `checkedSpecs` 컬럼 추가.
  - 옵션/보양/스펙 해석 3계층 우선순위 정립:
    - **1순위**: 출고의뢰 텍스트 명시 입력값
    - **2순위**: 현장 전용 설정값 (`site.paidOptions`, `site.protection`, `site.checkedSpecs`)
    - **3순위**: 고객사 기본값 (`customer.defaultPaidOptions`, `customer.defaultProtection`, `customer.defaultCheckedSpecs`)
    - **기술스펙**: 텍스트 추출 스펙 ∪ 현장 스펙 ∪ 고객사 기본 스펙 **지능형 합집합(Union) 자동 체크**.
- **전사 현장 일괄 전파 (Batch Propagation)**:
  - **스마트 출고 화면(`SmartDispatch.tsx`)**:
    - `[☑ 이 옵션·보양·스펙을 고객사 기본 설정으로 등록]` 체크 시 고객 마스터에 즉시 동기화.
    - `[☑ 등록된 모든 현장에도 이 옵션·보양을 동일하게 일괄 적용]` 체크 시 해당 고객사의 모든 현장에 일괄 전파.
  - **고객 관리 화면(`Customers.tsx`)**:
    - 고객사 모달 및 상세 카드에서 `[⚡ 모든 현장에 기본값 일괄 적용]` 원클릭 버튼 제공.
    - 현장 모달에서 `[✨ 고객사 기본값 불러오기]` 원클릭 동기화 버튼 제공.
    - 현장 목록 테이블에 등록된 옵션/보양 뱃지 실시간 표시.
- **단일 진실의 원천(SSOT) 21대 표준 스펙 공유 (`db.ts`)**:
  - 21대 고소작업대 표준 기술 요구스펙(`STANDARD_SPECS`)을 `db.ts`에 전사 SSOT로 정의하여 `smart_dispatch.tsx`와 `Customers.tsx`에서 100% 일관되게 공유 참조.

---

## 🚀 [스마트 출고요청 고객·현장 3대 지능형 처리 로직 구축] (v1.2.1.Build.47 - 2026-09-02)

### 1. 사장님 지시사항 100% 완벽 이행
- **🛡️ 1. 방어 가드 (Validation Guard)**:
  - 신규 고객이거나 기존 DB에 현장 상세 주소 / 현장담당자 연락처가 없는데 생략한 채 저장을 시도할 경우, **다음 단계 진행을 강력히 방어 및 차단**하여 깡통 데이터 생성을 원천 방지.
- **⚡ 2. 지능형 자동 상속 (Auto-Inheritance)**:
  - 텍스트 파싱(`handleParse`) 및 고객/현장명 입력 시, 텍스트에서 생략된 정보(현장주소, 현장담당자, 청구담당자, 계산서 메일, 마감일, 결제일 등)를 기존 고객/현장 DB에서 **100% 자동 상속 대입**.
  - 화면에 `[⚡ 기존 DB 자동 상속됨]` 상태 배지 및 각 입력 필드별 상속 태그 표출.
- **🔄 3. 최신화 동기화 업데이트 (Update & Augment)**:
  - 기존 고객/현장이 매핑된 상태에서 새로운 현장 주소, 새 담당자 연락처, 새 계산서 이메일이 입력되면, **고객 마스터(`customers`), 현장(`sites`), 담당자(`contacts`) 테이블을 최신값으로 즉시 동기화 UPDATE 및 신규 등록**.
- **📋 메신저 동의어 전수 포용 정규식 파서 업그레이드**:
  - `고객사명/고객명/업체/상호`, `현장상세주소/주소/배송지`, `상차/하차/도착`, `신청모델목록/모델명/규격`, `유상옵션`, `보양작업`, `마감일/결제일` 등 메신저 자유 형식 완벽 파싱 지원.

---

## 🚀 [외근 AS & 내근 정비 DB 스키마 물리 통합 및 자산 생애주기 연동] (v1.2.1.Build.45 - 2026-09-02)

### 1. 사장님 4대 인터뷰 결정사항 100% 완벽 이행
- **1-A. 단일 물리 통합 스키마 (`repairs`)**:
  - `fieldAsTickets` 테이블을 물리적으로 완전 제거하고 `repairs` 단일 테이블로 승격 통합.
  - `workCategory`('FIELD_AS' | 'YARD_INTERNAL'), `workLocation`('SITE' | 'YARD'), `stockSource`('VEHICLE_VAN' | 'CENTRAL_HQ') 등 통합 컬럼 탑재.
- **2-B. 미등록 자산 관리대장 오염 방지**:
  - 밴드 5,518건 적재 시 `assets` 마스터에 등록된 자산은 1:1 매핑, 미등록 관리번호는 정비이력에 `assetNo` 텍스트로 안전 보존.
- **3-B. 자산 상태 자동 임대가능 전환 금지**:
  - 정비가 부분 수리일 수 있으므로 내근 주기장 정비 완료 시에도 `AVAILABLE`로 자동 전환하지 않고 상태 무왜곡 보존.
- **4-A. 밴드 작성자 ➔ `users` 이름 1:1 자동 매칭**:
  - 밴드 게시글 작성자명을 직원 목록(`users`)과 1:1 자동 매칭하여 `mechanicId` 및 `mechanicName` 상속 연동.
- **자산 생애주기 타임라인 통합 (`AssetHistory.tsx`)**:
  - `AssetHistory`의 `REPAIR` 탭 및 자산 타임라인에서 외근 AS와 내근 정비가 일자, 관리번호, 모델명, 구분, 현장, 조치내용, 정비자, 비용과 함께 단일 타임라인으로 완벽히 표출.

---

## 🚀 [현장 AS 일정관리 & 차량 소모품 빅데이터 연동 시스템 구축] (v1.2.1.Build.44 - 2026-09-02)

### 1. 사장님 지시사항 및 R&R 100% 완벽 이행
- **`[AS 요청]` (`SmartAsRequest`)**: 영업사원 전용 10초 간이 의뢰 발행 화면 신설.
- **`[현장 AS 관리]` (`FieldAsManagement`)**:
  - **탭 1 (출동 스튜디오)**: 밴드 피드 카드뷰 + 1-Click 조치 패널 (기사 차량 재고 실시간 차감, 수거 부품 관리, 전/후 사진 첨부, 우하단 완료 버튼).
  - **탭 2 (AS 처리 대장)**: 38~42px 슬림 테이블로 5,518건 과거 데이터 포함 고밀도 그리드 및 엑셀 다운로드, 자산별 과거 AS 이력 드릴다운 모달.
  - **탭 3 (차량별 부품 적재 현황)**: 기사별 차량 소모품 모니터 및 주기장 ➔ 차량 이동(불출) 지원.
- **`[주기장 정비 관리]` (`Repairs`)**: 비임대 자산(임대가능, 수리중) 주기장 내부 정비 전담으로 명칭 및 역할 분리.
- **밴드 5,518건 빅데이터 전수 탑재 파이프라인**: 5,518건 게시글 및 2,171대 고유 장비 이력 시스템 기초 데이터 연동.

---

### 1. 사장님 지시사항 완벽 이행
- **입금액 대비 수납처리 완료 결과 대조**:
  - `[입금액 대비 수납결과]` 컬럼에서 총 입금액(+7,900,000원)과 함께 수납 완료 금액(`usedDepositAmount`) 및 잔여 가용잔액(`remBal`)을 2단 대조 표출.
  - 전액 수납 완결 시 `✓ 전액 수납완결 (7,900,000원)` 초록 배지 표출.
  - 일부 수납 시 `수납 5,643,000원 / 잔여 2,257,000원` 분할 표출.
  - 미수납 시 `미수납 (가용 7,900,000원)` 회색 배지 표출.
- **출금액 대비 매입정산 결과 대조**:
  - `[출금액 대비 정산결과]` 컬럼에서 총 출금액(-1,500,000원)과 함께 매입정산 대사 완료 금액(`matchedWithdrawAmt`)을 2단 대조 표출.
  - 매입정산 대사 완료 시 `✓ 정산대사 완료 (1,500,000원)` 초록 배지 표출.
  - 미대사 시 `⚠️ 미대사 출금 (-1,500,000원)` 경고 배지 표출.
- **상단 요약 바 금액 대조 집계 연동**:
  - `📥 입금 수납 대사`: 총 입금액 중 수납완료 금액(%) 및 미수납 잔액 실시간 합산 표출.
  - `💸 출금 지급 대사`: 총 출금액 중 정산대사 금액(%) 및 미대사 잔액 실시간 합산 표출.

### 2. 주요 수정 파일
- `src/pages/BankMatching.tsx`

---
**기록 일시**: 2026-08-31 17:16  
**작성 버전**: `v0.5.3.Build.76`






---

## [보류] 장비 할당 화면 - 전대 계약 미할당 오인 문제 (2026-09-01)

### 현상
- 장비 할당 화면 출고 대기 요청건에 전대(임차) 계약 530건이 미할당으로 표시됨
- 실제로 할당이 필요한 건은 당사 자산 미등록 31건뿐

### 원인
- asset_assignment.tsx L38: contractAssets.filter(ca => !ca.assetId) 에서 assetId=null인 모든 행을 출고 대기로 표시
- 전대 자산은 external_leases 테이블에 저장되므로 contract_assets.assetId가 구조상 null

### 발생 원천 (엑셀)
- 계약현황 시트 Col[12] 장비명(전대)이 있는 행 530건
- 당사 자산 미등록 31건 (G19911, G19912, G19913, 13004, G28881~G28886 등)

### 검토 필요 사항
1. 전대 계약행은 장비 할당 대상에서 제외할지 여부 (정책 결정 필요)
2. 당사 자산 미등록 31건 선제 자동등록 로직 검토

### 상태: 보류 - 사장님 판단 후 지시 대기
