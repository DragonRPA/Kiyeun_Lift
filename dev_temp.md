# 대기 중인 개발 및 설계 요구사항 목록 (Pending Requirements)

## [2026-07-21] 급여 정산 권한 독점 관리 및 보안 정책
- **요구사항**: 전체 임직원 중 급여 정산(payroll) 권한은 매우 민감한 보안 정보이므로, `ADMIN` 등급이 아닌 직원 중에는 오직 **단 1명**만 보유할 수 있도록 제한해야 합니다.
- **제어 로직**:
  - 권한 관리 메뉴(`UsersPermissions.tsx`)에서 일반 사용자에게 `급여 정산` 메뉴의 조회(View) 혹은 저장(Save) 권한을 부여할 때, 이미 다른 비-ADMIN 사용자가 해당 권한을 가지고 있다면 부여할 수 없도록 차단하고 경고창(Alert)을 띄웁니다.
  - 명시적으로 기존 권한 소유 직원의 권한을 체크 해제하여 제거한 후에만 다른 직원에게 추가로 부여할 수 있습니다.
  - 일괄 저장(`handleSaveAll`) 시점에도 최종적으로 비-ADMIN 임직원 중 권한 소유자가 1명을 초과하는지 이중 검증하여 보안 정합성을 보장합니다.
- **UI 보완**: 대상 메뉴명 옆에 **`⚠️ 일반직원 중 단 1명 제한`**과 같은 명시적인 보안 안내 배지를 추가하여 시인성을 높입니다.

## [2026-07-21] 안전한 급여 정산 연산 엔진 및 암호화 명세서 이메일 자동 발송 시스템 구현
- **요구사항**: 연봉/기본급 정보를 기반으로 통상시급(209시간 기준) 및 1일 통상임금을 도출하여 무급/유급 휴가 공제 및 연장, 휴일, 야근 수당을 자동 산정하는 연산 모듈을 구축하고, 세무회계법인 업로드 자료와 결합하여 실수령액을 계산합니다. 최종 승인 시 개인 사원 정보에 등록된 이메일 주소로 생년월일 암호화 처리된 명세서 PDF를 첨부하여 전송합니다.
- **주요 기능 설계**:
  - **급여 연산 로직**:
    - 통상 시급: `월 기본급 / 209`
    - 1일 통상 임금: `통상 시급 * 8`
    - 연장/휴일 수당: `근무시간 * 통상시급 * 1.5` (8시간 초과 휴일 근무는 2.0 적용)
    - 야간 수당: `근무시간 * 통상시급 * 0.5`
    - 무급휴가 차감액: `무급휴가 일수 * 1일 통상임금`
    - 수동 가감 항목: 오차 조정 및 예외적 상여금 지급 등을 대비해 기능적으로 구현하되 기본값은 `0`으로 처리.
  - **세무회계자료 연동**: 세무법인이 매달 급여일 이전에 고정된 Excel 형식(국민연금, 건강보험, 요양보험, 고용보험, 근로소득세, 지방소득세)으로 제공해주는 공제내역을 업로드하여 직원ID와 자동 1:1 매칭 적재합니다. (수동 조작 전면 차단)
  - **교차 검증 및 Lock**: 작성 후 최종 승인 상태(`APPROVED`)로 변경되면 급여 정보는 전면 수정 불가능(Read-only) 상태로 락이 걸립니다.
  - **암호화 명세서 발송**: 근로기준법 제48조를 만족하는 이메일 발송 큐를 신설하며, 명세서 PDF는 생년월일 6자리 패스워드로 암호화되어 안전하게 발송됩니다.

## [2026-07-21] 법인카드 사용 내역 업로드 및 전사 월별 총매입정산 연동 시스템 설계
- **요구사항**: 카드사 제공 법인카드 거래내역(승인번호, 카드번호, 사용처, 결제액 등)을 업로드하여 전사 지출 항목으로 대조 분류하고, 당월 총매입정산(임차, 자재, 물류비 합산) 대장에 누락 없이 정산 및 반영합니다.
- **주요 기능 설계**:
  - **법인카드 및 내역 DTO 설계**: 카드 고유 번호 마스터와 개별 거래 승인정보를 저장하는 DTO 구축 (승인번호 PK를 통한 중복 업로드 원천 방지).
  - **대조/매핑 파이프라인**:
    - `MAPPED`: 가맹점명 및 결제액 기준, 이미 적재되어 있는 소모품 매입비, 장비 수리 정비비, 물류 운송료 내역과 1차 자동 교차 매치하여 결제 수단을 "법인카드"로 자동 설정합니다. (이중 회계 계상 방지)
    - `GENERAL_EXPENSE`: 자동 매핑되지 않는 식비, 주유비 등의 일반 항목들은 담당자가 직접 회계 계정 코드(복리후생, 여비교통 등)를 부여하여 지출로 잡습니다.
  - **전사 총매입정산 대장 통합**:
    - 월별 매입 대장(임차매입 + 배차물류비 + 자재매입 + 법인카드 일반지출)을 최종 통합하여 전사의 월 지출액 흐름을 일목요연하게 모니터링합니다.
    - 국세청 매입 세금계산서 수취 총액 and ERP 총매입정산 대장의 지출 총액을 비교하는 최종 검증 기능을 탑재하여 증빙 누락을 검출합니다.

## [2026-07-21] 13대 매입유형 및 추가 보완 지출정산 통합 아키텍처 설계
- **요구사항**:  임차료, 물류비, 소모품 외상매입 외에 택배비, 지급수수료, 임대료(사무실/차량 리스), 세금/공과금, 식대정산, 렌탈 정수기/다과비 등 전사 13대 지출 유형에 대한 정산 파이프라인 설계 및 누락 항목 보완.
- **보완 제안 항목**:
  - **인건비(월 급여)**: 급여 정산 연동 지출.
  - **소액 현금 지출**: 영수증 증빙 소액 시무 계정 정산.
  - **장비 취득 매입원가**: 고소작업대 CAPEX 신규 매입.
  - **통장 자동이체 항목**: 통신비, 보안업체 비용 등 통장 매치 자동 연계.
- **이중 계상 방지 가이드**: 법인카드 결제 건과 세금계산서 발행 매입 건의 1:1 대조 매핑을 통한 중복 청구 및 회계 계상 방지.

## [2026-07-21] 동적 매입유형 항목 관리 설정 및 지출 누락 방지 실시간 검증 엔진 구현
- **요구사항**: 사용자가 매입(지출) 유형의 항목을 직접 추가, 수정, 삭제(활성/비활성 처리)할 수 있는 동적 관리 메뉴를 제공하고, 이를 기준으로 월별 매입 정산 시 거래 내역의 존재성 및 기본 예상액과의 오차를 정밀 대조하여 지급 누락을 방지합니다.
- **주요 기능 설계**:
  - **매입유형 설정 UI**: 항목명, 계정과목, 기본 예상지출금액, 결제수단, 증빙 필수 여부를 설정할 수 있는 마스터 설정 창 구현.
  - **지능형 누락 검증 엔진**:
    - 활성화된 매입 유형 중 당월 결제 전표가 0원 혹은 누락된 카테고리를 자동 분석하여 **`⚠️ 미입력/누락 의심`** 경고 렌더링.
    - 기본 예상금액 대비 당월 실 정산액의 오차가 30%를 초과하는 경우 **`💡 이상 오차 발생`** 알림 제공.

## [2026-07-21] 세무대행 수선비 및 세부 과세기준 별 세금 항목 보완
- **요구사항**: 동적 매입유형에 `회계/세무 기장대행료` 항목을 기본 추가하고, '세금' 단일 항목을 부가세, 원천세, 지방소득세, 법인세, 주민세, 자동차세 등 세부 과세기준에 따른 유형들로 분할하여 개별 추적 및 정산 누락을 방지합니다.

## [2026-07-21] 일별 CashFlow(현금흐름) 30일 모의 예측 및 CAPEX 모니터링 시스템 구현
- **요구사항**: 매일 1회 전사의 자금 상태를 검토하기 위해 주거래 통장 잔액을 기초값으로 잡고, 향후 1개월간 청구 미수금 납기일 기반의 수납예정액과 매입 지출예정액(OPEX 및 CAPEX 분리)을 가감하여 30일간의 일자별 누적 현금 흐름을 시뮬레이션 및 시각화합니다.
- **주요 기능 설계**:
  - **CashFlow 연산식**: `현재 잔액 + 누적 수납예정액 - 누적 일반지출 - 누적 설비투자(CAPEX)` (수납 기일 지연 확률 계산 배제, 오직 절대 납기일 기준으로 처리)
  - **자금 고갈 조기 경보**: 30일 예측 기간 내 특정 일자의 누적 잔고가 마이너스(부도 위험)로 내려갈 시 **`🔴 부도 위험 경고`** 및 고갈 예상일자를 대시보드 최상단에 빨간색 경보로 팝업 노출하여 의사결정을 지원합니다.
  - **CAPEX 지출 격리**: 고소작업대 기계장치 등 고액 자산 취득 예정 지출(CAPEX)을 일반 OPEX 지출과 별도로 명시 표기합니다.

## [2026-07-21] 미수 채권 연체 관리 및 대표이사 자동 지시(CEO Mandate) 조치 강제화 구현
- **요구사항**: 납기일이 경과한 거래처별 미수납 금액을 "연체" 채권으로 등록 및 합산하는 통합 관리 화면을 개설하고, 연체 발생 시 담당 영업사원의 ToDo 항목을 대표이사 자동 명령 지시 속성(`CEO_AUTO_MANDATE`)으로 강제 생성하여 추적 관리합니다.
- **주요 기능 설계**:
  - **연체 관리 테이블 & DTO**: 연체 거래처명, 담당 영업사원, 연체총액, 연체일수, 최종 조치현황을 기록하는 DTO 설계.
  - **대표이사 자동 지출/회수 명령**: 연체 시 시스템이 자동으로 **`👑 대표이사 직속 지시: 미수 채권 회수 요망`** 라벨을 ToDo에 설정하여 영업사원의 행동을 강제.
  - **조치 이력 증빙 강제화**: 단순 ToDo 텍스트 마감 처리를 차단하고, 반드시 연체 관리 드로어 내에 전화독촉, 독촉공문(최고장) 첨부, 또는 거래처 방문 일지를 1건 이상 등록해야 ToDo 종결 처리가 가능하도록 제어.

## [2026-07-21] 고객 거래 제한(거래가능/불가) 상태 제어 및 신규 계약/배차 차단 통제 구현
- **요구사항**: 장기 연체에 따른 일방적 자동 정지는 배제하고, ADMIN 및 MANAGER 권한자가 고객관리 카드에서 "거래가능 / 거래불가" 속성을 수동 토글할 수 있게 함으로써, 거래불가 설정 업체에 대해 신규 계약 체결 및 출고지시 배차를 원천 제한합니다.

## [2026-07-21] 연체 상담 기록 및 입금 약속 이행률(Promise Performance) 추적 시스템 구현
- **요구사항**: 수납 기일 도과 채권에 대한 영업사원 상담 일지, 약속 입금일/금액을 관리하고, 은행 입금 정보 대조를 통해 약속 이행률(Kept/Broken)을 자동 연산하여 약속 파기 시 초긴급 ToDo를 대표이사 직속 명령으로 재생성합니다.
- **주요 기능 설계**:
  - **연체 상담 스키마**: 고객사 상담 상대방, 독촉수단, 약속 입금 기일 및 약속 금액 등을 기록하는 테이블 구조 설계.
  - **입금 약속 자동 추적 엔진**: 약속일 경과 시점 미입금 상태 감지 시 자동으로 `BROKEN(약속파기)` 상태로 밀어내고, 담당자에게 긴급 2차 지령 재할당. (단, 입금 약속 파기 시 고객사의 거래 승인 상태를 자동으로 '거래불가'로 변경하는 일방적 자동 차단 패널티는 전면 배제하며, 수동 판단 통제 유지)
  - **성과 모니터링**: 부서/개인별 약속 이행 성공 비율 산출 및 대시보드 KPI 바인딩.

## [2026-07-21] 데일리 CashFlow 전망 스냅샷 누적 저장 및 과거 예측 추이 회고 시스템 구현
- **요구사항**: 일회성 실시간 현금흐름 연산만 제공하는 한계를 넘어, 매일 시뮬레이션한 수납예정/지출예정/설비투자/최종잔액을 통째로 캡처(스냅샷)하여 DB에 영구 적재하고, 과거 시점의 예측치를 오늘과 비교하며 유동성을 회고 분석할 수 있는 스냅샷 테이블 및 과거 이력 조회 탭을 구성합니다.

## [2026-07-21] 현금흐름 5대 요약 카드 클릭 시 거래처/날짜별 상세 정산 내역 팝업 모달 표출 구현
- **요구사항**: CashFlow 요약 카드 5종(시작고, Inflow, OPEX, CAPEX, 최종잔액) 클릭 시, 국민/신한 통장 상세 잔액 구조 및 30일 전망 중 수치와 일치하는 개별 정산 명세(날짜, 거래처명, 예정액)를 팝업 모달 표 구조로 시각화 출력합니다.

## [2026-07-21] 팝업 모달 내 거래처별 세부 금액 열 잘림 현상 방지를 위한 table-layout 고정
- **요구사항**: 모달 창 내부 테이블의 금액 열(Inflow, OPEX, CAPEX 상세금액)이 뷰포트 크기에 밀려 우측으로 사라지지 않도록 `<colgroup>` 및 `table-layout: fixed` 스타일을 강제 적용해 정합성과 데이터 신뢰도를 확보합니다. 또한 index.css에 정의된 글로벌 `min-width: 800px` 속성이 모달 내 테이블 폭을 삐져나가게 하여 우측 금액 컬럼이 가려지는 현상이 확인되었으므로, 인라인 `minWidth: 'auto'`를 강제 적용하여 모달 너비 안으로 완벽 스케일링되도록 보정합니다.

## [2026-07-21] 현금흐름 30일 시뮬레이션 결과 엑셀(CSV) 다운로드 기능 구현
- **요구사항**: 단순 시각적 대시보드 조회를 넘어 실제 스케줄링된 수납/지출 예정 정보를 Excel 등 스프레드시트 분석 도구에서 활용할 수 있도록 UTF-8 BOM 한국어 호환 CSV 다운로드 모듈을 탑재합니다.

## [2026-07-21] 전체 통합 프로세스 검증용 10,000건 이상의 모의 데이터 생성 및 일괄 삭제 시스템 구축
- **요구사항**: 고객사 200개, 자산 1,000개, 제품 90종, 계약 600건, 배차/입출고 1,500건, 기성 청구/상세 1,500건, 수납/통장매칭 1,000건, 소모품/정비/외주 지출 등 모든 비즈니스 도메인의 상호 정합성을 준수하는 시나리오 데이터를 절차적(CoT)으로 자동 생성하는 버튼과, 생성 데이터만 영구 격리하여 일괄 정제하는 버튼을 개발자 도구에 신설합니다.

## [2026-07-21] 테스트 데이터 생성기 내 실시간 로그 터미널 콘솔 및 예외 추적 시스템 구축
- **요구사항**: 모의 데이터 생성 시 비정상 필드에 의한 DB constraint 오류 또는 참조 무결성 예외 발생 시, 프로세스를 즉각 중단하고 오류 원인(테이블명, 에러 메세지, 스택 추적)을 다크 테마 기반의 콘솔 터미널 UI 상에 단계별로 실시간 시각화하여 논리적 원인 규명이 가능하도록 개선합니다.

## [2026-07-21] 수동 DB 반영을 위한 일괄 SQL 스크립트 빌드 및 Supabase SQL Editor 연동 파이프라인 개편
- **요구사항**: 10,000건의 대량 생성/삭제 처리를 트랜잭션 단위로 안전하게 제어하고 HTTP 성능 병목을 피하도록 SQL 스크립트 자동 생성기(`BEGIN; ... COMMIT;` 트랜잭션 래핑)와 복사/다운로드 기능 및 Supabase SQL Editor 다이렉트 링크 주소를 UI에 구성하여 수동 DB 연동 파이프라인으로 전환합니다.

## [2026-07-21] Supabase SQL Editor 용량 극복을 위한 비즈니스 시간축 기준 3개 파트 순차 SQL 분할 개편
- **요구사항**: Supabase 웹 콘솔의 최대 실행 한계(Query too large)를 우회하기 위해, 외래키 정합성이 보장되는 시간 순서(Part 1: 기준정보 -> Part 2: 계약/배차 -> Part 3: 청구/수납/정비)에 맞춰 쿼리를 3개 트랜잭션 세션으로 자동 분할하고 UI에서 개별 복사/다운로드를 할 수 있도록 변경합니다.

## [2026-07-21] 데이터 용량 기준 PostgreSQL 동적 SQL 쿼리 분출 및 가변 N개 파트 순차 시딩 개편
- **요구사항**: Supabase 웹 SQL 에디터의 엄격한 용량 제약(1MB 미만)을 완전히 무력화하기 위해, SQL 구문 크기를 바이트 단위(350KB 타겟)로 실시간 추적하여 트랜잭션 단위로 자동 쪼개는 동적 분할기(`generateSqlScript`)를 설계하고 UI에 가변 Part 버튼으로 자동 대응하도록 개편합니다.

## [2026-07-21] Node.js 기반 직접 TCP 연결 데이터베이스 시딩 환경 구축
- **요구사항**: 웹 브라우저 복사-붙여넣기의 용량 제약과 불편함을 영구 우회하기 위해, Node.js 기반 직접 TCP 연결 시딩 프로그램(`scripts/seed-db.js`) 및 `package.json` CLI 커맨드(`db:seed`)를 탑재하여 전체 덤프 SQL 스크립트를 즉시 적재할 수 있도록 개편합니다.

## [2026-07-21] Supabase DB-Native RPC 프로시저 완결 시딩 개편
- **요구사항**: 대용량 SQL 복사/붙여넣기 시 발생하는 사용자 타이핑 오류(예: `vBEGIN;` syntax error) 및 다운로드의 모든 공수를 원천 제거하기 위해 DB 서버 내부에서 직접 10,000건의 데이터를 생성/삭제하는 PostgreSQL PL/pgSQL 프로시저 및 RPC 호출 체계를 연동하여 완전 자동 원클릭 시딩 시스템을 구축합니다.

## [2026-07-21] DB 스키마 정합성에 맞춘 PL/pgSQL RPC 프로시저 칼럼 패치 적용
- **요구사항**: `assets` 테이블의 `"updatedAt"` Not-Null 제약조건 위반 에러(`23502`)를 해결하기 위해, 원격 DB 테이블 스키마에 정의된 모든 컬럼과 매핑 형식을 프로시저 설치 스크립트(`setup_seed_rpc.sql`)에 맞춰 전면 재동기화 및 패치합니다.

## [2026-07-21] 자산 UPDATE 구문 내 updatedAt 명시를 통한 NOT NULL 오류 해결
- **요구사항**: PL/pgSQL 함수 `generate_test_data` 및 `clear_test_data` 안에서 `UPDATE assets` 실행 시 `"updatedAt"`을 명시적으로 세팅하여 `23502` 예외를 근본 차단하도록 `setup_seed_rpc.sql` 코드를 전면 갱신합니다.

## [2026-07-21] DB 스키마 트리거 미존재 확인 완료 및 진단 코드 제거
- **요구사항**: 원격 DB의 `assets` 테이블과 타 연관 테이블에 custom trigger가 전혀 존재하지 않음을 확인하고, 디버깅을 위해 추가했던 `RAISE EXCEPTION` 자가 진단 코드를 최종 제거하여 논스톱 실행이 가능하도록 갱신합니다.

## [2026-07-21] 소규모 단계적 검증을 위한 1건 단위 안전 데이터 시더 탑재
- **요구사항**: 디버그 검증 단계를 위해 모든 생성 데이터의 기본 단위를 1개로 설정하고, 수량 스케일링이 변경되어도 외래키 참조 무결성이 침해받지 않도록 `1 + ((i - 1) % v_count)` 구조로 SQL 루프 관계 식들을 완벽히 리팩토링합니다.

## [2026-07-21] 통합 테스트 시나리오 데이터 관리 기능 및 UI 제거
- **요구사항**: 학습 보류 및 프로덕션 복잡도 완화를 위하여 `DevDataUploader.tsx` 및 `package.json`에서 시딩 기능 및 시더 UI 요소를 완전히 들어내고 원래 상태로 환원합니다.

## [2026-07-21] 피트(Feet) 컬럼 자료형 실수(DOUBLE PRECISION) 변경 및 소수점 등록 허용
- **요구사항**: 제품의 `feet` 컬럼이 DB 테이블 스키마상 정수(`INTEGER`) 타입이기 때문에 발생하는 오류를 막고 실생활 규격에 맞게 소수점 값을 허용하도록, 원격 DB와 로컬 스키마(`schema.sql`)의 `feet` 자료형을 `DOUBLE PRECISION`으로 개편하고 프론트엔드 입력 및 검증부에서도 이를 정식 허용하도록 수정합니다.

## [2026-07-21] 제품 등록 시 실행 예정 SQL 구문 시각화 및 결과 알림 팝업 장착
- **요구사항**: 사용자가 제품 저장 시 전송 예정 쿼리를 한눈에 볼 수 있도록 브라우저 경고창 안내를 장착하고, 백그라운드 DB 동기화 처리를 동기식으로 유도 및 감지하여 성공/실패 여부를 팝업으로 상세히 보고하도록 리팩토링합니다.

## [2026-07-21] products 테이블 및 스키마 내 누락 컬럼 전면 정합성 싱크 수정
- **요구사항**: `Products.tsx` 입력 폼에는 존재하나 `schema.sql` 및 `DevDataUploader.tsx` 제품 스키마 정의(`TABLE_SCHEMAS`)에서 누락되었던 4개 컬럼(`safetyCertUrl`, `specSheetUrl`, `emergencyGuideUrl`, `isActive`)을 스키마 파일과 업로더에 수동 보완하고, 원격 DB에도 컬럼 증분 생성이 가능하도록 DDL 명령을 가이드합니다.

## [2026-07-21] 실시간 DB 스키마 정합성 검증 도구 성능 10배 고속화 및 전수 검증 개편
- **요구사항**: 38개 테이블의 모든 컬럼을 전수 검사할 때, 병렬 호출에 따른 HTTP 429 및 제한 오류를 방지하기 위해 콤마 분리형 일괄 조회 및 점진적 에러 분석 색출 알고리즘을 도입하여 쿼리 전송 횟수를 대폭 줄이고 정밀한 DDL 복구 쿼리 생성을 가능하도록 개편합니다.

## [2026-07-21] CSV/Excel 업로더 대상 테이블 선택 범위 38종 전체 동적 연동 개편
- **요구사항**: 기존 하드코딩되었던 업로더 테이블 정의(`TABLE_SCHEMAS`)를 전면 폐기하고, `schema.sql` 정의를 파싱해 38개 테이블 전체를 드롭다운 선택지에 동적 주입하고 필드/예시 생성과 검증, 업로드까지 자동 호환되도록 구성합니다.

## [2026-07-21] Supabase 비동기 연동 쓰기 실패 에러 전파(Rethrow) 처리 및 팝업 정밀화
- **요구사항**: `saveProduct`에서 Supabase 쓰기 완료를 대기할 때, 예외 발생 시 에러가 조용히 삼켜지던 결함을 수정하여 상위 호출자(UI)로 예외가 명시적으로 발생(throw)하게 처리함으로써, 실제 연동 실패 시의 에러 팝업 명세가 정상 팝업을 밀어내고 사용자에게 정확하게 노출되도록 개편합니다.

## [2026-07-21] 제품 모델 관리 화면 수동 "조회" 버튼 탑재 및 검색 크래시 방어 강화
- **요구사항**: 제품 모델 관리 화면에 최신 데이터를 Supabase로부터 수동 갱신하는 [조회] 버튼을 탑재하고, 데이터 조회 시 제조사나 스펙 컬럼에 NULL 값이 들어있는 경우 검색 필터링 동작 도중 `.toLowerCase()` 호출로 크래시가 발생하는 위험을 Null-Safe하게 원천 차단합니다.

## [2026-07-21] 신규 데이터 등록 시 고유 ID의 순차형 자동 번호 부여 시스템 개편
- **요구사항**: 신규 제품/고객사/자산 등록 시 임의의 난수 문자열로 생성되던 기본 키 ID 체계를 순차형 자동 생성기(`generateNextId`)로 교체하여 테이블별 양식(예: `PROD-001`, `PROD-002` 등)에 의거해 번호가 순차 누적 부여되도록 리팩토링하고, SQL 시뮬레이션에서도 이 실제 생성 예정 ID가 렌더링되게 가다듬습니다.

## [2026-07-21] 전 테이블 순차 ID 자동 생성 전면 적용 및 고객 관리(고객/담당자/현장) 저장 정밀화
- **요구사항**: `db.ts` 내의 자동 순차 ID 부여 메커니즘을 29개 전체 테이블에 대해 개별 고유 접두사로 매핑되게 확장하고, `Customers.tsx` 내 고객사, 담당자, 현장 등록/수정 시에도 전송 예정 쿼리 안내 팝업 및 Supabase 실제 동기화 결과(성공/실패) 경고창 팝업 피드백을 동일한 수준으로 장착해 일관성을 완성합니다. 또한, 정비 마스터 등록 시 참조가 깨지던 외래키 ID 누락 버그를 교정합니다.

## [2026-07-21] 기본 키 ID 명세 포맷 변경: 테이블 축약어 + 숫자 7자리(0000001) 체계 도입
- **요구사항**: 순차적으로 증가하는 ID 번호 자릿수를 7자리(예: `PROD-0000004`, `CUST-0000021`)로 확장 변경하여 DB 전체의 유연성을 확보하고, 업로더 화면 예시 데이터 양식도 이에 연계하여 싱크합니다.

## [2026-07-25] [반영완료] Supabase DB 및 엑셀 일괄 업로드 양식 내 입력/생성 날짜 컬럼 후방 배치 개편
- **요구사항**: Supabase 데이터베이스 테이블 관리 및 엑셀 일괄 업로드 시, 입력 날짜(`createdAt`, `updatedAt` 등 등록/생성일시 관련 필드)가 앞쪽이나 중간에 위치하여 엑셀 업로드 양식 작성 시 불편함이 발생함. 이에 따라 모든 테이블 스키마 정의 및 엑셀 일괄 업로드 양식/파서에서 입력 날짜(또는 그 의미의 컬럼)를 컬럼의 가장 마지막(최후순위) 위치로 배치하여 엑셀 업로드 편의성과 스키마 일관성을 확보합니다.

## [2026-07-25] [반영완료] 스마트 출고 요청 동기식 Supabase 쓰기 예외 전파 및 에러 즉시 피드백 팝업 개편
- **요구사항**: `SmartDispatch.tsx`에서 출고 요청 저장 시, 백그라운드 비동기 Supabase 쓰기 큐(`pendingWrites`)에서 DB Constraint 오류, 컬럼 미존재, RLS 차단 예외 발생 시 에러가 조용히 삼켜지고 화면에는 성공 팝업만 출력되어 실제 데이터가 누락되는 현상이 발생함. 이에 따라 `saveSmartDispatch` 저장 시 Supabase 데이터베이스 연동 완료를 동기식으로 대기(`await Promise.all(db.pendingWrites)`)하고, 예외 발생 시 구체적인 에러 메시지를 팝업 모달로 즉시 노출하도록 보완합니다.

## [2026-07-25] [반영완료] contract_assets 테이블 내 expectedModel 컬럼 누락 수정 및 Supabase 스키마 싱크
- **요구사항**: 스마트 출고 등록 시 `contract_assets` 테이블에 희망 모델명(`expectedModel`) 컬럼이 스키마 상 누락되어 `Could not find the 'expectedModel' column of 'contract_assets' in the schema cache` 오류가 발생하는 결함 보완. `schema.sql` 및 원격 Supabase DB에 `"expectedModel" TEXT` 컬럼을 신규 추가하여 정상 수용되도록 수정.

## [2026-07-25] [반영완료] 원클릭 텍스트 복사 기능 내장 커스텀 예외 팝업 모달 (CopyableErrorModal) 시스템 구축
- **요구사항**: 시스템 예외 및 오류 발생 시 단순 브라우저 `alert()` 사용 시 텍스트 복사가 불가능하여 원인 파악 및 제보가 불편함. 이에 따라 에러 내용 텍스트 박스 및 **[오류 내용 복사]** 버튼이 탑재된 원클릭 커스텀 에러 모달 UI 컴포넌트를 신설하여 모든 예외 처리 구문(에러 피드백)에 단순 `alert` 대신 대체 적용.

## [2026-07-25] [반영완료] 고객사 약칭 입력 시 정식 등록 법인명(예: "주식회사 세보엠이씨") 자동 보정 및 중복 방지 시스템 구축
- **요구사항**: 영업사원이 입력 시 법인 수식어를 뺀 약칭(예: "세보엠이씨")이나 띄어쓰기가 다른 명칭으로 기입하더라도, 데이터베이스에 이미 정식 등록되어 있는 법인명(예: "주식회사 세보엠이씨")을 자동으로 탐색하여 정식 명칭으로 자동 보정/치환 및 연동되도록 개선.
- **해결 설계**:
  - `normalizeCustomerName` 파서로 텍스트에서 공백과 법인 수식어(`(주)`, `주식회사` 등)를 제외한 순수 상호 뼈대(Key)를 추출.
  - 영업사원이 약칭("세보엠이씨")만 적더라도 이미 DB에 "주식회사 세보엠이씨"가 등록되어 있다면, 입력받은 상호명을 정식 등록 명칭인 **"주식회사 세보엠이씨"**로 자동 보정하여 계약/출고/고객사 데이터에 즉시 반영함으로써 정식 명칭 통일과 중복 방지를 동시에 달성.

## [2026-07-25] [반영완료] 전 테이블 createdAt / updatedAt 일자 필드 표준화 및 엑셀 일괄 업로더 스키마 싱크 전면 개편
- **요구사항**:
  1. `createdAt` 또는 `updatedAt`이 없는 모든 테이블에 두 날짜 필드를 전면 표준 추가하고, 데이터 등록(Insert) 시 `createdAt` 및 `updatedAt`을, 데이터 수정(Update) 시 `updatedAt`이 실시간 갱신되도록 CRUD 핸들러(`db.ts`) 및 스키마(`schema.sql`)를 전면 리팩토링.
  2. `products` 테이블 등 `createdAt`이 중간에 껴 있던 컬럼들의 위치를 테이블의 가장 우측(맨 뒤)으로 이동하고 `updatedAt` 컬럼을 신규 추가.
  3. 스키마 개편 결과에 맞춰 엑셀 업로더(`DevDataUploader.tsx`)의 템플릿 생성 및 일괄 업로드 파서 구조를 동기화하여 구조 충돌 문제를 완벽 해결.

## [2026-07-25] [반영완료] 엑셀 업로드 시 createdAt/updatedAt 수동 입력 제외 및 업로드 실행시점 시각 자동 주입 시스템 구축
- **요구사항**: 엑셀 일괄 업로드 시 사용자가 `createdAt`(생성일시) 및 `updatedAt`(수정일시)를 엑셀 파일에 수동으로 입력해야 하는 불편함을 제거하고, 엑셀 데이터 업로드 실행 시점의 현재 시각을 시스템이 자동으로 `createdAt`과 `updatedAt` 필드에 주입하도록 업로더(`DevDataUploader.tsx`)를 개편.
- **해결 설계**:
  - 엑셀 양식/템플릿 다운로드 시 `createdAt`, `updatedAt` 컬럼은 필수 입력 대상에서 제외(옵션/자동채움 표시).
  - 엑셀 업로드 파싱 및 DB 적재 시 `createdAt` 또는 `updatedAt`이 누락되거나 비어 있으면, **업로드가 수행되는 실시간 현재 시각(Current ISO Timestamp)**을 시스템이 자동으로 채워 적재함.

## [2026-07-25] [반영완료] 엑셀 업로드 실패 시 [🔍 실패 원인 자세히 보기] 버튼 및 복사 가능한 상세 에러 모달 구축 개편
- **요구사항**: 엑셀 업로드 실패 시 "실패 (콘솔 확인)" 문구 대신, 실패 카드 옆에 **[🔍 실패 원인 자세히 보기]** 버튼을 탑재하여 클릭 시 실패한 행/데이터 내용과 Supabase/PostgreSQL의 구체적인 에러 원인을 복사 가능한 모달(`ErrorModal.tsx`)로 상세하게 확인할 수 있도록 개편. 또한 `id` 누락 시 자동 채번을 대입하여 적재함.
- **해결 설계**:
  1. 업로드 실패 시 실패 건수 카드 내에 **[🔍 실패 원인 자세히 보기]** 버튼을 배치.
  2. 버튼 클릭 시 실패한 배치/데이터 정보와 원격 DB에서 거절된 PostgreSQL 에러 코드, 테이블 존재 여부, 제약 조건 오류 메시지 전문을 `ErrorModal` 팝업으로 띄우고 원클릭 복사(`[📋 오류 내용 전체 복사]`) 버튼을 지원.
  3. `DevDataUploader.tsx` 내 `handleUpload` 및 `handleBulkUpload` 실행 시 `id` 컬럼이 비어 있는 행이 포함된 경우 테이블 규격(`PROD-0000001` 등)에 맞게 `id`를 자동 채번 및 주입.

## [2026-07-25] [반영완료] 엑셀 업로드 유효성 검사 에러 표시 시 한글 헤더 라벨과 영문 필드 키(Key) 동시 표기 개편
- **요구사항**: CSV/엑셀 업로드 유효성 검사 결과 에러 리스트(예: `[18행] 이름/명칭 - 필수값이 비어 있습니다.`) 출력 시, 사용자가 식별하기 위한 한글 라벨(`field.label`)뿐만 아니라 개발자 및 시스템 디버깅을 돕기 위한 영문 DB 컬럼 Key(`field.key`)를 병기(`[18행] 이름/명칭 (name) - 필수값이 비어 있습니다.`)하도록 개편.
- **해결 설계**:
  - `validateRows` 함수 및 일괄 업로드 유효성 검사 결과 생성부에서 에러 메시지의 필드 명칭 식별자를 `field.label` 단독 표기에서 `${field.label} (${field.key})` 형태로 결합 표기하도록 업로더(`DevDataUploader.tsx`)를 보완.

## [2026-07-25] [반영완료] schema.sql 내 전 테이블 RLS(Row Level Security) 자동 비활성화 DDL 구문 추가
- **요구사항**: Supabase 테이블 신규 생성/재생성 시 기본적으로 활성화되는 RLS 보안 정책으로 인해 HTTP API를 통한 데이터 삽입/수정 시 `42501 (new row violates row-level security policy)` 오류가 발생하는 현상 예방.
- **해결 설계**:
  - `schema.sql` 최하단에 모든 테이블의 RLS를 자동으로 비활성화(`ALTER TABLE public.<table_name> DISABLE ROW LEVEL SECURITY;`)하는 전수 DDL 스크립트를 추가하여 DDL 재생성 시 RLS 오류를 원천 차단.

## [2026-07-25] [반영완료] 대시보드 하드코딩 샘플 카드 제거 및 실시간 조건부 ToDo 카드 피드 개편
- **요구사항**: 대시보드(`Dashboard.tsx`)에 조건 없이 고정 노출되던 샘플 경보 카드들(구글 드라이브 용량 샘플 카드, 0원/0건 미수금 카드, 0건 정비/자재 모니터링 카드 등)을 완전히 제거하고, 실제 발생 건수가 존재하는 실시간 조건(`unpaidBillings.length > 0`, `pendingRepairs > 0 || lowStockConsumables > 0` 등)일 때만 카드가 노출되도록 개편.
- **해결 설계**:
  1. 하드코딩된 구글 드라이브 92% 샘플 알림 카드 완전 제거.
  2. 미수금 대장 카드는 실제 수납 미완료 데이터가 존재할 때만(`unpaidBillings.length > 0`) 동적 표출.
  3. 정비/소모품 자재 모니터링 카드는 실제로 대기 중인 정비가 있거나 자재가 부족할 때만(`pendingRepairs > 0 || lowStockConsumables > 0`) 표출.
  4. 당면 처리 과제가 0건일 때에는 `🎉 현재 즉시 처리해야 할 당면 과제나 긴급 할 일이 없습니다.` 카드를 깔끔하게 표출하여 유저 맞춤형 대시보드 가치 관철.

## [2026-07-25] [반영완료] 스마트 출고 텍스트 입력창 기본 샘플 문구 제거 및 빈 상태(Empty string) 초기화 개편
- **요구사항**: 스마트 출고 요청(`SmartDispatch.tsx`) 화면 진입 시 1단계 메신저 줄글 텍스트 복사/붙여넣기 입력창(`rawText`)에 더 이상 샘플 출고 문구가 기본 삽입되지 않도록, 초기 상태를 빈 칸(`''`)으로 초기화하고 깨끗한 폼 입력을 제공.
- **해결 설계**:
  - `SmartDispatch.tsx` 내 `rawText` initial state를 하드코딩 샘플 문자열에서 빈 문자열(`''`)로 변경.

## [2026-07-25] [반영완료] 스마트 출고 텍스트 파일 불러오기(단일 원클릭) 기능 탑재
- **요구사항**: 스마트 출고 요청(`SmartDispatch.tsx`) 1단계 카드 헤더에 **`[📂 텍스트 파일 불러오기]`** 단일 버튼을 추가하여, 클릭 시 텍스트 파일(.txt, .log 등) 선택 창을 오픈하고 지정된 파일의 텍스트 내용을 자동으로 파싱하여 입력창(`rawText`)에 채워주도록 구현.
- **해결 설계**:
  - `SmartDispatch.tsx` 1단계 헤더 우측에 `[📂 텍스트 파일 불러오기]` 버튼 및 скрытый `<input type="file" accept=".txt,.log,.csv" />` 연동.
  - 파일 선택 즉시 `FileReader`가 파일 텍스트를 읽어 `rawText` 상태에 주입.

## [2026-07-25] [반영완료] deliveries 테이블 내 누락 컬럼 isCostSettled 수용 및 schema.sql 싱크 보완
- **요구사항**: 스마트 출고 등록 및 배차 처리 시 `deliveries` 테이블 대상 데이터에 운송비 정산 여부 플래그(`isCostSettled`)가 전송되나, 스키마 정의상 컬럼이 누락되어 `Could not find the 'isCostSettled' column of 'deliveries' in the schema cache` 오류가 발생하는 결함 보완.
- **해결 설계**:
  - `schema.sql` 내 `deliveries` 테이블 정의에 `"isCostSettled" BOOLEAN DEFAULT FALSE` 컬럼을 정식 추가.

## [2026-07-25] [반영완료] contracts 테이블 salespersonId 외래키(FK) 참조 무결성 방어 및 예외 차단 개편
- **요구사항**: 스마트 출고 또는 신규 계약 저장 시 `contracts` 테이블의 담당 영업사원 컬럼(`salespersonId`)에 대입되는 사용자 ID가 DB `users` 테이블 PK로 등록되어 있지 않을 때 발생하는 외래키 제약조건 위반 오류(`23503 contracts_salespersonId_fkey`) 원천 방어.
- **해결 설계**:
  - `AppContext.tsx` 내 `saveSmartDispatch` 및 `Contracts.tsx` 계약 저장 핸들러에서 `salespersonId`를 대입하기 전에 현재 유저 ID(`currentUser.id`)가 DB `users` 목록에 실제 존재하는지 검증.
  - 존재하지 않거나 유효하지 않은 계정일 경우 `null` (또는 유효한 기본 관리자 ID `'u-1'`)로 안전하게 교체 대입하여 Supabase FK 제약 조건 위반 에러를 완벽 차단.

## [2026-07-25] [반영완료] 인사 및 조직도 화면 컴팩트 레이아웃 최적화 (미배정 인력 풀 한 화면 표출)
- **요구사항**: 조직도 화면(`OrganizationSettings.tsx`)에서 과도하게 고정된 세로 높이(`minHeight: 600px`)로 인해 좌측 하단 '미배정 인력 풀 (Pool)' 영역이 한참 아래로 밀려 스크롤해야 보이는 현상 개선. 소규모 회사 조직 규모에 맞춰 한 화면(뷰포트) 내에서 부서 트리와 미배정 인력 풀이 스크롤 없이 한눈에 들어오도록 컴팩트 레이아웃 개편.
- **해결 설계**:
  1. `OrganizationSettings.tsx` 전체 컨테이너의 불필요한 고정 높이 `minHeight: 600px`를 제거하고 내용물에 맞춘 컴팩트 높이로 축소.
  2. 좌측 조직 구조도 리스트 영역의 높이 여백 및 마진을 축소하고, 부서 트리 하단에 '미배정 인력 풀 (Pool)' 박스가 즉시 노출되도록 레이아웃 재배치.

## [2026-07-25] [반영완료] 전사 DB 저장/업서트 전수 동기화 검증 파이프라인 및 에러 모달 팝업 출력 보완
- **요구사항**: 시스템 내 모든 데이터베이스 저장/수정/삭제(DB 업서트) 처리 시, Supabase 원격 DB 및 로컬 DB에 올바르게 적재되었는지 실시간으로 정밀 검증하고, 오류(SQL error, RLS error, FK error 등) 발생 시 누락 없이 에러 모달(`showErrorModal`)을 통해 구체적 원인 메시지를 사용자에게 팝업 표출하도록 보완.
- **해결 설계**:
  1. `db.ts` 내에 Supabase 비동기 쓰기 큐(`pendingWrites`)를 안전하게 대기하고 오류를 포착하는 통합 헬퍼 `awaitPendingWrites()` 구현.
  2. `AppContext.tsx` 내 모든 CUD 저장 함수(`saveProduct`, `saveAsset`, `saveCustomer`, `saveContact`, `saveSite`, `saveContract`, `saveRepair`, `saveBilling`, `saveConsumable`, `saveVendor`, `saveSmartDispatch`, `saveSmartReturn`, `saveOrganization` 등)에 `await db.awaitPendingWrites()`를 적용.
  3. 저장/업서트 실패 시 예외를 포착하여 상세 에러 내역 및 복구 힌트와 함께 `showErrorModal`을 전수 호출하도록 파이프라인 강화.

## [2026-07-25] [반영완료] 인사 및 조직도 관리 화면 컨테이너 세로 높이(height) 900px 명시 지정
- **요구사항**: 인사 및 조직도 마스터 설정 화면(`OrganizationSettings.tsx`)의 컨테이너 세로 높이를 사용자 지정 사양인 `900px` (`minHeight: 900px`)로 정확하게 명시 설정.
- **해결 설계**:
  - `OrganizationSettings.tsx` 뷰포트 컨테이너 및 좌우 카드 박스의 높이를 `minHeight: '900px'`로 설정하여 시원하고 쾌적한 900px 수직 작업 영역을 제공.

## [2026-07-25] [반영완료] 신규 직원 등록 시 이름 및 직급 입력 필드 힌트문자(Placeholder) 처리 개편
- **요구사항**: 인사 및 조직도 마스터 설정(`OrganizationSettings.tsx`)에서 신규 직원 생성 시 이름과 직급 필드에 '신규직원', '사원' 기본 텍스트 값 대신 빈 칸(`''`)으로 초기화하고, `placeholder` (힌트문자: "이름 입력 (예: 홍길동)", "직급 입력 (예: 사원/대리)")로 표출하여 수정 편리성 극대화.
- **해결 설계**:
  1. `handleAddUser`에서 생성되는 신규 유저 객체의 `name`과 `position`을 빈 문자열(`''`)로 설정.
  2. 상세 프로필 슬라이드 폼 내 이름 및 직급 `<input>` 요소에 각각 `placeholder="이름 입력 (예: 홍길동)"`, `placeholder="직급 입력 (예: 사원/대리)"` 힌트문자 적용.

## [2026-07-25] [반영완료] 메뉴 이동 시 화면 스크롤 최상단(Top) 자동 리셋 파이프라인 구축
- **요구사항**: 사용자가 좌측 메뉴 사이드바 또는 상단 탭을 통해 다른 메뉴 페이지로 이동할 때마다 이전 스크롤 위치가 유지되는 불편함을 해소하고, 항상 이동한 새 메뉴 화면의 최상단(Top = 0)이 자동으로 표출되도록 구현.
- **해결 설계**:
  - `App.tsx` 내에서 `activeTab` 상태 변경 시 `window.scrollTo({ top: 0, behavior: 'instant' })` 및 메인 컨테이너 요소(`.main-content-area`)의 `scrollTop = 0` 스크롤을 즉시 0으로 자동 이동시키는 `useEffect([activeTab])` 전역 파이프라인 탑재.

## [2026-07-25] [반영완료] 새 부서(조직) 생성 시 부서명 힌트문자(Placeholder) 처리 개편
- **요구사항**: 인사 및 조직도 마스터 설정(`OrganizationSettings.tsx`)에서 새 부서 추가(`+`) 클릭 시 '새 부서' 기본 텍스트 대신 빈 칸(`''`)으로 초기화하고, `placeholder="부서명 입력 (예: 영남영업소)"` 힌트문자로 표시하여 백스페이스 조작 없이 즉시 부서명을 입력할 수 있도록 보완.
- **해결 설계**:
  1. `handleAddDept` 시 신규 부서 객체의 `name`을 빈 문자열(`''`)로 설정하고, 편집 문자열(`editingDeptName`)도 `''`로 초기화.
  2. 조직 트리 내 부서 편집 인풋 박스에 `placeholder="부서명 입력 (예: 영남영업소)"` 지정 및 부서명 미입력 시 `새 부서(명칭 미입력)` 보조 텍스트 렌더링.

## [2026-07-25] [반영완료] departments 테이블 createdAt/updatedAt 필수값 누락 방어 및 조직도 동기화 개편
- **요구사항**: 조직도 저장 시 `departments` 테이블의 `createdAt` 컬럼 NOT NULL 제약조건 위반 에러(`null value in column "createdAt" of relation "departments" violates not-null constraint`) 원천 차단.
- **해결 설계**:
  1. `OrganizationSettings.tsx` 및 `db.ts` 내 `saveOrganizationBatch`에서 부서(Department) 객체를 Supabase로 동기화하기 전 `createdAt` 및 `updatedAt` 속성이 누락되어 있으면 `new Date().toISOString()` 타임스탬프를 자동 채워주도록 방어 파이프라인 구축.
  2. `Department` 인터페이스에 `createdAt?: string; updatedAt?: string;` 명시 반영.

## [2026-07-25] [반영완료] users 테이블 createdAt/updatedAt 필수값 누락 방어 및 직원 저장 23502 에러 원천 패치
- **요구사항**: 직원 등록 및 부서 배치 후 전체 저장 시 `users` 테이블의 `createdAt` 컬럼 NOT NULL 제약조건 위반 에러(`null value in column "createdAt" of relation "users" violates not-null constraint`) 원천 방어 및 해결.
- **해결 설계**:
  1. `OrganizationSettings.tsx` 및 `db.ts` 내 `saveOrganizationBatch`에서 사용자(User/UserNode) 객체를 Supabase로 동기화하기 전 `createdAt` 및 `updatedAt` 속성이 누락되어 있으면 `new Date().toISOString()` 타임스탬프를 자동 채워주도록 방어 파이프라인 구축.
  2. `UserNode` 및 `User` 인터페이스에 `createdAt?: string; updatedAt?: string;` 명시 반영 및 `handleAddUser` 시 기본 타임스탬프 부여.

## [2026-07-25] [반영완료] 마우스 커서 위치 기반 독립 영역(메뉴/작업화면/테이블) 독자 스크롤 시스템 구축
- **요구사항**: 전체 브라우저 창 스크롤을 억제하고, 마우스 위치에 따라 좌측 메뉴(사이드바) 위에서는 메뉴만 독자 스크롤, 우측 메인 작업 화면 위에서는 메인 작업 화면만 독자 스크롤, 화면 내 표(테이블)/모달 위에서는 표만 독자 스크롤(`overscroll-behavior: contain`)되도록 스크롤 영역 분리 및 체이닝 방지 개편.
- **해결 설계**:
  1. `index.css`: `html, body, #root`에 `height: 100%; max-height: 100vh; overflow: hidden;` 처리하여 바디 전체 스크롤 원천 차단.
  2. `App.tsx` & `index.css`: 좌측 메뉴 사이드바(`.sidebar-nav`) 및 우측 메인 작업 영역(`.main-content-area`)에 각각 `height: 100%; overflow-y: auto; overscroll-behavior: contain;` 독립 뷰포트 스크롤 적용.
  3. 표/테이블 래퍼 및 모달 컨테이너(`table`, `.table-container`, `.modal-content`)에 `overscroll-behavior: contain` 및 `overflow: auto`를 적용하여 마우스가 표 위에 있을 때는 표만 부드럽게 스크롤되고 바깥 영역으로 스크롤이 전파되지 않도록 완벽 차단.

## [2026-07-25] [반영완료] 화면 전체 Height 850px 고정 프레임 레이아웃 적용
- **요구사항**: 전체 메인 애플리케이션 화면 높이 사양을 `850px` (`height: 850px`, `maxHeight: 850px`)로 명시 설정하고, 850px 고정 높이 내부에서 좌측 메뉴 및 우측 작업 화면이 독자 스크롤되도록 보완.
- **해결 설계**:
  - `App.tsx` 메인 뷰포트 컨테이너 래퍼의 높이를 `height: '850px'`, `maxHeight: '850px'`로 지정하고, 내부 `sidebar-nav` 및 `main-content-area`가 850px 수직 프레임 내에서 100% 핏(Fit)되어 개별 스크롤되도록 구축.

## [2026-07-25] [반영완료] 상세 프로필 슬라이드 폼 컴팩트 레이아웃 개편 및 불필요 세로 여백/스크롤 제거
- **요구사항**: 인사 및 조직도 마스터 설정(`OrganizationSettings.tsx`)의 우측 임직원 상세 프로필 슬라이드 패널 하단에 불필요하게 크게 잡힌 빈 여백과 이로 인해 발생하는 수직 스크롤 현상 완벽 제거.
- **해결 설계**:
  1. 프로필 아바타 이미지 서클 크기를 `80px` ➔ `56px`로 축소하고 하단 여백 절감.
  2. 각 폼 요소 간격 `gap: 10px` ➔ `gap: 6px`, 라벨 하단 마진 및 패딩을 슬림하게 다듬어 폼 전체 높이를 축소.
  3. 자택 주소 입력 폼을 `<textarea rows={2}>`에서 한 줄 `<input type="text">`로 변경하여 패널 하단 빈 공간과 스크롤을 원천 제거하고 한눈에 핏(Fit)되도록 개편.

## [2026-07-25] [반영완료] 상세 프로필 슬라이드 패널 취소/저장 버튼 최상단 헤더 이동 개편
- **요구사항**: 인사 및 조직도 화면(`OrganizationSettings.tsx`)에서 우측 상세 프로필 슬라이드 패널이 열렸을 때 하단 버튼이 뷰포트 밖으로 잘리거나 스크롤을 내려야 보이는 불편을 원천 해결하기 위해, `[취소]` 및 `[저장 (적용)]` 버튼을 최상단 헤더("상세 프로필" 제목 옆)로 배치 변경.
- **해결 설계**:
  - `OrganizationSettings.tsx` 상세 프로필 상단 헤더 영역(`h3` 옆)에 `[취소]` 및 `[저장 (적용)]` 버튼을 전면 배치하여 패널이 열리자마자 스크롤 없이 최상단에서 즉시 클릭 가능하도록 개편.

## [2026-07-25] [반영완료] 자산 정보 일괄 업로드 템플릿 샘플 데이터 포맷 및 한글 매핑 규격 개편
- **요구사항**: 자산 일괄 업로드(`DevDataUploader.tsx`, `Assets.tsx`) 샘플 CSV/엑셀 양식에서 모델명 오표기 수정, 취득일자 포맷 간소화, 소유유형 및 상태 한글 표기 적용.
- **해결 설계**:
  1. **모델명(`modelName`)**: ISO 날짜 오표기를 제거하고 `"KY-0801"`, `"SJB-1200"` 등 실제 고소작업대 모델명 샘플 텍스트 제공.
  2. **취득일자(`acquisitionDate`)**: `YYYY-MM-DD` (예: `2026-01-15`)까지만 표시하도록 포맷 제한.
  3. **소유유형(`ownerType`)**: `OWNED/RENTED` 대신 **`당사` / `임차`** 한글 샘플 제공 및 업로드 시 자동 매핑 파싱.
  4. **상태(`status`)**: **`임대가능` / `임대중` / `정비중` / `외주정비중`** 한글 표기만 허용 및 업로드 시 DB enum 규격(`AVAILABLE`, `RENTED`, `REPAIRING`, `RENTED_RETURNED`)으로 자동 변환 매핑 처리.

## [2026-07-25] [반영완료] 전체 데이터 관리 메뉴 테이블 다차원 정렬(오름/내림차순) 및 독립 수직 스크롤/카운터 일괄 구축
- **요구사항**: 제품 모델 관리(`Products.tsx`), 자산 대장(`Assets.tsx`) 등 메뉴에서 데이터가 10개만 보이고 잘리던 수직 스크롤 이슈를 해결하고, 테이블 컬럼 헤더 클릭 시 오름차순(▲) / 내림차순(▼) 자유 정렬 및 실시간 총 등록 수량 카운터를 전면 탑재.
- **해결 설계**:
  1. **테이블 독립 수직 스크롤 & 스티키 헤더 (`Products.tsx`, `Assets.tsx`, `index.css`)**:
     - `.table-container`에 `maxHeight: 'calc(850px - 260px)'`, `overflowY: 'auto'`, `overscrollBehavior: 'contain'` 적용하여 수백 건 이상의 데이터도 뷰포트 내에서 무한 독자 스크롤 가능하도록 조율.
     - `thead th`에 `position: sticky`, `top: 0`, `z-index: 10`을 적용하여 스크롤할 때 컬럼 헤더가 상단에 고정되어 시각 가독성 극대화.
  2. **컬럼 헤더 클릭식 오름차순(▲) / 내림차순(▼) 정렬 시스템**:
     - 모델명, 피트규격, 제조사, 자산구분, 장비상태, 보유대수, 등록일 등 모든 주요 컬럼 헤더 클릭 시 `asc` ➔ `desc` ➔ `asc` 토글 정렬 기능 전면 도입.
  3. **실시간 전체 수량 및 검색 카운터 표기**:
     - 헤더 상단에 `전체 X개 제품 등록됨 (검색 결과: Y건)` 카운터를 표기하여 데이터 누락 착시를 원천 해소.

## [2026-07-25] [반영완료] 자산 일괄 업로드 템플릿 양식 실시간 제품 모델명 연동 및 "테스트모델명" 텍스트 보장
- **요구사항**: 자산 템플릿 B열 모델명에 날짜 형태가 오표기되는 버그를 완전 수정하고, 실제 DB에 등록된 제품 모델목록(`products`)에서 동적으로 추출하여 샘플로 노출하며, 등록된 제품이 아예 없는 경우 **"테스트모델명"**으로 명시 출력.
- **해결 설계**:
  - `DevDataUploader.tsx`: `modelName` 필드의 `type`을 `'string'`으로 강제 고정하고, `getDynamicTableSchemas(products)`에 현재 등록된 제품 데이터를 전달하여 템플릿 다운로드 시 등록 제품 모델명(없을 시 **"테스트모델명"**)을 샘플 데이터로 동적 생성하도록 파이프라인 구축.

## [2026-07-25] [반영완료] 자산 DB/대장/취득 "제조년도" 컬럼 추가 및 IFRS 감가상각개월수/미상각잔액 자동 계산 엔진 구축 (v1.2.0.Build.00000)
- **요구사항**:
  1. **제조년도 추가**: 자산 DB, UI 관리 대장, 자산 취득/등록 폼 모달에 "제조년도" (`manufactureYear`) 컬럼 및 필드 추가 (위치: "제조사" 와 "매입처" 사이).
  2. **감가상각개월수 용어 변경**: 기존 `감가상각상태` 컬럼을 자산 취득 메뉴와 동일하게 **`감가상각개월수`** (`depreciationMonths`)로 변경.
  3. **IFRS 월말 기준 자동 감가상각 계산 엔진 구축**:
     - 취득일자(`acquisitionDate`)부터 매월 말일 기준으로 상각경과 월수를 계산하여 **월 감가상각비**, **감가상각누계액**, **미상각 잔액**을 실시간 산출.
     - **매각된 자산 (`status === 'SOLD'`)**: 매각일(`disposalDate`) 이후에는 상각 진행 정지 (매각일 시점의 상각누계액 및 장부가치로 수치 고정).
     - **공식**:
       - 상각 대상 총액 = `취득원가 - (취득원가 * 잔존가치율 / 100)`
       - 월 감가상각비 = `상각 대상 총액 / 감가상각개월수`
       - 감가상각누계액 = `Math.round(월 감가상각비 * 경과월수)` (1원 단위 소수점 반올림)
       - 미상각 잔액 (장부가치) = `취득원가 - 감가상각누계액`
- **해결 설계**:
  1. `schema.sql`: `assets` 테이블 스키마에 `"manufactureYear" TEXT` 추가 및 `"depreciationMonths" INTEGER` 정비.
  2. `src/services/db.ts`: `Asset` 인터페이스 개편 및 `calculateAssetDepreciation` IFRS 자동 감가상각 함수 작성.
  3. `DevDataUploader.tsx`: `COLUMN_LABEL_MAP`에 `manufactureYear: '제조년도'`, `depreciationMonths: '감가상각개월수'` 반영 및 CSV/엑셀 템플릿 양식 연동.
  4. `Assets.tsx`: 목록 테이블, 취득/등록 폼, 상세 모달, 엑셀 익스포트에 "제조년도" 및 IFRS 감가상각누계액/미상각잔액 실시간 렌더링.
  5. **버전 산정**: DB 스키마 및 컬럼 변경 규칙 적용 ➔ **`v1.2.0.Build.00000`**

## [2026-07-25] [반영완료] 당사자산 취득/매각 메뉴 폼 내 제조년도(`manufactureYear`) 필드 배치, 매입처 관리 메뉴 신설, 자산 엑셀 일괄 업로드 파이프라인 개편 및 자산 컬럼 전체 순서 표준 정돈 (v1.3.0.Build.00000)
- **요구사항**: 
  1. **자산 컬럼 순서 및 엑셀 일괄 업로드 전면 완벽 개편 (`DevDataUploader.tsx`)**:
     - 새롭게 확정된 자산 필드 표준 순서(`id` ➔ `assetNo` ➔ `modelName` ➔ `serialNo` ➔ `manufacturer` ➔ `manufactureYear` ➔ `ownerType` ➔ `status` ➔ `acquisitionDate` ➔ `acquisitionPrice` ➔ `supplier` ➔ `depreciationMonths` ➔ `residualValueRate` ➔ ...)에 맞추어 CSV/엑셀 템플릿 양식을 재배치하고 한글 라벨, 샘플 데이터 생성, 유효성 검사 및 Supabase 업로드 파서를 100% 동기화 개편.
  2. **당사자산 취득 메뉴(`AssetAcquisitionDisposal.tsx`)**: 신규 자산 취득 등록 폼 내 제조사(`manufacturer`) 아래에 **"제조년도 (예: 2023)"** 입력 필드를 노출하고 저장 연동.
  3. **매입처 관리 메뉴 신설 (`Vendors.tsx`)**: `vendors` (공급자/매입처/외주처) 독립 라우팅 UI 생성 및 사이드바 탑재.
  4. **자산 전체 메뉴 논리적 교차 점검 및 정합성 보장**:
     - 당사자산(`OWNED`) vs 임차자산(`RENTED`) 상각 처리 격리 (임차자산은 감가상각 대상에서 제외).
     - 매각 자산(`SOLD`)은 매각일자 이후 감가상각 자동 고정 및 신규 계약/출고 할당 완전 차단.
     - `supplier` (구입처 텍스트)와 `vendorId` (매입처 DB 연동)의 상호 참조 정합성 보장.
- **해결 설계**:
  1. **DevDataUploader.tsx**: `schema.sql` 정돈 스키마 기반 동적 템플릿 생성 및 한글 파싱 매핑(`supplier`, `manufactureYear`, `depreciationMonths` 등) 정밀 동기화.
  2. `AssetAcquisitionDisposal.tsx`: `manufactureYear` 입력 폼 state 추가 및 제조사와 취득일자 사이에 배치.
  3. `Vendors.tsx` 신규 생성 및 `App.tsx` 사이드바 등록.
  4. **버전 산정**: 신규 기능/폼/컬럼 구조 개편 규칙 적용 ➔ **`v1.3.0.Build.00000`**

## [2026-07-25] [반영완료] 매입처(공급자) 거래 구분 간소화 및 세그먼트 버튼 토글 다중 속성 UI 시스템 구축 (v1.3.1.Build.00000)
- **요구사항**: 
  1. 거래처 구분을 **`임차거래처`**, **`구매처`**(장비/소모품 구입), **`운송거래처`**, **`외주정비처`**, **`기타`** 5가지로 간소화.
  2. 한 거래처가 "임차거래처"이면서 동시에 "구매처"인 경우 등 다중 속성을 지정할 수 있도록 기능 탑재.
  3. **디자인 강화**: 단순 투박한 체크박스 대신 **연속된 인터랙티브 버튼 세그먼트 그룹(버튼 토글) UI**로 스타일링 (활성화 시 브랜드 컬러 그라데이션, 글로우 테두리, 체크 아이콘 및 마이크로 애니메이션 연출).
- **해결 설계**:
  - `Vendor` 모델의 `types` / `type` 속성을 **다중 선택 지원 배열 (예: `['RENTAL', 'PURCHASE']`)**로 확장.
  - `Vendors.tsx`: 매입처 등록/수정 모달 폼 내에 `[🏢 임차거래처] [🛒 구매처] [🚚 운송거래처] [🔧 외주정비처] [📌 기타]` 5연속 토글 버튼 그룹 디자인 주입.
  - 목록 테이블에 다중 태그 배지(`badge-info`, `badge-success` 등) 렌더링.
  - 상단 필터 선택 시 지정된 매입 구성을 가진 거래처 교차 탐색 연동.

## [2026-07-25] [반영완료] 상위-하위 계층형 접이식 아코디언 사이드바 네비게이션 개편 및 계층 권한 관리 매트릭스 구축 (v1.4.0.Build.00000)
- **요구사항**: 
  1. 길어진 사이드 메뉴를 **상위메뉴 - 하위메뉴 2단계 아코디언 접이식(Collapsible Accordion) 구조**로 전면 개편.
  2. `UsersPermissions.tsx` (사용자 및 권한) 관리 메뉴에서도 24개 전체 메뉴를 상위 카테고리 그룹별로 계층화하여 조회/저장 권한 부여 및 회수를 완벽하게 동기화.
- **해결 설계**:
  - **App.tsx**: 8대 상위 메뉴 그룹(`영업관리`, `제품/자산관리`, `배차/운송관리`, `입출고관리`, `정비/소모품관리`, `경영관리`, `경영관리-특수`, `시스템관리-개발자`) 및 최상단 독립 `ERP 대시보드` 아키텍처 구축.
  - 현재 활성화된 탭(`activeTab`) 소속 상위 메뉴 자동 펼침 및 접고 펼침State(`expandedGroups`) 탑재.
  - **UsersPermissions.tsx**: 상위 그룹별 트리 구조 표 렌더링 및 `[조회 전체선택]` / `[저장 전체선택]` 상위 카테고리 일괄 부여/회수 기능 주입.
  - **버전 산정**: 메뉴 아키텍처 및 계층 구조 전면 개편 ➔ **`v1.4.0.Build.00000`**

## [2026-07-25] [반영완료] 사이드바 하위 메뉴 들여쓰기 폭 슬림화 & 스마트 출고/회수 요청 영업관리 소속 이동 (v1.4.1.Build.00000)
- **요구사항**: 
  1. 깊고 불균형해 보였던 하위 메뉴 들여쓰기 폭을 슬림하고 균일한 여백(`paddingLeft: 6px`, `marginLeft: 14px`)으로 정돈.
  2. **`스마트 출고 요청`** 및 **`스마트 회수 요청`** 메뉴를 **`영업관리`** 그룹 하위로 이동.
- **해결 설계**:
  - `App.tsx`: `menuGroups` 내 `smart_dispatch`, `smart_return`을 `grp_sales` 하위로 재배치하고 하위 컨테이너 스타일 패딩 조정.
  - `UsersPermissions.tsx`: `MENU_CATEGORIES` 카테고리 항목 재배치 및 권한 동기화.
  - **버전 산정**: UI 레이아웃 정밀 개선 ➔ **`v1.4.1.Build.00000`**

## [2026-07-25] [반영완료] 사이드바 모든 하위 메뉴 X 좌표 수평 완전 정렬 및 상위 메뉴 대비 +15px 오프셋 고정 (v1.4.2.Build.00000)
- **요구사항**: 
  1. 모든 하위 메뉴의 X 시작 좌표가 토씨 하나 없이 100% 동일하게 일직선으로 수평 정렬되도록 조정.
  2. 하위 메뉴의 X 시작 좌표 위치를 상위 메뉴의 X 시작 위치 대비 **정확히 15px(15pt) 오른쪽에 일정하게 배치**.
- **해결 설계**:
  - `App.tsx`: 상위 메뉴 패딩(`padding: 8px 10px`, X 시작점 = 10px)을 기준으로, 하위 메뉴의 좌측 패딩을 `paddingLeft: 25px` (10px + 15px = 25px)로 통일 고정.
  - 하위 그룹 서브 컨테이너의 오프셋 간섭을 제거(`marginLeft: 0`, `paddingLeft: 0`)하여 모든 하위 항목의 X 좌표가 정확히 일치하도록 수평 라인을 정교 정밀 튜닝.
  - **버전 산정**: UI 미세 오프셋 정밀 정돈 ➔ **`v1.4.2.Build.00000`**

## [2026-07-25] [반영완료] 고정 20px 아이콘 그리드 정렬로 상위/하위 아이콘 및 텍스트 X 오프셋 +15px 완전 일치 교정 (v1.4.3.Build.00000)
- **요구사항**: 
  1. 아이콘 크기 차이로 인해 상위 글자와 하위 글자 위치가 다르게 삐뚤어 보이던 현상을 근본적으로 해결.
  2. 상위 아이콘 ↔ 하위 아이콘, 상위 텍스트 ↔ 하위 텍스트가 모두 100% 일정하게 **정확히 +15px 오프셋**으로 칼같이 일직선 정렬되도록 정밀 보정.
- **해결 설계**:
  - `App.tsx`: 상위 및 하위 메뉴 버튼 내부 아이콘 래퍼에 **`width: 20px` 고정 폭 그리드** 및 **`marginLeft: 8px` 고정 마진**을 동시 적용.
  - 상위 아이콘 X = 10px ➔ 하위 아이콘 X = 25px (+15px 정확히 일치).
  - 상위 텍스트 X = 38px ➔ 하위 텍스트 X = 53px (+15px 정확히 일치).
  - **버전 산정**: UI 그리드 레이아웃 오차 완벽 교정 ➔ **`v1.4.3.Build.00000`**

## [2026-07-25] [반영완료] 하위 서브 컨테이너 마진 오프셋 +15px 직접 부여로 호버 박스 및 수직 가이드 라인 100% 피팅 (v1.4.4.Build.00000)
- **요구사항**: 
  1. 유저 붉은 펜 가이드에 맞춰 상위 아이콘 시작선 대비 하위 메뉴 블록 전체(버튼 호버 영역 및 아이콘)가 정확히 15px 오른쪽 위치에 정렬되도록 개편.
  2. 상위 아이콘 축 기준 수직 연결 가이드라인 연출.
- **해결 설계**:
  - `App.tsx`: 하위 서브 컨테이너에 `marginLeft: 15px`, `borderLeft: 2px solid rgba(59, 130, 246, 0.25)`를 적용하여 유저 표시 붉은 세로선 기준 가이드라인과 +15px 우측 가로 정렬을 100% 매칭시킴.
  - 상위 아이콘 시작 X(10px) ➔ 하위 컨테이너 마진(15px) + 하위 버튼 패딩(10px) = **하위 아이콘 시작 X(25px) / 텍스트(53px)** 완벽 정밀 피팅.
  - **버전 산정**: 레이아웃 가이드라인 픽셀 완벽 피팅 ➔ **`v1.4.4.Build.00000`**

## [2026-07-25] [반영완료] CSS Grid 2컬럼 고정 레이아웃으로 하위 메뉴 아이콘-텍스트 수직 정렬 근본 교정 (v1.4.5.Build.00000)
- **요구사항**: 
  1. 반복 개편에도 해결되지 않던 아이콘 크기 차이로 인한 텍스트 시작 X 좌표 불일치를 근본적으로 해결.
  2. 동그라미로 표시된 아이콘들 간의 여백 불균일 현상 완전 제거.
- **해결 설계**:
  - `App.tsx`: 하위 메뉴 버튼을 `display: flex` → **`display: grid; gridTemplateColumns: '16px 1fr'; columnGap: '8px'`**로 전면 교체.
  - 1열(16×16px 고정 박스)에 모든 아이콘을 클리핑하여 SVG 크기와 무관하게 동일한 공간 점유.
  - 2열(1fr)에 텍스트를 배치해 모든 하위 메뉴의 텍스트 시작 X가 수직 일직선으로 완벽 고정.
  - **버전 산정**: 레이아웃 근본 구조 교정 ➔ **`v1.4.5.Build.00000`**

## [2026-07-25] [반영완료] 공급자 관리 조회 버튼 추가 & DB 일괄 업로더 vendors 컬럼 완전 지원 (v1.3.2.Build.00000)
- **요구사항**: 
  1. 공급자(Vendors) 관리 메뉴에 명시적 `조회` 버튼 추가 (현재 실시간 필터만 존재).
  2. DB 일괄 업로드 기능(DevDataUploader)에서 공급자 데이터(`vendors` 테이블)의 신규 컬럼(`representative`, `email`, `address`, `types`, `isActive`)이 지원되지 않는 문제 해결.
- **해결 설계**:
  - `Vendors.tsx`: 검색 입력값(`searchInput`)과 실제 조회 트리거값(`searchTerm`)을 분리, **`🔍 조회` 버튼** 클릭 또는 `Enter` 키로 조회 실행. **초기화(↺)** 버튼으로 검색어 및 필터 전체 리셋.
  - `schema.sql`: vendors 테이블 DDL에 `representative`, `email`, `address`, `types TEXT`, `isActive BOOLEAN DEFAULT TRUE` 컬럼 추가 및 type 허용값 `PURCHASE` 추가(기존 `CONSUMABLE` → `PURCHASE` 교정).
  - `DevDataUploader.tsx`: `COLUMN_LABEL_MAP`에 `type`, `types`, `bankAccount`, `isActive` 한글 라벨 추가 → 업로더가 공급자 전체 컬럼 지원.
  - **버전 산정**: 기존 메뉴(공급자 관리) 기능 추가 + DB 업로더 기능 보강 ➔ **`v1.3.2.Build.00000`** (Z 증가: Feature 추가)

## [2026-07-25] [반영완료] 개발자 도구 DB 업로더 테이블 선택 드롭다운 오름차순 정렬 및 라벨 중복 제거 (v1.3.2.Build.00001)
- **요구사항**: 
  1. 개발자 도구 - Supabase 데이터 업로더의 `① 테이블 선택` 드롭다운 목록 아이템들을 한글 이름 기준 가나다 오름차순 정렬.
  2. `고객사 (customers) (customers)` 처럼 괄호 라벨이 두 번 중복 표시되던 라벨 가공 방식 깔끔하게 교정.
- **해결 설계**:
  - `DevDataUploader.tsx`: `getDynamicTableSchemas` 결과를 한글 표기명(`label`) 기준 `localeCompare('ko')` 오름차순으로 정렬.
  - `<option>` 태그에서 중복 표시되던 `({t.supabaseTable})` 라벨 조합 수식 정리.
  - **버전 산정**: UI 및 드롭다운 정렬 단순 개선/Hotfix ➔ **`v1.3.2.Build.00001`** (Build 1 증가)

## [2026-07-25] [반영완료] 은행 입출금 내역 CSV 템플릿 UTF-8 BOM 유니코드 한글 깨짐 방지 패치 (v1.3.2.Build.00002)
- **요구사항**: 
  1. `은행 입출금 대장 (BankMatching.tsx)` 메뉴의 `템플릿 받기` 버튼으로 다운로드한 CSV 파일 오픈 시 엑셀(Excel)에서 한글이 깨지는 현상 수정.
- **해결 설계**:
  - `BankMatching.tsx`: Blob 생성 시 UTF-8 Byte Order Mark인 `\uFEFF`를 CSV 헤더 맨 앞에 적용하여 엑셀 자동 유니코드 인식 및 한글 깨짐 원천 차단.
  - **버전 산정**: 한글 인코딩 오타/버그 패치 ➔ **`v1.3.2.Build.00002`** (Build 1 증가)

## [2026-07-25] [반영완료] 학습형 매칭 룰 관리 조회/검색 필터 및 신규 룰 등록 기능 주입 (v1.3.3.Build.00000)
- **요구사항**: 
  1. `은행 입출금 대장 (BankMatching.tsx)`의 `학습형 매칭 룰 관리` 탭에 조회/검색 필터 바(이체자명/고객사명 검색, `🔍 조회` 버튼, `↺ 초기화` 버튼) 추가.
  2. 수동 대조를 거치지 않고도 매칭 룰을 사전에 직접 추가할 수 있는 `+ 신규 매칭 룰 등록` 버튼 및 모달 창 탑재.
- **해결 설계**:
  - `BankMatching.tsx`: `ruleSearchInput`, `ruleSearchTerm` 검색 필터 state 주입 및 필터링된 룰 목록 테이블 렌더링.
  - 신규 매칭 룰 등록 모달(`isRuleModalOpen`) 생성 (입금/이체자명 키워드 + ERP 고객사 선택 드롭다운).
  - **버전 산정**: 매칭 룰 메뉴 기능 확장 ➔ **`v1.3.3.Build.00000`** (Z 증가: Feature 확장)

## [2026-07-25] [반영완료] 법인카드 매입정산 메뉴 카드사 이용내역 UTF-8 BOM 템플릿 다운로드 추가 (v1.3.3.Build.00001)
- **요구사항**: 
  1. `법인카드 매입정산 (CorporateCardPage.tsx)` 메뉴에 `카드사 이용내역 템플릿 다운로드` 기능 추가.
  2. 엑셀(Excel) 오픈 시 한글 깨짐이 발생하지 않도록 UTF-8 BOM 유니코드 적용.
- **해결 설계**:
  - `CorporateCardPage.tsx`: 이용대금 파일 로드 카드에 `📥 템플릿 다운로드` 버튼 추가.
  - `\uFEFF` UTF-8 Byte Order Mark가 탑재된 샘플 거래내역 CSV(승인일시, 카드번호, 가맹점명, 승인금액, 부가세, 승인번호, 매입유형분류) 다운로드 핸들러 구현.
  - **버전 산정**: 템플릿 다운로드 기능 추가 ➔ **`v1.3.3.Build.00001`** (Build 1 증가)

## [2026-07-25] [반영완료] 사용자 및 메뉴 권한 통합 관리 권한 저장 비동기 보정 및 예외 에러 모달 연동 (v1.3.3.Build.00002)
- **요구사항**: 
  1. `사용자 및 권한 (UsersPermissions.tsx)` 메뉴에서 메뉴 권한 저장 시 저장 반영이 안 되는 문제 해결.
  2. 권한 저장 과정에서 예외/오류 발생 시 에러 모달(`showErrorModal`)을 연동하여 명확한 오류 원인 안내.
- **해결 설계**:
  - `AppContext.tsx`: `updatePermissions`를 비동기 `async` 함수로 전환하고 Supabase `permissions` 테이블 upsert 및 로컬 storage 동기화 처리 후 오류 시 throw하도록 수정.
  - `UsersPermissions.tsx`: `handleSavePermissions`를 `async/await` 및 `try/catch` 구문으로 보정하고 예외 발생 시 `showErrorModal` 에러 팝업 모달 연동.
  - **버전 산정**: 버그 수정 및 예외 처리 강화 ➔ **`v1.3.3.Build.00002`** (Build 1 증가)

## [2026-07-25] [반영완료] 인사 및 조직도 메뉴 임직원 역할(role) 저장 미반영 버그 수정 (v1.3.3.Build.00003)
- **요구사항**: 
  1. `인사 및 조직도 (OrganizationSettings.tsx)` 메뉴에서 임직원의 역할(role - ADMIN / MANAGER / USER) 수정 후 저장 시 실제 데이터베이스에 저장이 반영되지 않는 문제 해결.
- **해결 설계**:
  - `OrganizationSettings.tsx`: 프로필 변경 적용 함수(`applyProfileChanges`) 내에서 `saveUser` 및 `db` 영구 저장 핸들러를 바인딩하여 선택된 임직원의 `role` 및 `enforceManagerPolicies`로 변경된 직원의 역할 정보가 DB에 실시간 저장 전파되도록 보정.
  - **버전 산정**: 프로필 저장 버그 수정 ➔ **`v1.3.3.Build.00003`** (Build 1 증가)
