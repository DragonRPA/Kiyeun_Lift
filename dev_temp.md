# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 배차 기사 배정 폼내 [예상 운송비(필수)] vs [실제 운송비(선택)] 2열 분리 및 월말 운송료 대사 연동 대개편 (v1.15.0.Build.00000)

### 1. 사장님 지시 사항 및 프로세스 개편
- **지시 내용**:
  1. 배차 메뉴의 기사 배정 폼에서 **예상 운송비(`expectedCost`)**와 **실제 운송비(`finalCost`)**를 구분하여 입력받음. (예상 운송비 필수, 실제 운송비 선택적 입력)
  2. 운송비 대사 탭으로 넘어오면 실제 운송비(`finalCost`)가 존재할 경우 실제 운송비를 거래명세서와 대사하고, 실제 운송비가 없으면 예상 운송비(`expectedCost`)를 기준으로 대사 집행.

### 2. 주요 수술 내용
- **1. 배차 기사 배정 폼 UI 2열 분리 수술 (`TruckDispatch.tsx`)**:
  - `💰 예상 운송비 (필수)` (기본 70,000원 등) 와 `💵 실제 운송비 (선택)` (미입력 시 빈값, 입력 시 저장) 두 개의 전용 입력란으로 분리.
- **2. 1:1 대사 스튜디오 시스템 대사액(`systemCost`) 기준 개편**:
  - 실제 운송비(`finalCost`)가 존재(>0)하면 👉 **실제 운송비**를 대사 기준액으로 대조.
  - 실제 운송비가 미입력(0/null)이면 👉 **예상 운송비**를 대사 기준액으로 대조.
- **3. 좌측 시스템 배차 카드 직관 표출**:
  - **`💵 실제가: ₩100,000원 (💰 예상가 ₩70,000원)`** 형태로 한눈에 식별 가능하도록 시각화.
  - `[✏️ 실제가 수정]` 버튼을 누르면 실제 운송비(`finalCost`)를 입력 정정하여 DB에 영구 반영 및 검증.

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.15.0.Build.00000).
