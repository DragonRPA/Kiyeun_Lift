# 개발 지시 및 개편 완료 내역 (dev_temp.md)

- [완료] 출고 검수 장비 교체 모달 (`outbound_inspections.tsx`) 수리 미전환 시 버튼 활성화 및 비고 업서트 선택 처리 수술
  1. 수리정비 미전환(토글 OFF) 상태에서 비고를 작성하지 않더라도 **`[교체 실행]` 버튼이 100% 즉시 활성화**되도록 `disabled` 비활성화 조건식 수정 완료.
  2. 비고(교체 사유)가 적혀 있을 때만 장비 DB 비고에 업서트하고, 안 적혀 있으면 비고 업서트를 하지 않고 기존 이력을 온전히 보존하도록 조치.
