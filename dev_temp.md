# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 엑셀 헤더 감지 알고리즘 근본 버그 수술 (v1.14.0.Build.00006)

### 1. 핵심 버그 원인 및 수술 내역

**근본 원인**: 
- 이전 헤더 감지 방식이 `rowText = 행 전체 셀 이어붙인 문자열`에서 `.includes(keyword)` 를 사용
- 키워드 목록에 `'합계'`, `'금액'` 이 포함되어, 11행 "합계금액  7,304,000" 에서 `'합계'` + `'금액'` 두 키워드가 동시에 오탐 → **11행이 잘못 헤더로 인식**
- 이후 실제 헤더(13행)가 데이터 행으로 파싱되어 전체 컬럼 구조 붕괴

**수술 방식 (Cell-Level Exact Matching)**: 
- 헤더 키워드 목록에서 `'합계'`, `'금액'` 등 모호한 단어 제거
- 행 전체를 이어붙인 문자열이 아닌 **각 셀을 개별로 키워드 비교** (`cells.some(cell => cell === kw || cell.length <= 10 && cell.includes(kw))`)
- 매칭 임계값을 `>= 2` → `>= 3`으로 상향하여 오탐 가능성 차단

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.14.0.Build.00006).
