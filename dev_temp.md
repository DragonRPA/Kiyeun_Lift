# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 거래명세서 엑셀 동적 헤더 탐색 & 디토 기호 상속 & 다중 비고 3개 병합 파서 구축 (v1.13.2.Build.00000)

### 1. 주요 구현 내용
1. **스마트 동적 헤더 행 감지 (Dynamic Header Finder)**:
   - 엑셀 시트의 0~25번째 행을 탐색하며 알려진 헤더 키워드(`일자`, `상차지`, `하차지`, `톤수`, `차종`, `운송비`, `금액`, `청구금액`, `합계`, `현장명`, `업체명`, `기사명`, `비고` 등)가 2개 이상 들어있는 행을 진짜 헤더 행으로 자동 감지하여 헤더 위치와 명칭을 정확히 파싱.
2. **다중 비고 컬럼 자동 추출 및 텍스트 묶음 병합 (Multi-Column Memo Concatenation)**:
   - `현장명`, `업체명`, `비고`, `비고1`, `특이사항`, `메모` 등 비고 성격 컬럼이 2~3개 이상 존재하는 경우, 그 값들을 `[현장명: 아주렌탈] | [업체명: 류인관] | [비고: 현장대기]` 와 같이 텍스트로 예쁘게 병합하여 1:1 대사 스튜디오 카드에 가독성 있게 노출.
3. **디토 기호(" / · / 〃 / - ) & 빈칸 Fill-down 상속**:
   - `일자`, `상차지`, `하차지` 컬럼에 `"`나 `·`, `〃`, `-` 또는 빈칸이 반복되면 이전 행의 날짜/장소 값을 자동으로 상속(Fill-down)하여 데이터 유실 완전 차단.
4. **날짜 포맷 정규화 (Date Normalizer)**:
   - `6/1` -> `2026-06-01`, `06월 01일` -> `2026-06-01` 등 다양한 서식 일자를 ISO 표준 포맷(`YYYY-MM-DD`)으로 자동 변환.

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.13.2.Build.00000).
