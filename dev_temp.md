# 개발 지시 및 개편 완료 내역 (dev_temp.md)

- [완료] 스마트 출고 요청 (`smart_dispatch.tsx`) 자연어 원문 파싱 맞춤형 동적 라벨 표출 수술
  1. 원문 텍스트에서 `3면 함석` 감지 시, `4면 철망 / 함석 설치` 하드코딩 라벨 대신 **`☑ 3면 함석 설치`**로 정확한 맞춤형 라벨 표기명 렌더링.
  2. `4면 철망`, `3면 철망`, `4면 함석`, `상단 감지봉 (4EA)` 등 텍스트 내 세부 면수/수량 키워드를 정밀 추출하여 카드 라벨에 100% 동적 즉시 반영.
