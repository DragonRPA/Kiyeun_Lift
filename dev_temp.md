# 개발 지시 및 개편 완료 내역 (dev_temp.md)

## 🔒 [완료] 임직원 계약 기본급 DB 영구 보관 & 급여 정산 권한자 전용 격리 기능 개편 (v1.23.0.Build.106)

### 1. 사장님 개편 지시 사항 반영
- **임직원 스키마(`User`) 기본급 영구 보관**:
  - `User` 인터페이스에 `baseSalary?: number` 필드 신설 및 DB/LocalDB 영구 저장 처리.
- **급여 정산 권한자(`payroll` view/save 권한) 전용 조회 및 수정 격리**:
  - 급여 정산 권한이 없는 일반 사용자는 타인의 기본급 정보를 절대로 조회하거나 수정할 수 없도록 보안 차단.

### 2. 메뉴별 주요 연동 구현 내용
- **`[조직/인사 관리]` (`OrganizationSettings.tsx`)**:
  - 급여 권한자(`canViewPayroll`)에게만 사원 상세 프로필 모달 폼에 **`[💰 계약 기본급 (원)]`** 입력창 노출 및 수정 기능 제공.
- **`[급여 정산]` (`PayrollPage.tsx`)**:
  - 사원별 DB에 기록된 `u.baseSalary`를 동적으로 불러와 통상시급(`기본급 / 209시간`) 및 OT 수당(1.5배) 자동 계산.
  - 급여 대장 내 사원별 기본급 란에 **`[✏️ 수정]`** 인라인 버튼 및 모달 팝업 제공으로 즉시 기본급을 변경하고 DB에 영구 반영 조치.

### 3. 빌드 및 검증
- TypeScript `npx tsc -b` 컴파일 오류 없음 확인 ✅ (Exit code: 0)

---
**기록 일시**: 2026-08-09 14:32  
**작성 버전**: `v1.23.0.Build.106`
