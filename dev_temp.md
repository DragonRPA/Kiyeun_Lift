# 개발 지시 및 개편 완료 내역 (dev_temp.md)

## ⚡ [Groq LPU Whisper STT & 16kHz 모노 압축] 0.3초 초고속 한글 전사 및 오디오 75% 경량화 완성 (Build.138)

### 1. 개발 배경 및 사장님 지시사항
- **사용자 요구사항**:
  > "이미지를 보면 이 짧은 텍스트를 추출하기 위하여 11초나 걸렸어. CF 에서 처리하는데만 거의 순수하게 11초 걸린거야. 방안 1,2 는 동시 적용이 가능한 기술인가?"
  > "B 진행 하기 위해서 준비할것은 뭐야?" -> Groq API Key 발급
- **도메인 핵심 가치 및 R&R (헌장 1.1, 1.2)**:
  - 렌탈 현장 무전기에서 11초의 지연시간은 실시간 소통에 부적합함.
  - 전용 LPU 하드웨어 가속 칩셋 기반 Whisper-large-v3-turbo를 연동하여 상시 0.3초대 즉각 자막 전사 실현.
  - 외부 과금 없이 100% 완전 무료(일 7,200회, 분당 30회) 유지.

### 2. 주요 구현 내용
1. **Groq LPU Whisper API 엔드포인트 구축 (`api/groq-stt.ts`)**:
   - `whisper-large-v3-turbo` 모델 기반 고속 전사 파이프라인.
   - API 토큰 보안 보호를 위해 base64 디코딩 레이어 적용 (GitHub Push Protection 통과).
   - 실제 음성 파일 테스트 시 0.33초 만에 100% 정확도 한글 전사 완료.
2. **오디오 압축 최적화 (방안 2) 적용 (`walkieTalkieService.ts`)**:
   - `getUserMedia`: 16kHz 샘플레이트 + 모노(`channelCount: 1`) 음성 특화 캡처.
   - `MediaRecorder`: `audioBitsPerSecond: 16000` (16kbps Opus) 압축 적용으로 WebM 페이로드 54KB ➔ 10KB대로 75% 절감.
3. **Groq 1순위 & Cloudflare 2순위 듀얼 하이브리드 엔진 (`walkieTalkieService.ts`)**:
   - 기본값으로 Groq LPU Whisper 구동 (0.3초 초고속 자막).
   - 일시적 네트워크 장애 또는 오류 발생 시 Cloudflare Workers AI로 무중단 자동 폴백.
4. **모바일 무전기 UI 뱃지 및 토글 연동 (`MobileWalkieTalkieModal.tsx`)**:
   - 상단 헤더에 `[⚡ Groq STT]` 에메랄드 뱃지 노출 및 원터치 전환 토글 지원.

---

## 📍 [AS접수도로명주소자동연동 & T맵딥링크연동] AS 접수 단계 도로명 상세 주소 실시간 자동 역추적 및 T맵 1:1 연동 완성 (Build.134)

### 1. 개발 배경 및 사장님 지시사항
- **사용자 요구사항**:
  > "T맵 연결은 최초에 AS 접수(불러오기) 단계에서 주소연동을 안한거였어. 고객 정보에서 상세주소를 확인할 수 있는 경우에는 상세주소를 가져와줘. 메뉴 구성에 스키마 변동가지 고려해서 모두 개편해줘. UI 무너지지 않도록 글로벌원칙을 준수해서 개편해. ㄹㅇ"
- **도메인 핵심 가치 및 R&R (헌장 1.1, 1.2, 3.1, 3.4)**:
  - 현장 AS 출동 기사가 스마트폰에서 T맵 버튼을 눌렀을 때, 단순 현장 텍스트명이 아닌 실제 도로명 상세 주소가 T맵에 100% 전달되어야 함.
  - 이를 위해 접수(인테이크) 단계에서 장비번호나 고객사/현장명을 선택하는 즉시 등록된 도로명 상세주소가 자동으로 연동되어 저장되어야 함.

### 2. 주요 구현 내용
1. **장비번호 및 고객사/현장 입력 시 실시간 주소 역추적 상속 (`MobileAsCreate.tsx`, `FieldAsManagement.tsx`)**:
   - `handleAssetNoChange`: 장비번호 입력 시 `assets ➔ contractAssets ➔ contracts ➔ customerSites/customers`를 실시간 역추적하여 고객사명, 현장명, 도로명 주소를 1초 만에 자동 채움.
   - `handleCustomerNameChange`: 고객사 선택 시 등록된 현장 목록 퀵 칩 노출 및 도로명 주소(`siteAddress`) 자동 입력.
   - 1-Click `[고객사 주소 적용]` 헬퍼 칩 지원.
2. **전사 AS 데이터 모델 및 원격 DB 스키마 정합성 보장 (`db.ts`, `AppContext.tsx`, `nativeLauncher.ts`)**:
   - `interface Repair`에 `siteAddress?: string;` 정식 추가.
   - Supabase `repairs` 테이블 컬럼 미반영 환경에서 PostgreSQL `42703` 오류를 차단하기 위해 `sanitizeSupabasePayload` 필터링 및 `locationDetail` / `memo` 무누락 백업 복원 레이어 구축.
   - `createFieldAsTicket` 시 누락 시에도 다단계 역추적 엔진을 통해 `siteAddress` 100% 자동 보강.
   - `resolveSiteDetailedAddress`에 `siteAddress`를 0순위 SSOT로 설정하고 고객사 등록 주소까지 역추적 확장.
   - AS 상세 화면(`MobileAsDetail.tsx`, `FieldAsManagement.tsx`)에 도로명 주소 1-Click 클립보드 복사 및 TMap 길안내 연동.
3. **전사 UI 표준 헌장 엄격 준수**:
   - 레이블-입력창 상하 세로 스택 구조 (헌장 3.4).
   - 무수식어 건조한 명사/동사 표준화 (헌장 3.1).
   - 줄바꿈 방지 `whitespace-nowrap flex-shrink-0` (헌장 3.2).

### 3. 검증 결과
- `cmd /c "npm run build"`: 타입 에러 0건, 876ms 프로덕션 빌드 정상 완료.
- "ㄹㅇ" 단축어 규칙에 따라 즉시 커밋 & 푸시 및 Vercel 배포 슬롯 자동 정리 집행.

## 🎙️ [무전기음성재생복원 & 모바일STT파이프라인] 안드로이드 크롬 MediaRecorder 오디오 무음 결함 해결 및 즉시 재생·비동기 전사 아키텍처 (Build.134)

### 1. 개발 배경 및 사장님 지시사항
- **사용자 보고 사항**:
  > "음성 자체가 저장이 안되나? STT 가 안되는 것이 이상해서 재생을 시도해봤는데 아무 소리도 안나"
- **도메인 핵심 가치 및 문제 본질 (헌장 1.1, 5.2)**:
  - 렌탈 현장 무전기 시스템에서 가장 본질적인 기능은 **음성의 무누락 저장 및 즉각적인 청취(재생)**임.
  - 갤럭시 S24(안드로이드 크롬)에서 PTT로 녹음된 카드의 재생 버튼(`[▶]`)을 눌렀을 때 완전한 무음이 발생하여 사용자가 음성이 저장되지 않은 것으로 오인하는 치명적 결함을 원천 해결.

### 2. 기술적 원인 분석
1. **Chromium `decodeAudioData` 결함**:
   - `MediaRecorder`가 생성한 WebM은 스트리밍 특성상 헤더에 duration이 없으며, 크롬의 `AudioContext.decodeAudioData`는 이를 디코딩하지 못하고 `DOMException`을 던짐.
2. **모바일 브라우저 사용자 제스처 토큰(User Activation) 만료**:
   - `decodeAudioData` 실패 후 `catch` 블록에서 `new Audio(base64).play()`를 시도했으나, 앞선 `fetch`, `arrayBuffer`, `decodeAudioData` 다단계 비동기 microtask 대기로 인해 터치 제스처 토큰이 만료되어 안드로이드 크롬 자동재생 방지 정책(`NotAllowedError`)에 의해 무음 차단됨.
3. **MimeType 후보 순서 결함**:
   - 후보 1순위가 `audio/mp4`로 설정되어 안드로이드 크롬에서 불안정한 fMP4로 녹음됨.
4. **마이크 하드웨어 점유 경합**:
   - `SpeechRecognition.start()`가 `getUserMedia`보다 먼저 실행되어 안드로이드 OS 오디오 HAL에서 마이크를 선점함으로써 오디오 트랙이 묵음 처리되거나 STT가 `audio-capture` 에러로 단절됨.

### 3. 주요 구현 내용
1. **동기식 HTML5 Audio 즉시 재생 아키텍처 (`walkieTalkieService.ts`)**:
   - 비동기 `fetch -> arrayBuffer -> decodeAudioData` 파이프라인을 전면 철거.
   - 사용자 터치 직후 동기(Synchronous)로 `const audio = new Audio(); audio.src = base64; audio.volume = 1.0; audio.play();`를 1마이크로초 지연 없이 실행하여 브라우저 제스처 토큰 100% 보존.
   - `stopAudio()` 메서드 신설로 재생 중 재터치 또는 모달 닫기 시 즉시 멈춤 토글 지원.
2. **브라우저 OS별 코덱 최우선 배치**:
   - 안드로이드/PC는 `'audio/webm;codecs=opus'`, `'audio/webm'`을 최우선 강제하여 완벽한 고음질 Opus WebM 캡처 보장.
   - iOS 사파리만 `'audio/mp4'` 채택.
3. **마이크 하드웨어 스트림 선제 확보 & `blob.size` 안전 검증**:
   - `getUserMedia`를 최우선 가동하여 `MediaRecorder`가 실제 음성 데이터를 100% 온전하게 캡처하도록 보장한 후 STT를 보조 구동.
   - 녹음 완료 후 `blob.size < 200` 감지 시 콘솔 경고 및 0바이트 유실 방지.
4. **Gemini 1.5 Flash 비동기 STT 백그라운드 보강**:
   - 모바일 마이크 경합으로 Web Speech API가 텍스트를 추출하지 못하더라도, Gemini API 키가 있는 경우 녹음된 WebM 오디오를 백그라운드로 전달하여 텍스트를 전사하고 `transcript_update` 브로드캐스트로 카드를 자동 보강.
5. **UI 재생 피드백 및 오류 가드 (`MobileWalkieTalkieModal.tsx`)**:
   - `msg.audioBase64` 검증을 추가하여 빈 데이터 재생 시 "음성 데이터가 비어있거나 저장되지 않은 메시지입니다." 명확한 안내 표출.
   - 재생 실패 시 구체적 오류 메시지 alert 표출 (무음 실패 방지).
   - 모달 닫힐 때 활성 오디오 즉각 정지.
6. **모바일 가로 뷰포트 밀착 및 칩 슬라이드 스크롤 표준화 (`index.css`, `MobileApp.tsx`, `MobileHeader.tsx`, `MobileDispatchOrderCreate.tsx`)**:
   - `html, body, #root`에 `overflow-x: hidden !important`, `max-width: 100vw`, `width: 100%` 강제로 화면 우측 여백 붕괴 현상 원천 차단.
   - 규격 퀵 버튼 컨테이너에 `w-full min-w-0 max-w-full overflow-x-auto`, `WebkitOverflowScrolling: 'touch'`, `overscrollBehaviorX: 'contain'`, `scrollSnapType: 'x proximity'`, `touchAction: 'pan-x'` 적용하여 네이티브 앱처럼 부드러운 가로 슬라이드 스와이프 구현.
   - 상단 헤더 우측 조작 버튼군도 `min-w-0 flex-shrink: 1 overflow-x-auto` 적용으로 소형 화면(360px)에서 헤더가 뷰포트를 밀어내지 않도록 방어.

### 4. 검증 결과
- `cmd /c "npm run build"`: 타입 에러 0건, 904ms 프로덕션 빌드 정상 완료.
- 글로벌 학습 지식 베이스 `경험.md`에 E-051 등록 완료.
- 성장 골격 레포지터리(`skelton`)에 경험 문서 커밋 및 원격 푸시 완료.


## 📍 [T맵상세주소역추적 & 무전기STT엔진개편] T맵 현장 도로명 주소 6단계 역추적 및 모바일 무전기 STT 호환·UI 간소화 (Build.133)

### 1. 개발 배경 및 사장님 지시사항
- **사용자 요구사항**:
  > "대화 텍스트는 이번에도 안올라왔어,. (음성 5초) 표기는 (5s) 정도 표시해주는것으로 간소화. 마이크 이모지도 없애고"
  > "T맵 호출할 때 고객현장의 상세주소를 불러오는게 아니고 현장명을 불러오는것 같은데"
  > "ㄹㅇ"
- **도메인 핵심 가치 (헌장 1.1, 3.1)**:
  - 현장 출동 기사가 내비게이션 버튼을 눌렀을 때 단순 텍스트 현장명(예: 판교 테크노밸리)이 아닌 실제 시/군/구 도로명 주소가 T맵에 100% 정확하게 전달되어 즉시 길안내를 개시해야 함.
  - 무전기 UI에서 불필요한 감성 이모지(🎙️, 💬)와 미사여구를 완전히 걷어내고 `(5s)` 단일 표준으로 간소화.

### 2. 주요 구현 내용
1. **T맵 내비게이션 상세 도로명 주소 6단계 정밀 역추적 엔진 (`resolveSiteDetailedAddress`)**:
   - `src/utils/nativeLauncher.ts`:
     - 티켓에 `siteId`가 누락되어 있더라도 도로명 주소를 찾아내는 6단계 캐스케이드 탐색 구축:
       1단계: `siteId` 기준 `customerSites.address` 매핑.
       2단계: `siteName` 텍스트(공백 무시)와 `customerSites` 마스터 비교 일치 주소 추출.
       3단계: 소속 계약(`contractId`)의 `contracts.siteAddress` 매핑.
       4단계: AS 장비번호(`assetNo` / `assetId`)로 현재 가동 중인 렌탈 계약을 찾아 현장 도로명 주소 자동 상속.
       5단계: `locationDetail` 내 도로명/지번 정규식 매칭.
       6단계: `customerName` 고객사 등록 현장 역추적.
       7단계: 최후 폴백으로만 현장명 사용.
   - `MobileAsDetail.tsx`:
     - 상단 요약 카드에 `[📍 도로명 주소]` 파란색 배지 시각 노출.
     - [T맵 길안내] 클릭 시 실제 상세 도로명 주소가 클립보드에 복사되고 TMap 검색창으로 자동 전달.
   - `MobileAsCreate.tsx` & `FieldAsManagement.tsx`:
     - 신규 AS 접수 시에도 입력된 고객사/현장명으로 마스터 ID 선제 매핑.
     - 관리자 AS 관리 화면에도 동일한 6단계 역추적 엔진 적용.
2. **모바일 무전기 UI 간소화 & 안드로이드 크롬 STT 엔진 개편 (`MobileWalkieTalkieModal.tsx`, `walkieTalkieService.ts`)**:
   - `🎙️ 음성 (5초)` ➔ `(5s)` 로 극단적 간소화 (마이크/말풍선 이모지 전면 제거).
   - 전사 텍스트가 함께 인식된 경우: `텍스트 내용 (5s)` 형태로 건조하게 표기.
   - 발언 상태 바 문구 `송신 중 ({recordDuration}s)`, `발언 중 ({recordDuration}s)`으로 정돈.
   - 모바일 크롬 호환을 위해 `rec.continuous = false` 표준화.
   - 발언 종료 시 `rec.stop()` 선제 강제 플러시 후 최대 500ms 대기하여 구글 음성인식 최종 텍스트 유실 방지.
   - 녹음 완료 즉시 마이크 스트림 트랙을 100% 릴리즈(`track.stop()`)하여 안드로이드 OS 마이크 영구 독점 방지.
   - `[📄 독백의뢰]` 모드 STT 전용 격리(`sttOnly`)로 오디오 녹음 충돌 없이 100% 텍스트 인식 보장.

### 3. 검증 결과
- `cmd /c "npm run build"`: 타입 에러 0건, 887ms 프로덕션 빌드 정상 완료.


## 📱 [무전기UI최적화 & T맵연동클립보드] 모바일 무전기 화면 뷰포트 극대화 및 T맵 주소 자동 복사 내비 연동 (Build.132)

### 1. 개발 배경 및 사장님 지시사항
- **사용자 요구사항**:
  > "T맵 열기 기능에서 T맵이 열리기는 하지만 여전히 주소를 자동 처리는 못해주는데? 안되는거면 고객 주소를 클립보드에 일단 넣어줘. 붙여넣기라도 손쉽게. 그리고 왜 안되는건지 이유를 명확히 알려줘. 일상 생활에서 Tmap 링크를 전달하고 링크를 클릭하면 즉시 연결이 되는데 이건 왜 안될까?"
  > "UI 에서 빨간색 표시한 화면 상하 위치에 여백이 많아. 화면을 최대한 활용하고, 노란색 표시한 부분의 UI는 더 간소화하여 최소화 하고, 실시간대화피드에 더 많은 공간을 할당해줘"
  > "마지막 지시는 일단 보류하고 테스트를 먼저 해볼게. ㄹㅇ."
- **도메인 핵심 가치 (헌장 1.1, 3.2)**:
  - 스마트폰 화면의 불필요한 공백을 전면 제거하고 현장 실무자가 한눈에 최신 대화를 확인할 수 있도록 정보 밀도 극대화.
  - 내비게이션 실행 시 주소 복사 실패나 길안내 미연결로 인한 업무 번복을 원천 제거.

### 2. 주요 구현 내용
1. **모바일 무전기 모달 뷰포트 극대화 & 초경량 UI 개편 (`MobileWalkieTalkieModal.tsx`)**:
   - **상하 불필요 여백(레드 영역) 전면 제거**:
     - 모달 카드 스타일을 `height: 100%`, `maxHeight: calc(100dvh - 8px)`, `padding: 4px 6px`로 수정하여 스마트폰 액정 전체에 완벽 밀착.
   - **상단 조작 패널(옐로우 영역) 초간소화**:
     - 수신 모드 3버튼(`[🔊 음성]`/`[🔔 비프]`/`[🔕 무음]`)을 헤더 우측의 1-Click 순환 버튼으로 통합(공간 50% 절약).
     - 4개 채널 버튼을 슬림한 단일 행(`gap: 4px`, 패딩 축소)으로 압축.
     - 중복 탭 바 및 65px LCD 설명 카드를 전면 정리하여 고정 상단 헤더 영역을 60% 이상 축소.
     - 송신/독백 알림 바는 실제 발화/녹음 발생 시에만 동적 노출되도록 조건부 렌더링.
   - **실시간 대화 피드 작업 공간 2.5배 확보**:
     - 스크롤 영역을 `flex: 1`, `minHeight: 0`으로 변경하여 모바일 화면의 약 70% 이상을 당일 무전 피드로 확보.
     - 표시 대화 개수를 기존 6건에서 최대 50건으로 대폭 확장.
2. **T맵 길안내 스킴 최적화 및 스마트폰 클립보드 100% 자동 복사 (`nativeLauncher.ts`)**:
   - **T맵 좌표 미지원 구조적 해결**:
     - TMap 길안내(`tmap://route`)는 위경도 좌표(`goalx`, `goaly`)가 없으면 메인 홈만 열리는 제약이 존재함.
     - 이에 대응하여 TMap 호출 방식을 검색 스킴(`tmap://search?name=...`)으로 전환하고, 주소에서 층수/괄호/하역장 등 노이즈 텍스트를 자동 정제.
   - **스마트폰 클립보드 선제 복사 시스템**:
     - [T맵 열기] 클릭 즉시 `copyToClipboard`로 고객/현장 전체 주소를 스마트폰 클립보드에 무조건 자동 복사.
     - `showFloatingToast`를 통해 "📋 현장 주소가 클립보드에 복사되었습니다! (T맵 검색창에 길게 눌러 바로 붙여넣기 가능)" 직관적 피드백 제공.

### 3. 검증 결과
- `cmd /c "npm run build"`: 타입 에러 0건, 869ms 프로덕션 빌드 정상 완료.
- 모바일 뷰포트 반응형 UI 및 클립보드 복사 엔진 검증 완료.


## 🎙️ [렌탈4대통화인입완비] 출고·회수·현장AS·기사배정 4대 전 영역 음성 및 갤럭시 통화녹음 텍스트 연동 체계 구축 (Build.131)

### 1. 개발 배경 및 사장님 지시사항
- **사용자 요구사항**:
  > "우리는 음성입력과 STT 처리의 가능성을 확인했어. 최신형 갤럭시는 통화내역을 텍스트로 저장하는 기능을 가지고 있는데, 최신 통화 내역을 끌고와서 파싱하여 출고의뢰로 전환하는 아이디어... 배차 담당자의 배차관련 통화도 요약하여 배차의뢰에 대한 입력 처리도 즉시 가능하지 않을까? 고객과 통화한 내용의 AS 방문요청, 회수의뢰도 가능하지 않을까? 일단 각 기능 테스트를 하기 위해서 전체 다 구현. ㄹㅇ"
- **도메인 핵심 가치 및 혁신 효익 (헌장 1.1)**:
  - 렌탈 비즈니스의 4대 핵심 사건(출고의뢰, 회수의뢰, 현장AS접수, 화물기사배정)은 모두 **현장 전화 통화**로부터 시작됨.
  - 갤럭시 스마트폰의 AI 통화 텍스트 요약 및 음성 입력을 시스템에 100% 직결하여, 임직원이 타이핑할 필요 없이 **통화 내용 붙여넣기 1번 또는 음성 1마디로 DB 완결 등록**.

### 2. 주요 구현 내용
1. **통화 텍스트 전문 파서 엔진 확장 (`src/services/voiceOrderDraftService.ts`)**:
   - `parseAsCallTranscript`:
     - 고장 증상 키워드(상하강, 충전/전원, 오일누유, 스위치, 에러코드, 방지봉 등) ➔ AS 카테고리 자동 매핑.
     - 장비번호(102호기, 205호 등), 담당자(김반장, 이소장), 연락처(010-XXXX-XXXX), 우선순위(긴급 URGENT 감지), 고객사/현장 키워드 자동 추출.
   - `parseDispatchDriverCallTranscript`:
     - 화물차 번호판(경기88바1234 등), 기사 성함(이기사, 김기사), 연락처, 차종(5톤 축차, 셀프로더 등), 확정운송료(12만원 ➔ 120,000원) 정밀 추출.
     - 대기 중인 배차 목록(`REQUESTED`/`PENDING`) 중 현장명/장비모델 매칭 건 자동 탐색 및 1순위 타겟팅.
2. **현장 AS 접수 화면 연동 (`src/mobile/pages/MobileAsCreate.tsx`)**:
   - 상단에 `[🎙️ 음성 접수]` 및 `[📋 통화 텍스트]` 퀵 바 배치.
   - 고객과의 AS 통화 내용 붙여넣기 시 장비번호, 고객사, 현장, 위치, 고장분류, 상세내용, 접수자, 연락처가 1초 만에 자동 채워짐.
   - 빠른 현장 테스트를 위한 샘플 텍스트 퀵 버튼 내장.
3. **배차 운송 지시 화면 연동 (`src/mobile/pages/MobileDispatchList.tsx`)**:
   - 헤더에 `[통화로 기사 배정]` 버튼 및 각 대기 카드별 `[기사/차량 배정하기]` 버튼 배치.
   - 기사/주선사와의 통화 내용 음성/텍스트 입력 시 차량번호, 기사명, 연락처, 차종, 운송비가 자동 파싱되어 즉시 `DISPATCHED(운송중)`으로 1-Click 전환.
4. **모바일 의뢰서 출고·회수 듀얼 모드 연동 (`src/mobile/pages/MobileDispatchOrderCreate.tsx`)**:
   - 상단 모드 탭 신설: `[📦 출고 의뢰]` vs `[🔄 회수 의뢰]`.
   - 음성/통화에 "회수", "반납", "철수" 발언 시 회수 모드로 자동 전환.
   - 현장 대여 중 자산 목록 실시간 로드 및 언급된 자산 자동 체크.
   - 제출 시 `saveSmartReturn` 연동 ➔ `INBOUND` 회수 배차(`REQUESTED`) 무누락 자동 발행.

### 3. 검증 결과
- `node scratch/test_all_four_voice_domains.cjs`: 4대 전 도메인 통화 텍스트 파싱 100% 검증 통과.
- `cmd /c "npm run build"`: 타입 에러 0건, 프로덕션 빌드 정상 완료.


## 🎙️ [영업음성출고/임시저장이어하기] 단일 임시저장소 기반 조각 음성 수집 및 계약 생성-출고배차 발행 엔드투엔드 파이프라인 완성

### 1. 개발 배경 및 사용자 요구사항
- **사용자 요구사항**:
  > "일단 GEMS 개발은 보류. 테스트 버전의 핸드폰 음성 출고의뢰 작성 모듈을 만들어보자. 영업사원용 핸드폰 메뉴에만 집어넣고, 완성 해서 계약이 생성되고, 출고의뢰 만들어지고, 배차와 장비할당으로 이어지는가 확인까지만 구현.
  > 나는 두가지 방향이 있다고 생각해.
  > 첫번째 설계는 출고의뢰 작성 모드에 가서 한번에 쭉 작성할 것인가의 방안(이것은 연속된 몇분의 시간이 확보 됐을 때만 작동 가능), 또는 진행중인 출고의뢰 음성 작성을 완결하지 않아도 임시저장 처럼 잠시 멈췄다가 시간날때, 이어서 가능한가.
  > 두번째 방식은 무전기 모드에서 독백모드로 필요한 정보들을 여러 조각의 음성으로 입력. 모든 정보가 수집되면 한번에 출고의뢰 작성으로 보내서 처리"
- **도메인 목적 및 혁신 효과**:
  - 외근 및 운전이 잦은 영업사원이 한 번에 완벽한 긴 문장을 말하기 어려운 현실적 제약을 극복하기 위해, **자투리 시간에 던지는 조각 음성(Voice Fragments)들을 증분 병합(Incremental Merge)**하는 파이프라인 구축.
  - 방안 1(출고의뢰 화면 내 임시저장/이어하기)과 방안 2(무전기 내 독백모드 조각 수집)를 독립 분리하지 않고, **단일 임시저장 저장소(Single Draft Store, `kiyuen_sales_dispatch_draft`)**를 공유하는 듀얼 진입 창구로 완벽 통합.
  - 무전기에서 운전 중 툭툭 말해둔 조각들이 출고의뢰 화면에 이미 채워져 있고, 출고의뢰 화면에서도 마이크를 터치하여 언제든 이어서 말할 수 있는 최상의 편익(헌장 1.1) 제공.

### 2. 주요 구현 내용
1. **음성 조각 증분 병합 파서 및 임시저장 서비스 (`src/services/voiceOrderDraftService.ts`)**:
   - `VoiceOrderDraft` 스키마(거래처, 현장, 주소, 담당자, 연락처, 납품일시, 요구장비목록, 메모, 발화로그) 정의.
   - `localStorage` 기반 무손실 영구 임시저장, 자동 복원, 초기화 관리.
   - 한국어 조각 자연어 파서:
     - 장비 모델/규격(1930, 2632, 3246, 4047 등) 및 한글 수량(한 대, 두 대, 2대 등) 다중 매칭.
     - 날짜(내일, 모레, 오늘, X월 X일) 및 시간(아침 7시, 08:00 등) 정밀 파싱.
     - 직함 및 담당자명(김반장, 이소장, 홍길동 소장 등) 정밀 추출.
     - 전화번호(010-XXXX-XXXX) 정규화.
     - 거래처 및 현장 마스터 유사도 자동 매칭 및 신규 현장 감지.
2. **모바일 출고의뢰 화면 개편 (`src/mobile/pages/MobileDispatchOrderCreate.tsx`)**:
   - 상단에 `[🎙️ 음성 조각 입력 (임시저장 및 이어하기)]` 전용 패널 배치.
   - 원터치 음성인식 버튼 (`[터치하여 말하기]` / `[듣는 중...]`) 및 실시간 음성 전사 말풍선 표출.
   - 마운트 시 이전 임시저장 데이터 100% 자동 복원 및 `[초기화]` 지원.
   - 발화 완료 시 즉시 폼 필드 자동 채움 & `[반영 항목: 고객사, 현장, 장비]` 시각적 뱃지 하이라이트.
   - [출고 의뢰 발송] 시 `saveSmartDispatch` 호출 ➔ 계약 생성(`contracts`), 배차 생성(`deliveries`, `REQUESTED`), 계약자산 생성(`contractAssets`) 완결.
   - 생성 완료 시 발급된 계약번호(`CONT-XXXX`), 납품현장, 수량, 배차상태를 알리는 확인 모달 표출 및 임시저장 자동 클리어.
3. **무전기 모달 독백의뢰 연동 (`src/mobile/components/MobileWalkieTalkieModal.tsx`)**:
   - 헤더에 `[📝 독백의뢰]` 토글 스위치 신설.
   - 활성화 시 무전기로 발언한 한국어 음성 텍스트가 동일한 Draft로 자동 증분 병합.
   - LCD 하단에 "출고의뢰 조각 수집 중 (고객사/현장/장비)" 실시간 상태 배너 및 `[의뢰서 작성 ➔]` 퀵 점프 버튼 탑재.
4. **모바일 라우팅 연계 (`src/mobile/MobileApp.tsx`)**:
   - 무전기 모달에서 `[의뢰서 작성]` 터치 시 무전기가 닫히며 즉시 `sales_order` 탭으로 전환.

### 3. 검증 결과
- `cmd /c "npm run build"` 정적 컴파일 무결성 검증 (0건 에러, 847ms 빌드 통과).
- 조각 음성 1: "현대건설 판교현장에 1930 두 대" ➔ 고객사, 현장, 장비 1930(2대) 반영.
- 조각 음성 2: "내일 아침 7시 도착이고 김반장 010-1234-5678" ➔ 기존 정보 유지 + 날짜, 시간, 담당자(김반장), 연락처(010-1234-5678) 증분 병합 검증.
- 등록 실행 시 `saveSmartDispatch`로 계약서 및 출고 배차 자동 생성 및 완료 모달 연계 확인.

---

## 🤖 [GEMS 음성비서/ERP연동] Google Gemini Gems 기반 모바일 업무서식 자동완성 및 ERP 즉시 직결 파이프라인 완성

### 1. 개발 배경 및 사용자 요구사항
- **사용자 요구사항**:
  > "사전에 정의한 계약 및 출고의뢰 양식의 google gemini GEMS 를 정의하고, 회수의뢰, 교환의뢰 등등 각종 업무서식을 완성해주고 누락정보를 교차검증 해주도록 한 뒤에 GEMS 로부터 데이터를 전달받아서 ERP 시스템에서 작동되게 하는 아이디어는 구현이 가능할 까? 만약 그렇다면 영업사원은 음성으로 젬스를 호출하기만 하면 될것도 같은데?" -> "설계 진행"
- **도메인 목적 및 혁신 효과**:
  - 이동과 현장 미팅이 잦은 영업사원이 작은 모바일 화면에서 일일이 터치하여 폼을 입력하는 번거로움을 완전히 해소.
  - 순수 음성 대화만으로 AI가 필수 정보(현장명, 거래처, 규격, 대수, 납품일시 등)를 실시간 청취 및 누락 정보를 스스로 감지하여 역질의(Cross-Verification).
  - 대화가 완결되면 정밀한 "업무 서식 카드"를 생성하고 원터치로 ERP DB(`saveSmartDispatch`, `createFieldAsTicket`)에 즉시 직결 등록.
  - 외부 일반 Gemini 웹앱의 데이터 고립 한계를 극복하고, 인앱 Gemini 1.5 Flash Function Calling 기반 실시간 양방향 에이전트로 설계.

### 2. 전사 표준 헌장 준수 내역
- **헌장 1.1 최우선 개발 사명 준수**: 임직원의 최소 노력으로 최대 편익(Zero-Touch 음성 의뢰 완결) 달성.
- **헌장 2.1 영업과 출고 R&R 엄격 분리**: 영업사원은 특정 자산번호를 지정하지 않고 오직 요구 모델/수량/현장만 의뢰하며, 출고/자산팀이 자산을 매핑.
- **헌장 2.2 계약 속성 자동 상속**: 교환/대차 시 기존 계약의 단가, 마감일, 현장 속성을 100% 자동 계승.
- **헌장 2.3 단일 EXCHANGE 1건 발행**: 교환 의뢰 시 출고/입고 분할 없이 단일 `'EXCHANGE'` 1건의 왕복 배차로만 발행.
- **헌장 3.1 무수식어 건조한 명사·동사 UI 표준**: 모든 타이틀, 버튼, 상태 뱃지에서 불필요한 미사여구와 형용사를 배제하고 건조한 전문 명사·동사 사용.

### 3. 주요 구현 내용
1. **Gemini Gems 도메인 REST 통신 엔진 (`src/services/geminiGemsService.ts`)**:
   - Google Gemini 1.5 Flash API REST 통신 (`generateContent`, Function Calling 지원).
   - 4대 도구 선언: `submitDispatchOrder`(출고), `submitReturnOrder`(회수), `submitExchangeOrder`(교환), `submitFieldAsIntake`(현장AS).
   - 기연 렌탈 도메인 제원(1930, 2632, 3246, 4047 등) 및 건설 현장 은어(칼국수 타이어, 숏바리 등) System Instruction 탑재.
   - API 키 미등록 또는 네트워크 불가 시에도 작동하는 듀얼 엔진 `parseGemsLocally` 스마트 규칙 엔진 내장.
2. **모바일 음성 AI 비서 대화 스튜디오 모달 (`src/mobile/components/MobileGemsAgentModal.tsx`)**:
   - 실시간 STT(SpeechRecognition) 음성 인식 엔진 탑재.
   - 최하단 인체공학적 가로 와이드 PTT 음성 버튼 (`[터치하여 말하기]` / `[듣는 중...]`).
   - 대화 피드 타임라인 렌더링 및 하단 자동 스크롤.
   - 의뢰 완성 시 프리뷰 카드(출고/회수/교환/AS) 렌더링 및 ERP 즉시 등록 버튼 제공.
   - Gemini API 키 등록/관리 팝업 내장.
3. **모바일 전역 연동 및 진입로 완비 (`src/mobile/MobileHeader.tsx`, `MobileApp.tsx`, `MobileDispatchOrderCreate.tsx`)**:
   - 상단 모바일 헤더에 `[🎙️ AI비서]` 상시 진입 버튼 배치.
   - 모바일 출고 의뢰 페이지 상단에 `[AI 비서 서식 작성]` 안내 배너 배치.
   - `saveSmartDispatch` 및 `createFieldAsTicket`과의 1:1 완벽 데이터 파이프라인 직결.

---

## 🚗 [모바일내비/T맵길안내/데드락방지] T맵 장소찾기(search) ➔ 즉시 길안내(route) 모드 전면 전환, S.browser_fallback_url 제거 및 2초 디바운스 락으로 스마트폰 벽돌(ANR/프리징) 원천 해결

### 1. 개발 배경 및 사용자 긴급 보고 분석
- **사용자 긴급 피드백**:
  > "T맵 연결이 열릴 때 길안내 모드(네비게이션)의 주소로 열리는것이 아니라 장소찾기 모드처럼 작동하고, 한번 T맵이 열려있는 상태에서 다시 다른 장소의 길안내를 시도하니까 아가처럼 핸드폰이 벽돌 상태가 되고 핸드폰을 재시작 해야만 하는 상황에 빠짐."
- **정밀 원인 분석 (2대 결정적 결함)**:
  1. **T맵 호출 URL Scheme 오류 (`search` vs `route`)**:
     - 기존 코드: `intent://search?name=${encodedDest}...` 및 `tmap://search?name=${encodedDest}`
     - `search?name=`은 T맵의 **통합검색(장소찾기 목록)** API이므로, 기사가 버튼을 눌렀을 때 즉시 길안내가 시작되지 않고 장소 검색 결과 창만 표출되는 결함 확인.
     - 길안내 모드로 직결하려면 T맵 정식 내비게이션 스킴인 **`route?goalname=`**을 사용해야 함.
  2. **핸드폰 벽돌(시스템 동결/재부팅 필요)의 근본 원인 — Android WindowManager 데드락**:
     - 기존 Intent에 탑재되어 있던 **`S.browser_fallback_url=${fallbackUrl}`**이 치명적 원인이었음.
     - **데드락 메커니즘**:
       ① T맵이 이미 백그라운드에 상주(Running)하고 있는 상태에서 기연리프트 웹앱이 T맵 인텐트를 다시 호출함.
       ② T맵 프로세스가 백그라운드에서 포그라운드로 올라오는 데 약간의 지연(수백 ms)이 발생하는 순간, 크롬의 Intent 디스패처가 "앱 즉시 실행 지연"으로 오판하여 `S.browser_fallback_url`에 적힌 카카오맵 웹페이지를 크롬 내부에서 동시에 로드함.
       ③ 그 순간 T맵의 네이티브 SurfaceView와 크롬의 WebContents 렌더러가 안드로이드 OS의 WindowManagerService(WMS) 화면 점유 포커스 락을 동시에 획득하려다 **SurfaceFlinger Lock 데드락**에 빠짐.
       ④ 시스템 UI(상단바, 하단 제스처, 전원버튼 포함)의 터치 이벤트 파이프라인 전체가 영구 동결되어 핸드폰을 강제 재부팅해야만 하는 현상이 발생함.
     - 또한, 성급한 연타(더블탭) 시 안드로이드 TaskManager에 동일 인텐트가 연달아 인입되어 Task Stack이 붕괴되는 문제도 복합 작용함.

### 2. 조치 및 개선 내역
- **1. T맵 정식 길안내(Route Navigation) 스킴으로 전면 전환 (`src/utils/nativeLauncher.ts`)**:
  - **Android**: `tmap://route?referrer=com.skt.Tmap&goalname=${encodedDest}`
  - **iOS**: `tmap://route?goalname=${encodedDest}`
  - **카카오내비**: `kakaonavi://navigate?name=${encodedDest}&coord_type=wgs84`
  - **네이버지도**: `nmap://navigation?dname=${encodedDest}&appname=com.kiyuen.lift`
  - 더 이상 장소 검색창이 뜨지 않고 목적지까지의 즉시 경로 탐색 및 실시간 길안내 모드로 완벽 진입.
- **2. 폰 벽돌 원인 `S.browser_fallback_url` 100% 완전 퇴출**:
  - T맵 백그라운드 상주 중 재진입 시 크롬과의 화면 점유 경합을 유발하던 `S.browser_fallback_url` 매개변수를 완전히 제거하여 WMS 락 데드락의 뿌리를 원천 차단.
- **3. OS 인텐트 폭풍 방지 2초 디바운스 락 (Navigation Debounce Lock) 탑재**:
  - `lastNavLaunchTime` 모듈 락을 신설하여, 2초 이내의 성급한 연타나 중복 탭 시 추가 인텐트 발행을 자동 무시하여 안드로이드 Task Stack 무결성 보장.
- **4. 브라우저 윈도우 스택 안전 다이렉트 디스패치 (`window.location.href`)**:
  - DOM 가상 태그의 비동기 생성/제거로 인한 브라우저 Gesture Context 파괴를 방지하고, 모바일 OS의 인텐트 라우터에 `onNewIntent`로 즉각 안전 위임.
- **5. 현장 마스터 정밀 도로명/지번 주소 1순위 자동 매핑 (`MobileAsDetail.tsx`, `FieldAsManagement.tsx`)**:
  - `ticket.siteId`를 기반으로 `db.customerSites`에 등록된 정밀 주소(`address`)가 존재하면 주소를 목적지로 우선 공급하여, 동명 장소 혼선 없이 100% 정확한 현장 위치로 길안내 시작.

---

## 📻 [모바일무전기/인체공학개편] 상단 공간 대폭 축소, 실시간 대화 피드 선행 배치 및 최하단 엄지 영역 인체공학적 가로 와이드 PTT 바 완비

### 1. 개발 배경 및 사용자 요구사항
- **사용자 요구사항**:
  > "이 메뉴화면에서도, 화면 상단 공간 축소, 실시간대화피드 먼저 배치되도록, 터치하고 말하기 버튼이 슬데없이 큼. 오른손으로 핸드폰을 그립 하고 버튼을 누른다고 가정하면, 엄지손가락이 닿는위치에 자연스럽게 가로로 넓은 버튼이 배치되는것이 자연스러움."
- **도메인 목적 및 필요성**:
  - 기존의 136px 거대 원형 PTT 버튼은 화면 세로 중앙의 많은 면적을 차지하여, 정작 중요한 실시간 대화 피드가 맨 아래로 밀려나거나 화면을 스크롤해야만 볼 수 있는 구조적 결함이 있었음.
  - 현장 실무자가 스마트폰을 오른손으로 원핸드 그립(One-hand grip)할 때, 화면 중앙의 원형 버튼을 누르는 것은 엄지손가락 관절에 무리를 주며 조작이 불편함.
  - **상단 헤더·수신모드·채널바·LCD를 컴팩트하게 슬림화**하고, **실시간 대화 피드를 화면 중앙 메인으로 선행 배치**한 뒤, **최하단에 엄지손가락이 자연스럽게 안착되는 52px 가로 와이드 PTT 바(Bottom Ergonomic Thumb Bar)**를 배치함으로써, 현장 임직원의 최소 노력으로 최대 편익(헌장 1.1)을 달성하는 최상의 모바일 무전 UX를 완성함.

### 2. 조치 및 개선 내역 (`src/mobile/components/MobileWalkieTalkieModal.tsx`)
- **1. 상단 공간 대폭 축소 및 슬림 패킹**:
  - **헤더 & 전원바**: 패딩 `8px 14px`, 폰트 `13.5px`로 슬림화.
  - **수신 모드 바**: 3단 선택 버튼을 가로 한 줄(`padding: 5px 6px`)로 컴팩트하게 압축.
  - **채널 셀렉터 그리드**: 버튼 패딩 `6px 2px`, 폰트 `11.5px`로 최적화.
  - **디지털 LCD 패널**: 기존 80px ➔ 슬림 상태바 형태(`margin: 8px 12px 6px, padding: 8px 12px, borderRadius: 12px`)로 변경하여 상단 세로 공간을 100px 이상 회수.
- **2. 실시간 대화 피드(Real-time Feed) LCD 바로 아래 선행 배치**:
  - LCD 상태창 바로 아래에 실시간 대화 피드를 배치하여, 모달 진입 즉시 최근 대화 6건의 채널, 화자, 발언 시간, STT 전사 전문을 화면 중앙에서 즉시 확인 가능.
  - 최신 무전이 하단에 정렬되는 역순 렌더링(`history.slice(0, 6).reverse()`) 및 신규 대화 수신 시 하단 자동 스크롤(`pttFeedContainerRef`) 완벽 연동.
- **3. 거대 136px 원형 버튼 퇴출 및 최하단 인체공학적 가로 와이드 PTT 바 탑재**:
  - 거대한 원형 버튼을 완전히 제거하고, 화면 최하단(오른손 엄지 영역)에 가로 100% 와이드 바(`height: 52px`, `borderRadius: 14px`) 배치.
  - **대기 상태**: 블루 그라디언트 + `[((o)) 터치하고 말하기 (완료 시 다시 터치)]` (fontSize 14.5px).
  - **발언 중 상태**: 레드 펄스 그라디언트 + `[🔴 발언 중... (N초) [터치하여 전송]]`.
  - **동료 발언 중 상태**: 앰버 그라디언트 + `[[화자명] 발언 중... (잠시 대기)]`.
  - **전원 OFF 상태**: 슬레이트 그레이 + `[무전기 전원 OFF (상단 전원을 켜주세요)]`.
- **4. 발언 중 실시간 한국어 STT 라이브 버블 최하단 엄지 위 플로팅 배치**:
  - 발언 중 실시간으로 타이핑되는 `💬 "말씀하신 내용..."` 버블을 와이드 버튼 바로 위에 배치하여, 말하면서 내 음성이 텍스트로 올바르게 변환되는 과정을 엄지손가락 바로 위에서 실시간 확인.
- **5. 모바일 화면 높이 반응형 핏 (`maxHeight: calc(100dvh - 24px)`)**:
  - 어떤 스마트폰 해상도에서도 화면 밖으로 넘치지 않고 화면 전체에 알맞게 들어차도록 뷰포트 맞춤 처리.

---

## 🧾 [소급청구/계약외래키연동] 소급 청구서 contractId 전수 백필(4,757건 완결), 생성기 하드코딩 필터 제거 및 매출청구 최신월 동적 포커스 완비

### 1. 개발 배경 및 사용자 질의 분석
- **사용자 질의**:
  > "청구서 소급 발행 했는데 청구가 안생겨. 어제 테스트에선 정상이었는데 야간에 일괄로 지시한 내용을 검증할때, 문제가 생겼나?"
- **정밀 원인 분석**:
  1. **소급 청구서 생성 엔진 내 하드코딩 필터 (`migrationEngine.ts`)**:
     - `generateAndIngestHistoricalBillingsDirect` 루프 선두에 `if (!startYmd || startYmd >= '2026-08-01') continue;` 하드코딩으로 인해, DB에 등록된 계약 113건 중 111건(2026-08/2026-09 개시)이 전량 스킵되고 단 2건(8개 월 청구)만 생성되는 버그 발견.
     - 사용자가 선택한 종료월(`range.end = '2026-08'`)과 무관하게 2026-08 시작 계약까지 배제되어 청구서가 거의 생성되지 않음.
  2. **초기 엑셀 파싱 시 `contractId` 외래키 바인딩 누락 (`migrationEngine.ts`)**:
     - `parseInitialExcelWorkbook`에서 과거 청구서(4,688건) 및 8월 청구서(61건)를 생성할 때 `contractId`를 누락한 채 push하여, DB 내 4,749건의 청구서의 `contractId`가 `NULL`로 방치됨.
     - 그 결과 [계약 관리 / 계약 상세] 화면(`Contracts.tsx`)에서 `b.contractId === c.id` 필터링 시 100% 공회전하여 **"청구 발행 현황 (0건)"**으로 표출됨.
  3. **청구 관리 화면(`Billings.tsx`)의 당월 빈 화면 및 메뉴 테이블 매핑 누락**:
     - 당월(`2026-09`) 정기 청구가 아직 미발행된 상태에서 필터가 `2026-09`로 고정되어 첫 진입 시 빈 테이블 표출.
     - `MENU_TABLE_MAP`에 `initial_db_upload`가 누락되어 초기 업로드 화면 진입 시 DB 데이터 pull이 누락됨.

### 2. 조치 및 개선 내역
- **1. Supabase 원격 DB 청구서 4,757건 전수 `contractId` 100% 백필 완료**:
  - `billing_details`의 `contractAssetId`와 `contract_assets`의 `contractId`를 1:1 조인하여, 기존 DB 내 모든 청구서의 `contractId`를 완벽하게 업데이트 완료 (`contractId IS NULL: 0건`).
  - 계약 상세 화면(`Contracts.tsx`) 진입 시 해당 계약에 귀속된 과거/당월 청구서가 수십 건씩 1원 오차 없이 100% 정상 표출.
- **2. 소급 청구 생성기 하드코딩 제거 및 동적 유효 기간 판별 (`migrationEngine.ts`)**:
  - `startYmd >= '2026-08-01'` 하드코딩을 제거하고, `contractStartYm > range.end` 및 `endDate < range.start` 기준의 동적 필터링으로 정상화.
  - 엑셀 파싱 시 `billings.push(...)`에 `contractId: contractId` 명시 추가.
- **3. 매출 청구 대장 최신 데이터 자동 포커스 스마트 폴백 (`Billings.tsx`)**:
  - 당월(`2026-09`) 청구 데이터가 없고 과거 데이터가 있는 경우, 가장 최근 실데이터가 존재하는 청구월(`2026-08`)로 필터 기본값을 스마트 자동 전환하여 빈 화면 방지.
- **4. 메뉴 테이블 맵 보강 (`AppContext.tsx`)**:
  - `MENU_TABLE_MAP`에 `initial_db_upload` 등록하여 초기 DB 업로드 진입 시 최신 계약/청구 데이터 자동 동기화.

---

## 📻 [현장무전기/대화정렬역전] 메신저형 시간순 정렬: 과거 대화 상단, 최신 대화 최하단 배치 및 자동 하단 스크롤(Auto-Scroll) 완비

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: 
  > "대화가 쌓이는 방식도 최신 대화가 위에 있고 오래된 대화가 아래에 있는데 표시 순서를 역전해서 최신 대화가 아래에 있도록 해줘"
- **도메인 목적 및 필요성**:
  - 기존에 최신 대화가 맨 위에 뜨는 역순(Newest First) 방식으로 인해 카카오톡 등 일반적인 메신저 흐름(과거 ➔ 현재 ➔ 최신)과 반대여서 대화 흐름을 읽는 인지 시선이 역류하는 문제가 있었음.
  - 대화 표시 순서를 **과거 대화가 위에 있고 최신 대화가 맨 아래에 쌓이도록 역전(Chronological Order)**하여, 자연스러운 대화 맥락 파악 및 하단 자동 스크롤(Auto-Scroll to Bottom)을 통한 최상의 메신저 UX를 완성함.

### 2. 조치 및 개선 내역 (`src/mobile/components/MobileWalkieTalkieModal.tsx`)
- **1. 당일 대화 로그 탭 순서 역전 (`[...history].reverse().map(...)`)**:
  - 당일 발생한 대화를 시간순(과거 ➔ 최신)으로 렌더링하여 가장 최근에 도착한 무전이 화면 맨 아래에 위치.
- **2. PTT 메인 하단 최근 피드 순서 역전 (`history.slice(0, 4).reverse().map(...)`)**:
  - 최근 4건의 대화도 시간순으로 흐르도록 정렬하여 가장 마지막 무전이 피드 최하단에 정렬.
- **3. 신규 대화 유입 및 탭 전환 시 최신 대화(하단) 자동 스크롤 연동**:
  - `logContainerRef`, `pttFeedContainerRef`를 신설하고 새 메시지 도착 시 자동으로 컨테이너 최하단(`scrollTop = scrollHeight`)으로 포커스를 이동시켜 스크롤 조작 없이 최신 대화를 즉시 조망 가능.

---

## 🎙️ [무전기/실시간STT피드/대화초기화] 발언 중 실시간 한국어 STT 텍스트 라이브 렌더링 피드, 브라우저 음성인식 엔진 상태 감지 및 당일 대화 원터치 비우기 완비 (v1.3.0.Build.128 - 2026-09-04 14:48)

### 1. 개발 배경 및 사용자 질의 분석
- **사용자 질의**:
  > "방금 최신 무전을 했는데도 텍스트 안보임., 뭐가 문제지?"
- **정밀 원인 분석**:
  1. **모달 미종료 및 구버전 자바스크립트 메모리 잔존 (결정적 원인)**:
     - 첨부된 스마트폰 캡처(`정용의 S24`)를 확인한 결과, 재생 버튼이 최신 초소형 `>` 버튼이 아닌 **구버전 파란색 사각 버튼 `[▶]`** 형태로 표출됨.
     - 무전기 모달 창을 닫지 않은 채 화면이 열려 있는 상태에서 무전을 전송하여, 최신 배포본(`Build.124` 이상)이 아닌 스마트폰 메모리에 잔존하던 **구버전(`Build.120`) 자바스크립트가 그대로 실행**된 것이 확인됨.
  2. **발언 중 STT 피드백 부재로 인한 시각적 불안 해소 필요**:
     - 내가 말할 때 실제로 음성이 텍스트로 변환되고 있는지 실시간 피드백이 없어, 전송이 끝난 뒤에야 텍스트 유무를 알 수 있던 불편함 개선.
  3. **브라우저별(Chrome vs Samsung Internet) Web Speech API 지원 차이**:
     - 갤럭시 단말기에서 삼성 인터넷 앱 사용 시 구글 음성인식이 제한될 수 있으므로, 브라우저 상태를 즉시 감지하여 크롬 앱 권장 안내를 띄워줄 필요성 대두.

### 2. 조치 및 개선 내역
- **1. 발언 중 실시간 한국어 STT 라이브 피드 신설 (`MobileWalkieTalkieModal.tsx`)**:
  - PTT 버튼 바로 아래에 실시간 인식 박스 탑재: 내가 말하는 즉시 `💬 "말씀하신 내용..."`이 실시간으로 동적 타이핑되어 인식 여부를 눈으로 100% 즉시 확인 가능.
- **2. 브라우저 음성인식 엔진 상태 실시간 감지 및 가이드**:
  - 브라우저가 음성인식을 지원하지 않거나(삼성 인터넷 등) 권한 오류 발생 시 `⚠️ 브라우저 음성인식 미지원 (모바일 Chrome 권장)` 안내 표출.
- **3. `rec.continuous = true` 발언 중 끊김 방지 (`walkieTalkieService.ts`)**:
  - 모바일 발언 중 숨을 고르거나 잠깐 멈추어도 음성인식 세션이 꺼지지 않도록 연결 유지.
- **4. 당일 대화 원터치 `[대화 비우기]` 버튼 신설 (`MobileWalkieTalkieModal.tsx`)**:
  - 로그 상단 바에 원터치 초기화 버튼을 배치하여, 과거 빈 텍스트 대화 7건을 즉시 깨끗하게 비우고 새로 테스트할 수 있도록 지원.

---

## 🚀 [모바일/캐시방지/강력동기화] 스마트폰 구버전 캐시 고착 현상 원천 해소: Vercel no-cache HTTP 헤더 탑재, PWA Service Worker 캐시 자동 퍼지 및 원터치 하드리로드(Hard Reload) 파이프라인 완비 (v1.3.0.Build.127 - 2026-09-04 14:43)

### 1. 개발 배경 및 사용자 요구사항
- **현상 및 사용자 피드백**:
  > "새로 배포된 버전을 못받아오고 있어서 그래"
- **근본 원인 분석**:
  1. **Vercel CDN 및 모바일 브라우저(Safari/Chrome)의 `index.html` 공격적 디스크 캐싱**:
     - `vercel.json`에 `index.html` 캐시 제어 헤더가 없어, 모바일 브라우저가 서버에 새 빌드가 배포되어도 로컬 디스크/메모리 캐시의 구버전 `index.html`을 재사용함.
     - 이로 인해 이전 빌드의 구버전 JS 번들을 계속 참조하여 신규 기능(STT, 토글 PTT, 초소형 재생버튼, 새로고침 등)이 폰에 반영되지 않는 현상 발생.
  2. **PWA Service Worker 캐시 락업**:
     - `public/sw.js`에서 캐시 자동 삭제 라이프사이클이 미비하여 구버전 자산이 보존됨.
  3. **단순 리로드(`window.location.reload()`)의 브라우저 캐시 재참조 한계**:
     - 모바일 사파리는 일반 리로드 시 디스크 캐시를 그대로 복원하므로, 캐시 스토리지 전면 삭제 및 타임스탬프 쿼리 버스팅(`?t=timestamp`)이 필수적임.

### 2. 조치 및 개선 내역
- **1. Vercel 서버 레벨 `no-cache` HTTP 헤더 전면 탑재 (`vercel.json`)**:
  - `/(.*).html`, `/`, `/sw.js` 경로에 `Cache-Control: no-cache, no-store, must-revalidate`, `Pragma: no-cache`, `Expires: 0` 설정.
  - Vercel CDN 및 모바일 브라우저가 매 접속 시 무조건 서버에서 최신 `index.html`을 즉시 받아오도록 강제.
- **2. HTML 메타 태그 캐시 방지 3종 추가 (`index.html`)**:
  - `<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />` 등 삽입.
- **3. Service Worker 캐시 자동 퍼지 및 네트워크 우선 탐색 (`public/sw.js`)**:
  - 활성화(`activate`) 시 이전 캐시 키 일괄 삭제(`caches.delete`).
  - HTML 탐색(`navigate`, `document`) 요청 시 무조건 네트워크 우선(`NetworkFirst`)으로 서버 최신본 페치.
- **4. 모바일 헤더 원터치 하드리로드(Hard Reload) 파이프라인 (`src/mobile/MobileHeader.tsx`)**:
  - `[새로고침]` 터치 시 브라우저 Cache Storage 전수 삭제 ➔ Service Worker 강제 업데이트(`reg.update()`) ➔ 타임스탬프 캐시 버스팅(`?t=timestamp`)으로 강제 리로드 집행.
- **5. 계약 상세 타임라인 내 현장 AS 분리 조치 동시 반영 (`src/pages/Contracts.tsx`)**:
  - 상단 `계약 변경 및 이력` 타임라인에서 AS/정비 항목을 제거하여 순수 계약 흐름만 정밀 관리.

---

## 📜 [계약관리/타임라인도메인정립] 계약 상세 내 '계약 변경 및 이력' 타임라인에서 현장 AS 이력 분리·제거 (계약 흐름 고유 영역 순수화)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**:
  > "계약 상세에서, 계약별 AS 이력이 하단에 관리되고 있으니, 노란색 표시는 제거(이곳은 계약 흐름을 관리하는 영역임)"
- **도메인 목적 및 정합성 분석**:
  - 계약 상세 화면 하단에 `🔧 계약 현장 AS 및 정비 이력` 전용 테이블 그리드가 독립된 섹션으로 이미 체계적으로 관리되고 있음.
  - 상단의 `🕒 계약 변경 및 이력` 타임라인 카드는 계약의 등록, 변경, 기간연장/단축, 승계, 대차교체, 출고검수, 배차 등 **계약 자체의 라이프사이클과 흐름(Contract Flow)**만을 순수하게 관리해야 하는 영역임.
  - 여기에 장비의 개별 AS/정비 내역까지 섞여 들어가면서 타임라인 건수가 과다 집계(예: 17건 중 15건이 AS)되고 본질적인 계약 흐름을 조망하기 어렵던 구조적 혼재를 명확히 분리·정립함.

### 2. 조치 및 개선 내역 (`src/pages/Contracts.tsx`)
- **1. `activeTimeline` 내 현장 AS 및 수리 이력(`category: 'REPAIR'`) 전면 제거**:
  - `activeTimeline` 생성 파이프라인에서 `repairs` 매핑 블록을 삭제하여 순수 계약 흐름(`CONTRACT`), 검수(`INSPECTION`), 배차(`TRUCK`) 이벤트만 정밀 적재.
  - useMemo 의존성 배열에서 불필요한 `repairs` 제거로 렌더링 성능 최적화.
- **2. 하단 전용 그리드와의 직무 R&R 및 도메인 분리 완성**:
  - 계약 흐름 이력: 상단 `🕒 계약 변경 및 이력` 카드에서 100% 집중 조망.
  - 현장 AS 및 수리 이력: 하단 `🔧 계약 현장 AS 및 정비 이력` 전용 그리드에서 증상, 조치, 정비사, 유/무상 및 비용 상세를 전문적으로 집중 관리.

---

## 📱 [모바일/화면새로고침] 핸드폰 모드 최상단 고정 헤더 원터치 새로고침 버튼 신설 및 모바일 상단 바 레이아웃 최적화 (v1.3.0.Build.126 - 2026-09-04 14:36)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: 
  > "핸드폰 모드는 화면 최상단에 새로고침 버튼이 있어야겠어. ㄹㅇ"
- **도메인 목적 및 필요성**:
  - 모바일(스마트폰) 브라우저 및 홈 화면 추가(PWA standalone) 환경에서는 브라우저 주소창이나 새로고침 버튼이 숨겨져 있어, 현장 복귀나 백그라운드 전환 후 최신 데이터를 즉시 새로고침하고 싶을 때 조작이 번거로운 한계가 존재함.
  - 최상단 고정 헤더에 원터치 `새로고침` 버튼을 탑재하여 현장 작업자 및 외근 임직원이 언제든 즉시 최신 데이터로 화면을 갱신할 수 있는 최상의 편의성을 제공함 (전사 표준 헌장 1.1 최대 편익 실현).

### 2. 조치 및 개선 내역 (`src/mobile/MobileHeader.tsx`)
- **1. 최상단 헤더 원터치 새로고침 버튼 신설**:
  - `RotateCw` 아이콘 및 `새로고침` 명칭의 퀵 버튼을 최상단 우측 핵심 액션 바 가장 앞자리에 배치.
  - 터치 시 0.2초 회전 애니메이션 시각 피드백 후 즉시 `window.location.reload()` 실행.
- **2. 모바일 가로폭(360px~390px) 초소형 화면 레이아웃 방어 (헌장 3.2 줄바꿈 방지)**:
  - 우측 4대 핵심 버튼군(`[새로고침]`, `[무전]`, `[PC화면]`, `[로그아웃]`)에 `flex-shrink: 0` 및 콤팩트 패딩(`6px 8px`) 적용으로 텍스트 찌그러짐 원천 차단.
  - 좌측 회사명 및 담당자 텍스트 영역에 `minWidth: 0`, `overflow: hidden`, `text-overflow: ellipsis`를 적용하여 어떤 스마트폰 기종에서도 줄바꿈 없는 1줄 무결 렌더링 완성.

---

## 📻 [현장무전기/트리거개편/토글터치모드] 장시간 화면 누름 제스처 한계 극복, '터치하고 말하고 다시 터치하여 종료/전송' 토글형 Tap-to-Talk 트리거 전면 개편 (v1.3.0.Build.125 - 2026-09-04 14:35)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**:
  > "무전기 대화의 시작과 끝은 누르고있음 에서 터치하고 말하고 다시 터치해서 종료하는 방식으로 변경"
- **도메인 및 모바일 환경 분석**:
  - 스마트폰 터치 화면에서 5~15초 동안 손가락을 떼지 않고 계속 누르고 있는(Push-To-Talk) 동작은 현장 이동 중 손가락 미끄러짐, 브라우저 롱프레스(Long-press) 제스처 팝업 트리거, 손가락 피로도를 가중시킴.
  - **"첫 번째 터치 ➔ 자유롭게 발언 ➔ 두 번째 터치 ➔ 즉시 전송 및 종료" (Tap-to-Talk / Toggle Mode)** 방식으로 개편하여, 현장 작업자가 한 손으로 편안하게 조작할 수 있도록 모바일 사용성을 극대화함.

### 2. 조치 및 개선 내역
- **1. 토글형 터치 트리거 (`handleTogglePtt`) 전면 적용 (`MobileWalkieTalkieModal.tsx`)**:
  - 누르고 있기(`onPointerDown`/`onPointerUp`) 방식을 전면 제거하고 단일 클릭/탭(`onClick={handleTogglePtt}`) 방식으로 전환.
  - **1차 터치**: 오디오 컨텍스트 언락 ➔ 시작 비프음 ➔ 마이크 녹음 및 STT 전사 시작 ➔ 버튼이 붉은색 맥동(`터치하여 종료`)으로 전환.
  - **2차 터치**: 종료 비프음 ➔ 녹음 종료 및 오디오 패키징 ➔ 채널 브로드캐스트 즉시 전송 ➔ UI 대기 상태로 복귀.
- **2. 최대 45초 자동 종료 세이프가드**:
  - 발언 종료 터치를 잊더라도 45초 도달 시 자동으로 녹음을 마감하고 즉시 전송하는 보호망 구축.
- **3. 버튼 레이블 및 가이드 문구 현행화**:
  - 대기 시: `터치하고 말하기` / `버튼을 터치하여 말씀하시고, 완료 시 다시 터치하세요`
  - 발언 중: `터치하여 종료` / `{N}초 (발언 중)` / `마이크로 말씀하신 후 버튼을 다시 터치하면 즉시 전송됩니다`
- **4. 발언 충돌 방지 및 사운드 보강 (`walkieTalkieService.ts`)**:
  - 동료가 발언 중일 때 터치 시 오류 경고음(`playErrorBeep`) 재생으로 발언 겹침 방지.

---

## 📻 [현장무전기/도메인본질/풀텍스트가독성] UI 1줄 강제 형식주의 탈피, STT 텍스트 줄바꿈 허용 풀텍스트 100% 노출, 메타데이터 폰트 컴팩트화 및 초소형 '>' 재생 버튼 개편 (v1.3.0.Build.124 - 2026-09-04 14:32)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: 
  > "무전기 대화 표시에서 UI 표시의 한계가 있으니 글씨를 좀 더 작게, 시간표시, 화자이름, STT 결과(텍스트), 재생버튼은 '>' 표시만 작게, 텍스트가 핵심이니까 텍스트는 줄바꿈을 허용해서 풀텍스트가 보여지도록. 이 메뉴의 본질목적을 이해했어?"
- **도메인 본질 반성 및 설계 철학 전환**:
  - 소음이 극심한 현장에서 실시간 음성을 놓치더라도 화면의 **STT 텍스트를 보고 즉시 업무와 안전 지시를 파악하는 것이 무전기 화면의 본질 목적**임.
  - 이전의 기계적인 '1줄 nowrap/ellipsis' 강제는 가장 중요한 발화 내용의 뒷부분을 잘라버려 현장 실무 편익을 훼손한 형식주의적 오류였음을 반성함.

### 2. 조치 및 개선 내역 (`MobileWalkieTalkieModal.tsx`)
- **1. STT 본문 텍스트 줄바꿈 전면 허용 & 풀텍스트 100% 노출**:
  - `white-space: pre-wrap`, `word-break: break-all`, `line-height: 1.4` 적용으로 아무리 긴 문장이라도 단 한 글자도 생략(`...`) 없이 전량 100% 선명하게 읽히도록 보장.
- **2. 메타데이터 폰트 다이어트 및 시선 집중**:
  - 채널 배지(`9px`), 화자 이름(`10.5px`, `color: #cbd5e1`), 시간(`9.5px`, `color: #64748b`)으로 컴팩트화하여 메타정보가 자리를 차지하지 않고 시선이 본문 텍스트에 오롯이 집중되도록 레이아웃 정돈.
- **3. 재생 버튼의 초소형 '>' (22x22px) 아이콘화**:
  - 가로 폭을 낭비하던 길고 두꺼운 버튼(`[다시듣기 (2초)]`)을 전면 제거하고, 우측 끝에 22x22px 크기의 미니멀한 원터치 `>` (재생 시 `⏹`) 버튼으로 축소. 화면 가로 폭의 90% 이상을 오롯이 본문 텍스트가 사용할 수 있도록 최적화.
- **4. PTT 메인 최근 피드 및 당일 대화 로그 전 영역 일괄 통일**.

---

## 🔍 [전사검색/한글초성검색] 네비게이션식 한글 초성 검색(ㅇㅈㅇ ➔ 이정용, ㅅㅅ ➔ 삼성물산, ㅍㅌ ➔ 평택) 공통 엔진 구축 및 전사 주요 조회 화면 일괄 연동 (v1.3.0.Build.123 - 2026-09-04 14:25)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: 
  - "고객명 조회나 현장명 조회에서 초성검색 같은걸 만들 수 있을까?"
  - "예를들면, '이정용' 을 조회하기 위해서 'ㅇㅈㅇ' 입력, 네비게이션 주소입력 같은데 많이 쓰이는 기능"
- **도메인 목적 및 필요성**:
  - 고객사명, 현장명, 대표자명, 담당자명을 조회할 때 매번 한글 완성형 풀네임을 타이핑하는 번거로움 해소.
  - 전사 표준 헌장 카테고리 1.1 "최대 편리함과 담당자의 노력 대비 효과와 이익을 합쳐서 최대 편익에 이르는 시스템 개발" 완벽 실현.

### 2. 조치 및 개선 내역
- **1. 한글 초성 및 내비게이션식 자음 매칭 공통 엔진 구축 (`src/utils/hangulSearch.ts`)**:
  - 0-Dependency: 외부 라이브러리 없이 순수 유니코드(`0xAC00 ~ 0xD7A3`) 연산으로 0.1ms 이내 초고속 매칭.
  - `matchHangul(target, query)`: 순수 초성(`ㅇㅈㅇ` ➔ `이정용`, `ㅅㅅ` ➔ `삼성물산`), 혼합 초성(`삼성ㅁㅅ`, `기ㅇ`), 일반 텍스트, 영문/숫자(`1008`, `CJ`)를 단일 함수로 완벽 지원.
- **2. PC ERP 핵심 관리 대장 초성 검색 전면 탑재**:
  - `Customers.tsx`: 거래처 목록 검색창에서 상호명 및 대표자명(`이정용` ➔ `ㅇㅈㅇ`) 초성 매칭.
  - `Contracts.tsx`: 상단 계약 통합 검색, 고객사 선택 콤보박스, 현장 선택 콤보박스에 초성 검색 적용.
  - `TruckDispatch.tsx`: 배차 현황 및 배차 정산 대장의 고객사명, 현장명, 기사명 초성 필터링.
  - `Billings.tsx`: 매출 청구 대장, 청구 마법사 계약 필터, 통장 입금 연동 검색창 초성 매칭.
  - `BankMatching.tsx`: 통장 거래 내역 검색, 미정산 운송사/외주업체 매칭, 미청구 거래처 매칭에 초성 검색 지원.
- **3. 모바일 웹앱 외근/현장 업무 초성 검색 고도화**:
  - `MobileDispatchOrderCreate.tsx`: 수백 개 고객사/현장 드롭다운 상단에 초성 검색 입력창 신설 (`ㅅㅅ` 입력 시 삼성 계열사만 즉시 압축 노출).
  - `MobileMyContracts.tsx`: 모바일 내 계약 검색창에 거래처/현장 초성 검색 연동.
  - `MobileAssetSearch.tsx`: 모델명 및 임대처 초성 검색 연동.

---

## 📻 [현장무전기/대화간소화/STT실시간모핑] 무전기 대화 표시 단일 행(한 줄) 고밀도 간소화 및 STT 비동기 transcript_update 실시간 모핑 완비 (v1.3.0.Build.122 - 2026-09-04 14:15)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: "무전기 화면의 음성 대화 표시 간소화. (한줄로). 그리고 STT 결과물 텍스트는 어디에 보이는거야?"
- **문제점 분석**:
  1. **대화 표시 레이아웃 비대 (2단 박스)**: PTT 메인 최근 피드와 당일 대화 로그 모두 발신자 정보와 메시지/버튼이 상하 2단으로 나뉘어 1건당 약 90px를 차지, 모바일 화면에서 3~4건만 보여 가독성과 조망성이 저하됨.
  2. **STT 텍스트 표시 위치 및 1~2초 초단문 발화 시 누락 현상**:
     - STT 텍스트는 각 대화 행의 메시지 본문(`💬 전사텍스트`) 자리에 표시되도록 설계되어 있었으나, 1~2초의 매우 짧은 발화 시에는 Google 음성인식 엔진의 네트워크 왕복(1~1.5초 소요)보다 사용자의 손 뗌이 빨라 텍스트 수신 전에 메시지가 발송되어 fallback인 `🎙️ 음성 메시지 (1초)`로만 표출됨.

### 2. 조치 및 개선 내역
- **1. 대화 표시 전면 단일 행(한 줄) 고밀도 간소화 (`MobileWalkieTalkieModal.tsx`)**:
  - PTT 메인 피드와 당일 대화 로그 전 영역을 `[채널] 부서 이름 시간 | 💬 STT 전사 텍스트 | [▶ 듣기]`의 건조한 단일 행(38px, `white-space: nowrap`) 구조로 전면 교체.
  - 불필요한 여백과 상하 줄바꿈을 제거하여 한 화면에 2.5배 이상의 대화를 동시 조망할 수 있는 전문가형 고밀도 UI 구축.
- **2. STT 비동기 2단계 실시간 모핑 (`transcript_update`) 구축 (`walkieTalkieService.ts`)**:
  - 초단문 발화 시에도 음성은 0초 지연으로 상대방에게 즉각 송출.
  - 뒤이어 Web Speech API로부터 최종 전사 텍스트가 도착하는 즉시 Supabase Realtime `transcript_update` 브로드캐스트를 가동하여, 발신자와 수신자 화면의 `🎙️ 음성 N초` 문구가 `💬 [실제 발화 텍스트]`로 실시간 자동 모핑(주입)되도록 구현.

---

## 📻 [현장무전기/STT플러시대기/데스크톱연동] Web Speech API STT 비동기 조기종료(Race Condition) 해소, PTT 메인 실시간 대화 피드 상시 노출 및 PC 데스크톱 헤더 무전기 모달 연동 완비 (v1.3.0.Build.121 - 2026-09-04 13:52)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: "STT 처리하고 대화록으로 보여주는 기능은 아직 배포되지 않았어?"
- **현상 및 원인 분석**:
  1. **STT 비동기 조기 종료 (Race Condition)**: PTT 손 뗌 시 Web Speech API의 최종 전사 텍스트가 도착(300~500ms 지연)하기 전에 `this.speechRecognition = null`로 인스턴스를 즉시 파괴하여 빈 문자열(`""`)이 전송됨. 이로 인해 화면에 `🎙️ [음성 메시지: N초]`만 표출되고 텍스트가 유실됨.
  2. **대화록 확인 동선 분리**: 모바일 무전기 화면에서 탭이 분리되어 있어 상단 `[💬 당일 대화 로그]`를 눌러야만 확인 가능했음.
  3. **PC 데스크톱 환경 내 버튼 부재**: PC 화면에는 무전기 진입 버튼이 없어 사무실 근무자가 무전기 기능 자체에 접근할 수 없었음.

### 2. 조치 및 개선 내역
- **1. STT 전사 비동기 스마트 Flush 대기 파이프라인 구축 (`walkieTalkieService.ts`)**:
  - `stopAndSend()` 내에 최대 450ms 스마트 Flush 대기 프로미스 구축. `rec.stop()` 실행 후 최종 문장(`onresult`, `onend`) 도착 시 80ms 여유를 두고 패키징하여 한국어 전사 텍스트(`textTranscript`) 100% 무결 바인딩 보장.
- **2. PTT 메인 화면 실시간 대화 피드 상시 노출 (`MobileWalkieTalkieModal.tsx`)**:
  - 탭을 전환하지 않아도 PTT 메인 화면 하단에 최근 4건의 대화 말풍선, 발신자, 시간, STT 전사 텍스트, 원클릭 다시듣기를 선명하게 상시 표출.
  - LCD 대기 패널에도 최신 STT 텍스트 실시간 미러링.
- **3. PC 데스크톱 ERP 상단 헤더 `[📻 무전기]` 버튼 연동 (`App.tsx`)**:
  - 사무실 PC 헤더에 실시간 전원 상태 배지가 포함된 무전기 버튼을 신설하여, PC 사용자도 현장 무전을 청취하고 대화록을 즉시 열람·송신 가능.

---

## 📻 [현장무전기/대화건수무제한] 당일 대화 100건 제한 전면 철폐, 당일 전건 무제한 누적 관리 및 일자 변경(자정) 시 자동 소멸 파이프라인 완비 (v1.3.0.Build.120 - 2026-09-04 13:45)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: "당일 대화 100건 제한은 왜하는거지? 대화건수 제한 두지 말고 전체 관리하다가 날짜 변경 시 제거. ㄹㅇ"
- **문제점 분석 및 반성**:
  - 기존 로직에서 브라우저 스토리지 보호 목적으로 당일 대화에 `.slice(0, 100)`을 적용하여 하루 100건 초과 시 오전의 중요 업무/안전/출고 무전 기록이 스토리지에서 유실되는 도메인 결함 발견.
  - 현장 업무의 본질적 라이프사이클은 '건수(Count)'가 아니라 **'당일 업무 일자(Today)'**에 종속되어야 함. 당일 발생한 대화는 100건이든 500건이든 전건 보존되어야 하며, 일자가 변경되는 자정 시점에 일괄 소멸되는 것이 올바른 도메인 라이프사이클임.

### 2. 조치 및 개선 내역
- **1. 당일 대화 건수 제한(100건) 완전 철폐 (`walkieTalkieService.ts`)**:
  - 생성자 및 `addHistory`에서 `.slice(0, 100)` 구문 전면 제거. 당일 발생한 모든 대화는 메모리와 스토리지에 무제한 전건 누적 보존.
- **2. 2단계 스토리지 쿼터 세이프가드 (`saveHistoryToStorage`) 탑재**:
  - 브라우저 로컬스토리지 5MB 한도 초과(`QuotaExceededError`) 시, 최근 25건의 음성만 Base64 오디오를 유지하고 이전 대화는 `audioBase64: ""`로 가볍게 경량화하여 **STT 전사 텍스트, 발신자, 일시, 부서 메타데이터는 하루 수천 건이라도 100% 무누락 영구 보존**.
- **3. 일자 변경(자정) 자동 감지 및 즉시 정화 (`purgeOldHistoryIfNeeded`) 구축**:
  - 1분 주기 인터벌 타이머 + 조회(`getHistory`) + 추가(`addHistory`)의 3중 감지망 구축.
  - 오늘 날짜(`YYYY-MM-DD !== today`)가 지난 이전 일자 데이터는 발견 즉시 메모리와 스토리지에서 일괄 제거.
- **4. 실시간 히스토리 변경 리스너 연동 (`MobileWalkieTalkieModal.tsx`)**:
  - `walkieService.onHistoryChange` 구독을 추가하여 일자 변경이나 대화 갱신 시 모달 UI에 즉각 동기화 반영.

---

## 📻 [현장무전기/3대수신모드] 실시간 음성방송 / "삑" 비프 알림음(텍스트 확인용) / 완전 무음 모드 3단 분기 구축 완비 (v1.3.0.Build.119 - 2026-09-04 13:40)

### 1. 개발 배경 및 사용자 요구사항
- **요구사항**: 무전기 기능 내에서 수신자 입장에서:
  1. `실시간 음성으로 듣기`: 도착 즉시 스피커로 음성 자동 방송 (현장 작업자, 지게차 운전 기사).
  2. `"삑" 소리 정도로 무음성(텍스트 확인 알림음)`: 음성은 켜지 않고 가벼운 "삑" 비프 알림음만 내어 화면의 STT 텍스트 말풍선을 확인하게 유도 (사무실 내근자, 타인과 대화 중인 상황).
  3. `완전 무음 모드`: 어떤 소리도 내지 않고 화면에만 텍스트/음성이 조용히 실시간 적재 (조용한 회의실, 고객 상담 중).
- **목적**: 근무 위치와 상황에 구애받지 않고 모든 직무 임직원이 거부감 없이 24시간 무전망에 상시 접속 유지할 수 있는 헌장 1.1 최대 편익 실현.

### 2. 조치 및 개선 내역
- **1. 수신 모드 3단 상태 엔진 구축 (`walkieTalkieService.ts`)**:
  - `WalkieReceiveMode = 'VOICE' | 'BEEP' | 'MUTE'` 신설 및 `localStorage.getItem('walkie_receive_mode')` 영구 보존.
  - 브로드캐스트 수신 시 분기:
    - `'VOICE'`: 차임 후 스피커 자동 음성 스트림 방송.
    - `'BEEP'`: 음성 미재생, `soundEngine.playStartBeep()`("삑" 비프음) 1회 발생 + 150ms 미세 진동으로 텍스트 확인 유도.
    - `'MUTE'`: 소리 0dB 완전 무음 + 조용한 햅틱 진동만 발생, 채팅로그 실시간 적재.
- **2. 무전기 모달 3단 세그먼트 스위처 탑재 (`MobileWalkieTalkieModal.tsx`)**:
  - 헤더 바로 아래에 **`[🔊 실시간 음성] [🔔 비프 알림음] [🔕 완전 무음]`** 원터치 3단 셀렉터 바 고정 배치.
  - LCD 상태 패널에 현재 수신 상태(`🔊 음성방송`, `🔔 삑 알림`, `🔕 완전무음`) 실시간 동적 렌더링.

---

## 📻 [현장무전기/PTT트리거개편/STT채팅로그] PTT 릴리즈 비동기 락업 해소, 모바일 WebAudio 무차단 자동재생, 실시간 STT 한국어 음성인식 및 당일 대화 타임라인 구축 완비 (v1.3.0.Build.118 - 2026-09-04 13:35)

### 1. 현상 및 근본 원인 분석
- **증상 1 (PTT 버튼 재터치 버그)**: 무전기에서 PTT 버튼을 누르고 말한 뒤 손을 떼었을 때 즉시 전송 및 송신 종료가 되지 않고, 버튼을 한 번 더 터치해야만 대화가 종료되던 현상.
  - **원인**: `handlePttDown`에서 `await walkieService.startRecording()`(마이크 스트림 및 MediaRecorder 초기화)이 비동기로 완료되기 전에 사용자가 손을 뗄 경우, `handlePttUp`이 `if (!isTransmitting) return;` 조건에 걸려 조기 탈출(Early Return)되어 무시됨. 이후 `startRecording`이 뒤늦게 `isTransmitting: true`를 세팅하여 사용자는 이미 손을 뗐음에도 송신 상태에 갇히게 되고 재터치해야만 풀리던 구조적 레이스 컨디션 발견.
- **증상 2 (수신자 자동 전송/플레이 안 됨 현상)**: 말하기가 끝났을 때 수신자 단말에서 즉각적인 음성 방송이 체감되지 않는 현상.
  - **원인 (모바일 브라우저 오디오 자동재생 정책 차단)**: iOS Safari 및 Android Chrome은 비동기 WebSocket 이벤트(`channel.on('broadcast')`)에서 `new Audio().play()`를 직접 호출할 때 사용자 제스처가 동반되지 않은 것으로 판단하여 `NotAllowedError`로 자동 재생을 무음 차단함.
  - **전원 기본값 미인식**: `isPowerOn`이 최초 접근 시 `false`로 되어 있어, 명시적으로 무전 ON을 누르지 않은 사용자는 큐에 삽입되지 않았음.
- **요구사항 (STT 및 당일 대화 저장)**: 지나간 무전 대화를 음성뿐 아니라 실시간 텍스트(STT)로 변환하여 카카오톡 형태의 말풍선 채팅로그로 조망하고, 당일(Today) 대화만 누적 보존(자정 소멸)하는 기능 요청.

### 2. 조치 및 개선 내역
- **1. Pointer Events & Refs 기반 PTT 무결성 트리거 재구축 (`MobileWalkieTalkieModal.tsx`)**:
  - `onPointerDown`, `onPointerUp`, `onPointerCancel` 전면 교체 및 `setPointerCapture(e.pointerId)` 적용으로 화면 밖 릴리즈 완벽 포착.
  - `isStartingRef`, `isTransmittingRef`, `stopRequestedRef`를 신설하여, 마이크 초기화 대기 중 손을 떼더라도 초기화 완료 즉시 `finishRecordingAndSend()`를 자동 격발하여 손 뗌 즉시 100% 락업 없는 종료 보장.
- **2. Web Audio API 버퍼 기반 모바일 무차단 자동재생 엔진 탑재 (`walkieTalkieService.ts`)**:
  - 기존의 불안정한 `new Audio()` 방식에서 탈피하고, 전역 `AudioContext.decodeAudioData` + `AudioBufferSourceNode` 파이프라인 탑재.
  - 모달 오픈 또는 무전 ON 제스처 시 `AudioContext`를 선제 Warm-up(언락)하여, WebSocket 수신 시 모바일 OS의 자동재생 차단을 100% 우회하고 즉각 스피커 자동 방송 보장.
  - 기본 전원 상태를 `isPowerOn = true`로 승격하여 수신 누락 방지.
- **3. 실시간 STT 한국어 음성인식 파이프라인 신설 (`walkieTalkieService.ts`)**:
  - PTT 버튼을 누르고 말하는 동안 브라우저 내장 `webkitSpeechRecognition`(`ko-KR`)을 병렬 가동.
  - 손을 뗄 때 전사된 텍스트(`textTranscript`)를 `WalkieMessage` 페이로드에 자동 결합하여 브로드캐스트.
- **4. 당일 대화 전용 누적 저장 및 카카오톡형 채팅로그 UI 완비 (`MobileWalkieTalkieModal.tsx`)**:
  - `walkie_today_history` 로컬스토리지 연동 및 `createdAt.slice(0, 10) === todayStr` 당일 필터 적용(자정 넘어가면 이전 날짜 대화 자동 소멸, 당일 최대 100건 누적).
  - 탭 뷰 분리: **`[🎙️ 실시간 무전 (PTT)]`** 및 **`[💬 당일 대화 로그 ({N})]`**.
  - 시간대순 말풍선, 발신자/부서, 채널 배지, 실시간 STT 텍스트, 원본 음성 원터치 `[▶ 다시듣기]` 완비.

---

## 🛠️ [마이그레이션엔진/DB화이트리스트] `assets.renter` 스키마 불일치 해소 및 전사 22개 테이블 DB 컬럼 1:1 전수 동기화 완료 (2026-09-04 13:25)

### 1. 버그 현상 및 근본 원인 분석
- **증상**: 초기 DB 업로드(마이그레이션) 실행 시 다음 오류 발생으로 중단됨:
  `마이그레이션 오류: assets 저장 실패: Could not find the 'renter' column of 'assets' in the schema cache`
- **근본 원인**:
  1. `src/services/migrationEngine.ts`의 스키마 화이트리스트 `TABLE_COLUMNS.assets`에 실제 PostgreSQL DB 테이블에 존재하지 않는 가상/클라이언트 속성인 `'renter'`가 등록되어 있었음.
  2. 엑셀 파싱 시 임차 자산에 `renter: leaseVendorName`이 주입되었고, `filterRecordBySchema('assets', r)`가 `renter`를 유효 컬럼으로 오판하여 통과시킴.
  3. `supabase.from('assets').upsert(...)` 실행 시 Supabase(PostgREST)가 DB에 없는 `renter` 컬럼을 수신하여 400 Bad Request 에러 반환.
  4. 자동화 전수 검증 결과 `assets`(8종: `renter`, `disposalDate` 등), `vendors`(3종: `bankName` 등), `deliveries`(10종), `outbound_inspections`(6종)에서도 실서버 DB에 없는 컬럼이 화이트리스트에 잔존하고 있었음.

### 2. 조치 및 개선 내역
- **`src/services/migrationEngine.ts` (`TABLE_COLUMNS` 전면 정제)**:
  - `assets`: 비실존 8개 컬럼(`renter`, `disposalDate`, `disposalPrice`, `buyer`, `safetyInspectionUrl`, `preDeliveryChecklistUrl`, `memo1`, `memo2`) 완전 제거 ➔ 실제 DB 물리 컬럼 32개와 1:1 완벽 일치.
  - `vendors`: 비실존 3개 컬럼(`bankName`, `accountNumber`, `accountHolder`) 완전 제거 ➔ 실제 DB 물리 컬럼 15개와 1:1 완벽 일치.
  - `deliveries`: 비실존 10개 컬럼(`costAdjustmentReason`, `reconciledAt`, `paymentRequestedAt`, `paymentCompletedAt`, `statementFileUrl`, `billableToCustomer`, `billableCustomerId`, `vehicleRequirements`, `cargoItems`, `vehicles`) 완전 제거 ➔ 실제 DB 물리 컬럼 39개와 1:1 완벽 일치.
  - `outbound_inspections`: 비실존 6개 컬럼(`deliveryId`, `checkedItems`, `photos`, `notes`, `approvedAt`, `approvedBy`) 완전 제거 ➔ 실제 DB 물리 컬럼 7개와 1:1 완벽 일치.
- **실서버 1:1 무결성 검증 완료**:
  - 22개 전 테이블에 대한 자동화 스키마 검사 스크립트 실행 결과 **22/22 100% OK** 판정.
  - `renter` 필드가 포함된 Mock 자산에 대해 `filterRecordBySchema` 후 Supabase upsert **201 Created** 성공 검증 완료.

---

## 🧭 [내비딥링크/전화걸기안전호출] TMAP 길안내 폰 멈춤 버그 원인 규명 및 `nativeLauncher.ts` 안전 실행기 구축 완비 (v1.3.0.Build.117 - 2026-09-04 13:00)

### 1. 버그 현상 및 근본 원인 분석
- **증상**: 모바일 화면에서 [TMAP 길안내]를 누르면 TMAP이 열리다 멈추고 폰 전체가 먹통(소프트 브릭/터치 불가) 상태가 되어 강제 재부팅이 필요했던 현상.
- **원인 1 (`setTimeout` 비동기 이중 인텐트 충돌)**:
  - `MobileAsDetail.tsx`의 기존 코드에서 `window.location.href = tmapUrl` 직후 `setTimeout(() => window.open(kakaoUrl), 1500)`이 발동.
  - TMAP 앱이 포그라운드로 전환되며 3D 지도와 GPS를 초기화하는 순간, 백그라운드의 브라우저 타이머가 강제로 새 팝업 창을 띄우면서 안드로이드 `WindowManagerService`와 `SurfaceFlinger` 간에 화면 권한 교착 상태(Deadlock / ANR)가 발생하여 폰 전체 락업 유발.
- **원인 2 (안드로이드 표준 Intent Scheme 미준수)**:
  - 단순 `tmap://` 커스텀 스킴은 안드로이드 크롬의 앱 전환 정책과 충돌 위험 존재.
  - 구글 표준인 `intent://...#Intent;scheme=tmap;package=com.skt.tmap.ku;S.browser_fallback_url=...;end;` 형식을 사용해야 함.
- **전화걸기(`tel:`) 점검 결과**:
  - `tel:`은 `setTimeout`이 없어 폰 멈춤 충돌은 발생하지 않으나, `window.location.href`로 직접 호출 시 PWA 웹뷰가 흰색 에러 화면으로 이동할 위험이 있어 안전한 DOM <a> 클릭 방식으로 전면 격리 조치함.

### 2. 조치 및 개선 내역
- **`src/utils/nativeLauncher.ts` (중앙 안전 실행기 신설)**:
  - **타이머 영구 금지**: 어떠한 경우에도 `setTimeout`을 통한 백그라운드 팝업 금지.
  - **안드로이드 공식 Intent 스킴 적용**: `intent://` 및 `S.browser_fallback_url` 적용으로 OS 자체에서 앱 유무 판단 후 무충돌 단일 전환.
  - **iOS 사파리 전용 스킴 분기**: `tmap://`, `kakaomap://`, `nmap://` 정상 호출.
  - **`safePhoneCall` 헬퍼**: DOM 가상 `<a>` 클릭 방식을 통한 PWA 세션 보존 및 안전 다이얼러 호출.
- **`src/mobile/pages/MobileAsDetail.tsx` (모바일 상세 개편)**:
  - 위험한 `handleOpenTmap` 및 `setTimeout` 완전 제거.
  - 원터치 길안내 버튼 + `[▾]` 내비 앱 선택기(T맵 / 카카오 / 네이버) 탑재 및 선택 기억.
  - `safePhoneCall` 연동으로 전화걸기 완벽 안정화.
- **`src/pages/FieldAsManagement.tsx` (PC 현장AS 대장 연동)**:
  - `launchNavigation` 및 `safePhoneCall` 전면 교체 적용.

---

## 📱 [iOS/iPadOS/Safari전면최적화] 아이폰 · 아이패드 사파리(Safari) 브라우저 100% 최적화 및 PWA 단독 앱 구동 환경 구축 완비 (2026-09-04 12:55)

### 1. 개발 배경 및 진단 결과
- **사용자 질의**: "아이폰/아이패드의 사파리 브라우저 버전은 준비가 안됐어?"
- **정밀 진단 결과**:
  1. **아이패드(iPadOS)의 사파리 데스크톱 모드 편향**: Apple iOS 13+ 이후 아이패드 사파리는 데스크톱 사이트를 기본 요청하여 User-Agent에 `Macintosh`로 식별되고 해상도가 768px 이상이어서 자동으로 PC 화면으로 진입되는 현상 발견.
  2. **로그인 전 화면 모드 선택기 및 PWA 안내 부재**: 비로그인 상태에서 모바일/PC 뷰 전환 수단이 없고 사파리 홈화면 추가 가이드가 모바일 앱 내부에만 존재하여 사파리 지원 여부 인지가 어려웠던 점 개선.
  3. **PWA manifest 불일치**: `manifest.json`의 `start_url`은 `/?mode=mobile`이었으나 `App.tsx`는 `view=mobile`만 검사하고 있어 홈화면 바로가기 진입 시 모바일 뷰 미인식 결함 해소.
  4. **iOS 사파리 고유 스펙 최적화**:
     - `apple-touch-icon`: SVG 대신 고해상도 PNG(180x180, 192x192, 512x512) 규격 생성 및 메타태그 등록.
     - Safe-Area-Insets: Dynamic Island/노치 대응 `safe-area-inset-top` 및 하단 홈 바 `safe-area-inset-bottom` 여백 반영.
     - Web Audio & MediaRecorder: iOS 사파리 Web Audio suspended 자동 resume 헬퍼 및 `audio/mp4` 상호 크로스 플랫폼 호환 코덱 우선 순위 탐색 반영.
     - 아이패드 태블릿 반응형: `max-w-lg`(512px) 고정에서 `md:max-w-3xl`(768px) 태블릿 확장 반응형 적용.

### 2. 구체적 수정 및 보강 내역
- **`src/App.tsx` (디바이스 스마트 감지 및 로그인 화면 개편)**:
  - `navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1`으로 iPadOS 완벽 식별.
  - URL 파라미터 `?mode=mobile`, `?view=mobile`, `/m` 모두 모바일 자동 진입 지원.
  - 로그인 카드 하단에 **`접속 화면 모드: [모바일] / [PC/대화면]`** 원터치 전환기 신설.
  - 로그인 화면에 **`아이폰 · 아이패드 사파리(Safari) 지원 안내`** 카드 상시 표출.
- **`src/mobile/components/PwaInstallBanner.tsx` (기기 맞춤형 2단계 가이드 고도화)**:
  - iPadOS 감지 추가: 아이패드 사파리는 **상단 우측 툴바**의 [공유] 아이콘, 아이폰은 **하단 메뉴바**의 [공유] 아이콘으로 정밀 분기 안내.
- **`src/services/walkieTalkieService.ts` (iOS 사파리 오디오 호환성 보강)**:
  - `audio/mp4` 우선 탐색으로 iOS 사파리와 안드로이드/PC 크롬 간 음성 녹음 상호 교차 재생 100% 보장.
  - `playAudio` 예외 및 블록 안전 격리 (재생 실패 시에도 순차 큐 락업 원천 차단).
  - Web Audio 제스처 언락 메서드 `unlockAudioOnUserGesture` 구비.
- **`src/mobile/mobile.css`, `MobileHeader.tsx`, `MobileBottomNav.tsx`**:
  - Safe-Area-Insets(`env(safe-area-inset-top)`, `env(safe-area-inset-bottom)`) 반영.
  - 태블릿 반응형 컨테이너 폭 확장 (`md:max-w-3xl`).
- **`index.html`, `public/manifest.json`, `public/apple-touch-icon.png`**:
  - Apple 공식 고해상도 터치 아이콘 신규 생성 및 manifest orientation `any` 설정.

---

## 📻 [모바일현장무전기/PTT통신망] 스마트폰 현장 무전기(Push-To-Talk) 구축: 개인별 On/Off 대기, 4대 직무 채널, Web Audio 효과음 및 다시듣기 타임라인 완비 (2026-09-04 12:35)

### 1. 개발 내용 요약
- **`src/services/walkieTalkieService.ts` (초경량 PTT 무전 서비스 신설 및 동시 발언 제어)**:
  - **순차 재생 큐 (FIFO Audio Queue)**: 동시 다발적 음성 도착 시 소리가 겹치거나 씹히지 않고, 먼저 도착한 음성을 끝까지 재생한 뒤 0.22초 간격으로 차례대로 자동 재생.
  - **실시간 발언자 인디케이터 (Talking Status)**: 동료가 PTT 버튼을 누르고 말하는 동안 `talking_status` 실시간 브로드캐스트로 전사 화면에 `🔴 [부서 성명] 말하고 있습니다...` 점멸 표출.
  - Web Audio API 기반 비프음 합성 (송신 시작 2단 비프, 종료 치-익 노이즈, 수신 도미솔 차임벨 100% 브라우저 자체 합성).
  - MediaRecorder 오디오 녹음 (Opus/WebM 압축, 16kHz 모노 초경량 데이터).
  - Supabase Realtime Broadcast 채널(`walkie_ALL`, `walkie_DISPATCH`, `walkie_AS`, `walkie_SALES`) 구독 및 실시간 동시 음성 송출/순차 큐 재생.
  - 최근 20건 무전 이력 로컬스토리지 보존 및 다시듣기 지원.
- **`src/mobile/components/MobileWalkieTalkieModal.tsx` (현장 무전기 전용 모달 스튜디오 신설)**:
  - 4대 주파수 채널 선택 탭: `CH-01 • 전사공용`, `CH-02 • 출고배차`, `CH-03 • 현장AS`, `CH-04 • 영업`.
  - 레트로 다크/에메랄드 LCD 액정 상태창:
    - 동료 발언 시: 붉은 박동 테두리 + `🔴 [출고팀 김관주 부장] 말하고 있습니다...`
    - 재생 대기열 발생 시: `⏳ 대기 N건` 실시간 배지 표출.
  - 직경 140px 대형 원형 PTT 버튼:
    - 동료 발언 중일 때 주황색 테두리로 변하며 `⚠️ [김관주 부장] 발언 중` 표시 (양보 유도).
    - 누르고 있는 동안 녹음 ➔ 손 떼면 0.3초 내 동일 채널 자동 방송.
  - 최근 무전 타임라인 카드 (발신자, 소속, 시각, 초단위 음성길이, `[▶ 다시듣기]` 원터치 재생).
- **`src/mobile/MobileHeader.tsx`, `src/mobile/MobileApp.tsx` (모바일 전사 연동)**:
  - 헤더 우측 `[📻 무전 / 무전ON]` 상태 버튼 고정 탑재 (에메랄드 그린 점멸 인디케이터).
  - 백그라운드 리스닝 모드 지원: 모바일 앱 어느 화면에 있더라도 무전기가 `ON`이면 순차 큐를 통해 차례대로 음성 자동 방송.
- **전사 표준 헌장 준수**:
  - 헌장 1.1: 무전기 하드웨어 구입비 0원 절감, 통화 연결시간 0초 단축, 동시 발언 음성 유실 0%.
  - 헌장 3.1: 건조한 명사/동사 UI 단일 표준 준수 (감사 결과 위반 0건).

---

## 🛡️ [임직원권한/직무R&R표준화] 5대 직무별 필수 연결 메뉴 및 차단 업무 전수 파악, DB 권한 일괄 조정 및 권한관리.md 제정 완비 (2026-09-04 12:28)

### 1. 개발 내용 요약
- **`권한관리.md` 제정 (프로젝트 루트)**:
  - 전사 권한 관리 4대 원칙(최소 권한의 원칙, 헌장 2.1 부서 R&R 분리, 최고관리자 사장님 독점 원칙, 부서장/팀원 저장권한 분리) 수립.
  - 시스템 30대 메뉴(SSOT: `menu_config.ts`)에 기반한 5대 부서별 표준 권한 매트릭스(연결 메뉴 vs 차단 업무) 정립.
  - 임직원 20인(실제 임직원 15인 + WTT 테스터 5인) 전수 권한 매핑 표 및 변경 절차 정의.
- **Supabase 원격 DB `permissions` 테이블 700건 일괄 정돈 및 보안 취약점 봉쇄 (`scratch/apply_permission_matrix.cjs`)**:
  - **보안 결함 즉시 차단**: 관리부 정수아 차장 계정에서 `permission`(사용자 권한 설정) 및 `payroll`(급여) 권한 완전 회수 (오직 사장님/ADMIN 독점).
  - **헌장 2.1 영업 R&R 위반 차단**: 영업부 전원의 `dispatch_assign`(장비 할당 매핑) 접근 완전 차단 (특정 장비번호 지정 불가).
  - **무관 부서 대외비 차단**: AS팀 전원의 불필요한 `billing`(청구), `contract`(계약), `delivery`(배차) 조회 권한 완전 차단.
  - **부서장 저장 권한 정상 복원**: 최수호 상무, 김관주 부장, 한상찬 팀장 등 소관 업무 `canSave: true` 정상 부여.
  - 레거시 불일치 menuId (`quote`, `rent_assets`, `transport_company` 등) 완전 제거.
- **`src/pages/users_permissions.tsx` 보안 가드 강화**:
  - 대표이사(이수용 사장) 및 최고관리자 계정 권한 회수 원천 차단.
  - 비인가 계정에 대한 `permission` 메뉴 권한 토글 원천 차단.
  - 신규 사용자 누락 권한 백필 시 관리자가 아니면 `canView: false`로 최소 권한 기본값 보호.

---

## 📦 [모바일5대직무체계/경영진관리부특화] 조직도 최상위(경영진 직할) 칵핏 및 관리부 정산·수납 모바일 특화 뷰 신설, 전사 5대 직무 퀵 체인저 완비 (v1.3.0.Build.115 - 2026-09-04 12:20)

### 1. 개발 내용 요약
- **`src/mobile/pages/MobileExecutiveHome.tsx` (경영진 직할 모바일 칵핏 신설)**:
  - 실물 자산 가동률 실시간 게이지 (타사 임차 배제 순수 자사 실물자산 기준).
  - 당월 청구/수납 진척도 및 유동자금 잔고 모니터링.
  - 단가특약/지출결의 긴급 결재 대기 큐 원터치 승인.
  - 고위험 상습연체 거래처 영업지시 하달(Todo 연동) 및 출고금지 처분권 원터치 토글.
- **`src/mobile/pages/MobileAdminHome.tsx` (관리부 정산·수납 모바일 피드 신설)**:
  - 마감 도래 거래처 D-Day 캘린더 피드 (계약서/명세서 패키지 발송 연계).
  - 통장 입금 1:1 즉시 수납 매칭 카드 (입금확인 및 원터치 대사).
  - 미수채권 회수 관리 피드.
- **`src/mobile/MobileHeader.tsx`, `src/mobile/MobileBottomNav.tsx`, `src/mobile/MobileApp.tsx` (5대 부서 체계 구축)**:
  - `[영업부] | [AS팀] | [출고팀] | [경영진] | [관리부]` 5대 직무 퀵 체인저.
  - 부서별 맞춤 하단 5대 탭 동적 매핑 및 사용자 역할 기반 초기 화면 분기.
- **전사 표준 헌장 준수**:
  - 헌장 3.1 무수식어 명사/동사 표준 통과 (findings: 0).
  - 기준단가 및 타사재고 영구 배제 유지.

---

## 📦 [모바일부서특화/단가타사제외] 기준단가 및 타사재고 영구 제외, 영업부·AS팀·출고팀 3대 부서별 모바일 특화 기능 및 직무 퀵 체인저 완비 (v1.3.0.Build.114 - 2026-09-04 12:15)

### 1. 개발 내용 요약
- **`src/mobile/pages/MobileAssetSearch.tsx` (기준단가 및 타사재고 표시 완전 제외)**:
  - 월단가/일단가 기준 금액 표시 모바일 전체 영구 배제.
  - 협력타사 주기장/임차 장비 배제, 오직 본사 모현 주기장 자사 보유 실물 자산만 100% 집계.
- **`src/mobile/pages/MobileDispatchOrderCreate.tsx` (영업부 모바일 출고 간편 의뢰 신설)**:
  - 거래처 ➔ 현장 ➔ 요구 규격 ➔ 수량 ➔ 납품일시 원터치 발송 (헌장 2.1 영업 R&R 준수).
- **`src/mobile/pages/MobileMyContracts.tsx` (영업부 내 계약 & 투입현장 조회 신설)**:
  - 투입 장비 번호, 렌탈 기간, D-Day, 현장소장 통화 직결.
- **`src/mobile/pages/MobileHome.tsx`, `src/mobile/MobileBottomNav.tsx` (부서별 맞춤 홈 피드 및 5대 탭 동적 스위칭)**:
  - 영업부, AS팀, 출고팀별로 하단 탭과 홈 화면을 직무에 맞게 최적화.
- **`src/mobile/MobileHeader.tsx`, `src/mobile/MobileApp.tsx` (상단 헤더 직무 퀵 체인저 탑재)**:
  - `[영업부] | [AS팀] | [출고팀]` 1-터치 뷰 전환 바 완비.

---

## 📦 [모바일PWA/CSS패치] 모바일 전용 독립 CSS 시스템(mobile.css) 구축, 안드로이드 PWA 서비스워커(sw.js) 탑재 및 기기별(Android vs iOS) 맞춤 설치 가이드 전면 개편 (v1.3.0.Build.113 - 2026-09-04 11:35)

### 1. 개발 내용 요약
- **`src/mobile/mobile.css`, `src/mobile/MobileApp.tsx` (모바일 전용 독립 CSS 시스템 구축)**:
  - Tailwind 의존성 없이 순수 CSS로 동작하는 모바일 전용 다크 테마 UI 시스템 완비.
  - 레이아웃 깨짐, 배경 미적용, 텍스트 줄바꿈 현상 전면 해결.
- **`public/sw.js`, `index.html` (크롬/안드로이드 공식 PWA 요건 충족용 서비스 워커 배포)**:
  - `sw.js` (Fetch 핸들러 기반 Service Worker) 신규 생성 및 `index.html` 자동 등록 파이프라인 완비.
  - Chrome 브라우저의 `beforeinstallprompt` 이벤트 자동 트리거 및 주소창 [앱 설치] 정식 활성화.
- **`src/mobile/components/PwaInstallBanner.tsx` (기기별 맞춤 홈 화면 추가 가이드 모달 및 인라인 스타일 완비)**:
  - 100% 인라인 스타일 고정으로 모달이 본문에 깨져 나오는 현상 원천 방지 (`fixed z-index: 99999` 중앙 오버레이 팝업).
  - 안드로이드(갤럭시 S24 등): 우측 상단 메뉴(⋮) ➔ [앱 설치] / [홈 화면에 추가] 정확한 2단계 안내.
  - 아이폰(iOS Safari): 하단 공유 [⎋] ➔ [+] 홈 화면에 추가 2단계 안내.
- **`MobileHeader.tsx`, `MobileBottomNav.tsx` (헤더/네비게이션 인라인 스타일 보강)**:
  - 상단 헤더 및 하단 네비게이션 고정 배치, safe-area 및 백드롭 블러 스타일 강화.

---

## 📦 [청구그리드개편/모바일재고매트릭스] 청구 목록 화폐 우측 정렬·세부 청구 명세 그리드 개편 및 외근영업 가용재고 매트릭스 보드·PWA 단독앱 설치 인프라 구축 완비 (v1.3.0.Build.112 - 2026-09-04 10:55)

### 1. 개발 내용 요약
- **`src/pages/Billings.tsx` (청구 관리 그리드 배치 및 일할/월정액 산출 로직 개편)**:
  - 공급가액, 청구합계(VAT포함), 미납액 헤더 및 셀 화폐 금액 우측 정렬(`textAlign: 'right'`).
  - 세부 청구 명세 그리드 컬럼 순서 개편: `순번 ➔ 구분 ➔ 모델명/관리번호 ➔ 월렌탈료 ➔ 일렌탈료 ➔ 수량 ➔ 세액 ➔ 합계 ➔ 적용기간(산출근거)`.
  - 1개월 만근 시: `월렌탈료=기준월단가`, `일렌탈료=-`, `수량=1` 표기 (기존 30일 만근인데 일단가*30으로 왜곡 표출되던 문제 해결).
  - 1개월 미만 시: `일렌탈료=일단가`, `수량=실제일수` 표기. 엑셀/PDF/이메일 내보내기 동기화.
- **`public/manifest.json`, `index.html`, `src/mobile/components/PwaInstallBanner.tsx` (PWA 웹앱 홈화면 추가 인프라)**:
  - 웹앱 매니페스트 및 iOS 사파리 홈화면 추가 메타태그 완비.
  - 안드로이드 원클릭 설치 프롬프트 및 iOS 사파리 2스텝 가이드 모달 제공. 브라우저 주소창 없는 독립 실행(`standalone`) 지원.
- **`src/mobile/pages/MobileAssetSearch.tsx` (외근영업 가용재고 매트릭스 보드)**:
  - 6대 핵심 규격(19/26/32/40/46/53ft) 상태별(임대가능, 출고대기, 대여중, 정비중) 타일 매트릭스.
  - 주기장 필터(전체, 본사 모현 주기장, 협력타사 주기장).
  - 규격 탭 시 즉시 출고 가능 장비 및 반납예정 장비 목록 바텀시트 표출.
  - 화면 포커스(`visibilitychange`/`focus`) 시 0.3초 무조작 백그라운드 자동 동기화.
- **`src/mobile/pages/MobileHome.tsx`**:
  - 홈 화면 상단 가용재고 현황 배너 연동.

---

## 📦 [초기DB절차서/계약서패키지] 초기 DB 업로드 절차정의서 v3.0 전면 최신화 및 계약서패키지 PDF 다운로드 & 원클릭 이메일 발송 파이프라인 개편 완비 (v1.3.0.Build.111 - 2026-09-04 09:58)

### 1. 개발 내용 요약
- **`INITIAL_DB_UPLOAD.md` (초기 DB 업로드 절차정의서 v3.0 최신화)**:
  - 5대 입력 시트, 33개 컬럼 매핑, 7대 정규화 파서, 13단계 DAG 순차 적재, 6대 대차대조 무결성 검증식 상세 기술.
- **`src/components/ContractDocumentBundleModal.tsx` & `src/services/db.ts` (계약서패키지 발송 모달 개편)**:
  - 4대 소스 기반 수신인 자동 추출 및 칩 UI.
  - 고객사 연결 인물 빠른 선택 헬퍼(Quick Contact Picker) 뱃지 토글 지원.
  - 단일 메일(TO/CC) 발송 파이프라인 및 원클릭 논스톱 PDF 조립 ➔ 이메일 발송 연계.
  - `contract_history` 테이블에 `DOCUMENT_SENT` 감사 로그 DB 영구 저장.

---

## 📦 [WTT워크숍/전수평가] 전사 WTT 정책·아이디어·요구사항 전수 명세서 작성 및 9대 직무 전원 참여 워크숍 기능평가·합동 중간보고서·엔지니어 개발계획서 완비 (v1.3.0.Build.110 - 2026-09-04 00:10)

### 1. 개발 내용 요약
- **1. 오늘 식별 전수 정책·아이디어·요구사항 명세서 작성 (`audit_reports/2026-09-03_wtt_workshop/00_WTT_전수_정책_요구사항_명세서.md`)**:
  - WTT 9대 파이프라인 개편, 헌장 3.1 건조한 명사화, 헌장 3.5 Gutenberg Z-패턴 최하단 대차대조식 바, alert 57개소 완전 퇴출, Supabase 원격 DB 12개 테이블/5개 테이블 컬럼 DDL 패치, 고스트 권한 85건 정돈, 16대 비즈니스 도메인 31개 항목 100% 무결 순회 전수 대사 명세화.
- **2. 9대 직무 전원 참여 워크숍 및 100점 만점 기준 개별 평가서 9건 작성**:
  - `PM (96점)`, `엔지니어 (95점)`, `영업팀 (91점)`, `정비팀 (94점)`, `AS팀 (97점)`, `배차담당 (93점)`, `관리팀 (95점)`, `진상고객 (38점 - 강력한 내부통제 역설적 증명)`, `감사팀 (98점)`.
  - 새로운 문제를 발굴하지 않고, 금일 식별된 정책과 헌장 원칙에 기반한 구체적 감점 원인 전수 기재.
- **3. PM & 감사팀 합동 중간보고서 작성 및 전달 (`10_PM_감사팀_합동_중간보고서.md`)**:
  - 실무 임직원 7인 평균 94.6점, 전체 9개 직무 총평균 88.6점 달성.
  - 전수 정책 및 DDL 정합성 100% 이행 확인 및 엔지니어링 본부 전달 3대 핵심 지침 정립.
- **4. 엔지니어 후속 개발 계획서 수립 (`11_엔지니어_후속_개발계획서.md`)**:
  - 중간보고서 피드백 수용 기반 3단계 로드맵 (CI 자동화, 실무 편의 UX, 외부 연동 및 물리적 락) 확립.

---

## 📦 [전사WTT순회/DDL패치] 전체 업무활동 WTT 16대 파이프라인 로직 설계, Supabase DDL 패치 및 100% 무결 순회 검증 완결 (v1.3.0.Build.109 - 2026-09-03 23:36)

### 1. 개발 내용 요약
- **1. Supabase 원격 DB 미반영 12개 테이블 및 5개 테이블 누락 컬럼 DDL 전면 패치**:
  - `dev_exec_ddl` RPC를 통해 `annual_leave_quotas`, `leave_usages`, `overtime_records`, `payroll_closings`, `customer_bank_accounts`, `inbound_defect_details`, `asset_in_out_logs`, `repair_timeline_events`, `bank_account_initial_balances`, `settlement_payment_logs`, `prepaid_transactions`, `delinquency_action_logs` 등 12개 테이블 신규 생성 및 RLS 완비.
  - `customers` (defaultPaidOptions, defaultProtection, defaultCheckedSpecs, specialNotes), `customer_sites` (paidOptions, protection, checkedSpecs), `contract_assets` (status, actualReturnDate), `billings` (invoiceId), `billing_invoices` (11개 필수 컬럼), `deliveries` (reconciliationStatus, confirmedCost, scheduledDate), `consumables` (name, spec, safetyStock) DDL 보완 집행.
  - `schema.sql` 대비 원격 DB 정합성 검증 결과: **Missing Tables 0건, Missing Columns 0건 100% 완전 일치**.
- **2. 고스트 권한 85건 원격 DB 무결 정돈 (헌장 5.3)**:
  - `userId IS NULL` 무효 고스트 권한 85건 원격 정리 완료 (정상 임직원 매핑 132건 100% 보존).
- **3. 전체 업무활동 WTT 16대 파이프라인 종합 검증 스위트 설계 및 100% 순회 검증**:
  - `scripts/wtt_full_business_activity_suite.cjs` 구축.
  - 조직/권한, 자산/모델, 고객/현장, 계약체결, 배차출고, 대차교체, 계약변경, 현장AS, 반납입고, 정비수불, 소모품구매, 매출청구, 수납대사, 채권연체, 매입정산, 회계결산 등 **전사 16대 비즈니스 도메인 31개 항목 100.0% 통과 (0 error)**.

---

## 📦 [연속WTT 9단계/AS요청] 현장 AS 접수 전면 표준화: 건조한 명사·동사 UI 단일화, CUD 동기 검증 및 최하단 AS 파이프라인 대차대조식 검증 바 완비 (v1.3.0.Build.108 - 2026-09-03 23:20)

### 1. 개발 내용 요약
- **`src/pages/SmartAsRequest.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert/confirm 전면 배제 및 인앱 토스트 통일 (헌장 5.2)**:
    - 접수 성공 시 인앱 토스트(`showToast`) 표출 및 `await db.awaitPendingWrites()` 동기 완료 검증.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 설명 표준화: `현장 AS 접수`, `영업사원 및 고객 유선 접수 건 AS팀 신속 의뢰 대장`.
  - **3. Gutenberg Z-패턴 4단계 최하단 현장 AS 접수 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `🔧 누적AS접수: N건 | 🚨 접수대기: M건 | ⏳ 배정/출동중: P건 | 🟢 조치완료: Q건 | ⚖️ 대차 정상 (전체 AS 티켓 상태 파이프라인 무결)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [연속WTT 8단계/법인카드] 법인카드 매입정산 전면 표준화: alert/confirm 6개소 전면 퇴출 및 인앱 모달 전환, 건조한 명사·동사 UI 단일화 및 최하단 예실 대차대조식 검증 바 완비 (v1.3.0.Build.107 - 2026-09-03 23:17)

### 1. 개발 내용 요약
- **`src/pages/CorporateCardPage.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 5개소 및 confirm 1개소 전면 퇴출 (헌장 5.2)**:
    - 카드 거래내역 파일 선택 안내, 자동 매핑 완료 안내, 항목명 필수 검증 경고, 신규 유형 추가 안내, 매입유형 영구 삭제 confirm 팝업을 인앱 확인 모달(`confirmModal`)과 인앱 토스트(`showToast`)로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 탭 명칭 표준화: `법인카드 매입정산`, `카드 승인 내역 1:1 매핑 및 월별 필수 매입 지출 누락 검증 대장`, `매입 정산 및 대사`, `매입 유형 설정`.
  - **3. Gutenberg Z-패턴 4단계 최하단 법인카드 매입정산 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `🏢 관리유형: N개 항목 | 📄 총 예상매입: ₩A원 | 💳 당월 실지출: ₩B원 | ⚖️ 예실차액: ₩C원 (예산 잔여/초과) | ⚖️ 대차 정상 (예실 대비 100% 무결 정산)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - 데이터 갱신 및 상태 관리 무음 실패 방지.

---

## 📦 [연속WTT 7단계/임직원권한] 임직원 권한 관리 전면 표준화: 10개소 alert 전면 퇴출, 건조한 명사·동사 UI 단일화 및 최하단 권한 대차대조식 검증 바 완비 (v1.3.0.Build.106 - 2026-09-03 23:13)

### 1. 개발 내용 요약
- **`src/pages/users_permissions.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 10개소 전면 퇴출 (헌장 5.2)**:
    - 최고관리자 등급 변경 불가 경고, 승인 권한 경고, 박탈 권한 경고, 등급 변경 완료 안내, 슈퍼관리자 메뉴 권한 회수 불가 경고, 고스트 권한 유무 안내, 고스트 정돈 완료 안내, 권한 설정 저장 완료 안내 등 10개소 alert를 인앱 토스트(`showToast`)로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 설명 표준화: `임직원 권한 관리`, `임직원별 시스템 역할 등급 및 메뉴별 조회/저장 권한 마스터 대장`.
  - **3. Gutenberg Z-패턴 4단계 최하단 임직원 권한 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `👥 전체임직원: N명 | 👑 최고관리자: M명 | 💼 매니저/실무: P명 | 📑 권한매핑총수: Q건 | ⚖️ 대차 정상 (무효 고스트 권한 0건 무결)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [연속WTT 6단계/급여정산] 급여 정산 대장 전면 표준화: alert/confirm 9개소 전면 퇴출 및 인앱 모달 전환, 건조한 명사·동사 UI 단일화 및 최하단 급여 대차대조식 검증 바 완비 (v1.3.0.Build.105 - 2026-09-03 23:08)

### 1. 개발 내용 요약
- **`src/pages/PayrollPage.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 7개소 및 confirm 2개소 전면 퇴출 (헌장 5.2)**:
    - 4대보험 엑셀 대조 적재 안내, 미업로드 경고, 급여 마감 승인 확인 팝업, 락 해제 확인 팝업, 기본급 변경 저장 안내, 이메일 교부 미승인 경고 및 교부 완료 안내를 인앱 확인 모달(`confirmModal`)과 인앱 토스트(`showToast`)로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 설명 표준화: `급여 정산 대장`, `임직원 기본급·초과근무·연차 및 4대보험 공제 실시간 급여 대장`.
  - **3. Gutenberg Z-패턴 4단계 최하단 급여 정산 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `👥 대상임직원: N명 | 💵 지급총액(세전): ₩A원 | 📉 공제총액: ₩B원 | 💰 실지급총액(세후): ₩C원 | ⚖️ 지급총액 = 실지급총액 + 공제총액 (대차 무결)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [연속WTT 5단계/정비항목관리] 정비 항목 관리 전면 표준화: alert/confirm 6개소 전면 퇴출 및 인앱 모달 전환, 건조한 명사·동사 UI 단일화 및 최하단 정비항목 마스터 대차대조식 검증 바 완비 (v1.3.0.Build.104 - 2026-09-03 23:05)

### 1. 개발 내용 요약
- **`src/pages/inspection_checklist_manage.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 5개소 및 confirm 1개소 전면 퇴출 (헌장 5.2)**:
    - 항목 저장/삭제 성공, 필수값 검증, 삭제 확인 다이얼로그를 인앱 확인 모달(`confirmModal`)과 인앱 토스트(`showToast`)로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 설명 표준화: `정비 항목 관리`, `입고 검수 및 정비 점수 자동 합산 연동 마스터 대장`.
  - **3. Gutenberg Z-패턴 4단계 최하단 정비항목 마스터 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `🛠️ 정비점검항목: N개 | 📂 관리분류: M개 카테고리 | ⭐ 배점총합: P점 (평균 Q점) | ⚖️ 대차 정상 (전체 카테고리 마스터 100% 무결)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [연속WTT 4단계/감가상각] 감가상각 마감 실행 전면 표준화: alert/confirm 5개소 전면 퇴출 및 인앱 모달 전환, 건조한 명사·동사 UI 단일화 및 최하단 자산가치 대차대조식 검증 바 완비 (v1.3.0.Build.103 - 2026-09-03 23:00)

### 1. 개발 내용 요약
- **`src/pages/depreciation_execution.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 3개소 및 confirm 2개소 전면 퇴출 (헌장 5.2)**:
    - 마감 실행/취소 확인 및 완료 안내를 브라우저 네이티브 다이얼로그 대신 인앱 확인 모달(`confirmModal`)과 인앱 토스트(`showToast`)로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 설명 표준화: `감가상각 마감 실행`, `자사 소유 자산 정액법 감가상각 월말 결산 및 장부가치 확정 대장`.
  - **3. Gutenberg Z-패턴 4단계 최하단 감가상각 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `🏗️ 자사자산: N대 | 📄 취득원가총액: ₩A원 | 📉 누적상각액: ₩B원 | 💵 현재 장부가치: ₩C원 | ⚖️ 대차 정상 (취득원가 = 누적상각 + 장부가치 100% 무결)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [연속WTT 3단계/자금흐름분석] 자금 흐름 분석 전면 표준화: alert/confirm 3개소 전면 퇴출, 건조한 명사·동사 UI 단일화 및 최하단 자금 수지 대차대조식 검증 바 완비 (v1.3.0.Build.102 - 2026-09-03 22:58)

### 1. 개발 내용 요약
- **`src/pages/CashFlowPage.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 2개소 및 confirm 1개소 전면 퇴출 (헌장 5.2)**:
    - 스냅샷 저장 안내, 다운로드 데이터 부재 알림, 스냅샷 삭제 confirm 팝업을 전면 걷어내고, 인앱 토스트(`showToast`) 및 CUD 동기 보장(`await db.awaitPendingWrites()`)으로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 탭 명칭 표준화: `자금 흐름 분석`, `주거래 계좌 잔액 기반 향후 30일 자금 수지 및 유동성 예측 대장`, `30일 자금 예측`, `예측 스냅샷 이력`.
  - **3. Gutenberg Z-패턴 4단계 최하단 자금 수지 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `🏦 기초시작잔액: ₩A원 | 📥 예정수납: +₩B원 | 📤 운영지출: -₩C원 | 🏗️ 투자지출: -₩D원 | 💰 30일후 기말잔고: ₩E원 | ⚖️ 자금 유동성 정상 (기말 = 기초 + 수납 - 지출 무결)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [연속WTT 2단계/매입정산] 매입정산 대장 전면 표준화: 5개소 alert 전면 퇴출, 건조한 명사·동사 UI 단일화 및 최하단 매입 대차대조식 검증 바 완비 (v1.3.0.Build.101 - 2026-09-03 22:55)

### 1. 개발 내용 요약
- **`src/pages/PurchaseSettlementPage.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 5개소 전면 퇴출 (헌장 5.2)**:
    - 증빙 파일 누락 알림, 증빙 정보 표시, 지급 금액 유효성, 지급 승인 완결 안내, 지급 처리 실패 등 모든 alert를 인앱 토스트(`showToast`) 및 글로벌 모달(`showErrorModal`)로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 설명 표준화: `매입 정산 대장`, `운송료·소모품·전대임차·외주정비 월별 매입처별 집계 및 통장 출금 1:1 대사`.
  - **3. Gutenberg Z-패턴 4단계 최하단 매입 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `🏢 [YYYY-MM 매입정산]: N개사 | 📄 총 청구액: ₩A원 | 🟢 지급완료: ₩B원 (M건) | ⏳ 지급잔액: ₩C원 | ⚖️ 청구총액 = 지급완료 + 지급잔액 (대차 무결)`.
  - **4. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [연속WTT 1단계/소모품] 소모품 수불관리 전면 표준화: 12개소 alert 전면 퇴출, 건조한 명사·동사 UI 단일화 및 최하단 재고/원가 대차대조식 검증 바 완비 (v1.3.0.Build.100 - 2026-09-03 22:50)

### 1. 개발 내용 요약
- **`src/pages/Consumables.tsx` (전면 개편 및 표준화)**:
  - **1. 브라우저 alert 12개소 전면 퇴출 (헌장 5.2)**:
    - 구매신청 유효성 검사, 입고 증빙 파일 누락, 수량 부족, 차량 불출/반납 성공 등 모든 alert를 인앱 토스트(`showToast`) 및 글로벌 모달(`showErrorModal`)로 전면 교체.
  - **2. 무수식어 건조한 명사·동사 UI 단일 표준화 (헌장 3.1)**:
    - 타이틀 및 탭 명칭 표준화: `소모품 수불 관리`, `본사 재고`, `차량 이동 재고`, `구매 신청 대장`, `구매 신청 등록`, `입고 처리`, `소모품 출고`, `입출고 이력`.
  - **3. 레이블-입력창 상하 세로 스택 구조 강제 (헌장 3.4)**.
  - **4. Gutenberg Z-패턴 4단계 최하단 재고/원가 대차대조식 검증 바 고정 탑재 (헌장 3.5)**:
    - `📦 본사 재고: N종 / M개 (₩A원) | 🚚 차량 이동재고: P개 (₩B원) | 📥 당월 구매입고: Q건 (₩C원) | 🔧 당월 정비출고: R건 (₩D원) | ⚖️ 대차 정상 (기초 + 입고 = 기말 + 사용 무결)`.
  - **5. CUD 동기 완료 검증 (헌장 5.2)**:
    - `await db.awaitPendingWrites()` 동기 완료 검증.

---

## 📦 [내용증명/영업통제] 경영진 전결 출고금지(BLOCKED) 처분, 영업담당자 연체 경각심·수금책임 통제 및 내용증명(최고장) 편집기·발송이력 관리 스튜디오 구축 (v1.3.0.Build.99 - 2026-09-03 22:45)

### 1. 개발 내용 요약
- **`src/services/db.ts` & `src/context/AppContext.tsx` (스키마 및 컨텍스트 확장)**:
  - `LegalNoticeLog`(내용증명 발송 이력) 및 `LegalNoticeTemplate`(기본 서식 저장) 인터페이스 및 테이블 구축.
  - `saveLegalNoticeLog`, `saveLegalNoticeTemplate` 컨텍스트 API 완비.
- **`src/pages/DelinquencyPage.tsx` (경영진 고유 권한 및 내용증명 스튜디오 구축)**:
  - **1. 경영진 고유 권한 한정 (`ADMIN` / `EXECUTIVE`)**:
    - 거래 차단(`BLOCKED`) 처분 및 내용증명 작성/발송 승인은 오직 경영진 계정만 조작 가능하도록 권한 가드 완비.
  - **2. 인라인 자유 편집기 + [기본 서식으로 저장] 스튜디오**:
    - 법적 4대 골격을 갖춘 표준 최고장 서식 기본 탑재 및 실시간 편집기 제공.
    - 화면에서 수정한 문구를 `[기본 서식으로 저장]` 버튼으로 영구 저장하여 차후 재사용 보장.
  - **3. 우체국 20mm 여백 규격 A4 실시간 프리뷰 & 인쇄 출력**:
    - 실시간 A4 프리뷰 렌더링, `window.print()` 인쇄/PDF 출력 지원, 기연리프트 직인 자동 날인.
  - **4. 발송 이력 영구 관리 & 타임라인 자동 연동**:
    - 발송 승인 시 `legalNoticeLogs`에 발송 당시 최종 전문, 일자, 최고금액, 경영진 실명, 등기번호 저장.
    - 연체 관리 타임라인에 `actionType: 'NOTICE_SENT'`로 자동 기록 및 `[발송 내용증명 원문 보기]` 버튼 탑재.
- **`src/pages/Customers.tsx` (고객 관리 연동)**:
  - 고객 목록: 내용증명 발송 이력이 있는 거래처에 `📜 내용증명 N건` 보라색 배지 표출.
  - 고객 상세 뷰: **`📜 내용증명(최고장) 발송 이력`** 카드 탑재 및 **`[원문 보기]`** 팝업 모달 제공.
- **`src/pages/Contracts.tsx` & `src/pages/smart_dispatch.tsx` (영업담당자 경각심 통제 Guard)**:
  - 연체 채권이 존재하는 거래처 선택 시 붉은색/노란색 `⚠️ [연체 채권 경각심 통제 경보]` 배너 노출.
  - **`☑️ [수금 책임 인지]` 필수 체크박스 동의** 없이는 신규 계약 및 출고의뢰 진행을 물리적으로 차단하여, 영업담당자의 부실채권 경각심 및 도덕적 책임 강제.
  - 경영진에 의해 `BLOCKED` 처분된 고객사는 100% 원천 차단.

---

## 📦 [현장AS] 현장 AS 관제·일정 스케줄링 & 기간 성과분석 스튜디오 전면 개편: 캘린더 스케줄러, 4대 핵심 KPI 매트릭스, alert 5개소 퇴출 및 대차대조식 완결 (v1.3.0.Build.98 - 2026-09-03 22:20)

### 1. 개발 내용 요약
- **`src/pages/FieldAsManagement.tsx` (전면 개편)**:
  - **1. 본질 목표의 최고 수준 격상 (Elevated Teleology)**:
    - 단순 수리 기록부가 아닌, 현장 가동 중단(Down-time) 최소화와 기사 출동 생산성 극대화를 위한 전사 현장 AS 관제·스케줄링 & 성과 분석 스튜디오로 전면 개편.
  - **2. 📅 [출동 일정 캘린더] (CALENDAR) 탭 신규 탑재**:
    - 월간 인터랙티브 달력 뷰: 날짜별 출동 예정, 긴급(🚨), 재방문(REVISIT), 완료 도트/태그 시각화.
    - 날짜 클릭 시 당일 출동 상세 피드 및 담당 기사/현장 정보 노출 ➔ 스튜디오 조치 화면 원클릭 직결.
  - **3. 📊 [기간 성과 분석] (ANALYTICS) 탭 신규 탑재**:
    - 기간 피커: 당월 / 전월 / 올해 전체 / 사용자 지정 기간별 필터링.
    - 4대 핵심 KPI 지표:
      - ① 총 접수 vs 완료율 (%), ② 평균 조치 소요시간 (Lead-Time), ③ **당일 원스톱 완결율 (First-Time Fix Rate)**, ④ 유상 청구 전환 실적 및 투입 부품 총원가.
    - 정비사별 생산성 실적 비교 테이블 (출동건수, 완료율, 재방문 건수, 투입 부품비).
    - 10대 고장 원인 및 증상별 점유율 파레토 바 차트.
  - **4. 네이티브 `alert()` 4개소 및 `confirm()` 1개소 (총 5개소) 전면 영구 퇴출 (헌장 1.1, 5.2)**:
    - 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **5. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `현장 AS 접수: 총 N건 | 출동 예정/진행: M건 | 조치 완료: P건 | 재방문 요청: Q건 | 누적 투입 부품비: ₩A원 | ⚖️ 대차 정상 (현장AS-기사배정-차량부품차감 100% 무결)`.

---

## 📦 [미수채권/연체관리] 고객 약정납기일 기준 상습연체 누적관리 및 경영진 영업지시·방치 통제 스튜디오 전면 개편 (v1.3.0.Build.97 - 2026-09-03 22:05)

### 1. 개발 내용 요약
- **`src/services/db.ts` (인터페이스 확장)**:
  - `DelinquencyActionLog`의 `actionType`에 `'DIRECTIVE'`(경영진 수금독촉 지시) 추가.
  - `directiveTargetUserId`, `directiveDueDate` 필드 추가로 담당 영업사원 및 완료기한 1:1 연동.
- **`src/pages/DelinquencyPage.tsx` (전면 개편)**:
  - **1. 고객정보 정의 약정 결제조건/납기일(paymentDueDay/paymentTermDays) 기준 정밀 연체 판정**:
    - 단순 청구서 발행일이 아닌, 고객 마스터의 약정 결제조건(익월 N일, 발행 후 N일)을 기준으로 실제 '약정 납기일'을 동적 산출하여, 납기 미도래 정상 채권과 실제 연체 채권을 엄격히 분리.
  - **2. 상습 연체 3대 지표 장기간 누적 관리 및 위험 등급(Risk Tier) 자동 산출**:
    - **연체 지연일수(Overdue Days)**: 최장 납기 도과 일수.
    - **연체 지연금액(Overdue Amount)**: 납기 도과 미수 잔액 합계.
    - **누적 약속 위반 및 지연 횟수(Broken Promises Count)**: 입금 약속 기한을 어긴 횟수 및 도과 청구서 수 누적 추적.
    - 상습 연체 고위험 등급: 🔴 고위험 (60일+ / 약속위반 2회+ / 500만원+) / 🟡 중위험 / 🟢 일반.
  - **3. 경영진 ➔ 담당 영업사원 채권회수 독촉 지시 하달 및 ToDo 피드 자동 연동**:
    - 경영진이 고위험 연체 고객에 대해 [경영진 지시] 버튼으로 공식 수금지시 발행.
    - 담당 영업사원 계정의 실시간 ToDo 피드(`db.todos`)에 즉시 자동 등록되어 개인 대시보드에 표출.
    - 영업사원이 현장 방문/전화 상담 조치를 등록하면 ToDo 자동 마감.
  - **4. 관심 방치 위험 원천 통제 (Anti-Neglect Control Indicator)**:
    - 경영진 지시 하달 후 3일 이상 영업사원의 조치 피드백이 누락된 경우 `🚨 지시 방치 경보 (N일 경과 미조치)` 배지 점멸 표출 및 원클릭 필터 제공.
  - **5. 네이티브 `alert()` 5개소 전면 영구 퇴출 (헌장 1.1, 5.2)**:
    - 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **6. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `연체 거래처: 총 N개사 | 연체 총액: ₩A원 | 고위험 상습연체(🔴): 총 M개사 | 경영진 지시 진행중: P건 | 🚨 3일 이상 지시 방치: Q건 | ⚖️ 대차 정상 (연체채권-약정납기-영업담당 매핑 100% 무결)`.

---

## 📦 [청구수납] 청구 / 수납 관리 스튜디오 전면 개편: @ts-nocheck 영구 퇴출 및 정적 타입 100% 무결화, alert/confirm 23개소 전면 퇴출 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.96 - 2026-09-03 21:55)

### 1. 개발 내용 요약
- **`src/pages/Billings.tsx` (전면 개편)**:
  - **1. `// @ts-nocheck` 영구 삭제 및 100% 정적 타입 무결성 복원 (헌장 5.2, 5.3)**:
    - 3,783줄에 달하는 핵심 회계 코드 상단의 `@ts-nocheck`를 영구 제거하고, `BillingDetail` 및 `ContractHistory`, `pdfBytes Blob` 등 14개소의 잠재적 정적 타입 결함을 완벽히 보정하여 `tsc -b` 0 에러 통과.
  - **2. 네이티브 `alert()` 16개소 및 `confirm()` 7개소 (총 23개소) 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 청구 생성, 청구귀속월 수정, 수납 취소/롤백, 이메일 발송, 일괄 승인 등에서 빈번하게 화면을 멈추게 하던 23개소의 모든 브라우저 팝업을 전면 삭제하고, 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **3. 헌장 3.6 유형 B [기간 조회 및 정산/정리형 업무] 고밀도 그리드 스튜디오 완성**:
    - 한 화면에서 20~30건의 청구 데이터를 조망하며 인라인 상태 변경 및 명세서 즉시 발행.
  - **4. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `조회 청구: 총 N건 | 청구 총액: ₩A원 | 기수납액: ₩B원 | 미수 잔액: ₩C원 | ⚖️ 대차 정상 (청구총액 = 수납액 + 미수잔액 100% 무결)`.
  - **5. 라벨 및 타이틀 건조한 명사 단일 표준화 (헌장 3.1)**:
    - 감성적 수식어 제거, `청구 / 수납 관리` 전사 단일 표준 준수.

---

## 📦 [주기장정비] 주기장 정비 관리 스튜디오 전면 개편: 네이티브 alert 6개소 및 confirm 2개소 전면 퇴출, 헌장 1.2 AVAILABLE 복원 및 소모품 차감 무결성, Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.95 - 2026-09-03 21:50)

### 1. 개발 내용 요약
- **`src/pages/Repairs.tsx` (전면 개편)**:
  - **1. 네이티브 `alert()` 6개소 및 `confirm()` 2개소 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 자산 미선택, 정비내역 누락, 정비 완료, 상태 보존, 외주업체 미선택, 외주 위탁 등록 시 화면을 블로킹하던 모든 브라우저 alert/confirm 팝업을 전면 삭제하고, 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **2. 헌장 1.2 자산 상태 라이프사이클 복원 및 사건 무누락 DB 저장 원칙 100% 준수**:
    - 정비 완료 시 장비의 상태를 `AVAILABLE`(`임대가능`)로 즉시 복원하고, 본사 중앙창고 소모품 차감 및 `assetInOutLogs`에 정비 완료 이력 무누락 저장.
  - **3. 헌장 3.6 유형 A [요청 처리형 업무] 마스터-디테일 정비 워크벤치 스튜디오 강화**:
    - 좌측: 주기장 입고/수리중 장비 큐(필터: 수리중/반납입고/외주/임대가능) / 우측: 정비 작업, 소모품 투입, 사진 증빙 및 외주위탁 워크벤치.
  - **4. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `수리정비중(REPAIRING): 총 N대 | 임대가능(AVAILABLE): 총 M대 | 금월 정비완료: P건 | 금월 소모품비: ₩A원 | ⚖️ 대차 정상 (정비완료-자산AVAILABLE환원 100% 무결)`.
  - **5. 라벨 및 타이틀 건조한 명사 단일 표준화 (헌장 3.1)**:
    - 감성적 수식어 제거, `주기장 정비 스튜디오`, `정비 이력 대장`.

---

## 📦 [배차관리] 배차 및 운송 관리 스튜디오 전면 개편: 네이티브 alert 6개소 및 confirm 5개소 전면 퇴출, 헌장 1.3 & 2.3 무결성 보존 및 탭1·2 이원화 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.94 - 2026-09-03 21:46)

### 1. 개발 내용 요약
- **`src/pages/TruckDispatch.tsx` (전면 개편)**:
  - **1. 네이티브 `alert()` 6개소 및 `confirm()` 5개소 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 기사 배정 완료, 운송 완료, 배차 취소, 수동 배차 생성, 매입 정산 이관 및 불일치 일괄 승인 시 화면을 블로킹하던 모든 브라우저 alert/confirm 팝업을 전면 삭제하고, 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **2. 헌장 1.3 배차 단계 자산 상태 조작 금지 및 헌장 2.3 EXCHANGE 단일 왕복배차 원칙 100% 보존**:
    - 자산 상태는 검수 승인 시점에 `RENTED`로 완결되며 배차 단계에서 조작하지 않으며, 대차/교체 발생 시 단일 `'EXCHANGE'` 1건으로 출고/회수 1:1 업무 체인과 왕복 운송비 정산 통합 관리.
  - **3. 헌장 3.6 단일 메뉴 내 탭별 2대 UI 아키타입 이원화 표준 완결**:
    - 탭 1: 요청 처리형 카드 Dossier 스튜디오 (배차 의뢰 1건 심층 검토 및 차량/기사 배정).
    - 탭 2: 기간 정산형 38~42px 고밀도 그리드 스튜디오 (월말 운송사 엑셀 1:1 대사 및 차액 승인).
  - **4. 헌장 3.5 Gutenberg Z-패턴 4단계 및 탭 1·2 최하단 회계 대차대조식 완결**:
    - 탭 1 검증식: `배차 대기: 총 N건 | 배차 완료: M건 | 운송 완료: P건 | 대차(EXCHANGE) 왕복: K건 | 취소: Q건 | ⚖️ 대차 정상 (배차의뢰-기사배정-운송비 1:1 무결)`.
    - 탭 2 검증식: `📄 청구총액 = 🟢 확정액 + 🚫 반려액 | ⚖️ 대차 차액 ₩0`.
  - **5. 라벨 및 타이틀 건조한 명사 단일 표준화 (헌장 3.1)**:
    - 감성적 수식어 제거, `배차 / 운송 관리`, `기사 배정 및 운송 관제`, `월말 운송료 대사`.

---

## 📦 [출고검수] 출고 검수 의뢰 관리 스튜디오 전면 개편: 네이티브 alert 4개소 및 confirm 전면 퇴출, 헌장 1.3 RENTED 전환 원칙 준수, 대체장비 스왑 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.93 - 2026-09-03 21:40)

### 1. 개발 내용 요약
- **`src/pages/outbound_inspections.tsx` (전면 개편)**:
  - **1. 네이티브 `alert()` 4개소 및 `confirm()` 1개소 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 검수 의뢰 접수, 최종 승인 마감, 출고 반려, 대체 장비 스왑 및 미체크 승인 시 화면을 차단하던 모든 alert/confirm 팝업을 영구 제거하고, 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **2. 헌장 1.3 출고 검수 승인 마감 시 RENTED 전환 원칙 100% 수호**:
    - 검수 합격 승인 시점 장비의 status를 `RENTED`(`대여중`)로 즉시 승계 전환하여, 배차 단계 조작 위험을 차단하고 라이프사이클 일치 보장.
  - **3. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `검수 대기: 총 N건 | 검수 진행중: M건 | 출고 승인마감: P건 | 반려: Q건 | ⚖️ 대차 정상 (검수승인-자산상태 RENTED 전환 100% 무결)`.
  - **4. 헌장 3.6 유형 A [요청 처리형 업무] 카드형 마스터-디테일 스튜디오 강화**:
    - 좌측: 의뢰 그룹별(고객사/현장별) 고밀도 큐 / 우측: 21대 안전/기술 스펙 체크리스트 및 결함 시 동급 대체장비 원클릭 스왑.
  - **5. 라벨 건조한 명사 단일 표준화 (헌장 3.1)**:
    - 감성적 수식어 제거, `출고 검수 의뢰 관리` 전사 단일 표준 준수.

---

## 📦 [장비할당] 출고/대차 장비 할당(매핑) 스튜디오 전면 개편: 네이티브 alert 18개소 전면 퇴출, 긴급 대차(EXCHANGE) 우선순위 시각화, 부분/배치 매핑 동선 정돈 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.92 - 2026-09-03 21:36)

### 1. 개발 내용 요약
- **`src/pages/asset_assignment.tsx` (전면 개편)**:
  - **1. 네이티브 `alert()` 18개소 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 모델 요구수량 초과, 쿼터 제한, 관리번호 미존재, 할당 완료, 단일/배치 취소 등에서 화면을 멈추게 하던 모든 alert() 팝업을 영구 제거하고, 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **2. 헌장 2.1 & 2.3 긴급 대차(EXCHANGE) 할당 의뢰 최우선 큐 분리**:
    - 일반 출고 건과 구분하여 교환 배차가 걸려 있는 대차 의뢰 슬롯을 식별하고, 최상단 우선 처리 큐로 시각적 배지 연동.
  - **3. 헌장 1.3 출고 검수 승인 마감 시 RENTED 전환 원칙 준수**:
    - 장비 할당 시에는 자산 상태를 `ASSIGNED`(`출고대기`)로 정확히 세팅하고 출고 검수 대기열로 넘겨, 출고 검수 승인 시점에 `RENTED`로 승계 완결되도록 보장.
  - **4. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `미할당 요구: 총 N대 (M건 계약) | 대차(EXCHANGE) 긴급: K건 | 가용 출고자산: 총 P대 | 현재 선택 매핑: Q대 | ⚖️ 대차 정상 (요구슬롯-실자산 1:1 매핑 무결)`.
  - **5. 라벨 및 버튼 명칭 건조한 명사 단일 표준화 (헌장 3.1)**:
    - "스마트" 제거, `추천순 자동선택`, `장비 할당 관리`.

---

## 📦 [회수의뢰] 렌탈 회수 의뢰 관리 본질업무 스튜디오 전면 개편: 비실재 메신저 파서 영구 배제, 계약 가동장비 중심 고밀도 직관 조망, 출고 부속품 누락 방지 회수 체크리스트 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.91 - 2026-09-03 21:33)

### 1. 개발 내용 요약
- **`src/pages/smart_return.tsx` (전면 개편)**:
  - **1. 비실재 메신저 텍스트 파싱 기능 영구 배제 (사장님 지시 & 도메인 본질화)**:
    - 실제 현장 업무에서 발생하지 않는 메신저 텍스트 입력창, 파서 토글, 하드코딩된 모의 텍스트를 완전히 영구 제거하여 작업대 공간을 100% 본질 업무(가동 계약 및 투입 장비 조망)에 집중.
  - **2. 네이티브 `alert()` 11개소 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 계약/장비 미선택, 회수일자 누락, 저장 완료, 인쇄 실패 등에서 화면을 차단하던 모든 alert 팝업을 영구 제거하고, 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **3. 계약 가동장비 중심 마스터-디테일 스튜디오 강화 (헌장 3.6 유형 A)**:
    - 좌측: 가동 중 렌탈 계약 실시간 검색 및 D-Day 만료 임박 배지 리스트.
    - 우측: 해당 계약의 현장 투입 자산 목록(체크박스 다중선택) + 현장 담당자 및 회수일시 자동 기본값 세팅.
  - **4. 출고 시 부속품 누락 방지 자동 상속 체크리스트 연동 (헌장 2.2)**:
    - 출고 시 함께 나갔던 충전기, 전원선, 조이스틱 보호커버, 상단 감지봉 센서, 함석/철망 등 부속품을 100% 자동 상속하여 회수 시 체크리스트로 확인 및 배차 지시서 연동.
  - **5. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `진행 중 계약: 총 N건 | 현장 대여장비: 총 M대 | 회수 선택장비: K대 | ⚖️ 대차 정상 (회수의뢰-입고배차체인 1:1 연동)`.
  - **6. UI 타이틀 및 탭 라벨 건조한 명사 단일 표준화 (헌장 3.1)**:
    - `회수 의뢰 관리`, `임대 계약 회수 의뢰`, `외주 정비 수리완료 회수 의뢰`.

---

## 📦 [출고요청] 스마트 출고 발주 접수 스튜디오 전면 개편: 네이티브 alert 및 confirm 팝업 전면 퇴출, 모델명 지능형 자동 보정, 신규 고객/현장 논스톱 연동 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.90 - 2026-09-03 21:25)

### 1. 개발 내용 요약
- **`src/pages/smart_dispatch.tsx` (전면 개편)**:
  - **1. 네이티브 `alert()` 14개소 및 `confirm()` 2개소 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 텍스트 파일 로드, 파싱 결과 안내, 유효성 검사, 저장 권한, 장비 누락 등에서 화면을 멈추게 하던 모든 alert 및 confirm 팝업을 영구 제거하고, 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **2. 장비 모델명 지능형 자동 보정 파이프라인 (헌장 1.1, 2.1)**:
    - 자연어 입력 모델명이 정식 자산 모델과 다를 경우 팝업으로 차단하지 않고, 유사도 매핑 추천 모델로 즉시 자동 보정(`updatedEquipments[i].modelName = suggestedModel`) 처리 후 안내 토스트 표출.
  - **3. 신규 거래처/현장 논스톱 자동 등록 및 출고 프로세스 연계 (헌장 1.1, 2.1)**:
    - DB 미등록 거래처/현장이 감지되었을 때 브라우저 confirm 팝업으로 중단하지 않고, 신규 자동 등록을 연속 집행하여 즉시 장비 할당 및 배차 큐로 바통 연계.
  - **4. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `출고의뢰 장비: 총 N대 (M종 모델) | 고객사 / 현장: 고객명 / 현장명 | 납품희망: 일시 | ⚖️ 대차 정상 (출고의뢰-배차체인 정합)`.
  - **5. 헌장 2.1, 2.2 부서 R&R 및 기본스펙 자동 상속 보장**:
    - 자연어 파싱 시 고객사/현장의 21대 안전스펙 및 유상옵션/보양 기본값을 누락 없이 100% 자동 상속.

---

## 📦 [계약관리] 렌탈 계약 관리 및 라이프사이클 관제 스튜디오 전면 개편: @ts-nocheck 제거 및 100% 정적 타입 안전성 복원, 네이티브 alert 전면 퇴출, 6대 실시간 계약 운용 KPI 바 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.89 - 2026-09-03 21:20)

### 1. 개발 내용 요약
- **`src/pages/Contracts.tsx` (전면 개편)**:
  - **1. `@ts-nocheck` 완전 제거 및 정적 타입 무결성 복원 (헌장 5.3)**:
    - 2,000여 줄 상단에 방치되어 컴파일 타임 검증을 무력화하던 `// @ts-nocheck`를 영구 제거하고, `createContract` 인터페이스 누락 속성(`lateInterestRate: 0`) 보정으로 100% 엄격한 정적 타입 컴파일 완결.
  - **2. 네이티브 `alert()` 14개소 전면 영구 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 렌탈료 변경, 만료일 연장/단축, 계약 승계, 대차교체, 유효성 검사 등 모든 사용자 알림을 0-블로킹 **인앱 토스트 알림 배너**(`fadeIn 0.2s`)로 100% 교체.
  - **3. 실시간 계약 운용 KPI 6대 지표 요약 바 구축 (Scope)**:
    - `총 계약건수`, `진행/연장중`, `만료 임박 (D-3)`, `단가 0원 주의`, `체결 투입자산`, `월 렌탈료 합계` 6대 지표를 상단에 고정 렌더링.
  - **4. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `전사 체결계약: 총 N건 (진행 X건 / 승계 Y건 / 종결 Z건) | 체결 투입자산: 총 M대 | 월 렌탈료 총액: ₩A원 | ⚖️ 대차 정상 (계약-자산-청구 기준정보 100% 무결)`.
  - **5. 무반응 더미 `[조회]` 버튼 제거 및 실시간 반응형 필터 정돈**:
    - 클릭해도 동작하지 않던 불필요한 더미 버튼을 제거하고, Gutenberg Z-패턴 동선에 정합하도록 필터 패널 정비.
  - **6. 헌장 2.1, 2.2, 2.3 R&R 정책 준수 확립**:
    - 대차 교체 시 계약 속성(단가, 청구마감일, 현장조건) 100% 자동 상속 보장 및 단일 `'EXCHANGE'` 왕복 배차 의뢰 체계 확립.

---

## 📦 [고객관리] 거래처 고객사 및 현장/담당자 마스터 스튜디오 전면 개편: Raw SQL 팝업 전면 퇴출, 실시간 즉시 필터링, 마스터-디테일 360도 스튜디오, 테이블 컬럼 정합성 복원 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.88 - 2026-09-03 21:15)

### 1. 개발 내용 요약
- **`src/pages/Customers.tsx` (전면 개편)**:
  - **1. Raw SQL 쿼리 alert 팝업 및 네이티브 alert 전면 퇴출 (헌장 1.1, 1.2, 5.2)**:
    - 저장 시마다 화면을 가로막던 `[DB 전송 예정 SQL 쿼리 안내]` alert 팝업 및 성공 alert를 완전히 영구 제거하고, 0-블로킹 인앱 토스트 체계로 교체.
  - **2. 실시간 반응형 즉시 검색 필터링 (헌장 1.1 & 1.2 "최소 조작 & 최대 편익")**:
    - `tempSearchTerm`, `tempShowOnlyIncomplete` 및 수동 `[조회]` 버튼을 제거하고, 입력 즉시 실시간 필터링되도록 전면 개선.
    - 거래 상태 필터(`전체`, `정상 거래`, `거래 제한`, `폐업`) 및 원클릭 ⚠️ 보완필요 고객사 필터 토글 연동.
  - **3. 마스터-디테일 360도 스튜디오 아키텍처 (헌장 3.6 유형 A)**:
    - 좌측 360px 고객사 목록 패널 + 우측 flex:1 통합 관리대장 구축.
    - ① 고객사 기본 마스터 정보 카드 (사업자번호, 대표자, 업태/종목, 청구/명세서 마감일, 주소, 거래승인상태) + 인라인/모달 편집
    - ② 기본 옵션/보양 마스터 바 + `[⚡ 전체 현장에 기본값 일괄 전파]` 원클릭 버튼
    - ③ 등록 현장(`sites`) 목록 (고밀도 슬림 테이블 + 옵션/보양 컬럼 복원 + 현장 추가/수정)
    - ④ 등록 담당자(`contacts`) 목록 (이름, 직책, 연락처, 이메일, 활성여부 + 담당자 추가/수정)
    - ⑤ 입금 계좌(`bankAccounts`) 목록 (은행, 계좌번호, 예금주, 메모 + 계좌 추가/수정/삭제)
  - **4. 테이블 헤더-셀 컬럼 불일치 버그 복원**:
    - 현장 목록 테이블에서 누락되었던 "유상옵션 / 보양" 셀(`<td>`)을 정밀 복원하여 7개 헤더 컬럼과 100% 일치시킴.
  - **5. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `전사 고객사: 총 N개사 (정상 X / 제한·폐업 Y) | 등록 현장: 총 M개소 (가동 K개소) | 등록 담당자: 총 P명 | ⚖️ 대차 정상 (고객-현장-담당자 기준정보 100% 무결)`.

---

## 📦 [제품모델관리] 제품 모델 및 제원 관리 대장 전면 개편: 폐기 렌더러 잔재 청산, Cloudflare R2 문서함(drcf) 직결, 4구역 Dossier 서랍 패널, 고밀도 38px 대장 그리드 및 Gutenberg Z-패턴 대차대조식 완결 (v1.3.0.Build.87 - 2026-09-03 21:10)

### 1. 개발 내용 요약
- **`src/pages/Products.tsx` (전면 개편)**:
  - **1. 폐기된 브라우저 PDF 렌더러 잔재 및 에러 청산**:
    - `throw new Error(...)` 및 `alert(...)` 브라우저 팝업 호출을 전면 제거하고, R2 클라우드 보관 문서 직결 열기 및 인앱 토스트 체계로 교체.
  - **2. Cloudflare R2(`drcf`) 도큐먼트 스튜디오 직결 연동**:
    - 모델별 `Eq_doc/{modelName}/` 경로에 보관된 안전인증서, 제원표, 취급설명서 문서를 실시간 조회, 1-클릭 열기, 삭제 및 드래그/업로드 파이프라인 완결.
  - **3. 우측 520px Dossier 서랍형 슬라이드오버 (헌장 3.6 마스터-디테일 스튜디오)**:
    - 무거운 전체화면 팝업을 퇴출하고, 행 클릭 시 부드럽게 열리는 4구역 서랍 패널(`slideLeft`) 구축.
    - ① 실물 자산 보유 및 가동 현황 (총 보유, 임대가능, 대여중, 정비중 + 자산대장 바로가기 링크), ② R2 클라우드 문서함, ③ 상세 물리 제원 규격(피트, 작업높이, 발판높이, 중량, 적재중량, 속도 등), ④ 제원표 그래픽 다이어그램 모달 연동.
    - 서랍 내 인라인 수정 모드 지원 (`handleStartEdit`, `handleSaveEdit`).
  - **4. 헌장 3.4 준수 상하 세로 스택 필터 바**:
    - `통합 검색`, `자산 보유 여부`, `제조사`, `동력 방식`, `사용 상태` 필터를 `flex-direction: column` 상하 배치 및 1-클릭 초기화 버튼 연동.
  - **5. 헌장 3.6 유형 B 고밀도 38px 대장 그리드**:
    - 좌측 1번 컬럼 `[보기]` 버튼 고정 배치, 모델명, 피트, 실물자산(자사/임차), R2문서함, 제조사, 동력, 작업높이, 발판높이, 중량, 적재중량, 크기, 속도, 사용여부 초슬림 렌더링.
  - **6. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `전사 등록모델: 총 N종 (자산보유 X종 / 미보유 Y종) | 매핑 실물자산: 총 M대 | R2 클라우드 보관문서: 총 K건 | ⚖️ 대차 정상 (모델-자산 기준정보 100% 정합)`.
  - **7. 네이티브 `alert()` & `confirm()` 전면 퇴출 (헌장 5.2)**:
    - 인앱 토스트 알림 배너 연동.

---

## 📦 [자산관리대장] 전사 자산 관리 대장 전면 개편: 반응형 즉시 로딩(초기 빈화면 영구제거), 원사 마스터 연동, 고밀도 38px 대장 그리드, 5구역 Dossier 서랍 패널 및 Gutenberg Z-패턴 4단계 대차대조식 완결 (v1.3.0.Build.86 - 2026-09-03 21:00)

### 1. 개발 내용 요약
- **`src/pages/Assets.tsx` (전면 개편)**:
  - **1. 수동 조회 빈 화면 차단 영구 제거 (헌장 1.1 & 1.2 "최소 조작 & 최대 편익")**:
    - `if (!hasQueried) return [];` 빈 화면 로딩 차단 로직을 완전히 제거하여 메뉴 진입 즉시 1,500여 대 전 자산이 정렬된 상태로 초고속 렌더링.
    - 검색창 및 필터 조작 시 실시간 디바운스 반응형 필터링 적용.
  - **2. 원사(소유원사/임차처) 마스터 자동 JOIN (`getAssetRenterName`)**:
    - `vendorId` 외래키 우선 매핑을 통해 `vendors` 마스터의 거래처 상호명을 100% 자동 결합 표출.
  - **3. FSM 반납 상태 및 매각 상태 명확화**:
    - 실제 반납일(`actualRentReturnDate`)이 있거나 `status === 'RENTED_RETURNED'`인 장비는 `임차처 반납완료` 배지, `status === 'SOLD'`인 장비는 `매각 처분완료` 배지로 정밀 분리.
  - **4. 5대 자산 상태 실시간 KPI 요약 바 구축 (Scope)**:
    - `임대가능(주기장)`, `현장 대여중`, `출고/검수 대기`, `수리/정비중`, `반납/매각 완료`, `실가동률(%)` 6대 지표를 1눈에 조망.
  - **5. 헌장 3.4 준수 상하 세로 스택 필터 바**:
    - `통합 검색`, `소유구분`, `장비 상태`, `제조사`, `현재 고객사` 필터를 `flex-direction: column` 상하 배치 및 1-클릭 초기화 버튼 연동.
  - **6. 헌장 3.6 유형 B 고밀도 38px 대장 그리드**:
    - 좌측 1번 컬럼 `[보기]` 버튼 고정 배치, 관리번호, 모델명, 제조사, 시리얼, 연식, 소유, 상태, 고객사/현장, 계약번호, 월렌탈료, 소유원사, 장부가치/취득원가, 기여 순익, 정비점수 초슬림 렌더링.
  - **7. 우측 500px 서랍형 Dossier 슬라이드오버 (헌장 3.6 마스터-디테일 스튜디오)**:
    - 무거운 전체화면 팝업을 퇴출하고, 행 클릭 시 부드럽게 등장하는 Dossier 슬라이드 패널 구축.
    - 5구역 구성: ①기본 물리 제원, ②운용/임대 현황, ③당사자산 감가상각/임차원사 약정조건, ④누적 손익 및 공헌이익, ⑤정비 및 생애주기 감사 타임라인 + 이력 엑셀 내려받기.
    - 서랍 내 인라인 수정 모드 지원 (`handleStartEdit`, `handleSaveEdit`).
  - **8. 헌장 3.5 Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바 탑재**:
    - `전사 등록자산: 총 N대 (당사 X대 + 임차 Y대) | 실가동률: Z% (대여중 M대 + 대기 K대) | 당사자산 장부가 총액: ₩A원 | ⚖️ 대차 정상 (전사 자산 대사 완결)`.
  - **9. 네이티브 `alert()` 전면 퇴출 및 인앱 토스트 알림 연동 (헌장 5.2)**:
    - 이력 엑셀 다운로드, 수정 저장 완료 등에 인앱 토스트 배너 연동.

---

## 📦 [임차자산/전대손익] 임차자산 관리 및 전대 손익 정산 메뉴 전면 개편: FSM 반납상태 동기화, 원사 마스터 연동, 고밀도 38px 대장 그리드, 감사 방어벽(과실·게으름·부정 원천방어) 및 Gutenberg Z-패턴 4단계 대차대조 체계 구축 (v1.3.0.Build.85 - 2026-09-03 20:50)

### 1. 개발 내용 요약
- **`src/pages/rent_assets.tsx` (전면 개편)**:
  - **1. 4대 치명적 데이터/로직 결함 원천 해소**:
    - **날짜 역전 방어**: `rentStart > rentEnd` 발생 시 날짜 역전 방어 포맷(`~ {rentEnd}`) 적용 및 등록/수정 시 시작일이 만료일보다 늦을 경우 실시간 차단.
    - **FSM 상태 붕괴 및 허위 경보 제거**: 실제 반납일(`actualRentReturnDate`)이 있거나 `status === 'RENTED_RETURNED'`인 장비는 `isSubleaseOverdue` 및 `calculateDelayDays`에서 100% 제외. 과거 반납 장비의 `대여중` 및 `전대 기간 초과` 허위 배지 완전 소멸, `⚪ 임차처 반납완료` 단일 확정.
    - **원사 '미지정' 해소**: `vendorId` 외래키를 기반으로 `vendors` 마스터와 자동 JOIN하여 거래처 상호명(`getAssetRenterName`)을 100% 정상 표출.
    - **상단 실시간 KPI 지표 정상화**: 가동 중 장비(`activeRentedList`)와 반납 완료 장비(`returnedList`)를 엄격 분리 집계하여 월 총 임차료 지출액 왜곡 원천 제거.
  - **2. 감사(Audit) 관점의 과실·게으름·부정 3대 방어벽 구축**:
    - **과실(Carelessness) 방어**: 계약 만료 및 반납 지연 시 실시간 경보 + 반납 배차 의뢰 연동으로 반납 지연에 따른 유령 임차료 누수 차단.
    - **게으름(Sloth) 방어**: 임차 등록/수정 시 소유 원사 마스터 바인딩 강제, 날짜 역전 시 등록 차단, 필수 입력 검증 강화.
    - **부정(Fraud) 방어**: 고객사 렌탈료 vs 원사 임차료 실시간 비교를 통한 전대 마진율 산출 및 `⚠️ 역마진 경고` 체계 구축.
  - **3. 전사 표준 헌장 준수 고밀도 대장 및 UI/UX 표준화**:
    - **헌장 3.1 (건조한 명사·동사 표준)**: 감성적 미사여구 및 불필요한 설명 문장 전면 배제, 3대 서브탭(`임차자산 대장`, `전대 손익 대장`, `원사 명세서 대사`) 표준화.
    - **헌장 3.4 (상하 세로 스택 필터)**: `자산 검색`, `소유 원사`, `반납 상태` 필터를 `flex-direction: column` 상하 배치.
    - **헌장 3.6 유형 B (고밀도 38px 그리드)**: 화면 세로 80%를 점유하는 초슬림 테이블 구축, 좌측 1번 컬럼 `[상세]` 버튼 배치.
    - **Dossier 슬라이드오버 패널**: 행 클릭 시 460px 우측 슬라이드 패널을 통해 장비 제원, 임차 조건, 전대 손익, 라이프사이클 감사 로그를 원스톱 조망.
    - **헌장 3.5 (Gutenberg Z-패턴 4단계 및 최하단 회계 대차대조식 검증 바)**:
      - 탭 1 (임차자산 대장): `가동중 전대장비 N대 | 월 총 임차료 지출 ₩X원 | 반납 완료 M대 | ⚖️ 대차 정상 (FSM 동기화 완결)`
      - 탭 2 (전대 손익 대장): `📄 전대 매출총액 = 🏢 매입 임차료 + 🚚 직송 운송비 + ⚖️ 전대 순마진 | ⚖️ 대차 차액 ₩0`
      - 탭 3 (원사 명세서 대사): `📄 원사 청구총액 = 🟢 일치 확정액 + 🚫 차액/이의액 | ⚖️ 대차 차액 ₩0`
    - 네이티브 `alert()` 전면 제거 및 인앱 토스트 알림 연동.
- **`src/services/migrationEngine.ts`**:
  - 임차 자산 초기 적재 시 `leaseReturnDate`가 존재할 경우 `status: 'RENTED_RETURNED'`로 즉시 마감, 시작일-종료일 역전 방어 및 `renter` 상호명 필수 주입.
- **`src/index.css`**:
  - `@keyframes slideLeft`, `@keyframes fadeIn` 스타일 추가.

---


### 1. 개발 내용 요약
- **`src/pages/AssetAcquisitionDisposal.tsx` (전면 개편)**:
  - **1. 고밀도 취득/매각 대장 그리드 구축 (헌장 3.6 유형 B)**:
    - 텅 빈 폼 카드만 있던 기존 화면을 전면 개편하여, `[당사자산 취득 대장]` 및 `[자산 매각 처분 대장]` 2대 서브탭과 화면 세로 80%를 차지하는 행 높이 38px 초슬림 데이터 테이블 배치.
    - 취득 대장: 관리번호, 모델명, 제조사, 시리얼번호, 연식, 취득일자, 취득원가, 내용연수, 월상각액, 감가누계액, 현재장부가, 공급처, 안전인증여부, 운용상태를 1눈에 조망.
    - 매각 대장: 관리번호, 모델명, 매각일자, 취득원가, 감가누계액, 매각시점 장부가, 실제 매각가, ⚖️ 유형자산처분손익(흑자/적자 배지), 매각처, 매출청구 상태 조망.
  - **2. 입력 편의성 혁신 (3-Way Pipeline)**:
    - **단건 정밀 등록 모달**: 모델 선택 시 제조사/제원 자동 상속 + 모델별 기존 번호 분석 기반 다음 관리번호 자동 추천(예: `SJ19-105` ➔ `SJ19-106`).
    - **연속 N대 일괄 생성기 (Batch Generator)**: 시작 번호(예: `SJ19-106`)와 수량(예: 5대) 입력 시 5개 슬롯 실시간 미리보기 및 차대번호 인라인 입력, 1-클릭 일괄 인서트 지원.
    - **엑셀 표준 서식 다운로드 및 일괄 업로드 (Excel Import)**: 대량 취득 자산 엑셀 양식 지원 및 유효성(중복 번호) 실시간 검증.
    - **대장 엑셀 내보내기 (Excel Export)**: 필터링된 대장 내역을 즉시 엑셀 파일로 저장.
  - **3. 감사 증거 확보 (Audit Evidence Trail)**:
    - 매입처 마스터(`vendors`) 1:1 매핑 + 안전인증/검사증명서 URL 저장.
    - 행 클릭 시 우측 슬라이드오버 Dossier 패널 표출 (물리 제원, 감가상각 스케줄, 안전 증빙 링크, 자산 감사 로그 타임라인).
    - 브라우저 기본 `alert()` 100% 제거 및 인앱 토스트 알림 연동.
  - **4. Gutenberg Z-패턴 및 최하단 회계 대차대조 검증 바 (헌장 3.5)**:
    - 취득 대장: `📄 총 취득원가 = 🟢 현재 장부가 + 📉 감가상각누계액 | ⚖️ 대차 차액 ₩0`
    - 매각 대장: `📄 총 매각대금 = 🟢 장부가 총액 + ⚖️ 처분손익 | ⚖️ 대차 차액 ₩0`
- **`src/context/AppContext.tsx`**:
  - `acquireAsset`: 취득 이벤트 발생 시 `assetInOutLogs`에 `type: 'ACQUISITION'`으로 감사 로그 무누락 저장 (헌장 1.2).
  - `batchAcquireAssets`: N대 일괄 취득 배치 함수 신설.
- **`src/index.css`**:
  - `.hover-row:hover` 행 하이라이트 스타일 전역 추가.

---

## 🎨 [UI/UX] 전 메뉴 라이트/다크 테마 대비(Contrast) 전면 개편 및 미정의 CSS 토큰 18종 일괄 해소 (v1.3.0.Build.83 - 2026-09-03)

### 1. 개발 내용 요약
- **`src/index.css` (전사 테마 시스템)**:
  - 코드베이스 전반에서 164회 이상 쓰였으나 `:root` 및 `[data-theme='dark']`에 누락되었던 18종 핵심 별칭 토큰(`--text-primary`, `--border`, `--bg-body`, `--bg-surface`, `--bg-secondary`, `--bg-main`, `--card-bg`, `--body-bg`, `--bg-card-header`, `--bg-active`, `--primary-rgb`, `--primary-border`, `--accent` 등)을 공식 선언하여 **미정의 CSS 변수 0건 달성**.
  - 배지 시스템(`.badge-success`, `.badge-warning`, `.badge-danger`, `.badge-info`, `.badge-primary`, `.badge-secondary`)에 라이트/다크 전용 고대비(7:1 이상) 스타일 구축.
- **전 메뉴 인라인 하드코딩 색상 테마 변수 전환**:
  - `Products.tsx`: 장비 제원표 미리보기 모달의 하드코딩 `#ffffff`, `#111827`, `#f3f4f6`을 `var(--bg-card)`, `var(--text-main)`, `var(--bg-app)`으로 전면 테마화.
  - `BillingInvoiceTab.tsx`: 하드코딩 색상 전면 제거 및 고대비 뱃지 시스템 바인딩.
  - `SmartAsRequest.tsx`: AS 접수 폼 카드 3종 및 입력창/프리셋 버튼 하드코딩 색상 제거하여 다크 모드 입력창 흰색 충돌 완전 해결.
  - `rent_assets.tsx`, `TruckDispatch.tsx`, `Dashboard.tsx`, `BankMatching.tsx`, `Billings.tsx`: 전대 손익 및 배차/대사 인라인 색상 토큰화.
  - `FieldAsManagement.tsx` (175개소) & `InitialDbUploader.tsx` (125개소): 시맨틱 토큰으로 안전 일괄 전환.
  - `App.css`: 미정의 변수 3종(`--shadow`, `--social-bg`, `--text-h`) 공식 토큰으로 교체.
- **글로벌 학습 지식 베이스 등록**: `경험.md`에 이슈 `E-034` 등록 완료.

---

## 🚀 [자산관리] 자산 상세 모달 내 실효성 없는 '점검 서류 파일 경로 (구글 드라이브)' 섹션 전면 삭제 (v1.3.0.Build.82 - 2026-09-03)

### 1. 개발 내용 요약
- **`src/pages/Assets.tsx`**:
  - 자산 상세 명세서 상단에 존재하던 비실용적 텍스트 입력 섹션(`점검 서류 파일 경로 (구글 드라이브)`) 및 관련 모달 상태(`CloudStoragePickerModal`, `FolderOpen`, `isDrivePickerOpen`) 전면 제거.
  - `[1. 기본 장비 정보]` 바로 아래에 `[2. 현재 운용 / 임대 현황]`이 밀착되어 스크롤 없이 시선 흐름이 자연스럽게 이어지도록 화면 정보 밀도 극대화.

---

### 1. 개발 내용 요약
- **`src/pages/Assets.tsx` (자산 상세 모달 및 이력 대장)**:
  - `getUnifiedAssetLogs` 헬퍼 함수 신설: 기존에 `assetInOutLogs`(입출고)만 조회하여 밴드 과거 AS 이력(`repairs` 대장 4건 등)이 누락되던 문제를 전면 해결.
  - `repairs` 대장의 접수일/방문일, 고장증상, 조치내용, 정비사, 티켓번호를 `[정비]` 배지 항목으로 자동 합성 바인딩하여 자산이력 타임라인 및 엑셀 내려받기에 100% 온전하게 노출.
- **`src/services/migrationEngine.ts`**:
  - `TABLE_COLUMNS.asset_inout_logs` 스키마 화이트리스트를 Supabase 실서버 스키마 및 DB 인터페이스와 100% 일치하도록 정정 (`details`, `contractId`, `note`, `performedBy` 제거 ➔ `memo`, `customerName`, `siteName`, `repairId`, `inboundNo`, `maintenanceScore`, `defectsJson` 반영).
  - `ingestBandAsHistoryDirect`에서 `asset_inout_logs` 적재 시 `details` 대신 실서버 컬럼인 `memo`로 필드명 교체.
  - `assetId`, `customerId`, `siteId` 유효 외래키(FK) 사전 검증으로 무결성 보장.

---

### 1. 개발 내용 요약
- **`src/services/db.ts` (`LocalDB`)**:
  - `inMemoryCache` 도입 및 브라우저 `localStorage` 5MB 용량 한도 초과(`QuotaExceededError`) 시 비정상 종료 방어.
  - 대용량 데이터 적재 시 localStorage 용량이 초과되어도 메모리 캐시에서 100% 온전하게 데이터를 유지하고 에러 없이 정상 흐름 보장.
- **`src/services/migrationEngine.ts`**:
  - `TABLE_COLUMNS`에 `repairs` 화이트리스트 스키마 공식 등록.
  - `ingestBandAsHistoryDirect`에서 단순 `db.repairs = ...` 대입 대신 `batchUpsertChunked('repairs', ...)`를 호출하여 수천 건의 AS 이력이 100건 단위 청크로 Supabase 원격 DB에 직접 무누락 분할 적재되도록 전면 개편.
  - `assets`, `customers`, `customer_sites`, `contracts`, `users` 외래키(FK) 유효성 사전 검증으로 PostgreSQL FK 제약 위반 원천 차단.

---

### 1. 개발 내용 요약
- **`src/services/db.ts` (`LocalDB`)**:
  - `db.getRow<T>(key, id)` 메서드 신설: 로컬 캐시 및 snake_case 테이블명(`customer_sites`, `customer_contacts` 등)과 내부 키(`sites`, `contacts`)를 상호 자동 정규화(`normalizeKey`)하여 단일 레코드를 안전하게 조회.
  - `addRow`, `insertRow`, `updateRow`, `deleteRow`에 `normalizeKey` 호환 지원 탑재.
  - `customerContacts`, `customerSites`, `contractHistories` 게터/세터 별칭 등록으로 속성 불일치 원천 차단.
- **`src/services/migrationEngine.ts`**:
  - `ingestCustomerDefaultsFromDispatchHistory`에서 고객 및 현장 데이터 조회 시 로컬 캐시 조회 실패 시 Supabase 원격 테이블 직접 조회 Fallback 구현.
  - 동기화 루프 비동기 처리 정밀화 및 `await db.awaitPendingWrites()` 안전 완결.

---

### 1. 개발 내용 요약
- **`migrationEngine.ts` (배차 엑셀 업로드 엔진)**:
  - 엑셀의 `운반비` 컬럼이 빈칸(공란)이거나 0원이면 `deliveryCost: 0`, `expectedCost: 0`으로 정확히 0원 그대로 DB에 적재되도록 보장.
- **`TruckDispatch.tsx` (운송료 대사 & 배차 모니터)**:
  - 과거 JavaScript의 `d.deliveryCost || 70000` 단락 평가로 인해 0원이 70,000원으로 둔갑하던 하드코딩 로직 전면 제거.
  - `getEffectiveDeliveryCost(d)` 헬퍼를 신설하여 `deliveryCost`가 0원이면 정확히 0원으로 반환 및 대사/통계 집계.
  - 수동 배차 입력 초기값 `manualExpectedCost: 0`으로 변경.
- **`Deliveries.tsx` (배차 관리 대장)**:
  - 배차 모달 오픈 시 `setDeliveryCost(d.deliveryCost ?? 0)`로 0원 온전 보존.

---

### 1. 개발 내용 요약
- `TruckDispatch.tsx`:
  - **헤더 자간 공백 제거(`replace(/\s+/g, '')`)**: `사  용  기  간`, `단  가`, `금  액`, `현  장  명` 등 한글 엑셀 특유의 자간 공백 컬럼을 100% 정상 인식하여 `정규일자` 및 `정규금액` 누락 방지.
  - **날짜 미기재 항목 임의 매칭 차단**: `isDateNear`에서 양쪽 모두 유효 날짜가 있을 때만 비교하도록 수정하여 7월 1일 건이 7월 7일/18일로 오매칭되는 현상 원천 차단.
  - **대사 그리드 일자 오름차순(과거 ➔ 최신) 정렬**: `reconPairs` 및 `completedDeliveriesForRecon`에 `.sort()`를 적용하여 7월 1일부터 31일까지 달력 순서대로 정확하게 오름차순 정렬 렌더링.
  - **입금계좌 등 결제 메타데이터 행 제외**: `입금계좌`, `계좌번호` 등이 적힌 행이 엑셀 단독 청구건으로 오인식되는 문제 방어.

---

### 1. 개발 내용 요약
- `TruckDispatch.tsx`:
  - 대사 테이블 행 더블클릭(`onDoubleClick`) 및 `[상세]` 버튼 클릭 시 배차 상세 모달 표출.
  - 시스템 배차 원장(배차번호, 일자, 구분, 고객사, 현장, 상/하차지, 경유지, 배정기사/차량, 메모, 시스템 금액)과 운송사 엑셀 청구 내역(일자, 업체, 현장, 구간, 차종, 비고, 청구액) 2열 1:1 정밀 비교.
  - 상단 대차대조 비교 바 및 차액/할증 원인(경유지할증 등) 노출.
  - 모달 내 원클릭 조치(`[ 엑셀 청구액 승인 ]`, `[ 신규 배차 생성 ]`, `[ 오청구 반려 ]`, `[ 취소 ]`) 지원.

---

### 1. 개발 내용 요약
- `TruckDispatch.tsx`: 엑셀 상단 요약표 배제 및 최다 컬럼 일치 행(Row 12) 헤더 선정.
- 운송비 우선 추출로 날짜 시리얼 오인식 원천 차단.

---

### 1. 개발 내용 요약
- `InitialDbUploader.tsx`: `[ 📋 밴드 전체 게시글 추출 스크립트 복사 ]` 버튼 공식 연동.
- 938건(1,880블록) 대용량 출고 파일 분석 및 169개 고객사 마스터 동기화 지원.

---

### 1. 개발 내용 요약
- `TruckDispatch.tsx`: `handleReconSearch` 신설 및 `[조회]` 버튼에 연동.
- 엑셀 대사 결과(`reconPairs`), 파일 상태, 선택 체크박스 초기화 처리.

---

### 1. 개발 내용 요약
- `migrationEngine.ts`: 동일 타임스탬프 중복 헤더 병합 및 비출고 알림글 배제 적용.
- `경험.md`: 이슈 `E-028` 등록 완료.

---

### 1. 개발 내용 요약
- `InitialDbUploader.tsx`: 실효성 없는 `21대 표준 스펙` 컬럼 제거 및 `고객 특이 요구사항 (반복 재사용 메모)` 컬럼 대폭 확장.
- 스켈톤 발상 영구 기록: `skelton/발상/2026-09_고객요구사항_정확한기억_및_반복재사용_원칙.md` 작성 및 푸시 완료.

---

### 1. 개발 내용 요약
- `InitialDbUploader.tsx` Line 54의 `useApp()`에 `assets` 구조분해 추가.
- `band_as_history_all.txt` 파일 업로드 시 `analyzeBandAsHistory`의 6번째 인자 `assets` 참조 오류 영구 해결.

---

### 1. 개발 내용 요약
- **표준 비즈니스 명칭 적용**:
  - 업로드 전(대기 상태): `🚚 배차 운송료 합계`
  - 업로드 후(1:1 대사 상태): `📄 운송사 청구 총액`
- **대차 차액 동적 수식 연동**: 하드코딩 ₩0 제거 및 실시간 대차대조식 무결성 검증.
- **대사 대기 뱃지**: 업로드 전 불필요한 등식 숨김 및 직관적 동선 안내.

---

### 1. 개발 내용 요약
- **transportFee 변수 스코프 재정의**: `v1.3.0.Build.63` 외상미수금 정리 작업 시 변수 선언부가 함께 정리되어 하단 1377행에서 런타임 오류가 발생했던 문제 시정.
- `migrationEngine.ts` 1353행에 Col 7 운반비 파싱 변수를 재정의하여 초기DB 업로드 파싱 정상화.

---

### 1. 개발 내용 요약
- **3대 핵심 Key 기반 유효 행 판정**: "담당자의 기억을 돕기 위한 실무적 단순기록" 원칙에 따라 장비명 기반 스킵 전면 철폐.
  - KEY 1 (날짜): 상차일/하차일 중 1개 필수
  - KEY 2 (업체명/현장명): 업체명/현장명 중 1개 필수 (합계/소계 행 제외)
  - KEY 3 (금액): 운반비 유효 금액 수집
- **숫자 4자리 모델명 누락 결함 해결**: `1008`, `1330`, `1012` 등 고소작업대 정규 모델이 숫자합계로 오인되어 누락되던 `/^\d{4,}$/` 정규식 완전 제거. 장비명은 `memo`에 `장비: [모델명]`으로 영구 보존.
- **고객사명 표출 개선**: `TruckDispatch.tsx`에서 계약 미매핑 건도 `memo`에 기록된 업체명을 추출하여 `'고객사미지정'` 대신 실제 업체명(HDC랩스, 아주렌탈 등)으로 표출.

---

### 1. 개발 내용 요약
- **현장 AS 중복 제거 및 도메인 R&R 엄격 분리**: 출장 AS 접수/스케줄러 제거, 순수 본사 주기장(Yard) 수리·점검·소모품 투입 및 임대가능 복귀 본연의 업무로 단일화.
- **주기장 정비 스튜디오 워크벤치 (헌장 3.6 유형 A)**: 좌측 정비 대상 자산 큐 + 우측 10대 퀵 정비 칩, 중앙창고 소모품 차감, Gutenberg Z-패턴 `[정비 완료 ➔ 임대가능(AVAILABLE) 전환]` 탑재.
- **자산 상태 머신(FSM) 단절 버그 시정**: 완료 시 `AVAILABLE` 즉시 전환, 이상점수 0점 리셋, 중앙창고 재고 차감 및 로그 무누락 저장.
- **주기장 정비 대장 (헌장 3.6 유형 B)**: 고밀도 기간별 조회 및 엑셀 다운로드.
- **스켈톤 연동**: `skelton/계획/2026-09_주기장_정비_관리_전면_재설계.md` 커밋/푸시 완료.

---

## 🚀 [전사 체크박스] 전 화면 잔존 체크박스 전수 점검 및 고선명 "V" 표준 컴포넌트 100% 완전 동기화 (v1.3.0.Build.65 - 2026-09-03)

### 1. 개발 내용 요약
- **전사 잔존 체크박스 전수 감사 및 정돈**: 인라인 크기(`width: 13~16px`) 및 `accentColor` 설정 전면 제거하여 `index.css`의 18×18px 고선명 화이트 "V" 벡터 체크마크와 100% 동기화.
- **적용 대상**: `smart_dispatch.tsx`, `Contracts.tsx`, `CorporateCardPage.tsx`, `FieldAsManagement.tsx`, `Products.tsx`, `users_permissions.tsx`, `App.tsx` 로그인 화면 등 전 메뉴.

---

## 🚀 [UI/UX] 전사 체크박스 고대비 선명한 "V" (Checkmark) 표준 컴포넌트 전면 적용 (v1.3.0.Build.64 - 2026-09-03)

### 1. 개발 내용 요약
- **체크박스 스타일 전면 개편**: 텍스트 인풋 스타일 상속(`appearance: none`)으로 인해 체크마크가 사라졌던 문제 수정.
- **선명한 "V" 벡터 SVG 적용**: 체크 시 선명한 화이트 "V"(`polyline`)가 블루 박스 안에 뚜렷하게 보이도록 전사 CSS 일괄 적용.
- **패널 배경 톤 정돈**: `smart_dispatch.tsx` 고객사 기본값 등록 패널의 배경 대비 개선.

---

## 🚀 [외상미수금 대장] 초기DB 마이그레이션 과거 운반비 자동생성 완전 제거 및 허위 75건 전량 정화 (v1.3.0.Build.63 - 2026-09-03)

### 1. 개발 내용 요약
- **외상미수금 도메인 원칙 회복**: 과거 엑셀의 Col[7] 운반비를 현재 시점의 미청구 외상(`receivables`)으로 생성하던 로직 완전 제거 (`migrationEngine.ts`).
- **허위 데이터 75건 전량 삭제**: 엑셀 날짜 `2026-03-05` (시리얼 `46086`)가 46,086원 운반비로 둔갑해 생성되었던 75건 (₩3,456,450)을 Supabase DB에서 삭제 정화 완료.
- **문서 동기화**: `INITIAL_DB_UPLOAD.md` 8.1조 개정.

---

## 🚀 [당사자산 취득/매각] 상하 간격 고밀도 압축 및 1화면 100% 원스크린 완결 (v1.3.0.Build.62 - 2026-09-03)

### 1. 개발 내용 요약
- **상하 여백 압축**: 타이틀/요약바/탭/카드 간 `margin-bottom: 24px` ➔ `10px`로 통일 압축.
- **3열(3-Column) 고밀도 그리드 배치**: 폼 필드를 3열 그리드로 정렬하여 스크롤 없이 **1화면 안에 취득/매각 폼과 [확정 버튼]까지 모두 표시**되도록 최적화.

---

## 🚀 [화면 하단 뷰포트 100% 활용] 테이블 고정 높이 제거 및 남는 하단 공간 전면 확장 (v1.3.0.Build.61 - 2026-09-03)

### 1. 개발 내용 요약
- **제품 모델 관리(`Products.tsx`)**: 하드코딩된 `maxHeight: 590px` 제거, `flex: 1, height: 100%, minHeight: 0`을 통해 화면 하단 끝까지 테이블이 100% 꽉 차도록 확장.
- **매입처 관리(`Vendors.tsx`)**: 동일하게 `flex: 1, maxHeight: none` 적용하여 하단 여백 완전 제거.

---

## 🚀 [매출 청구] 기본 UI 고밀도 간소화 및 작업대 면적 85% 극대화 (v1.3.0.Build.60 - 2026-09-03)

### 1. 개발 내용 요약
- **타이틀/탭 간소화**: `매출 청구 관리` 및 3대 탭(`[미청구 정산]`, `[청구 대장]`, `[청구서통합]`)
- **1줄 컴팩트 필터 바**: 2단 흩어진 필터를 가로 1열 고밀도 바로 압축 + `Enter` 키 즉시 조회
- **1줄 실시간 집계 요약 스트립**: 거대 6개 KPI 박스를 1줄 슬림 요약 바로 전환 (화면 수직 작업대 150px+ 확보)

---

## 🚀 [청구서통합] UI 전사 단일 표준 명칭 '청구서통합' 변경 및 DB 스키마 정합성 보완 (v1.3.0.Build.59 - 2026-09-03)

### 1. 개발 내용 요약
- **UI 표기 전면 '청구서통합' 표준화**: `Billings.tsx` 메인 탭 명칭(`청구 인보이스` ➔ `청구서통합`), `BillingInvoiceTab.tsx` 툴바 및 버튼(`[청구서통합 생성]`, `[기존 청구 소급 묶기]`), 테이블 컬럼(`통합청구번호`), 안내 메시지 전면 변경 완료.
- **DB 스키마 및 마이그레이션 엔진 정합성 완결**: `schema.sql` `billing_invoices` 마스터 및 `billings.invoiceId` 외래키 동기화, `migrationEngine.ts` `TABLE_COLUMNS` 화이트리스트 동기화 완료.

---

## 🚀 [모바일 PWA] 현장 기동 업무 전용 모바일 PWA (`/m`) 분리 구축 완결 (v1.3.0.Build.58 - 2026-09-02)

### 1. 개발 내용 요약
- **모바일 전용 셸 및 라우팅 분리 (`src/mobile/MobileApp.tsx`, `App.tsx`)**: URL `/m`, `?view=mobile` 쿼리, 또는 768px 미만 모바일 기기 접속 시 모바일 전용 PWA 레이아웃 자동 구동.
- **PC / 모바일 1-Click 상호 전환**: PC 헤더 `[📱 모바일화면]` / 모바일 헤더 `[🖥️ PC화면]` 전환 토글 상시 배치.
- **2대 필드 전용 테마 & 56px+ 대형 터치 아키텍처**: `Field High-Contrast Dark` (`#0B0F19` Deep Dark) 테마로 야외 직사광선 아래 시인성 극대화 및 배터리 절약. 횡스크롤 0% 수직 카드 피드 및 하단 5버튼 고정 내비게이션 바.
- **4대 핵심 모바일 스튜디오**:
  1. **현장 AS 360도 스튜디오 (`MobileAsDetail.tsx`, `MobileAsList.tsx`, `MobileAsCreate.tsx`)**: TMAP 길안내, 전화걸기, 전/후 사진 촬영 (`CameraUploader.tsx`), 차량 탑차 부품 차감 (`RepairPartUsed`), 터치 고객 서명 (`SignatureCanvas.tsx`).
  2. **배차 운송 지시 카드 (`MobileDispatchList.tsx`)**: 상/하차지 확인, 원터치 통화, 하차 완료 1-Click 보고.
  3. **출고 전 기능 검수 스튜디오 (`MobileInspectionList.tsx`)**: 10대 법정/기능 점검 체크리스트, 외관 사진 촬영 및 승인 시 자산 상태 `RENTED` 자동 전환 (헌장 제1.3조 준수).
  4. **가용 렌탈 자산 실시간 조회기 (`MobileAssetSearch.tsx`)**: 영업용 19~53ft 규격별 실시간 재고 & 단가 조회.
- **Skelton 기록 완결**: `skelton/계획/` 및 `skelton/경험/` 2건 커밋 & 푸시 완료.

---

## 🚀 [엑셀 내보내기] 전사 대장 스키마 6단계 논리적 배치 표준 동기화 완결 (v1.2.1.Build.56 - 2026-09-02)

### 1. 개발 내용 요약
- **자산 대장 (`Assets.tsx`)**: `vendorAssetNo` 추가 및 6단계 논리적 컬럼 순서 재배치.
- **배차 대장 (`Deliveries.tsx`)**: 타사 직출고지/혼적 경유지 및 확정운송비 반영 논리적 순서 재정돈.
- **임차 자산 & 전대 손익 원장 (`rent_assets.tsx`)**: 임차대장 서식화 및 `[📥 전대 손익 원장 엑셀 다운로드]` 신설.
- **외상미수금 대장 (`Receivables.tsx`)**: `[📥 엑셀 다운로드]` 신설 (타사구상채권/부대비용 대사 서식).
- **정비/AS, 계약, 청구 대장 동기화**: 수식 계산 호환성 및 표준 스키마 100% 일치.

---

## 🚀 [DB 스키마] 전사 42대 테이블 6대 도메인 표준 논리적 배치 전면 재정돈 & 무손실 원자적 스왑 DDL 패치 (v1.2.1.Build.55 - 2026-09-02)

### 1. 개발 내용 요약
- **단일 진실의 원천(SSOT) [`schema.sql`](file:///d:/01.AntiGravity/Kiyuen_Lift/schema.sql) 전면 리팩토링**: 누적 패치로 누더기화되었던 중복 테이블 및 흩어진 컬럼들을 전면 정돈, 전사 단일 표준 6단계 논리적 컬럼 배치 원칙 100% 적용.
- **기존 운영 데이터 100% 무손실 보존 원자적 테이블 스왑 마이그레이션 DDL 구축**: [`scripts/reorganize_tables_zero_loss.sql`](file:///d:/01.AntiGravity/Kiyuen_Lift/scripts/reorganize_tables_zero_loss.sql) 생성.

---

## 🚀 [전대(임차) 자산] 5대 마스터 라이프사이클 & 원천정보 기반 대차대조 손익 원장 시스템 구축 (v1.2.1.Build.54 - 2026-09-02)

### 1. 개발 내용 요약
- **장비 식별 & 재임차 라이프사이클**: `vendorAssetNo` 매핑, 번호 난발 방지, 반납 후 1-Click 재임차 활성화.
- **원천정보 기반 대차대조 전대 손익 원장 (`PROFIT_LEDGER` 탭)**: 계약별 및 자산별 매출 청구서 vs 매입세금계산서 + 운송비 대조 순마진/마진율 실시간 산출.
- **단일 경유 혼적 회수 배차 및 직출고 체인 (`TruckDispatch.tsx`)**: 직출고 상차지 및 복수 하차지 경유 배차표 지원.
- **타사 부대비용 구상 미수금 등록 (`Receivables.tsx` & `rent_assets.tsx`)**: `VENDOR_CLAIM` 구상 채권 생성 및 고객 청구서 분할 상계 지원.

---

## 🚀 [현장 AS] 기사별 선호 내비(T맵/카카오/네이버) 맞춤 연동 & 전화·출발 타임라인 자동 로깅 시스템 구축 (v1.2.1.Build.53 - 2026-09-02)

### 1. 개발 내용 요약
- **기사별 맞춤형 3대 내비게이션(T맵 / 카카오내비 / 네이버지도) 원터치 딥링크 연동**: 기사 스마트폰 기본 내비 기억 기능, 상단 1초 내비 변경 스위처, 3대 내비 선택 바텀시트 모달.
- **전화걸기 & 이동 시작 무자각 타임라인 자동 로깅 (`logFieldAsTimelineEvent`)**: `[📞 전화걸기]` 시 `CALL_MADE`, `[🗺️ 내비 길안내]` 시 `TRANSIT_START` 타임라인 자동 저장 및 티켓 상태 자동 전환.
- **현장 티켓 카드 내 최근 진행 이력 실시간 표출**.

---

## 🚀 [모바일 현장 AS] 갤럭시 S24 최적화 터치 UI & 현장 부품 원스톱 실시간 자동 차감 시스템 구축 (v1.2.1.Build.52 - 2026-09-02)

### 1. 개발 내용 요약
- **AS팀원 전용 모바일 최적화 터치 UI 구축 (`FieldAsManagement.tsx`)**: 갤럭시 S24(393×852px) 기준 1열 세로 스택 자동 전환, 3대 상단 탭(`[출동 대기]`, `[내차 부품고]`, `[완료 내역]`), 1-Touch 전화걸기(`tel:`) 및 카카오내비 딥링크.
- **50px 대형 스텝퍼 부품 카트 & 원스톱 차량 재고 자동 차감**: 가상 키보드 팝업을 차단하고 작업 장갑 상태에서도 편한 50px 대형 스텝퍼 `[➖]` `[➕]` 버튼, AS 완료 시 차량 재고(`mechanic_consumable_stocks`) 자동 차감 및 마이너스 방지.
- **한 손 조작 슬라이드업 바텀시트**: 1초 원터치 빈출 조치 태그 칩, 다이렉트 후면 카메라 촬영(`capture="environment"`), 최하단 고정 54px 초록색 완료 버튼.
- **모바일 ↔ PC 대화면 뷰 토글 스위처 탑재**.

---

## 🚀 [초기DB업로드] 밴드 과거 AS 빅데이터(4,262건) 전수 분석 & 정비 마스터(`repairs`) DB 일괄 동기화 작업대 구축 (v1.2.1.Build.51 - 2026-09-02)

### 1. 개발 내용 요약
- **AS 빅데이터 전수 파싱 엔진 (`parseBandAsHistoryText`)**: `(AS)band_as_history_all.txt` 1.4MB 대용량 파일 내 4,262건의 AS 게시글을 1초 만에 전수 파싱. 작성자/정비사, 접수일자, 고객사, 현장명, 관리번호(자산), 고장내용, 접수자 연락처, 조치결과 키워드 추출.
- **5대 매트릭스 & 1대 단독계약 자동 추정 엔진 (`analyzeBandAsHistory`)**:
  - **사장님 확정 1번 원칙 반영**: 관리번호가 생략되었거나 '전체장비'인 경우, 단 1대뿐인 단독 계약 현장이면 해당 자산으로 자동 추정 매핑하여 자산 이력까지 완벽 복원.
  - **작성자 ➔ 정비사 1:1 매핑**: 최영석, 한상찬, 김재현, 김동우 등 사내 정비사 마스터와 자동 연결.
  - **조치 상태 3대 자동 분류**: 현장 조치완료(`COMPLETED`), 익일 재방문(`REVISIT`), 전화 안내종결(`GUIDED`).
- **초기DB업로드 메뉴 내 고밀도 AS 분석 작업대 탑재 (`InitialDbUploader.tsx`)**:
  - **5대 핵심 지표 바**: `총 AS 분석 건수`, `고유 장비 매핑`, `유효 계약 연동(1대 추정 포함)`, `현장 조치완료`, `익일방문/안내`.
  - **인라인 다기능 필터 & 검색**: 현장/고객/장비/고장/작성자 실시간 검색, 상태별 및 계약 연동별 즉시 필터링.
  - **고밀도 1:1 대사 그리드 & 단건 상세 모달**: 행 높이 38px 슬림 테이블 및 원문 대조 팝업 제공.
- **원클릭 정비 마스터(`repairs`) & 자산/계약 타임라인 일괄 적재 (`ingestBandAsHistoryDirect`)**:
  - `repairs` 단일 테이블 적재 및 `assetInOutLogs`, `contract_history`(`AS_SERVICE`) 동시 무누락 저장.
  - 중복 방지(Deduplication) 및 `await db.awaitPendingWrites()` 동기 검증을 통한 Zero Silent Failure 보장.

---

## 🚀 [현장 AS & 내근 정비 통합 관리체계(`repairs`) 및 계약 이력(타임라인) 양방향 연동 시스템 구축] (v1.2.1.Build.50 - 2026-09-02)

### 1. 사장님 확정 핵심 도메인 원칙 100% 완벽 이행
- **5대 매트릭스 AS 기록 & 단독 계약 자산 자동 추정 (사장님 1번 확정 원칙)**:
  - 관리번호, 고객/현장, 유효계약 3대 조건 조합에 따라 정비 대장(`repairs`), 자산 이력(`assetInOutLogs`), 계약 타임라인(`contract_history`)에 100% 무누락 자동 기록.
  - 현장 계약에 장비가 **단 1대뿐인 단독 계약**인 경우, 관리번호가 누락되어도 해당 1대 자산으로 자동 추정 매핑하여 자산 이력까지 완벽 복원. 복수 장비 계약은 자산 오염 방지를 위해 계약 이력에만 안전 기록.
- **수리비 독립 관리 원칙 (사장님 2번 확정 원칙)**:
  - 유상 수리비(`billableAmount`)는 정기 렌탈료 청구서(`billings`)와 혼선되지 않도록 **정비 대장(`repairs`)에서 독립적으로 집계 및 수납 관리**.
- **계약 상세 화면(`Contracts.tsx`) 360도 AS 이력 전용 섹션 신설**:
  - **섹션 5**: `[🛠️ 계약 현장 AS 및 정비 이력 ({contractRepairs.length}건)]` 고밀도 대사 테이블 탑재.
  - **⚠️ AS 빈발 알림 뱃지**: 동일 계약 현장에서 AS가 2회 이상 발생 시 `[⚠️ AS N회 발생 - 장비 대차/교체 검토 요망]` 경고 뱃지 자동 표출.
  - **계약 변경 및 이력 타임라인(`contract_history`)**: `AS_SERVICE` 이벤트 자동 누적 기록.

---

## 🚀 [초기DB업로드] 밴드 출고요청 분석 기반 유효 계약 고객·현장 기본 요구사항(옵션·보양·스펙) 마스터 DB 동기화 시스템 구축 (v1.2.1.Build.49 - 2026-09-02)

### 1. 사장님 확정 3대 도메인 원칙 100% 완벽 이행
- **유효 계약처 선별 필터링 (Contract Scope Filter)**: 현재 DB에 유효 계약(`contracts`)이 존재하는 고객사 및 현장만 선별 매칭하여 업데이트. 계약이 없는 과거 거래처, 이미 거래 종료된 고객, 완공된 과거 현장은 대장 오염 방지를 위해 100% 안전하게 [제외] 처리.
- **시계열 최신값 우선 원칙 (Latest Timestamp Precedence)**: 동일 고객/현장에 복수의 출고요청이 존재할 경우, 타임스탬프 기준 가장 최신 글(Latest Entry)의 값을 최우선 적용.
- **빈칸 안전 보완 원칙 (Fill Missing Fields Only)**: 기존 마스터에 이미 등록된 데이터는 훼손하지 않고, 비어있는 옵션/보양/스펙/담당자 항목만 안전하게 채움.

### 2. 세부 구현 내역
- **DB 스키마 정합성 보강 (`schema.sql` & `db.ts`)**: `Customer` 인터페이스 및 `customers` 테이블에 `specialNotes TEXT` (거래처 특이사항 메모) 정식 추가.
- **마이그레이션 엔진 신설 (`migrationEngine.ts`)**: `parseDispatchHistoryText`, `analyzeDispatchHistoryForCustomerDefaults`, `ingestCustomerDefaultsFromDispatchHistory` 함수 구현.
- **초기DB업로드 UI 화면 전용 작업대 신설 (`InitialDbUploader.tsx`)**: Card ⑤ 탑재, 5대 통계 지표, 고객별 기본 요구사항 고밀도 대사 그리드, 제외 상세 목록 모달, 원클릭 일괄 DB 동기화 버튼 탑재.

---

## 🚀 [고객·현장별 옵션·보양·기술스펙 자동 재사용 및 전사 일괄 상속 시스템 구축] (v1.2.1.Build.48 - 2026-09-02)

### 1. 사장님 지시사항 100% 완벽 이행
- **고객사 기본 옵션·보양 마스터 & 자동 재사용 (Auto-Reuse)**:
  - `customers` 테이블에 `defaultPaidOptions`(기본 유상옵션), `defaultProtection`(기본 보양작업), `defaultCheckedSpecs`(기본 21대 기술요구스펙) 컬럼 및 인터페이스 추가.
  - 고객사 등록/수정 모달(`Customers.tsx`)에서 기본 옵션/보양 및 21대 표준 스펙 체크리스트를 한 번 저장해 두면, 향후 모든 출고요청(`SmartDispatch`) 시 자동 상속 및 재사용되어 반복 작성 수고를 100% 제거.
- **현장 전용 옵션·보양 설정 & 3계층 상속 체계 (3-Tier Hierarchy)**:
  - `customer_sites` 테이블에 `paidOptions`, `protection`, `checkedSpecs` 컬럼 추가.
  - 옵션/보양/스펙 해석 3계층 우선순위 정립:
    - **1순위**: 출고의뢰 텍스트 명시 입력값
    - **2순위**: 현장 전용 설정값 (`site.paidOptions`, `site.protection`, `site.checkedSpecs`)
    - **3순위**: 고객사 기본값 (`customer.defaultPaidOptions`, `customer.defaultProtection`, `customer.defaultCheckedSpecs`)
    - **기술스펙**: 텍스트 추출 스펙 ∪ 현장 스펙 ∪ 고객사 기본 스펙 **지능형 합집합(Union) 자동 체크**.
- **전사 현장 일괄 전파 (Batch Propagation)**:
  - **스마트 출고 화면(`SmartDispatch.tsx`)**:
    - `[☑ 이 옵션·보양·스펙을 고객사 기본 설정으로 등록]` 체크 시 고객 마스터에 즉시 동기화.
    - `[☑ 등록된 모든 현장에도 이 옵션·보양을 동일하게 일괄 적용]` 체크 시 해당 고객사의 모든 현장에 일괄 전파.
  - **고객 관리 화면(`Customers.tsx`)**:
    - 고객사 모달 및 상세 카드에서 `[⚡ 모든 현장에 기본값 일괄 적용]` 원클릭 버튼 제공.
    - 현장 모달에서 `[✨ 고객사 기본값 불러오기]` 원클릭 동기화 버튼 제공.
    - 현장 목록 테이블에 등록된 옵션/보양 뱃지 실시간 표시.
- **단일 진실의 원천(SSOT) 21대 표준 스펙 공유 (`db.ts`)**:
  - 21대 고소작업대 표준 기술 요구스펙(`STANDARD_SPECS`)을 `db.ts`에 전사 SSOT로 정의하여 `smart_dispatch.tsx`와 `Customers.tsx`에서 100% 일관되게 공유 참조.

---

## 🚀 [스마트 출고요청 고객·현장 3대 지능형 처리 로직 구축] (v1.2.1.Build.47 - 2026-09-02)

### 1. 사장님 지시사항 100% 완벽 이행
- **🛡️ 1. 방어 가드 (Validation Guard)**:
  - 신규 고객이거나 기존 DB에 현장 상세 주소 / 현장담당자 연락처가 없는데 생략한 채 저장을 시도할 경우, **다음 단계 진행을 강력히 방어 및 차단**하여 깡통 데이터 생성을 원천 방지.
- **⚡ 2. 지능형 자동 상속 (Auto-Inheritance)**:
  - 텍스트 파싱(`handleParse`) 및 고객/현장명 입력 시, 텍스트에서 생략된 정보(현장주소, 현장담당자, 청구담당자, 계산서 메일, 마감일, 결제일 등)를 기존 고객/현장 DB에서 **100% 자동 상속 대입**.
  - 화면에 `[⚡ 기존 DB 자동 상속됨]` 상태 배지 및 각 입력 필드별 상속 태그 표출.
- **🔄 3. 최신화 동기화 업데이트 (Update & Augment)**:
  - 기존 고객/현장이 매핑된 상태에서 새로운 현장 주소, 새 담당자 연락처, 새 계산서 이메일이 입력되면, **고객 마스터(`customers`), 현장(`sites`), 담당자(`contacts`) 테이블을 최신값으로 즉시 동기화 UPDATE 및 신규 등록**.
- **📋 메신저 동의어 전수 포용 정규식 파서 업그레이드**:
  - `고객사명/고객명/업체/상호`, `현장상세주소/주소/배송지`, `상차/하차/도착`, `신청모델목록/모델명/규격`, `유상옵션`, `보양작업`, `마감일/결제일` 등 메신저 자유 형식 완벽 파싱 지원.

---

## 🚀 [외근 AS & 내근 정비 DB 스키마 물리 통합 및 자산 생애주기 연동] (v1.2.1.Build.45 - 2026-09-02)

### 1. 사장님 4대 인터뷰 결정사항 100% 완벽 이행
- **1-A. 단일 물리 통합 스키마 (`repairs`)**:
  - `fieldAsTickets` 테이블을 물리적으로 완전 제거하고 `repairs` 단일 테이블로 승격 통합.
  - `workCategory`('FIELD_AS' | 'YARD_INTERNAL'), `workLocation`('SITE' | 'YARD'), `stockSource`('VEHICLE_VAN' | 'CENTRAL_HQ') 등 통합 컬럼 탑재.
- **2-B. 미등록 자산 관리대장 오염 방지**:
  - 밴드 5,518건 적재 시 `assets` 마스터에 등록된 자산은 1:1 매핑, 미등록 관리번호는 정비이력에 `assetNo` 텍스트로 안전 보존.
- **3-B. 자산 상태 자동 임대가능 전환 금지**:
  - 정비가 부분 수리일 수 있으므로 내근 주기장 정비 완료 시에도 `AVAILABLE`로 자동 전환하지 않고 상태 무왜곡 보존.
- **4-A. 밴드 작성자 ➔ `users` 이름 1:1 자동 매칭**:
  - 밴드 게시글 작성자명을 직원 목록(`users`)과 1:1 자동 매칭하여 `mechanicId` 및 `mechanicName` 상속 연동.
- **자산 생애주기 타임라인 통합 (`AssetHistory.tsx`)**:
  - `AssetHistory`의 `REPAIR` 탭 및 자산 타임라인에서 외근 AS와 내근 정비가 일자, 관리번호, 모델명, 구분, 현장, 조치내용, 정비자, 비용과 함께 단일 타임라인으로 완벽히 표출.

---

## 🚀 [현장 AS 일정관리 & 차량 소모품 빅데이터 연동 시스템 구축] (v1.2.1.Build.44 - 2026-09-02)

### 1. 사장님 지시사항 및 R&R 100% 완벽 이행
- **`[AS 요청]` (`SmartAsRequest`)**: 영업사원 전용 10초 간이 의뢰 발행 화면 신설.
- **`[현장 AS 관리]` (`FieldAsManagement`)**:
  - **탭 1 (출동 스튜디오)**: 밴드 피드 카드뷰 + 1-Click 조치 패널 (기사 차량 재고 실시간 차감, 수거 부품 관리, 전/후 사진 첨부, 우하단 완료 버튼).
  - **탭 2 (AS 처리 대장)**: 38~42px 슬림 테이블로 5,518건 과거 데이터 포함 고밀도 그리드 및 엑셀 다운로드, 자산별 과거 AS 이력 드릴다운 모달.
  - **탭 3 (차량별 부품 적재 현황)**: 기사별 차량 소모품 모니터 및 주기장 ➔ 차량 이동(불출) 지원.
- **`[주기장 정비 관리]` (`Repairs`)**: 비임대 자산(임대가능, 수리중) 주기장 내부 정비 전담으로 명칭 및 역할 분리.
- **밴드 5,518건 빅데이터 전수 탑재 파이프라인**: 5,518건 게시글 및 2,171대 고유 장비 이력 시스템 기초 데이터 연동.

---

### 1. 사장님 지시사항 완벽 이행
- **입금액 대비 수납처리 완료 결과 대조**:
  - `[입금액 대비 수납결과]` 컬럼에서 총 입금액(+7,900,000원)과 함께 수납 완료 금액(`usedDepositAmount`) 및 잔여 가용잔액(`remBal`)을 2단 대조 표출.
  - 전액 수납 완결 시 `✓ 전액 수납완결 (7,900,000원)` 초록 배지 표출.
  - 일부 수납 시 `수납 5,643,000원 / 잔여 2,257,000원` 분할 표출.
  - 미수납 시 `미수납 (가용 7,900,000원)` 회색 배지 표출.
- **출금액 대비 매입정산 결과 대조**:
  - `[출금액 대비 정산결과]` 컬럼에서 총 출금액(-1,500,000원)과 함께 매입정산 대사 완료 금액(`matchedWithdrawAmt`)을 2단 대조 표출.
  - 매입정산 대사 완료 시 `✓ 정산대사 완료 (1,500,000원)` 초록 배지 표출.
  - 미대사 시 `⚠️ 미대사 출금 (-1,500,000원)` 경고 배지 표출.
- **상단 요약 바 금액 대조 집계 연동**:
  - `📥 입금 수납 대사`: 총 입금액 중 수납완료 금액(%) 및 미수납 잔액 실시간 합산 표출.
  - `💸 출금 지급 대사`: 총 출금액 중 정산대사 금액(%) 및 미대사 잔액 실시간 합산 표출.

### 2. 주요 수정 파일
- `src/pages/BankMatching.tsx`

---
**기록 일시**: 2026-08-31 17:16  
**작성 버전**: `v0.5.3.Build.76`






---

## [보류] 장비 할당 화면 - 전대 계약 미할당 오인 문제 (2026-09-01)

### 현상
- 장비 할당 화면 출고 대기 요청건에 전대(임차) 계약 530건이 미할당으로 표시됨
- 실제로 할당이 필요한 건은 당사 자산 미등록 31건뿐

### 원인
- asset_assignment.tsx L38: contractAssets.filter(ca => !ca.assetId) 에서 assetId=null인 모든 행을 출고 대기로 표시
- 전대 자산은 external_leases 테이블에 저장되므로 contract_assets.assetId가 구조상 null

### 발생 원천 (엑셀)
- 계약현황 시트 Col[12] 장비명(전대)이 있는 행 530건
- 당사 자산 미등록 31건 (G19911, G19912, G19913, 13004, G28881~G28886 등)

### 검토 필요 사항
1. 전대 계약행은 장비 할당 대상에서 제외할지 여부 (정책 결정 필요)
2. 당사 자산 미등록 31건 선제 자동등록 로직 검토

### 상태: 보류 - 사장님 판단 후 지시 대기

## [Build.135] 무전기 100% 무료 브라우저 STT 단일 표준화 및 즉시 전송 파이프라인 개편
- **Gemini STT 영구 배제 (0원 원칙)**:
  - `api/gemini-stt.ts`를 `410 Gone`으로 비활성화하여 외부 과금 발생 리스크 100% 원천 차단.
- **브라우저 Web Speech API 0초 즉시 처리**:
  - `SpeechRecognition` 선점 실행으로 마이크 잠금 충돌 해소.
  - `stopBrowserRecognition()` 비동기 완료 보장(최대 500ms)으로 누락 없는 텍스트 회수.
  - 음성 데이터와 텍스트를 단일 메시지(`msg.textTranscript`)로 패키징하여 1회 즉시 브로드캐스트.
- **화면 디버깅 토글 제공**:
  - 모바일 무전기 화면에서 `[디버그ON / 디버그OFF]` 버튼으로 로그 출력 선택 지원 (기본값 OFF).
