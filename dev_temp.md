# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 장비 할당 보드(AssetAssignment.tsx) 내 장비 관리번호/모델명 실시간 검색 필터 탑재 (v1.15.0.Build.00003)

### 1. 사장님 지시 사항
- **지시 내용**: 장비할당 보드에서 그린 헤더 바 상단 표시 위치에 **장비 관리번호 조회 필터** 추가.

### 2. 주요 수술 내용
- **`AssetAssignment.tsx` 조회 필터 바 탑재**:
  - 그린 헤더 바 바로 상단에 **`🔍 장비 관리번호 / 모델명 빠른 검색 (예: G15140, GS-1930...)`** 실시간 검색 입력창 배치.
  - 검색어 입력 시 `availableAssets` 목록이 장비 관리번호(`assetNo`), 모델명(`modelName`), 시리얼번호(`serialNo`) 조건으로 실시간 필터링되어 바둑판 카드가 갱신됨.
  - 검색어 원클릭 `[초기화]` 버튼 추가.

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.15.0.Build.00003).
