# 개발 지시 및 개편 완료 내역 (dev_temp.md)

## 🎨 [완료] 입고 고유번호, 불량 증상 하위번호(INB-XXXX-01), 사진 첨부, 정비연동 & 자산이력 토글/엑셀다운로드 구축 (v1.60.0.Build.145)

### 1. 사장님 요구사항 완벽 집행
- **입고 고유번호 및 불량 증상 하위 번호 부여**:
  - 입고 시 **`INB-YYYYMMDD-001`** 고유 입고 번호 자동 채번.
  - 선택된 증상별로 **`INB-20260809-001-01`**, **`-02`** 등 2자리 서브 번호 1:1 결합 관리.
- **모바일 사진 촬영 & PC 탐색기 사진 선택 지원**:
  - 모바일 디바이스 카메라 연동 (`capture="environment"`) 및 PC 파일 선택 지원.
- **`자산 정비수리` (`Repairs.tsx`) 자동 연동**:
  - 정비 필요 점수 발생 시 `repairs` 대장에 `PENDING` 정비 건 자동 생성.
  - 정비 전 입고번호, 증상 하위번호 및 파손 사진 이미지 표출.
- **`자산 관리 (대장)` (`Assets.tsx`) 개편**:
  - 자산 상세 모달 내 정비 정보 및 검수 현황 카드 배치.
  - 명시적 **`[📜 자산 이력 조회]`** 버튼 클릭 시에만 전체 히스토리 타임라인 토글 표출.
  - 수평 배치된 **`[📥 엑셀 내려받기]`** 버튼으로 해당 자산의 라이프사이클 전체 이력 엑셀 다운로드 제공.

### 2. 주요 수정 파일
- `asset_history.tsx`, `Repairs.tsx`, `Assets.tsx`, `AppContext.tsx`, `db.ts`

### 3. 빌드 및 검증
- TypeScript `npx tsc -b` 컴파일 오류 없음 확인 ✅ (Exit code: 0)

---
**기록 일시**: 2026-08-09 18:31  
**작성 버전**: `v1.60.0.Build.145`
