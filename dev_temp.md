# 개발 지시 및 개편 완료 내역 (dev_temp.md)

## 🎨 [완료] 입고 검수 항목 관리 메뉴 신설, 수동 점수 입력 제거 및 정비필요점수 자동 합산 구축 (v1.58.0.Build.143)

### 1. 사장님 지시사항 및 도메인 원칙 완벽 이행
- **불필요한 "대여 중인 자산 선택" 드롭다운 전면 제거**:
  - `asset_history.tsx`의 붉은 원 안 드롭다운 제거 ➔ 관리번호 입력/검색 단일 입력 창으로 통합.
- **수동 점수 입력 기능 전면 제거 (휴먼에러 차단)**:
  - 담당자 주관 판단에 따른 수동 점수 입력창(`input type="number"`) 삭제.
- **"입고 검수 항목 관리" DB 스키마 및 마스터 메뉴 신설**:
  - DB 스키마: `InspectionChecklistItem` 및 `inspectionChecklistItems` 테이블 생성.
  - 시드 데이터: `A 불량` (5점), `B 불량` (10점), `유압유 누유` (15점), `배터리/전선 단선` (20점), `타이어 찢어짐` (10점) 등 5종 기본 탑재.
  - 신규 관리 메뉴: `제품/자산관리` ➔ **`입고 검수 항목 관리`** (`inspection_checklist_manage.tsx`) 신설 및 CUD 관리 제공.
- **정비 필요 항목 체크박스 선택 ➔ 총 정비필요점수 100% 자동 계산 합산**:
  - 입고 등록 화면에서 검수자가 정비 필요 항목 체크박스를 선택하기만 하면 점수가 자동 계산 합산됨.
  - 0점: `AVAILABLE` (`임대가능`) 자동 즉시 전이.
  - 1점 이상: `RENTED_RETURNED` (`입고반납/검수대기`) 자동 전이.
- **특이사항 메모 선택 항목화**:
  - 필수값에서 제외하고 `(선택사항)`으로 변경.

### 2. 주요 수정 파일
- `inspection_checklist_manage.tsx` (신규), `asset_history.tsx`, `AppContext.tsx`, `db.ts`, `menuConfig.ts`, `menu_config.ts`, `App.tsx`

### 3. 빌드 및 검증
- TypeScript `npx tsc -b` 컴파일 오류 없음 확인 ✅ (Exit code: 0)

---
**기록 일시**: 2026-08-09 18:19  
**작성 버전**: `v1.58.0.Build.143`
