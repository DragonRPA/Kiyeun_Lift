# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 배차 시 `finalCost` 기본값 0 처리 & 대사 시 `finalCost` 확정 정돈 및 불필요한 레거시 컬럼 정리 (v1.15.0.Build.00001)

### 1. 사장님 개편 지시 명세
- **지시 내용**:
  1. 배차 등록 시 실제 운송료(`finalCost`)를 모르면 null/공백 대신 **숫자 `0`** 으로 초기화 세팅.
  2. 운송료 대사 탭에서 실제가 미정산건은 **`💵 실제가(final): ₩0원`** 으로 표출.
  3. 운송료 대사 탭에서 금액을 정정 및 대사 완료 시 최종적으로 **`finalCost` (최종 확정 운송비)**가 거래명세서 엑셀 금액과 100% 일치하도록 정돈 (`finalCost = excelCost`).
  4. 사용하지 않거나 중복되던 레거시 컬럼들은 `expectedCost` (예상 운송비) 와 `finalCost` (최종 확정 운송비) 2개의 단일 진실의 원천(SSOT)으로 완벽하게 정리.

### 2. 주요 수술 내용
- **1. 배차 등록 시 `finalCost` 0원 초기화**:
  - `assignedVehicles` 및 `deliveries` 저장 시 `finalCost: 0` 으로 명확히 세팅.
- **2. 운송료 대사 탭 렌더링 및 수정**:
  - 미정산건에 대해 **`💵 실제가(final): ₩0원 (💰 예상가: ₩70,000원)`** 형태로 표출.
  - 대사 수정 시 `finalCost` 컬럼을 정정 및 저장하며, 대사 완료 시 `finalCost`가 엑셀 청구 금액과 100% 동기화됨.
- **3. DB 컬럼 체계 정리**:
  - `expectedCost` (등록 시 필수 예상가) + `finalCost` (최종 대사 확정가) 2개 핵심 컬럼 중심으로 체계화 완료.

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.15.0.Build.00001).
