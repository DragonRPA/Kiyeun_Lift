# dev_temp.md — 미반영 개발 요구사항

---

## [1] 모달/레이어 팝업 시 버튼 포커스 탈출 & 이중 제출(Enter 연타) 방지 패턴 적용

- **요청일**: 2026-07-27
- **현상**: 모달/레이어 팝업 오픈 시 브라우저 커서 포커스가 트리거 버튼에 남아 있어, 엔터(Enter) 키 입력 시 버튼 클릭 이벤트가 반복 중복 실행되어 1건의 요청이 여러 건의 데이터를 생성하는 이중 제출(Double-Submit) 버그 발생.
- **수정 방향**:
  1. **모달 팝업 시 기존 포커스 해제 및 모달 내부 포커스 이동 (`Blur & Focus Control`)**:
     - 모달이 열릴 때 `(document.activeElement as HTMLElement)?.blur()`를 실행하여 직전 버튼의 포커스를 즉시 탈출.
     - 모달 내부의 첫 번째 입력 필드(`input autoFocus`) 또는 닫기 버튼으로 포커스를 안전하게 이동시킵니다.
  2. **전역 이중 제출 방지 가드 (`isSubmitting` Protection)**:
     - 수동 배차 생성, 스마트 출고 요청, 모달 승인 처리 등의 비동기 제출 버튼에 `isSubmitting` 비활성화(`disabled={isSubmitting}`) 가드를 적용하여 엔터 연타 및 중복 클릭에 의한 재발화 원천 차단.
