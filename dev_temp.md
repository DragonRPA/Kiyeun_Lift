# 개발 지시 및 개편 대기 내역 (dev_temp.md)

## [완료] 무헤더 컬럼(I열 현장대기 등) 정밀 흡수 & 상하단 표 예외 파싱 엔진 강화 (v1.14.0.Build.00005)

### 1. 주요 구현 내용
1. **무헤더 / 제목 없는 열(I열 등 빈 헤더 셀) 비고 변환**:
   - `운송비거래명세표` 서식의 13행 `I열`처럼 헤더 셀 텍스트가 빈 셀이더라도, 데이터 행에 들어있는 `현장대기` 등의 텍스트가 무시되거나 누락되지 않도록 자동으로 `비고` 컬럼으로 할당 및 병합 흡수 파싱.
2. **상단 공급자 정보 표 및 하단 합계 행 예외 처리**:
   - 1~12행의 `공급가액`, `합계금액`, `부가세`, `등록번호`, `상호`, `사업장주소` 표 데이터 및 하단 `합계`/`소계` 행이 실제 운송 데이터 행으로 잘못 파싱되던 현상을 정밀 예외 처리로 완전 차단.
3. **톤수 / 운송비 동적 매핑**:
   - `톤수`, `운송비`, `상차지`, `하차지`, `현장명`, `업체명` 등 다양한 형태의 명세서 헤더 구조를 100% 정밀 파싱.

---

✅ **상태**: 구현 및 컴파일 검증 완료 (v1.14.0.Build.00005).
