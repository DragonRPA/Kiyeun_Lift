# 대기 중인 개발 지시사항 목록 (Pending Instructions)

## [2026-07-21] 구글 관리자 설정 내 개발모드/실무모드 스위치 추가 및 메일 수신처 우회 기능 개발 요청
- **목표**: 구글 설정 페이지(`GoogleConfig.tsx`)에 개발모드/실무모드를 제어하는 스위치(토글)를 신설하고, 시스템 내 메일 발송 로직을 이에 맞추어 분기 처리합니다.
- **세부 요구사항**:
  - **모드 전환 스위치 추가**: `GoogleConfig` 스키마에 `isDevMode` (boolean) 필드를 추가하고 UI 상에 토글 스위치 제공.
  - **개발모드 고정**: 현 단계에서는 개발이 완료될 때까지 기본값을 `개발 모드 (true)`로 고정 제공.
  - **이메일 수신인 강제 변경**:
    - **적용 대상**: **이메일을 발송하는 모든 메뉴 기능 전반** (예: 계약서 이메일 발송, 스마트 출고의뢰/배차 지시 메일 전송, 청구서/거래명세서 발행 메일 전송 등).
    - **동작**: `isDevMode`가 `true`일 경우 원래 지정된 모든 수신인 주소를 무시하고 기연리프트 이정용 담당자 이메일 주소 **`77.victor.lee@gmail.com`**으로 일괄 치환하여 송신하도록 우회 처리.
  - **개발 모드 경고 알림 (UX 보호)**:
    - **동작**: 개발모드(`isDevMode === true`)인 상태에서 메일 발송 버튼 클릭 시, 발송 직전에 사용자에게 **"현재 시스템이 개발 모드입니다. 메일은 실제 수신인이 아닌 개발용 주소(77.victor.lee@gmail.com)로 우회되어 안전하게 발송됩니다."**라는 경고(Alert 또는 Confirm 안내)를 출력하여 사용자가 테스트 상태를 인지할 수 있도록 보장합니다.

## [2026-07-21] 제품 모델별 구글 드라이브 기술 문서 연동 및 이메일 자동 첨부 기능
- **목표**: 제품 관리 메뉴에서 각 모델 규격별로 안전인증서, 제원표, 비상조작방법 문서를 구글 드라이브에서 지정할 수 있도록 개발하고, 장비 할당 후 이메일 발송 시 이 문서들을 자동으로 첨부하여 발급하도록 처리합니다.
- **세부 요구사항**:
  - **제품(Product) 데이터 모델 스키마 확장**:
    - `safetyCertUrl?: string` (안전인증서 파일 경로/링크)
    - `specSheetUrl?: string` (제원표 파일 경로/링크)
    - `emergencyGuideUrl?: string` (비상조작방법 파일 경로/링크)
  - **제품 관리 UI 개편 (`Products.tsx`)**:
    - 제품 모델 신규 등록 및 수정 화면에 상기 3종 문서(안전인증서, 제원표, 비상조작방법)를 지정할 수 있는 입력 컨트롤과 파일 선택 대화상자 인터페이스 구축.
  - **이메일 자동 첨부 연동 (`Contracts.tsx` 등 메일 발송 로직)**:
    - 장비 할당 완료 후 고객사/현장 담당자에게 확인 메일을 발송할 때, 계약에 포함되어 있는 제품 모델 규격(들)을 조회하여 등록된 문서(안전인증서, 제원표, 비상조작방법)가 있을 경우, 메일 첨부파일 리스트에 모델별로 각각 1부씩 자동으로 바인딩 및 첨부하여 전송되도록 고도화.

## [2026-07-21] 장비 할당 후 메일 전송 시 견적서/계약서 동적 생성 및 회사 증빙, 장비별 점검문서 자동 첨부 기능
- **목표**: 장비 할당 완료 시점에 견적서 및 계약서를 자동 조립하여 가상 파일로 첨부하고, 기연리프트 회사 증빙(사업자등록증, 통장사본) 및 각 장비별 점검 문서(안전점검결과서, 반입전 체크리스트)를 구글 드라이브와 데이터베이스에서 자동으로 모아 이메일에 함께 동시 발송합니다.
- **세부 요구사항**:
  - **구글 설정 스키마 및 UI 확장 (`GoogleConfig.tsx` / `db.ts`)**:
    - `quotationTemplateUrl?: string` (견적서 양식 절대경로/링크)
    - `contractTemplateUrl?: string` (계약서 양식 절대경로/링크)
    - `bizRegCertUrl?: string` (기연리프트 사업자등록증 파일 절대경로/링크)
    - `bankbookCopyUrl?: string` (기연리프트 통장사본 파일 절대경로/링크)
    - `safetyInspectionTemplateUrl?: string` (안전점검결과서 양식 절대경로/링크) - **추가**
    - `preDeliveryChecklistTemplateUrl?: string` (반입전 체크리스트 양식 절대경로/링크) - **추가**
    - 상기 6개 이메일 발송 첨부 파일들의 **로컬 시스템 절대경로(Absolute Path)**를 설정하고 편집할 수 있는 인풋 폼을 구글 관리자 설정 화면에 추가.
  - **회사 기본 증빙(사업자등록증, 통장사본) 연동**:
    - 이메일 발송 시 구글 설정에 보존된 사업자등록증 및 통장사본 절대경로 링크를 가져와 메일 첨부파일 리스트에 1부씩 기본으로 탑재.
  - **개별 장비 자산별(호기별) 점검 문서 연동**:
    - **자산(Asset) 데이터 모델 스키마 확장**:
      * `safetyInspectionUrl?: string` (안전점검결과서 파일 경로/링크)
      * `preDeliveryChecklistUrl?: string` (반입전 체크리스트 파일 경로/링크)
    - **자산 관리 UI 개편 (`Assets.tsx`)**:
      * 장비 등록 및 호기별 정보 수정 모달에 안전점검결과서 및 반입전 체크리스트 경로/파일을 등록할 수 있는 폼 필드 추가.
    - **이메일 자동 첨부**:
      * 메일 발송 시 계약서에 할당되어 있는 개별 장비 자산(Asset)의 관리번호(호기)와 매핑된 `safetyInspectionUrl` 및 `preDeliveryChecklistUrl`을 조회하여, 자산당 각 1부씩 자동으로 수집하여 첨부파일 목록에 취합 및 전송 처리.
  - **이미지 기반 점검 결과서/체크리스트의 동적 조립**:
    - 장비 할당 완료 시점의 개별 자산(예: 관리번호 `G06004`, 모델명 `GTJZ0608ME`)에 매핑되는 **안전점검 결과서** 및 **반입 전 CHECK LIST** 문서를 다음 이미지 분석 결과에 근거하여 동적으로 조립 및 매핑하여 첨부파일로 발행:
      1. **고소작업대(T/L) 안전점검 결과서**:
         - 사업장명: SINOBOOM (주)기연리프트
         - 제조사(렌탈사): **제품 정보에 포함된 제조사 정보 동적 반영** (예: `product.manufacturer` 조회하여 `SINOBOOM (주)기연리프트` 등으로 동적 표시).
         - **글자 크기 유동적 조절 (Fluid Font Scaling)**: 해당 `제조사(렌탈사)` 칸 내부의 텍스트가 칸 경계를 넘거나 넘치지 않도록, 글자 수 길이에 따라 폰트 크기가 자동으로 축소 조정되도록 CSS/JS 가변 연동 처리 (예: 15자 이상 시 `font-size` 축소).
         - 사용업체: 계약서의 실제 고객사명 (예: 화성엔지니어링 주식회사)
         - 모델명: 할당된 자산의 모델명
         - 차량번호: 할당된 자산의 관리번호 (예: G06004)
         - 제조년도, 안전점검일시, 점검자(김관주) 등의 세부 필드 맵핑.
         - 1~8번 공통사항 및 안전장치 검사결과('O' 또는 '-') 일괄 마킹.
      2. **반입 전 CHECK LIST**:
         - 모델명 및 관리번호 매핑.
         - 1번부터 71번까지의 8개 파트(입고검사, 세차, 도장, 유압, 전기장치, 차체, 출고정비, 옵션장착) 점검 내역에 대해 '양호'란에 일괄 체크 표시 적용 및 점검자(김관주) 서명 맵핑.
  - **고소작업대 임대차 계약서 동적 생성 및 매핑**:
    - 제공된 임대차 계약서 이미지 양식을 분석하여, 메일 발송 시 아래 고객 정보 및 계약 상세 데이터를 동적으로 채운 **정밀 임대차 계약서 양식(HTML/Base64)**을 생성 및 첨부:
      * **임대인 (갑)**: 상호 `주식회사 기연리프트`, 대표자 `이수용`, 등록번호 `138-81-83251` (고정)
      * **임차인 (을)**: 계약서상의 고객명, 대표자명, 사업자등록번호 동적 매핑
      * **임대차 계약 내용**: 
        - 장비 인도장소: 현장명 (예: 평택삼성전자 P1)
        - 장비 인도 예정일: 배차 지정일 및 요일
        - 현장 상세 위치: 현장 주소
        - 임차 신청자: 고객사명, 현장 담당자 이름, 연락처
      * **장비 상세 테이블**: 품목(모델명), 수량, 장비번호/일련번호(관리번호 및 제조번호), 임대료(1대/1개월 단가), 합계 금액 동적 집계 반영
      * **기타 결제 계좌 및 Footer**: 신한은행 결제계좌 및 기연리프트 본사/영업소 정보, 영업 담당자 이름/연락처 맵핑
  - **렌탈 견적서 동적 생성 및 매핑**:
    - 제공된 렌탈 견적서 이미지 양식을 분석하여, 메일 발송 시 아래 견적 상세 데이터를 동적으로 채운 **정밀 렌탈 견적서 양식(HTML/Base64)**을 생성 및 첨부:
      * **TO**: 수신인 정보 (고객명, 부서/담당자명, 연락처), 견적일자, 유효기간(견적일로부터 1개월) 동적 매핑.
      * **From**: 상호 `유식회사 기연리프트`, 대표자 `이수용`, 등록번호 `138-81-83251` 고정. 담당자(영업 담당자명), 연락처, 이메일 동적 맵핑.
      * **견적 상세 테이블**: 품목(모델명), 장비 규격/사양(동력, 작업높이, 발판높이, 적재중량, 장비중량, 크기 등 제품 상세 설명 매핑), 수량, 단가(30일 기준 임대료), 공급가액, 세액(10%), 비고란 동적 반영.
      * **운임비 라인**: 협착 및 왕복 운송비 관련 항목을 단가 테이블 하단에 1회 청구비로 연계하여 동적 출력.
      * **합계**: 공급가액 및 세액 총합 집계 반영.
      * **하단 운송약관 및 결제조건**: 이미지 서식과 완전히 동일하게 고정 문구 렌더링.
  - **로컬 프로젝트 내 편집 가능한 HTML 템플릿 양식 파일 생성**:
    - 사용자가 구글 드라이브 업로드 및 개별 편집에 직접 활용할 수 있도록, 프로젝트 루트 디렉토리 아래 **`templates/`** 폴더를 신설하고 다음 4종의 고품격 프린트용 HTML 양식 파일을 생성하여 제공:
      1. `templates/렌탈견적서_양식.html`
      2. `templates/고소작업대_임대차계약서_양식.html`
      3. `templates/고소작업대_안전점검결과서_양식.html`
      4. `templates/반입전_CHECK_LIST_양식.html`
    - 각 파일은 `{{...}}` 플레이스홀더 토큰 구조를 포함하여 텍스트 에디터에서 누구나 쉽게 편집하여 사용할 수 있는 완결된 구조로 제작.

## [2026-07-21] 구글 관리자 설정 데이터 보존을 위한 스마트 마이그레이션 적용 및 초기화 방지
- **목표**: 사용자가 수정한 구글 서비스 계정 정보 및 절대 경로 등의 환경 설정 데이터가 향후 배포나 업데이트 시 초기화(롤백)되지 않도록 구조적으로 보호합니다.
- **세부 요구사항**:
  - **강제 초기화 코드 제거**: `AppContext.tsx` 내의 `localStorage.removeItem('erp_googleConfigs')` 등 전체 데이터를 소멸시키는 원천 초기화 코드를 제거합니다.
  - **스마트 병합(Merge) 마이그레이션 구현**: 기존 브라우저 스토리지에 `erp_googleConfigs` 데이터가 있는 경우, 기존 필드(계정 이메일, 패스워드, SMTP 보안 키 등 사용자 지정값)를 그대로 보존하면서 신규 스키마 컬럼(`isDevMode`, `quotationTemplateUrl` 등)만 동적으로 머지해주는 안전한 마이그레이션 논리를 연동합니다.

## [2026-07-21] 구글 설정 내 클라우드 양식 지정을 위한 구글 드라이브 파일 탐색기 모달 개발
- **목표**: 환경 설정의 6대 첨부파일 지정을 로컬 PC 절대경로 뿐만 아니라, 클라우드 상의 구글 드라이브 가상 저장소 내 파일들을 선택하여 매핑할 수 있도록 구글 드라이브 파일 탐색기 모달 및 버튼을 추가합니다.
- **세부 요구사항**:
  - **구글 드라이브 탐색기 버튼 제공**: `GoogleConfig.tsx`의 6대 서류 입력란 옆에 '구글 드라이브 탐색' 버튼을 추가하여 입력창과 행(Row)으로 정렬합니다.
  - **드라이브 탐색기 모달 구현**:
    - 모달 활성화 시 `drive.ts` 서비스와 연동하여 폴더 구조(트리 탐색) 및 파일 목록을 출력합니다.
    - 상위 폴더 이동 및 하위 폴더 진입이 가능해야 합니다.
    - 파일 목록에서 원하는 파일을 선택하여 '확인' 버튼을 누르면 해당 파일의 클라우드 주소(`webViewLink` 등)가 입력창에 자동으로 대입됩니다.
