# 설계 변경 및 승인 이력 (Planning & Architecture Log)

본 문서는 프로젝트 진행 과정에서 제안되고 승인된 주요 설계 변경 및 아키텍처 의사결정의 이력을 형상 관리 차원에서 기록합니다.

---

## [2026-07-19] 조직도 및 구성원 마스터 설정 기능 (Organization Settings)
* **제안 사유**: "이후에 시스템이 작동할때, 누가 무슨 일을 진행할 지 알아야 되니까 조직구성원 을 관리하는 기능이 우선 필요할 것 같아" (사용자 요청)
* **승인된 계획**:
  * `ADMIN` 및 `MANAGER` 권한을 가진 사용자 전용 인사 관리 대시보드 신설.
  * 좌측 패널(부서 트리 뷰) + 우측 패널(직원 그리드 뷰)의 하이브리드 레이아웃.
  * Drag & Drop 및 키보드 타이핑 병행을 통한 부서 이동 및 계층 이동 구현.
  * 퇴사(RETIRED) 처리 시, 진행 중인 미결 업무를 강제로 후임자/상급자에게 이관(Hand-off)하는 UI 적용 준비.
* **상태**: 승인 및 구현 완료

---

## [2026-07-19] 임직원 상세 프로필 관리 및 레이아웃 고도화 (v3)
* **제안 사유**: 생년월일, 주소, 전화번호, 이메일, 개인 사진 등 임직원 상세 정보를 관리할 수 있는 명부(Pool) 시스템 필요성 대두. 또한 부서명칭 변경 로직 누락 보완.
* **승인된 계획**:
  * `schema.sql`: `users` 테이블에 `birthDate`, `address`, `phone`, `email`, `profileImageUrl` 신규 컬럼 5개 추가.
  * 레이아웃 개편: 입력 필드가 10여 개로 늘어남에 따라, 직원 카드를 클릭하면 우측에서 튀어나오는 **사이드 패널(Slide-out)** 형태의 하이브리드 프로필 폼 채택.
  * 미배정(Unassigned) 명부 풀을 독립시켜, 여기서 특정 부서로 Drag & Drop 할 수 있도록 배치 로직 강화.
  * 부서명 인라인(In-line) 수정 로직 도입.
* **상태**: 승인됨 (진행 중)

---

## [2026-07-19] 데이터베이스 스키마 전면 개정 및 매출/매입 분리
* **제안 사유**: 매입과 매출의 명확한 분리, 다차원 수익(ROI) 분석, 메카닉-영업사원 간의 독립적인 배차 및 정비 청구 판단 지원 필요성.
* **승인된 계획**:
  * `vendors`, `purchase_billings` 신설로 비용 추적 이원화.
  * `departments` 테이블 신설 및 `users` 내 계층 구조(`managerId`, `status`) 삽입.
  * `collaboration_requests` 등 소통/조율(최대 4회 제한)을 위한 3대 협업 테이블 세트 추가.
  * `schema.sql`로 덮어쓰기 및 `DATABASE_MIGRATION.md` 작성.
* **상태**: 구현 완료
