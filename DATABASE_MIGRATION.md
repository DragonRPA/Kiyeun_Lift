# 데이터베이스 마이그레이션 및 적용 가이드 (v1.1.2)

본 가이드는 새롭게 정의된 기연리프트 ERP 요구사항(인사/조직도, 매출/매입 이원화, 대시보드 3대 소통, 외주/배차 정산 연동 등)을 반영한 **[schema.sql](file:///d:/Kiyeun_Lift/schema.sql)**의 마이그레이션 및 적용 방법을 설명합니다.

---

## 1. 스키마 개정 주요 파급 효과 (Impact)
이번 `schema.sql` 업데이트는 단순 컬럼 추가가 아닌 **전사 데이터 흐름의 구조적 개편**을 포함하고 있습니다.
* **외래키(Foreign Key) 전면 재배치**: `billings` 테이블이 매출/매입으로 쪼개졌고, 매입처(`vendors`) 테이블이 신설됨에 따라 자산(`assets`), 정비(`repairs`), 배차(`deliveries`), 구매(`consumables` 등) 테이블의 참조 키가 완전히 재구성되었습니다.
* **순환/계층 참조 도입**: 조직도를 그리기 위해 `users` 테이블에 직속 상급자를 가리키는 `managerId`가 추가되었고, 부서 테이블(`departments`)이 상위 부서를 가리키도록 설계되었습니다.
* **새로운 도메인 추가**: 공지사항, 지시사항, 요청사항 관리를 위해 6개의 신규 테이블 세트가 추가되었습니다.

---

## 2. 적용 방법 (Supabase 기준)

### 2-1. 개발/테스트 환경 (데이터 초기화 권장)
현재 저장된 데이터가 테스트용 더미 데이터라면, 가장 깔끔하고 안전한 방법은 기존 스키마를 모두 날리고 새로 생성하는 것입니다.
1. Supabase 프로젝트 대시보드에 접속합니다.
2. 좌측 메뉴에서 **SQL Editor**를 엽니다.
3. 새로운 Query 탭을 열고, 최신화된 `schema.sql` 파일의 전체 내용을 복사하여 붙여넣습니다.
4. **[RUN]** 버튼을 클릭하여 실행합니다.
   * *참고: `schema.sql` 상단에 `DROP TABLE IF EXISTS ... CASCADE;` 구문이 꼼꼼하게 배치되어 있으므로, 외래키 충돌 없이 기존 테이블이 모두 삭제된 후 신규 테이블과 기초 시드 데이터가 일괄 생성됩니다.*

### 2-2. 운영(Production) 환경 (데이터 보존 필요 시)
만약 운영 데이터가 존재하여 데이터를 보존해야 한다면, 전체 스크립트를 실행해서는 안 됩니다. (`DROP TABLE` 구문 때문에 모든 실데이터가 삭제됩니다).
1. Supabase의 Database Backups 기능으로 현재 DB의 전체 덤프를 생성하여 다운로드합니다.
2. `schema.sql`을 직접 실행하지 말고, 아래와 같은 순서로 **ALTER TABLE** 수동 마이그레이션 스크립트를 작성하여 적용해야 합니다.
   * `departments`, `vendors`, `purchase_billings` 등 신규 테이블 먼저 `CREATE`.
   * 기존 `users` 테이블에 `ALTER TABLE ADD COLUMN`으로 신규 컬럼 추가 (생년월일, 주소, 전화번호, 이메일, 프로필사진 5종 추가).
   * 기존 `repairs`, `deliveries` 등에도 신규 컬럼 추가.
   * 기존 데이터에 외래키를 맞추는 데이터 정제 작업(Migration script) 수행 후 외래키(`ADD CONSTRAINT`) 제약 활성화.
   *(현재 기연리프트 프로젝트는 초기 릴리즈 단계이므로 가급적 2-1 방식의 Clean Reset을 권장합니다).*

---

## 3. 프론트엔드 연동 주의사항
데이터베이스 스키마가 대폭 변경되었으므로, 프론트엔드(React/Vite) 단에서 TypeScript 인터페이스(Type Definitions)와 쿼리 로직의 대규모 수정이 필요합니다.
* 특히 기존 `billings`를 호출하던 로직은 매출인지 매입인지 확인하여 엔드포인트를 쪼개야 합니다.
* `schema.sql`이 적용된 직후, Supabase 대시보드에서 제공하는 **Type Generator (TypeScript Types)**를 사용하여 클라이언트 단의 타입을 갱신해 주시기 바랍니다.
